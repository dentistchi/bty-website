# Microsoft Entra ID (Azure AD) for Admin Dashboard
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# NextAuth Configuration
# NEXTAUTH_URL must be the deployed Pages URL (https://<project>.pages.dev)
# After first deploy, update this to your Cloudflare Pages URL and redeploy
NEXTAUTH_URL=https://your-project.pages.dev
NEXTAUTH_SECRET=

# Admin Access Control
# Comma-separated list of allowed admin emails
BTY_ADMIN_EMAILS=

# Supabase Configuration
# Service role key must never be exposed to client; only used in server routes
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Notes:
# - Redirect URI must be updated in Entra ID:
#   https://<project>.pages.dev/api/auth/callback/azure-ad
# - Service role key must never be exposed to client; only used in server routes
# - NEXTAUTH_URL must match your deployed Cloudflare Pages URL exactly
