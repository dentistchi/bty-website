import DashboardRouteLoadingShell from "./DashboardRouteLoadingShell";

/** Foundry 대시보드 라우트 — locale 로딩 + `<main>` (`bty.dashboardSuspenseMainRegionAria`). */
export default function DashboardLoading() {
  return <DashboardRouteLoadingShell />;
}
