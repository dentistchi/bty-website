import ProfileRouteLoadingShell from "./ProfileRouteLoadingShell";

/** Foundry 프로필 라우트 — locale 로딩 + `<main>` (`bty.profileRouteSuspenseMainRegionAria`). */
export default function ProfileLoading() {
  return <ProfileRouteLoadingShell />;
}
