import GrowthRouteLoadingShell from "./GrowthRouteLoadingShell";

/** Growth 허브·하위 라우트 — locale 로딩 + `<main>` 랜드마크 */
export default function GrowthLoading() {
  return <GrowthRouteLoadingShell />;
}
