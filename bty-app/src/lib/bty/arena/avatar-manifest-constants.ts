/**
 * `generate-avatar-manifest` · `avatar-assets.json` 검증 시 사용하는 목표 인벤토리.
 * 실제 id 목록은 `public/avatars/outfits`·`accessories` 폴더 스캔 결과가 단일 진실.
 */
export const AVATAR_MANIFEST_EXPECTED_OUTFIT_FILES = 20;
export const AVATAR_MANIFEST_EXPECTED_ACCESSORY_FILES = 24;
