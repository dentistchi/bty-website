"use client";import{useState}from"react";export default function ClinicalCaseBuilder(){const[chief,setChief]=useState("");const[patient,setPatient]=useState("");const[exam,setExam]=useState("");const[test,setTest]=useState("");const[reference,setReference]=useState("");const[attested,setAttested]=useState(false);const[message,setMessage]=useState("");const save=async(publish=false)=>{const truth={caseId:"clinical-draft",version:"1.0.0",chiefComplaint:chief,patientFacts:patient?[{id:"patient-1",kind:"patient",aliases:[],response:patient}]:[],objectiveFindings:exam?[{id:"exam-1",kind:"exam",aliases:[],response:exam}]:[],tests:test?[{id:"test-1",kind:"test",aliases:[],response:test}]:[],media:[],hiddenReference:{rubric:[],clinicianValidationStatus:"pending"}};const r=await fetch("/api/bty/clinical-encounter/cases",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:"draft",version:"1.0.0",state:publish?"published":"draft",truth,referencePath:reference?[{id:"reference-1",domain:reference,importance:"important",acceptableIds:[]}]:[],mediaAttested:attested})});setMessage(r.ok?publish?"Published version saved":"Draft saved":"Unable to save draft")};return <main className="mx-auto max-w-2xl space-y-4 p-6"><h1 className="text-2xl font-bold">Clinical Cases</h1><input aria-label="Chief complaint" className="w-full border p-2" value={chief} onChange={e=>setChief(e.target.value)} placeholder="Chief complaint"/><textarea aria-label="Patient fact" className="w-full border p-2" value={patient} onChange={e=>setPatient(e.target.value)} placeholder="Patient fact and authored response"/><textarea aria-label="Objective finding" className="w-full border p-2" value={exam} onChange={e=>setExam(e.target.value)} placeholder="Objective finding"/><textarea aria-label="Diagnostic test" className="w-full border p-2" value={test} onChange={e=>setTest(e.target.value)} placeholder="Diagnostic test result"/><input aria-label="Reference checkpoint" className="w-full border p-2" value={reference} onChange={e=>setReference(e.target.value)} placeholder="Author reference checkpoint"/><label><input type="checkbox" checked={attested} onChange={e=>setAttested(e.target.checked)}/> This training media is synthetic or deidentified and authorized for training use.</label><div className="space-x-2"><button onClick={()=>save(false)}>Save draft</button><button onClick={()=>save(true)} disabled={!chief}>Publish version</button></div><p aria-live="polite">{message}</p></main>}
