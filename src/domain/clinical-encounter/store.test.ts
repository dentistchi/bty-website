import{it,expect}from"vitest";import{EncounterStore}from"./store";const t={traceId:"t",caseId:"c",caseVersion:"1",schemaVersion:"clinical-encounter-trace/v2" as const,events:[]};it("protects ownership and immutable designation",()=>{const s=new EncounterStore();s.save("a",t);expect(()=>s.get("b","t")).toThrow("FORBIDDEN");s.complete("a","t");expect(()=>s.save("a",t)).toThrow("IMMUTABLE");expect(()=>s.designate(false,"t","author_reference_trace")).toThrow("FORBIDDEN");s.designate(true,"t","author_reference_trace")})
