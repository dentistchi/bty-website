import type {ClinicalEncounterCaseV2} from"./encounter";export type CaseLifecycle="draft"|"published"|"archived";export type ReferenceCheckpoint={id:string;domain:string;importance:"required"|"important"|"optional";acceptableIds:string[];authorNote?:string};export type CaseDraft={id:string;version:string;state:CaseLifecycle;truth:ClinicalEncounterCaseV2;referencePath:ReferenceCheckpoint[];mediaAttested:boolean};export function validateDraft(d:CaseDraft){const e:string[]=[];if(!d.truth.chiefComplaint)e.push("chief complaint required");if(!d.truth.caseId||!d.version)e.push("case identity required");if(d.truth.media.length&&!d.mediaAttested)e.push("media attestation required");return e}export function publishDraft(d:CaseDraft,authorized:boolean):CaseDraft{if(!authorized)throw Error("FORBIDDEN");const errors=validateDraft(d);if(errors.length)throw Error(errors.join(","));return{...d,state:"published"}}export function nextVersion(d:CaseDraft):CaseDraft{return{...d,id:crypto.randomUUID(),version:`${d.version}-next`,state:"draft"}}export function compareReference(asked:string[],path:ReferenceCheckpoint[]){const important=path.filter(x=>x.importance!=="optional").map(x=>x.id);return{importantAsked:important.filter(x=>asked.includes(x)),importantMissed:important.filter(x=>!asked.includes(x))}}
