import{describe,it,expect}from"vitest";import{CASE_001_ENCOUNTER}from"./encounter";import{publishDraft,validateDraft}from"./builder";const d={id:"d",version:"1",state:"draft" as const,truth:CASE_001_ENCOUNTER,referencePath:[],mediaAttested:true};describe("builder",()=>{it("requires authority and validates drafts",()=>{expect(validateDraft(d)).toEqual([]);expect(()=>publishDraft(d,false)).toThrow("FORBIDDEN");expect(publishDraft(d,true).state).toBe("published")})});
