import{it,expect}from"vitest";import{startEncounter,submit}from"./session";it("runs authored Case 001 encounter in ordered raw events",()=>{let t=startEncounter("t");t=submit(t,"When did this happen?");t=submit(t,"cold test #8");t=submit(t,"Show me the PA");expect(t.events.map(e=>e.type)).toContain("patient_answered");expect(t.events.some(e=>e.response?.includes("#8 responds"))).toBe(true);expect(t.events.map(e=>e.sequence)).toEqual(t.events.map((_,i)=>i+1))})
