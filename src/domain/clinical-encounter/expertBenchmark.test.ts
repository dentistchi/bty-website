import{it,expect}from"vitest";import{designateExpert,expertBenchmarks}from"./expertBenchmark";const t={traceId:"t",caseId:"c",caseVersion:"1",schemaVersion:"clinical-encounter-trace/v2" as const,events:[]};it("is separately authorized and zero-safe",()=>{expect(expertBenchmarks([],"c","1")).toEqual([]);expect(()=>designateExpert(t,true,false,"c","1","a")).toThrow("FORBIDDEN");expect(designateExpert(t,true,true,"c","1","a").role).toBe("expert_benchmark")})
