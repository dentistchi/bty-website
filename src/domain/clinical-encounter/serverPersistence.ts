import type{EncounterTraceV2}from"./encounter";export const V2_SCHEMA="clinical-encounter-trace/v2";export function validateEncounterTrace(x:unknown):x is EncounterTraceV2{if(!x||typeof x!=="object")return false;const t=x as EncounterTraceV2;return t.schemaVersion===V2_SCHEMA&&typeof t.traceId==="string"&&typeof t.caseId==="string"&&typeof t.caseVersion==="string"&&Array.isArray(t.events)&&t.events.every((e,i)=>e.sequence===i+1&&typeof e.timestamp==="string"&&typeof e.elapsedMs==="number")}export function encounterRecord(userId:string,t:unknown,status:"active"|"completed"="active"){if(!validateEncounterTrace(t))throw Error("INVALID_TRACE");if(status==="completed"&&!t.events.some(e=>e.type==="encounter_completed"))throw Error("INVALID_COMPLETION");return{trace_id:t.traceId,user_id:userId,case_id:t.caseId,case_version:t.caseVersion,trace_schema_version:V2_SCHEMA,trace_role:"learner",raw_trace:t,derived_metrics:{decisionEvidenceStates:t.events.filter(e=>/diagnosis|treatment|followup/.test(e.type)).map(e=>({sequence:e.sequence,evidenceIds:t.events.filter(x=>x.sequence<e.sequence).flatMap(x=>x.canonicalIds??[])}))},status,source_product:"BTY Clinical Encounter Simulator",content_provenance:{},started_at:t.events[0]?.timestamp,completed_at:status==="completed"?t.events.at(-1)?.timestamp:null}}
