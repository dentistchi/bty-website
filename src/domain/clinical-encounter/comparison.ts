import type{EncounterTraceV2}from"./encounter";import type{ReferenceCheckpoint}from"./builder";const ids=(t:EncounterTraceV2)=>t.events.flatMap(e=>e.canonicalIds??[]);export function compareEncounter(t:EncounterTraceV2,path:ReferenceCheckpoint[]=[],references:EncounterTraceV2[]=[]){const asked=ids(t),important=path.filter(p=>p.importance!=="optional").map(p=>p.id),decision=t.events.find(e=>e.type.includes("treatment")||e.type.includes("diagnosis"));const before=decision?t.events.filter(e=>e.sequence<decision.sequence).flatMap(e=>e.canonicalIds??[]):asked;return{label:"Author Reference",importantDomainsCovered:important.filter(x=>asked.includes(x)),importantDomainsMissed:important.filter(x=>!asked.includes(x)),actionsBeforeDecision:before,actionsAfterDecision:asked.filter(x=>!before.includes(x)),authorReferenceTraceCount:references.length,compositeScore:undefined}}
