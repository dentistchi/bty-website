export * from "./types"; export { CASE_001 } from "./case001"; export { appendEvent, createTrace, revealedEvidenceIds } from "./trace"; export { reasoningMetrics } from "./metrics"; export { neurosightExport } from "./neurosight"; export { learnerFeedback } from "./feedback"; export { BrowserClinicalReasoningTraceStore } from "./persistence";
import type { ClinicalEvidenceItemV1, ClinicalReasoningCaseV1 } from "./types"; const norm=(v:string)=>v.trim().toLowerCase();
export function findEvidence(c:ClinicalReasoningCaseV1,query:string):ClinicalEvidenceItemV1[]{const q=norm(query);return q?c.evidence.filter(e=>[e.label,...e.aliases].some(t=>norm(t).includes(q)||q.includes(norm(t)))):[];}
export function validateCase(c:ClinicalReasoningCaseV1):string[]{const ids=new Set<string>(),aliases=new Set<string>(),errors:string[]=[];for(const item of c.evidence){if(ids.has(item.id))errors.push(`duplicate evidence id: ${item.id}`);ids.add(item.id);for(const alias of item.aliases){const v=norm(alias);if(aliases.has(v))errors.push(`duplicate alias: ${alias}`);aliases.add(v);}}for(const id of [...c.rubric.criticalEvidenceIds,...c.rubric.earlyEvidenceIds])if(!ids.has(id))errors.push(`unknown rubric evidence: ${id}`);return errors;}
