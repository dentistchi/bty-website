import type { ClinicalReasoningCaseV1, ClinicalReasoningEventV1, ClinicalReasoningTraceV1 } from "./types";
export function createTrace(c: ClinicalReasoningCaseV1, subjectRef: string, traceId: string, startedAt = new Date().toISOString()): ClinicalReasoningTraceV1 { return { traceId, caseId: c.caseId, caseVersion: c.version, subjectRef, startedAt, events: [{ sequence: 1, timestamp: startedAt, elapsedMs: 0, type: "case_started" }] }; }
export function appendEvent(trace: ClinicalReasoningTraceV1, event: Omit<ClinicalReasoningEventV1, "sequence" | "timestamp" | "elapsedMs">, timestamp = new Date().toISOString()): ClinicalReasoningTraceV1 { const elapsedMs = Math.max(0, new Date(timestamp).getTime() - new Date(trace.startedAt).getTime()); return { ...trace, completedAt: event.type === "case_completed" ? timestamp : trace.completedAt, events: [...trace.events, { ...event, sequence: trace.events.length + 1, timestamp, elapsedMs }] }; }
export function revealedEvidenceIds(trace: ClinicalReasoningTraceV1, beforeSequence = Number.POSITIVE_INFINITY): string[] { return [...new Set(trace.events.filter((e) => e.sequence < beforeSequence && e.type === "evidence_revealed" && e.evidenceId).map((e) => e.evidenceId!))]; }
