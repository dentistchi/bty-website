import AssessmentRouteLoadingShell from "./AssessmentRouteLoadingShell";

/** Center 50문항 진단 — locale 로딩 + `<main>` 랜드마크 */
export default function AssessmentLoading() {
  return <AssessmentRouteLoadingShell />;
}
