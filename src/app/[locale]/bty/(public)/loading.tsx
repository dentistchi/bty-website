import BtyPublicRouteLoadingShell from "./BtyPublicRouteLoadingShell";

/** Foundry 공개 라우트(로그인·비밀번호 찾기) — locale 로딩 + `<main>` 랜드마크 */
export default function BtyPublicLoading() {
  return <BtyPublicRouteLoadingShell />;
}
