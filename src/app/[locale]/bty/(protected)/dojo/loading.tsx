import DojoRouteLoadingShell from "./DojoRouteLoadingShell";

/** Foundry Dojo 라우트 — locale 로딩 + `<main>` (`bty.dojoSuspenseMainRegionAria`). */
export default function Loading() {
  return <DojoRouteLoadingShell />;
}
