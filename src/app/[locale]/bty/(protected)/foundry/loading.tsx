import { FoundryHubLoadingShell } from "./FoundryHubLoadingShell";

/** Foundry 허브 라우트 — locale 로딩 + `<main>` (`bty.foundryHubSuspenseMainRegionAria`). */
export default function Loading() {
  return <FoundryHubLoadingShell />;
}
