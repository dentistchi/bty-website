import MentorRouteLoadingShell from "./MentorRouteLoadingShell";

/** Foundry 멘토 라우트 — locale 로딩 + `<main>` (`bty.mentorSuspenseMainRegionAria`). */
export default function Loading() {
  return <MentorRouteLoadingShell />;
}
