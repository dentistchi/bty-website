import EliteRouteLoadingShell from "./EliteRouteLoadingShell";

/** Foundry Elite 라우트 — locale 로딩 + `<main>` (`bty.eliteSuspenseMainRegionAria`). */
export default function Loading() {
  return <EliteRouteLoadingShell />;
}
