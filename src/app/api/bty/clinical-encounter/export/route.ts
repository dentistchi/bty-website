import{NextRequest,NextResponse}from"next/server";import{requirePlatformAdmin}from"@/lib/authz";import{getSupabaseAdmin}from"@/lib/supabase-admin";import{exportEncounterV2}from"@/domain/clinical-encounter/neurosight";export async function GET(r:NextRequest){const a=await requirePlatformAdmin(r);if(!a.ok)return NextResponse.json({ok:false},{status:a.status});const d=getSupabaseAdmin(),id=r.nextUrl.searchParams.get("traceId");const x=await d?.from("clinical_reasoning_traces").select("user_id,raw_trace,status").eq("trace_id",id).eq("trace_schema_version","clinical-encounter-trace/v2").maybeSingle();if(!x?.data||x.data.status!=="completed")return NextResponse.json({ok:false,error:"NOT_FOUND"},{status:404});return NextResponse.json(exportEncounterV2(x.data.raw_trace,x.data.user_id),{headers:{"Cache-Control":"private, no-store"}})}
