import { NextRequest,NextResponse } from "next/server"; import { requirePlatformAdmin } from "@/lib/authz"; import { getSupabaseAdmin } from "@/lib/supabase-admin";
export async function GET(req:NextRequest){const auth=await requirePlatformAdmin(req);if(!auth.ok)return NextResponse.json({ok:false,error:"FORBIDDEN"},{status:auth.status});const caseId=req.nextUrl.searchParams.get("caseId");if(!caseId)return NextResponse.json({ok:false,error:"CASE_ID_REQUIRED"},{status:400});const db=getSupabaseAdmin();if(!db)return NextResponse.json({ok:false,error:"SERVER_ERROR"},{status:503});const {data,error}=await db.from("clinical_reasoning_traces").select("trace_id,trace_role,case_version,derived_metrics,raw_trace,completed_at").eq("case_id",caseId).eq("status","completed");if(error)return NextResponse.json({ok:false,error:"SERVER_ERROR"},{status:500});return NextResponse.json({ok:true,traces:data??[]},{headers:{"Cache-Control":"private, no-store"}})}
