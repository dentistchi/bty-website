import TeamsLinkStart from "@/components/teams/TeamsLinkStart";

export const dynamic = "force-dynamic";

/** `/teams/link` — first-ever sign-in popup, start page. Slice A0. */
export default function TeamsLinkPage() {
  return <TeamsLinkStart />;
}
