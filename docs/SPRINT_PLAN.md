# BTY SPRINT PLAN

**Single source of truth for all Cursor work is the repo-root file.**

All Cursors must read **docs/SPRINT_PLAN.md** (at repository root) before executing tasks.

This file under `bty-app/docs/` is only a pointer. The actual sprint, TASK BOARD (C2–C7), Gate Command, and Gate Result live in:

- **docs/SPRINT_PLAN.md**

Do not use this file as the task board. Open the root `docs/SPRINT_PLAN.md` for CURRENT SPRINT and TASK BOARD.
