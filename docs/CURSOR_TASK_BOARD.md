# CURSOR TASK BOARD — 2026-03-23

**[x] 3.2L-R11.4K-R1-CONTINUITY-ELIGIBILITY — VERDICT A · STAGING DEPLOYED (2026-08-08; inner `71c53cd4`; Worker `71c53cd4`; NO migration; zero canonical mutation).** Closes the two R11.4K acceptance gaps. Resume eligibility is now SERVER-owned: a read-only `GET …/program-draft?attempt=` returns `{eligible, reason}` and never a sentence of prose, applying seven owner-scoped predicates (owned · same draft · success · digest present · not applied · current fingerprint · newest digest-bearing success). Continuity fails CLOSED — missing attempt id, missing checker, refusal, or network error all withhold the proposal and CLEAR the cache. Invalidation is client cleanup only: no row, digest, `applied_at`, draft or history touched. Tamper proven through the REAL PATCH route on an isolated fixture: one changed character → `proposal_mismatch`, marker stripped from the write, no receipt stamped, other work still saved; a null-digest attempt is never adoptable. Apply-success clears the cache and no stale offer returns. 11 full-Builder + 4 real-route + 8 unit tests; 0 new failures. Counts identical 19/21/1/0; `496302b6` still success/digest/unapplied. **R11.4K continuity authority CLOSED; one new governed generation now ready (not executed).** NOT PASS/CLOSED for 3.2L.

**[x] 3.2L-R11.4K-PROPOSAL-CONTINUITY — VERDICT A · STAGING DEPLOYED (2026-08-08; inner `ed75635c`; Worker `ed75635c`; NO migration; zero canonical mutation; no generation by me).** A successful generation was unfinished user work with no path back: the proposal lived only in `ProgramAuthorship` state, the Builder is keyed by draft id and re-lists on tab re-entry, and the route exports POST only — so ordinary navigation destroyed it and nothing could serve it back. R7 forbids durable SERVER storage of generated prose, not the Host's own session; continuity is now `sessionStorage` keyed by draft, fingerprint-gated, cleared on Apply/Discard, superseded by a newer success, and never authority — Apply still recomputes `journeyDigest` and refuses `proposal_mismatch`. 16 tests including 8 that actually unmount the real Builder. **`496302b6` PROPOSAL BODY IS LOST FROM REACHABLE STATE** — no recovery faked; that attempt is now SUCCESSFUL BUT UNAPPLYABLE / HISTORICAL, and one further governed generation will be needed. Separately measured: the Korean-draft landing was ordinary use — the Founder generated twice on `35773b57` (`e6d9935d`, `364aefd9`, both `scenario_without_pressure`), which is why parents moved 17→19; the canonical draft's `updated_at` never moved, proving it was never reopened. **NOT PASS/CLOSED.**

**[x] 3.2L-R11.4J-FIRST-DIGEST-BORN-SUCCESS — VERDICT A · READ-ONLY (2026-08-08; zero mutation by me; no Apply).** Fourth gesture produced the arc's first `success` WITH a digest: parent `496302b6` (16→17), calls 17→19 both under that one parent — sequence 1 `authorship` refused semantically at `elements.scenario`, sequence 2 `authorship_retry` succeeded. **The R11.4I bounded repair is what produced it, in a paid live window.** `proposal_digest = program_proposal_digest_v1:a2ba0962…9f48`, digest count **0→1 exactly**, nothing backfilled, applied still 0. Fingerprint byte-identical to the server recomputation AND to `15108cf3`; 7 required kinds, no `reflection`; newest success on the draft. Independent pre-Apply recomputation is UNOBSERVABLE by privacy design and no equality was fabricated — Apply itself recomputes `journeyDigest` from the journey being adopted and refuses `proposal_mismatch`, with null digests refused outright. Canonical draft still `draft`/step 2, answers byte-identical, `programAdoptionV1` absent, seed journey still 4 elements. Apply predicate matrix **9/9 PASS → ELIGIBLE FOR ONE CONTROLLED APPLY**, not performed. **NOT PASS/CLOSED.**

**[x] 3.2L-R11.4I-EVIDENCE-POLICY-SINGLE-SOURCE — VERDICT A · STAGING DEPLOYED (2026-08-08; inner `d218e137`; Worker `d218e137`; NO migration; zero canonical mutation; no generation).** Third gesture `db4eaef2` refused `evidence_overclaim` again (parents 15→16, calls 16→17, one call). Cause = **B**, proven: the deployed runtime did carry R11.4H (prompt tokens 2980→3356), but the validator held 18 phrases + 2 structures and the hand-written brief named only part of them. Two hand-maintained lists cannot be kept honest, so `evidence-policy.ts` now holds 11 rules, each carrying the sentence the model is told; validator and prompt are generated from the same array, and a rule whose line never reaches the brief fails a test. Matrix generated from the policy — every forbidden sample refused by its OWN rule, every legal rewrite and every honest denial still legal. Nothing relaxed; the derived tests found two real defects ("does" vs "does not"; `outcomeClaimIndex` matching the ceiling sentence itself). Classification **B**: the same-parent bounded-repair path already existed for shape faults, so the two honesty families now get ONE repair under the same parent and intent, revalidated in full, terminal on a second refusal, no loop, no migration. Three materially different 7-section programs validate. 13 new tests, 0 new failures, counts 16/17/0/0. **NOT PASS/CLOSED.**

**[x] 3.2L-R11.4H-EVIDENCE-CEILING-AUTHORSHIP — VERDICT A · STAGING DEPLOYED (2026-08-08; inner `afd61d0c`; Worker `afd61d0c`; NO migration; zero canonical mutation; no generation).** Second authorized generation `cdd16aaf` refused `evidence_overclaim` (parents 14→15, calls 15→16, one call, no retry) — and NOT `material_fabrication`, so R11.4G held and the failure moved up a layer. Cause proven from code, not prose: the prompt banned five outcome nouns against a validator refusing 32. The ladder (exposed→sustained), already encoded in `deriveEvidenceCeiling` since R8, is now named and derived; `evidenceClaimBrief` states allowed claims first and derives the forbidden set from the validator itself, with a test that fails if the prompt narrows again. Follow-up must be prospective. Validator EXTENDED: all four claims this slice had to refuse passed the old rules; `claimsAboveCeiling` now catches habituality and proof-of-high-rung while negated and prospective sentences stay legal. Cases A–D accept, E–H refuse; the full 7-section program validates inside the ceiling. R11.4G approval copy confirmed present. 15 new tests, 1233/1233 in the program suites, 0 new failures. Counts 15/16/0/0. **NOT PASS/CLOSED.**

**[x] 3.2L-R11.4G-MATERIAL-HONEST-AUTHORSHIP — VERDICT A · STAGING DEPLOYED (2026-08-08; inner `afe3a684`; Worker `afe3a684`; NO migration; zero canonical mutation; no generation).** `7abebd5b` was refused because the model got a bare material noun plus prohibitions and had to guess what it could write. `deriveMaterialAuthority` now states the truth — a link or upload proves EXISTENCE, contents are never read by this product, so that authority is unreachable — and the prompt opens with what the author MAY do: a self-contained program whose records the participant CREATES. The validator was extended, not relaxed: `claimsMaterialContent` refuses speaking for an unread material (link or upload), with a creation-frame exemption. A full 7-section program with zero artifacts passes the real validator; the same program with one "provided template" sentence is still refused. Part 8 classified **B** — measured that approve/publish never read the journey, so a host-authored-only training is legitimate and the enabled CTA is correct authority; the UI now says what it approves instead of leaving a Host who just read "Nothing was added" to guess. 19 new tests, 1218/1218 in the program suites, 0 new failures. Counts held at 14/15/0/0. **NOT PASS/CLOSED.**

**[x] 3.2L-R11.4F-FIRST-DIGEST-BORN-GENERATION-FORENSIC — VERDICT D · READ-ONLY (2026-08-08; zero mutation by me; no generation, no Apply).** The one authorized generation ran and was recorded exactly — parent attempts 13 → 14 (`7abebd5b`, canonical draft, deploy `abc32509`), child calls 14 → 15 (`bba75545`, one provider call, no retry) — but the server outcome is **`validation_refused` / `material_fabrication`**, not success, so `proposal_digest` is null and the count stayed **0 → 0**. The refusal is the guard working: the draft has zero uploaded assets and a YouTube-only material, so nothing grounded the template the model leaned on. Context authority is intact (fingerprint byte-identical to `15108cf3`, 7 required kinds, no `reflection`); canonical answers byte-identical to the pre-gesture hash; `current_step` still 2; applied count still 0; `15108cf3` and `aa980c82` untouched. **No attempt in the system is adoptable** — a controlled Apply still needs a digest-born success. **NOT PASS/CLOSED.**

**[x] 3.2L-R11.4E-R2-STABLE-REVIEW-URL — VERDICT A (two named exceptions) · STAGING DEPLOYED (2026-08-08; inner `abc32509`; Worker `abc32509`; NO migration; ZERO canonical writes).** The Review URL is now an address, not a bootstrap token: the shell stops consuming the draft link on mount and narrows it (pure `narrowDraftDeepLink`) only when it stops being true — `view` on a real move off the review, the whole address on Builder close or leaving Learn. Consent for the staging automation account was completed through the **ordinary Legal Accept UI**, never the database. Deployed end-to-end on the full app: no Comeback over the canonical URL, URL byte-identical after 15 s, after reload + 15 s, and after a second reload; ordinary `/en/app` with the same away-state still prompts; `train/day` still resumes; canonical `current_step` still 2 with `updated_at` unmoved. **Exception 1:** one `POST /api/me/day/open` per page load — identical on an ordinary entry, nothing draft-related. **Exception 2:** the automation account is not a Foundry host (403 `foundry_host_required`), so rendered review CONTENT for an owning Host remains unproven. Tests 6 + 8 new, 713/713 in the touched suites, 0 new failures. **NOT PASS/CLOSED.**

**[x] 3.2L-R11.4E-R1-LIVE-DEEP-LINK-REDIRECT — VERDICT B · REPAIRED · STAGING DEPLOYED (2026-08-08; inner `e52e5692` + `a3c4d63e`; Worker `a3c4d63e`; NO migration; NO DB write; NO provider call).** The live `/en/train/day/28` redirect was not the deep link — it was `<Comeback/>`, mounted by the locale layout on every page, offering to resume the training journey after 3+ days away. R11.4E's gates mounted `ModuleBuilderShell` alone and could never have seen it, so that PASS was false-green. An explicit link now outranks the implicit resume prompt (entry URL captured in a render-phase lazy initializer, ahead of the shell's param-cleanup effect); a second defect the live run itself exposed — a consent gate folding the target into `?return=<encoded>` — is read one level deep. New regression mounts the component that actually redirected (7/7). Live A/B on the deployed bundle: control prompts, deep link does not, URL stable across 12s, 0 non-GET. Deployed chunk grepped by string literal. **HONEST LIMIT:** the only staging account I hold is consent-gated and can never reach `/en/app`, so the canonical render is proven by mechanism, not by observation — closing it needs the Founder's session or one authorized write, neither taken. tsc clean · terminology 44 · cf:build clean · unit 17 failed / 9329 passed (same 17 with the change stashed, 0 new). Canonical draft `updated_at` unmoved since R11.4C; 13/14/0/0; drafts 32. **NOT PASS/CLOSED.**

**[x] 3.2L-R8-SINGLE-COMPLETION-AUTHORITY — IMPLEMENTED · STAGING DEPLOYED · PHYSICAL PREVIEW GATE PENDING (2026-08-06; commit `497b7eac`; Worker `8fbacac6` 100%; NO migration; NO paid provider call).** The v5 window SUCCEEDED — first real seven-section proposal in the arc — and reading it exposed four BTY-side defects: "It is complete when receive a confirmation…" (a free-text completion signal with no subject), APPLY IT giving a second different definition of done, "In during a team meeting…" plus a scenario that discarded the trigger and moved the trained action, and an evidence ceiling that said both "not competence" and "equipped to implement". Fixed by making completion a named confirmer + a visible act rendered as "you see X do Y", deleting the competing application evidence field, anchoring IN CONTEXT to the trigger with a preposition-safe context clause, requiring the application moment to be an instance of the trigger, and DERIVING the evidence ceiling instead of letting the model write it. `program_authorship_v6` / `bty_guided_program_v6`; no migration. tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 38/38. **NOT PASS/CLOSED**.

**[x] 3.2L-R7-PROPOSED-BEHAVIOR-AUTHORITY — PROMPT REPAIRED · BEHAVIOUR DIAGNOSTICS LIVE · STAGING DEPLOYED · V5 WINDOW READY-NOT-AUTHORIZED (2026-08-06; commits `839224f2` + `0e30bea1` + `5f84c096`; Worker `c94fa520` 100%; migration `20260810000000` executed + tracker repaired; NO paid provider call).** The v4 window's refusal was caused by the prompt: the Host's "Create a shared handoff standard" was labelled "the behavior expected afterwards", the model was told to "Describe CREATING it" and simultaneously forbidden from making the standard about creating one, four rules forbade invention with no counterweight, and the derived `authority_mode=proposed` never reached the model. Now: "The change the host wants", the construct passed as PROPOSED, an explicit statement that designing a future behaviour is not inventing a fact, both sides of the boundary shown, creation language scoped to narrative. Grounding untouched. `program_authorship_v5` (wire schema still v4). Behaviour-contract field+reason now persist on the exact child call — shadow-proven 13/13 on PG 17.6, executed live, gate flipped. Also fixed "must state … and identifies …". tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 37/37. **NOT PASS/CLOSED**.

**[x] 3.2L-R6.4-INFLECTION-PRECEDENCE + BUILD IDENTITY — IMPLEMENTED · STAGING DEPLOYED · BUILD-IDENTIFIED PHYSICAL GATE PENDING (2026-08-06; commit `e7b634dc`; Worker `f4bbee98` 100%; NO migration; NO paid provider call).** R6.3 preserved every all-caps head, which protected SBAR but knowingly emitted "must STATES". The rules now run case-insensitively first and the RESULT decides: a token the rules CHANGE was inflected (STATES→state, DELEGATES→delegate, including verbs no list anticipates); a token they leave alone is a shout worth keeping (SBAR). Only S-final acronyms defeat that test, so SOS and friends are named in a short closed set. The preview banner now shows `Build <sha8>` from the same env chain `/api/version` reads, server-rendered on a force-dynamic route, so a physical recording carries its own build identity — verified live: /api/version 5/5 `e7b634dc`, banner `Build e7b634dc`. tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 37/37. **NOT PASS/CLOSED**.

**[x] 3.2L-R6.3-CANONICAL-ACTION-CASING — IMPLEMENTED · STAGING DEPLOYED · ACTION-CASING PHYSICAL GATE PENDING (2026-08-06; commit `8c17aef3`; Worker `8c991343` 100%; NO migration; NO paid provider call).** R6.2's physical gate passed on grammar; the reported "expected to Say it blunt" **could not be reproduced** — the deployed commit already routed the follow-up through the canonical phrase, and this deploy produced live evidence of the likely cause: `/api/version` returned the previous SHA on 2 of 8 probes before converging 10/10. Auditing the implied leak found a REAL defect one step further in: `baseForm` lowercased the whole head word, so "SBAR the handoff" became "must sbar the handoff" and a blind strip would turn "SOS" into "SO". All-caps tokens are now preserved verbatim (documented trade-off: "must STATES" is awkward but faithful). All six action contexts derive from one `baseActionPhrase` call; no renderer consumes raw observableAction; the Host's control still shows their original text. tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 36/36. **NOT PASS/CLOSED**.

**[x] 3.2L-R6.2-DERIVED-SENTENCE-GRAMMAR — IMPLEMENTED · STAGING DEPLOYED · GRAMMAR-CORRECT PHYSICAL GATE PENDING (2026-08-06; commit `b9fbe92d`; Worker `b171b35f` 100%; NO migration; NO paid provider call).** R6.1's physical gate passed structurally and exposed four grammar defects: "doctors faces", "doctors states", "I will … at your next shift change", "said when you say it blunt". Cause: renderers pasted a free-text actor in front of a third-person-inflected action and reused one moment across first and second person. Fixed by normalising the action to a base form behind the invariant modal "must", carrying scenario pressure after a colon, storing the moment perspective-free and rendering it per section with an ANCHORED possessive strip, and asking the follow-up "when you were expected to …". A colloquial Host phrase is preserved ("must say it blunt"); only a value with no verb-shaped head is refused, with plain guidance. Provenance scoped per field so the first-person decision keeps "Drafted by BTY" when only the actor changes. tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 36/36. **NOT PASS/CLOSED** — grammar-correct physical gate, then one paid v4 window.

**[x] 3.2L-R6.1-STRUCTURED-REVIEW-AUTHORITY — MIGRATION APPLIED LIVE · DIAGNOSTICS ENABLED · STAGING DEPLOYED · PHYSICAL GATE PENDING (2026-08-06; commits `12b2a24a` + `03ba775d` + `36f17ab2`; Worker `359e2c24` 100%; migration `20260809000000` executed + tracker repaired; NO paid provider call).** Derived sections became read-only text plus an "Edit details" disclosure over the contract deltas, so adjusting one value re-renders every dependent sentence and APPLY IT can no longer contradict THE STANDARD. Provenance reads "Adjusted by you", Apply is blocked with plain guidance when the adjusted state wouldn't have passed as generated, Reset restores BTY's contracts. **Corrected an R6 error:** the claim that a PG17 shadow proof needed the live password was wrong — a local isolated container needs only a local credential. Proven 13/13 on PG 17.6 against a seeded 7/8 replica; migration then executed live by the Founder and the recorder gate flipped on. Independently re-verified: 3 columns on child, 0 on parent, 8/8 NULL, counts and digests unchanged. tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 31/31. **NOT PASS/CLOSED** — physical structured-edit gate, then one paid v4 window.

**[x] 3.2L-R6-CANONICAL-CONSTRUCT-IDENTITY — IMPLEMENTED · STAGING DEPLOYED · V4 RECHECK PENDING (2026-08-06; commits `eb40492a` + `74dde8ef` + `52d4320b`; Worker `85bf163a` 100%; NO applied migration; NO paid provider call).** R5's window was refused on WHY THIS MATTERS — narrative — because the program had no canonical identity for the construct it was proposing, and five sections were still independent prose checked only after generation. The construct now has ONE system-derived identity (the model cannot assert existence), and all six INSTRUCTIONAL sections derive from one behavioural authority: decision/apply from an application contract that inherits actor and action, completion and follow-up from ENUMS. The R3 live inversion is now unrepresentable, not refused. Dependency scope is by semantic role — narrative may name a construct, instructional sections consume the same authority, follow_up stays dependency-bearing. v4 schema; no migration applied. A dependency-diagnostics migration is written and HELD (no shipped code depends on it) and is NOT shadow-tested — no DB credential — so it awaits a Founder SQL gate. tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 30/30. **NOT PASS/CLOSED** — one paid v4 window remains.

**[x] 3.2L-R5-BEHAVIOR-GROUNDED-SCENARIO — IMPLEMENTED · STAGING DEPLOYED · PHYSICAL DEVICE GATE PENDING (2026-08-06; commits `3cf53832` + `43917997` + `6aac3e9f`; Worker `78c138e3` 100%; NO migration; NO paid provider call).** R4's window was refused by a PRE-R4 rule: scenario relevance was a lexical overlap check with no stemming over a 2–4 token reference, so a handover-worded scenario shared nothing and was rejected. IN CONTEXT is now DERIVED from the behaviour contract plus a new `scenario_contract` (pressure + context), making relevance structural; `scenario_without_pressure` replaces the retired `scenario_unrelated`. Refusal copy is now exhaustive and compile-checked — ten semantic codes previously fell through to "didn't meet our honesty rules" for what were relevance and coherence faults. Retry is uniform: no terminal refusal shows a generate button. NEW `/dev/program-review-preview` renders the real review component from a static fixture with zero network, so the R4 AutoTextarea repair can be checked on the Founder's phone WITHOUT a paid generation. v3 schema; no migration (`proposal_version` unconstrained). tsc clean · terminology 44 · cf:build clean · full suite 0 new failures · WebKit 30/30. **NOT PASS/CLOSED** — device gate + one paid coherence window remain.

**[x] 3.2L-R4-BEHAVIORAL-STANDARD-CONTRACT — IMPLEMENTED · STAGING DEPLOYED · COHERENCE + READABILITY RECHECK PENDING (2026-08-06; commits `b5bc14a4` + `3c2e3296`; Worker `1e221a06` 100%; NO migration; NO paid provider call).** The fifth window passed every technical gate and still produced an unusable program. THE STANDARD is now derived from a validated four-field `behavior_contract` instead of a `wordCount >= 4` check, and the program is validated as an ordered dependency graph (`dependency_inversion`) so no section can require a construct an earlier one never defined. Construct nouns deliberately kept OUT of `ARTIFACT_NOUNS` — proposing a new way of working is the product; only an EXISTING-resource claim is a fabrication. Schema `bty_guided_program_v2` / `program_authorship_v2`; no migration needed (`proposal_version` has no CHECK). Review fields moved to the shared `AutoTextarea` after the recording showed clipped last lines; new `webkit-mobile` Playwright project proves it in the real engine with a mutation proof (23/23). Repaired literal control bytes that made grep skip `program-authorship.ts` entirely. tsc clean · terminology 44 (0 new) · cf:build clean · full suite 0 new failures. **NOT PASS/CLOSED** — one paid coherence recheck + a physical device gate remain.

**[x] 3.2L-R3-CALL-LEVEL-STRUCTURAL-DIAGNOSTICS — IMPLEMENTED · MIGRATION APPLIED LIVE · STAGING DEPLOYED (2026-08-06; commits `f8d3d3bb` + `088b5e8a`; migration `20260808000000` executed by the Founder via SQL Editor on `mveycersmqfiuddslnrj`; NO paid provider call this turn).** Founder review found the previous migration semantically insufficient: it added all six diagnostic columns to the PARENT `foundry_program_generation_attempts` and never referenced the child call table, so call 1 and call 2 could not retain independent structural diagnostics. Corrected: the five nullable columns (`validation_stage`, `offending_path`, `expected_type`, `actual_type`, `structural_retryable`) now live ONLY on `foundry_program_generation_attempt_calls`, which already carries `call_sequence` — so no `failed_call_sequence` was added and the parent schema is unchanged. Live read-only verification: 5 columns present on the child (HTTP 200), all six absent from the parent (42703), counts still 4 parents / 5 calls, all five new columns NULL on every historical child row, canonical draft `093b0361` unchanged (status draft, step 8, answers hash `c50a643f…1018`, journey absent, `program_id` null). Nine real-service fake-provider gates pass, including two NEW transport gates proving a provider 500 / timeout stays transport-only and is never mislabeled structural. Full suite at the recorded baseline (17 failed / 8 files, 0 new). Staging Worker `3530c4e0` at 100%, `/api/version` = `088b5e8a…701f`. **Structural reliability recheck (one paid window) PENDING — not executed.**

**[x] 3.2I-PRACTICE-R5B1A.1-R2.60 — CANONICAL FIELD-REPAIR ARC — PASS · LIVE FUNCTIONAL CLOSURE (2026-08-02; ONE authorized replay session, 2 provider invocations, 0 retries; documentation-only reconciliation — no source change, no deploy, no migration, no database, no push).** A real provider repair was accepted, server-expanded and merged into a complete review matrix. Artifact `practice-review.boundaryreplay.live.20260802T160030Z.pass2.c18-constrained-clinical.a2.3b87d68bb972.json` sha256 `b971ba04…97d89`, `/6` + observability `/2`, bound to HEAD `dc75a243` (implementation `48a5cf09`, canary `dc75a243`). Counters: 1 replay · 2 invocations (1 review + 1 patch) · 0 retries · 0 legacy · 0 fallback · 0 generation · 0 broad review. Live plan 13 ops / 9 groups / 3 frozen surfaces vs the retained 14/10/2 — VALID PROVIDER VARIATION over an identical frozen subject (subject, surface-map, lineage, source-artifact and alternatives digests all unchanged); one attempt-1 row (`branch[1].action[1]`) came back complete and valid this time and therefore froze instead of needing repair — the frozen semantic miss R2.48/R2.50 recorded, now answered as the human oracle preferred without any prompt change. Response: 8 scalar repairs + 1 group selection `{158644920b4e034a, b0e54cfa5c730e41, reason}` authoring no canonical scalar; server expanded 5 with `canonical_alternative_expansion`; 8+5 = 13 = plan, 0 missing, 0 extra. Matched `governed_action_prerequisite_not_established`, `model_required`, reason shape `<model-prose:43:809e3242b124>`. Accepted · mergeAttempted true · mergeAccepted true · mergedRowInvalid 0 · frozenMutation 0 · matrix 12/12. Final `boundary_review_reject` with `outputContractFailure false` — a valid reviewer verdict after a successful merge, not a repair failure: `branch[1].resulting_world_state` (`resulting_state_missing_prerequisite`, earliestCausal) + `branch[1].action[1]` (`boundary_reopened_after_prior_compliance`); `prohibited_action_present` and `explicit_boundary_contradiction` absent. No credential in 177KB. **Replay budget CONSUMED · no further replay authority.** **Product quality NOT established by this replay** — one reconstructed frozen c18 subject only. Deferred: `state_requirement` (fail-closed, zero captured usage), genuine multi-group fixture, cosmetic runner docstring. Inner commits `f53adef4`…`dc75a243` remain local and unpushed. **R2.48–R2.60 canonical field-repair arc: PASS · CLOSED.**

**[ ] 3.2I-PRACTICE-R5B1A.1-R2.25 — REVIEWER RERUN AUTHORITY + ATTEMPT EVIDENCE CAPTURE — IMPLEMENTED · REPLAY RUNNER PREPARED NOT EXECUTED (2026-07-31; generation-contract tooling only — no live model call, no deploy, no migration, no database, no production endpoint).** From R2.24 forensics: the first complete six-case live run produced ZERO human-usable scenarios, and 4 of 14 attempts ended because the reviewer voted `accept` while its own structured detail fields derived ≥1 defect (`review_verdict_contradicts_details` fires ONLY at `overallVerdict === "accept" && derivedDefects.length > 0`). The pipeline then discarded the scenario and asked the generator for a NEW one with content-free feedback ("Rejection codes: review_contradictory" / "Produce a scenario whose quality is unambiguous") — spending a generation attempt to recover from a REVIEWER defect and reporting it as `generation_rejected`. Two of the four discarded scenarios were, on inspection, serviceable. **Fix:** NEW `domain/foundry/arena-draft/reviewSubject.ts` freezes scenario + confirmed boundaries + active scope + language + review contract (prompt/schema/sampling digest) before the first review and digests them as `reviewSubjectSha256`; both attempts must carry the same value, the digest is RECOMPUTED from content (so keeping a recorded digest while swapping the scenario is caught), and any drift fails CLOSED before the second call. NEW `domain/foundry/arena-draft/reviewRerun.ts` = pure state machine: attempt-1 contradiction → rerun the REVIEWER over the frozen subject (never regenerate); attempt-2 contradiction → NEW terminal `reviewer_terminal_failure`; max 2 review calls + max 2 generation attempts, neither raised; only parsed-but-self-inconsistent codes are rerunnable (truncation/not-JSON/schema stay infrastructure). Inverse contradiction `review_reject_without_defect` handled identically, plus the 4 no-safe self-contradiction codes. `reviewer_terminal_failure` = THIRD outcome class (not content, not infrastructure, non-aborting) with new independent `reviewerHealthy` hard-gate dimension; `executionComplete` stays true when an immutable terminal artifact exists. **Evidence before reduction:** `validateSemanticReview` now returns the parsed DTO + derived defect list on the contradiction branch (it previously discarded `unique`, which is why the contradicting field is permanently unknowable for R2.23D-R4); every attempt records `ReviewEvidence` (parsed DTO, overallVerdict, derivedDefects, consistency, finishReason, truncated, latency, reviewSubjectSha256) and the draft is captured at `review_subject_frozen` before any terminal decision. New metrics counted from attempt records, never log lines: generationCallCount / reviewCallCount / reviewRerunCount / reviewerRecoveredCount / reviewerTerminalFailureCount / generatorRejectedCount / deterministicRejectedCount / semanticRejectedCount / generationRetryCount; deterministic vs semantic rejection separated by presence of a review (both log `gate_level_*`); a reviewer rerun is never a generation retry. **PREPARED NOT EXECUTED:** reviewer-only replay over the 4 historical frozen subjects — `reviewReplay.ts` + `reviewFrozenSubject.ts` + `replayArtifact.ts` + `scripts/practice-review-replay.ts` import NO generation function (generation is unreachable, asserted by test and by a runner grep gate); 6 distinguishable outcomes (consistent_accept / consistent_reject / repeated_contradiction / provider_failure / schema_failure / subject_digest_mismatch); a consistent accept = REVIEWER recovery, never a product-quality pass. Tracked fixture `fixtures/r225-reviewer-contradiction-subjects.json` (sha256 `8f30aaf0…1399`) carries provenance + digests + structure with ALL learner-facing prose redacted (0 unredacted prose fields, asserted); the live replay reads the real scenario from the untracked immutable artifact and verifies `liveScenarioSha256` first. c18 safety fixture records why the fix may never be "trust the reviewer's top-level verdict". Mock replay executed: 4/4 subjects, 4 immutable artifacts, 0 generation calls. Runner `/tmp/r225_reviewer_replay_canary.sh` (sha256 `6acc7c99…5c08`) bound to final HEAD `a473ecc5` + manifest `fd11c535…cb69` + fixture + runtime + 4 subject digests — **NOT EXECUTED**. Verify: npm ls OK; tsc clean (app + scripts); terminology 44 baseline; targeted 18+26+11 new tests pass; full suite 16 failed / 7 files = BASELINE UNCHANGED, 0 new (7150 passed, +55); cf:build complete. Migration NONE. Inner `a473ecc5` == origin/inner-main, tracked tree clean. Six R2.23D-R4 artifacts re-verified byte-identical. **Deferred (measured, out of scope): (A) non-clinical urgency-gate overreach in c01 · (B) c09 phase repetition + primary-question re-asking · (C) c18 generator failure to keep boundaries binding · (D) construction-metadata burden · (E) model routing.** Next: Commander verifies the runner digest and authorizes ONE four-subject reviewer-only execution.

**[ ] 3.2I-PRACTICE-R5B1A.1-R2.23D-R4 — POST-CREDENTIAL EXECUTION WIRING + FULL MOCK RUN AUTHORITY — RUNTIME WIRING PROVEN · LIVE RUN NOT YET EXECUTED (2026-07-31; generation-contract tooling only — no deploy, no migration, no database, no production endpoint, no live provider call).** R2.23D-R3 cleared all 22 bound-contract checks and both provider capability checks, then died at the EXECUTION block on `EXPECT_MANIFEST: unbound variable` — AFTER an operator had already entered a credential. Root cause (measured, not inferred): R2.23D-R1 replaced the `EXPECT_*` shell variables with `check '<label>' '<path>' '<expected>'` lines and only `EXPECT_HEAD` survived (the git comparison needs it); R2.23D-R3 then wrote the orchestrator invocation as `--manifest "$EXPECT_MANIFEST"`, against a naming convention that no longer existed. Measured on the installed R3 file: `grep -o 'EXPECT_[A-Z_]*'` → 4×`EXPECT_HEAD`, 1×`EXPECT_MANIFEST`; `grep -n '^EXPECT_[A-Z_]*='` → line 28 `EXPECT_HEAD` only. It survived every test because the tests asserted strings were PRESENT and `--credential-boundary-check` exited before the line could execute under `set -u`. **Fix — the shell stops reconstructing the contract after the credential prompt:** NEW `src/lib/bty/foundry/arena/runtimeConfig.ts` = one validated JSON config carrying every non-secret run value under a single canonical name `contractManifestSha256` (no alias, no env fallback, no default; missing value = refusal; secret-shaped fields REFUSED not stripped) + NEW `scripts/practice-runtime-config.ts` emits+validates it and prints its frozen SHA-256; orchestrator and collator each take one `--config <path>` parsed by that same module, so a drifted name is a parse error instead of a runtime death; `caseArtifact.ts` gains `mode: "mock"|"live"` in the FILENAME and the PAYLOAD (path now 9 dot-parts) and the collator filters by it, so mock evidence can never enter a live collation (proven in all 3 directions: live collation over a mock dir → 0/6 INCOMPLETE exit 6; live config + mock transport → refused; mock config without mock transport → refused). **The runner now proves its runtime the way it proves its contract — by executing it, before any credential:** builds+validates BOTH configs, then runs the exact tracked provider preflight, orchestrator and collator end to end on a mock transport — 6 real immutable artifacts written and collated — and blocks the live run on any wiring failure (`RUNTIME WIRING FAILED · LIVE RUN BLOCKED`, exit 7). **Tests now check BINDING, not presence:** a static audit resolves every shell expansion against its declaration (+`bash -n`, +`set -Eeuo pipefail` asserted) and is run against the INSTALLED file, with the regression proved against REALITY — the audit names `EXPECT_MANIFEST` in the still-present R3 runner (exit 1) and clears R4 (exit 0). **Measured run** `/tmp/r223d_r4_live_practice_stability_canary.sh --credential-boundary-check` → exit 0, all 11 preflight steps, `PREFLIGHT CONTRACT PASS · CREDENTIAL NOT REQUESTED`, `PROVIDER PREFLIGHT MOCK PASS · LIVE PROVIDER NOT CALLED`, `RUN mock-20260801T020320Z · completed 6/6`, `FULL STABILITY MOCK PASS · 6/6 CASES`, `COLLATION MOCK PASS · 6 IMMUTABLE CASE ARTIFACTS`, `MOCK EVIDENCE DISCARDED · LIVE PRODUCT QUALITY NOT MEASURED` — i.e. it ran PAST the exact line where R3 died. **Failed-run artifact directory, measured not inferred:** `.eval-artifacts/` exists with 2 entries (`practice-generation.canary.json`, `practice-generation.latest.json`, both from earlier slices); case artifacts = 0 live, 0 mock, 0 total; no `live_practice_stability_*` result/review file exists. Source ordering proves the orchestrator process never started (the shell aborted expanding line 230 before exec at line 227), so LIVE GENERATION REQUESTS = 0; the live provider preflight at line 211 DID run with a real credential = 2 capability requests. Verify: tsc clean; terminology 44 (baseline, no regression); runner/runtime suites 133/133 + 20/20 contract; full suite 16 failed / 7 files = BASELINE UNCHANGED, 0 new (7065 passed, +37 new tests). Migration NONE. Inner `31a8ab83` + `341c20e9` pushed to origin/inner-main; runner regenerated at binding HEAD `341c20e9`, manifest `d816a3dc…`; R2.23D-R3 runner PRESERVED unchanged. **LIVE RUN NOT EXECUTED — no product-quality evidence exists; live execution requires a separate Founder authorization.**

**[ ] 3.2H-FOUNDRY-PRACTICE-SCENARIO-QUALITY-V1 — DIFFICULT-CHOICE GENERATION CONTRACT — IMPLEMENTED LOCALLY · VERIFIED · DEPLOYMENT PAUSED (2026-07-29; no commit/push/deploy/migration/data mutation; Founder authorized local-only until report review + staging LLM key/model verification + explicit deploy authorization).** Generated Practice scenarios read as a correct-answer quiz (one responsible option vs an obviously careless/passive one) → now a genuine tradeoff. C-refined placement unchanged; wording-polish "R1" PARKED (reverted, reconstructable). Extend, not replace: (1) LLM prompt rewritten to the difficult-choice contract (every option = legitimate protected value + concrete action + benefit + real cost; ≥2 competing values; no do-nothing; balanced; no correct/incorrect/moral/answer-key; compliance-under-pressure framing; `isActionCommitment` internal only); (2) deterministic template (en+ko) rewritten to two-values-in-tension, passes the gate; (3) NEW pure `domain/foundry/arena-draft/quality.ts` `validateDifficultChoice` (deterministic anti-pattern rejection: moral/answer-key language, hollow throwaway, action act-vs-avoidance, extreme asymmetry); (4) gate wired into generation → AI failing it falls back to the passing template (never silent), quality warnings additive. Feedback = GATE ONLY (no schema/player change; on-screen feedback deferred). Verify: tsc clean; terminology 44 (no regression); quality+gen tests 19/19; arena-draft+foundry/arena 100/100; full suite 6105 pass / 16 fail ALL PROVEN PRE-EXISTING (stash-to-baseline reproduces identical 16; 0 new); cf:build complete. Migration NONE. Files: `arenaScenarioGenerationService.ts`, `arenaScenarioTemplate.ts`(+test), NEW `quality.ts`+`quality.test.ts`. Founder review set = 5 deterministic golden fixtures in `quality.test.ts` (protected-value/cost annotations for review/tests only). Status IMPLEMENTED LOCALLY · VERIFIED · DEPLOYMENT PAUSED — no Worker UUID/staging/live-output/device PASS claimed. Next: Founder report review → LLM key/model verify → deploy authorization.

**[x] 3.2G-TODAY-LEADERSHIP-ATTENTION — TODAY LEADERSHIP ATTENTION — PASS · CLOSED (2026-07-30, FINAL PARENT CLOSURE; Founder physical-iPhone gates PASSED; documentation-only reconciliation — no code/deploy/migration/data mutation).** A Host/leader sees who needs attention on Today, enters the correct follow-up on the FIRST tap, never leaves the app, and returns to Today without losing context. Founder device (closing revision R4): R4-A cold first entry (first tap, no Safari/reload/Learn-home flash/empty body; control room or "Opening follow-up…" immediately, no 2nd tap) · R4-B repeated entry identical · R4-C Today return in-shell · R4-D Learn preservation · R4-E account boundary (no prior-account row/resolving/control flash) · R4-F shell continuity = ALL PASS · DEVICE. Final product: Today shows canonical FOLLOW_UP_OVERDUE/NEEDED rows (label·training·reason, server order, preview-3+Show more, empty→no shell, SHARED_REVIEW_DUE separate count, no private body); rows are semantic in-shell buttons (no raw anchor/Safari/reload); first Learn-bound render is control-room-bound (no home flash) with "Opening follow-up…" while resolving (no empty body), distinct ready/unavailable/authz states; Today→Back→Today, Learn→Back→Learn, direct deep-link init preserved; account switch clears pending control state, server authz canonical. Inner `b7b1a1b4` == origin/inner-main; staging Worker `9788d74b-1a57-4c79-aca7-77325200cba0` (100%) = build of `b7b1a1b4` (BUILD_ID `DWm1I_gZHSv0hfkPDyi5L`); outer ledger `a1e11c25` intact (descendant `796323d3` = unrelated bty-karaoke); production UNCHANGED; migration NONE. **3.2G-TODAY-LEADERSHIP-ATTENTION PASS · CLOSED (R4 PASS · CLOSED).** R1/R2/R3/R4 = superseded implementation arc, not re-marked as separate closed slices.

**[ ] 3.2G-TODAY-LEADERSHIP-ATTENTION-R4 — CONTROL-BOUND RESOLVING SURFACE V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-30, bounded loading-surface fix inside OPEN 3.2G; no server/query/schema change; no migration; production unchanged).** R3 removed the home flash but Founder device showed the Learn header over an EMPTY body ~1s while the Event snapshot loaded. Root cause: `FoundryEventControlRoom` event-null branch returned `<div aria-hidden min-h-[40vh]/>` (EVENT FETCH LOADING HAS NO SURFACE); deep-link control view has no initialSnapshot. Fix (no timers): render a compact control-bound resolving surface immediately — "Opening follow-up…" / "후속 조치를 여는 중…" (role=status, aria-live=polite, aria-busy; testid `control-room-resolving`); swap to the control room when ready; once SETTLED without an owned event (error/not-found/empty) show a calm `control-room-unavailable` (never spins forever, never disguises authz as loading). `useEventSnapshot` gains a `settled` flag. Back available throughout. Server authz + R2 button + R3 control-first + R1 Back + direct-URL + Learn-origin all preserved; no hostAttentionService/API/domain/DB/migration change. Verify: inner `b7b1a1b4` == origin/inner-main; tsc + cf:build clean; tests — control-room resolving suite (resolving w/ role+aria-busy while loading, no home content; → real room no 2nd click; 403 + settled ok-null → unavailable; initial snapshot → no resolving; KO) + composition (Today→tap→control room real content "배가 고파", never empty) + R3 Profiler + TodayHome/hostDeepLink/FoundryEventRooms pass; full suite 16 pre-existing / 0 new / 6092 passed. Staging: Worker `bty-arena-staging` active Version `9788d74b-1a57-4c79-aca7-77325200cba0` (100%), deployed 2026-07-30T02:30:41.815Z, staging only NOT production; live BUILD_ID `DWm1I_gZHSv0hfkPDyi5L` == local, live chunk `page-6016ab4d1e87d5af.js` (200) carries `control-room-resolving`/"Opening follow-up"/`today-followup-open`, `&from=today` producer gone (0). Device recheck PENDING (R4-A cold first entry no empty body · R4-B repeated · R4-C Today return · R4-D Learn preservation · R4-E account boundary · R4-F shell continuity). Parent 3.2G NOT closed.

**[ ] 3.2G-TODAY-LEADERSHIP-ATTENTION-R3 — ATOMIC CONTROL-ROOM HANDOFF V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-30, bounded transition-continuity fix inside OPEN 3.2G; no server/query/schema change; no migration; production unchanged).** R2 kept entry in-shell but Founder device showed the Learn root flashing ~0.75s before the control room (first + repeated entry). Root cause: `FoundryEventRooms` init `view={kind:"home"}` + switched to control in a post-render effect → first commit painted home. Fix (no timers): lazy-init the view from the pending target via pure `computeInitialFoundryView` so the FIRST render is the control room; keep the one-shot effect (functional updater bails when lazy-init already opened the room → no redundant render) to cover the late-target race + consume params once. Direct-URL init (now atomic), R1 origin-aware Back, R2 button entry, Learn-origin home — all preserved. No hostAttentionService/API/domain/DB/migration change. Verify: inner `d9891266` == origin/inner-main; tsc + cf:build clean; tests — `computeInitialFoundryView` unit + per-commit Profiler (control "←" from FIRST commit + every commit; catches the flash) + no-target→home + TodayHome/hostDeepLink/FoundryEventRooms + R2 composition pass; full suite 16 pre-existing / 0 new / 6085 passed. Staging: Worker `bty-arena-staging` active Version `50e2cb85-cb14-4b58-bdc3-1d3b48913bb8` (100%), deployed 2026-07-30T00:18:42.668Z, staging only NOT production; live BUILD_ID `aieOhch0LFJKoFxcGDGlo` == local, live chunk `page-3f2d3340c50010b1.js` (200) carries `today-followup-open`, `&from=today` producer gone (0). Device recheck PENDING (R3-A cold first entry / no Learn flash · R3-B repeated entry · R3-C Today return · R3-D Learn preservation · R3-E account boundary · R3-F shell continuity). Parent 3.2G NOT closed.

**[ ] 3.2G-TODAY-LEADERSHIP-ATTENTION-R2 — FIRST-TAP IN-SHELL FOLLOW-UP ENTRY V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-29, bounded transport fix inside OPEN 3.2G; no server/query/schema change; no migration; production unchanged).** Founder device: from Today, the FIRST tap on "배가 고파" escaped via the web path (full document nav/remount); later taps sometimes stayed in-shell. Root cause: row was `<a href={deepLink+'&from=today'}>` → RAW ANCHOR HARD NAV + PRE-HYDRATION ACTIVATION (WKWebView navigation policy handles the cold first tap). Fix: TodayHome renders a `<button>` (no href); on tap derives the canonical `{eventId,section,focusId}` from the SERVER deepLink via shared `parseHostDeepLink` and calls `onOpenLeadershipFollowUp`; BtyDailyAppShell sets existing control-room state + `hostReturnTab="today"` + `setTab("learn")` — no href/window.location/reload/URL. Direct-URL deep-link init untouched; R1 origin-aware Back preserved. No hostAttentionService/API/domain/DB/migration change; SHARED_REVIEW_DUE count, reviews→Practice, ordering, preview-3+Show more, learner Today, QR/XP/Practice/account-switch untouched; no PII. Verify: inner `b71e5ae5` == origin/inner-main; tsc + cf:build clean; focused tests (button-not-anchor; first activation → callback once with structured target; NEEDED→shared-understanding; no window.location) + shell-composition (mounted Today → first tap → control room in-shell → Back → Today, no doc nav, no 2nd tap) + hostDeepLink + FoundryEventRooms pass; full suite 16 pre-existing / 0 new / 6082 passed. Staging: Worker `bty-arena-staging` active Version `2b490001-a87c-4ae9-ac33-0f89acc0a45d` (100%), deployed 2026-07-29T23:59:09.080Z, staging only NOT production; live BUILD_ID `Ujz5KS2RzfnB9Vh49C2UA` == local, live chunk `page-0b94d69cdd0e3c1d.js` (200) carries `today-followup-open`, `&from=today` producer gone (0). Device recheck PENDING (R2-A cold first tap · R2-B repeated entry · R2-C Today return · R2-D Learn preservation · R2-E relaunch/account boundary · R2-F shell continuity). Parent 3.2G NOT closed. (Baseline: outer was 1 unrelated bty-karaoke commit ahead; R1 ledger `3841fed3` intact.)

**[ ] 3.2G-TODAY-LEADERSHIP-ATTENTION-R1 — TODAY-ORIGIN RETURN PATH V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-29, bounded navigation fix inside OPEN 3.2G; no server/query/schema change; no migration; production unchanged).** Founder device: 3.2G rows render + open the correct control-room detail, but Back from the detail (opened from Today) landed on the Learn root, not Today. Root cause: control-room `onBack={backHome}` had no origin. Fix (canonical target unchanged): TodayHome tags the follow-up href with bounded `from=today`; `hostDeepLink` parses it into `returnTab` (closed enum `'today'|null`; hostile/unknown → null, no authz/redirect); shell carries it one-shot, strips `?from` on consume, passes `onReturnToOrigin={()=>setTab('today')}`; `FoundryEventControlRoom` Back = origin-aware (today→Today in-shell, no reload/dup-history; all other entry → existing Learn-home back). No hostAttentionService/API/domain/DB/migration change; SHARED_REVIEW_DUE count, reviews→Practice, learner Today, QR/XP/Practice/account-switch untouched; TodayPersonalBrief not rebuilt. Verify: inner `00740049` == origin/inner-main; tsc + cf:build clean; focused tests (parse from=today + hostile→null; FoundryEventRooms Today-origin→onReturnToOrigin / Learn-origin→home; TodayHome href = deepLink+`&from=today`) pass; full suite 16 pre-existing / 0 new / 6079 passed. Staging: Worker `bty-arena-staging` active Version `8a642634-d163-4d4c-b8ba-caca2a00ca13` (100%), deployed 2026-07-29T23:18:52.106Z, staging only NOT production; live BUILD_ID `suGtA_1MsDvWK7vDfJR37` == local, live chunk `page-93d8d77fe2a66c71.js` (200) carries `from=today`/`onReturnToOrigin`/`returnTab`/`today-followup-row`, no retired surface. Device recheck PENDING (R1-A Today return · R1-B Learn preservation · R1-C shell continuity). Parent 3.2G NOT closed.

**[ ] 3.2G-TODAY-LEADERSHIP-ATTENTION — RESTORE HOST FOLLOW-UP ATTENTION ON TODAY — IMPLEMENTED · STAGING DEPLOYED · DEVICE GATE PENDING (2026-07-29, UI render-only; no server/query/schema change; no migration; production unchanged).** From read-only 3.2G-PREFLIGHT (baseline `d08a124b` no-drift). Gap: Host/leader Today never rendered overdue/needed learner follow-ups — `TodayHome` fetched `hostAttention` but narrowed the type to `{stableId,category}` and rendered only the `SHARED_REVIEW_DUE` count; the `FOLLOW_UP_*` rows (participant/training/reason/deepLink already on the `/api/me/today/brief` wire) were dropped after the four-tab shell refactor (shipped originally at 3.1B-3L; renderer left only in unmounted `TodayPersonalBrief`). Fix (`TodayHome.tsx` + i18n): render `FOLLOW_UP_OVERDUE`/`FOLLOW_UP_NEEDED` as compact deep-linked rows under one "Your team needs you" header (preview-3 + Show more), server domain-priority order preserved by filter (no UI re-rank), reviews count row unchanged (same testids + Practice nav), no double-count, no PII. No API/domain/migration change; retired `TodayPersonalBrief` NOT rebuilt; no Practice/Event/QR/XP/account change. Verify: tsc + cf:build clean; +7 TodayHome tests pass; full suite 16 pre-existing / 0 new / 6074 passed; built bundle carries new copy + `today-followup-row`. Inner `52a3f843` == origin/inner-main (2 approved files only), rebuilt + deployed to staging Worker `bty-arena-staging` — active Version `12aeafb2-3e0b-42bb-b88f-b1a3a9882962` (100%), deployed 2026-07-29T22:37:37.839Z, staging only NOT production; live client chunk `page-b30b225acbde3b12.js` (200) carries `today-followup-row`/`today-host-attention`/new copy, no retired surface (live-verified). Remaining: Founder device gates G-A…G-E. Founder deferred-direction: Practice stays rehearse-only (Practice=rehearse · Today/Reality=act · Me=remember); a future slice MAY add a "Do this for real" Action-Contract bridge. See [[project_slice_32g_preflight_selection]].

**[x] 3.2F-EVENT-PARTICIPANT — EVENT PARTICIPATION IN THE ME WEEKLY STORY — PASS · CLOSED (2026-07-29, FINAL PARENT CLOSURE; Founder consolidated physical-iPhone gates PASSED; documentation closure only — no code/deploy/migration/data mutation).** GATE P PERSISTENCE PASS · DEVICE (force-quit → relaunch under ddshanbit restored "1 event joined" + same seven-day attendance; count stable; no touch ring). GATE I ACCOUNT ISOLATION PASS · DEVICE (second account showed only its own count/attendance; no prior-account flash; switching back restored ddshanbit's). Final product: This Week = static summary incl. participant-owned "N event(s) joined", no popup; Me Orb = attendance-only popup (seven-day states + active-day count, living-light markers, no full summary/Event titles/touch ring); participant Event visible in own week, account-isolated, survives relaunch, no other-participant PII, no Host/QR/scan change, no Core-XP exposed. Inner `d08a124b` == origin/inner-main; staging Worker `3c3d7da1-55fe-4b2d-abd0-035de75a003b` (100%); prior closure `cd80d78a` intact; anon summary+detail 401; production UNCHANGED; migration NONE. **3.2F-EVENT-PARTICIPANT PASS · CLOSED — participant loop complete (Host loop closed at 3.2E-EVENT-HOST).** Obsolete R1/R2/R4 + ORB-R1/R2 = superseded implementation history, not re-marked.

**[x] 3.2F-ORB-WEEKLY-ATTENDANCE-R3 — REMOVE VISIBLE ORB TOUCH RING V1 — PASS · CLOSED (2026-07-29, Founder physical-iPhone recheck PASSED; documentation closure only — no code/deploy/migration/data mutation).** Founder device: R3-A touch appearance (no gray oval) · R3-B close/reopen · R3-C visual preservation · R3-D interaction reliability = PASS · DEVICE; R3-E web keyboard focus = PASS · SOURCE/TEST-PROVEN (not hardware-keyboard device evidence). Inner `d08a124b` == origin/inner-main; staging Worker `3c3d7da1-55fe-4b2d-abd0-035de75a003b` (100%); outer ledger `dcaa012e` intact; production UNCHANGED; migration NONE. **3.2F-ORB-WEEKLY-ATTENDANCE-R3 PASS · CLOSED.** Parent 3.2F-EVENT-PARTICIPANT NOT closed — persistence (force-quit/relaunch) + formal account-isolation gates not yet recorded PASS; R1/R2/R4 + ORB-R1/R2 remain DEVICE RECHECK PENDING.

**[ ] 3.2F-ORB-WEEKLY-ATTENDANCE-R3 — REMOVE VISIBLE ORB TOUCH RING V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE VISUAL RECHECK PENDING (2026-07-29, interaction-visual polish inside OPEN 3.2F; no server/query/schema change; no migration; production unchanged).** Tapping the Orb exposed a large gray oval around the Orb + caption. Root cause: the Orb button's focus-visible:ring-2 ring-white/40 on a rounded-full wrapper fired on iOS touch, and WeeklyOrb's caption sits INSIDE the button → ring wrapped orb+caption. (.bty-arena-area global rule ruled out — Me shell outside that scope.) Fix: large hit target stays but is invisible — appearance-none, border-0, bg-transparent, no rounded-full, tap-highlight transparent, no focus-visible ring on the wrapper; web keyboard focus preserved via an Orb-local inner ring gated to [@media(pointer:fine)]:group-focus-visible:ring-2 (never on touch, never the caption). Interaction/popup/markers/data/detail-timing/This Week/main Orb unchanged. Inner HEAD `d08a124b`; staging Worker `3c3d7da1-55fe-4b2d-abd0-035de75a003b` (100%); production UNCHANGED; NO migration. Tests: R3 contract (no ring/border/oval on wrapper, aria-expanded toggles no ring, fine-pointer Orb-local keyboard focus, first tap opens); 17 attendance-suite pass; full suite 16 pre-existing/0 new/6067 passed; tsc+cf:build clean; built CSS gates ring behind @media(pointer:fine), wrapper has no focus-visible ring; anon detail 401. **Device visual recheck PENDING (R3-A…R3-E) — 3.2F NOT closed.**

**[ ] 3.2F-ORB-WEEKLY-ATTENDANCE-R2 — RESTORE LIVING LIGHT ATTENDANCE MARKERS V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE VISUAL RECHECK PENDING (2026-07-29, marker-only visual fix inside OPEN 3.2F; no server/query/schema change; no migration; production unchanged).** R1 interaction/data PASS but active markers looked like gold donuts. Root cause: the R1 marker put a navy ● glyph on a gold span → gold ring + dark center. Restored the proven filled-gold light (R3 me-week-days / MeThisWeek me-week-dots = solid bg-[#E5B769], no glyph) + a soft amber glow via layered box-shadow (gold #E5B769=rgb(229,183,107); static, no images). Active = filled warm-gold core + bloom + haze; rest = smaller dim filled point (bg-white/20), no glow, no ring, no red. Distinguishable by size+luminosity+glow+aria. This Week + Orb interaction + detail-fetch timing + attendance rules unchanged. Inner HEAD `62299b35`; staging Worker `b43635df-af1e-49f4-a392-87279855009a` (100%); production UNCHANGED; NO migration. Tests: R2 living-light assertions (active filled+glow, no border/glyph; rest dim/filled/no red; no ●/○ glyph); 34 attendance-suite pass; full suite 16 pre-existing/0 new/6063 passed; tsc+cf:build clean; bundle carries living light, drops both donut glyphs; anon detail 401. **Device visual recheck PENDING (R2-A…R2-E) — 3.2F NOT closed.**

**[ ] 3.2F-ORB-WEEKLY-ATTENDANCE-R1 — ORB-OWNED WEEKLY ATTENDANCE V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-29, bounded Orb attendance-disclosure inside OPEN 3.2F; no server/query/schema change; no migration; production unchanged).** Founder: an inert Orb lacks meaning → the Orb owns ONE role: tap to reveal the seven-day ATTENDANCE rhythm (attendance only; NOT the removed full popup — new id me-attendance-popup). Rule: "This Week is the summary; the Orb reveals attendance." Orb: presence → semantic attendance button (aria-label "Show weekly attendance", aria-expanded/controls, keyboard, press feedback, one canvas); ✕/Escape close + return focus. Popup: "Weekly attendance" title + week range + 7 weekday states (filled ●/hollow ○, distinguishable w/o color + aria) + "N active days"; NO points/learned/created/Center/action/events/XP/Stage. This Week unchanged static summary. Data: canonical dated 7-day attendance only in ?detail=1 → fetched LAZILY on Orb activation (never on mount), reading ONLY attendance; weeklyActivity.server.ts untouched; refreshKey drops attendance on account switch. Inner HEAD `1b16ffe5`; staging Worker `56a290b3-a4c3-4352-8d6f-343ddbc888f9` (100%); production UNCHANGED; NO migration. Tests: corrected R4 Orb-inert assertions + new ORB-ATTENDANCE-R1 suite (role separation, button semantics, lazy fetch never-on-mount, attendance-only content, no summary categories, close/reopen, one canvas, isolation); 43 Me-suite pass; full suite 16 pre-existing/0 new/6061 passed; tsc+cf:build clean; bundle carries attendance popup, none of the removed full-detail UI; anon detail 401. **Device recheck PENDING (Gates A–E) — 3.2F NOT closed.**

**[ ] 3.2F-EVENT-PARTICIPANT-R4 — REMOVE REDUNDANT WEEKLY POPUP V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-29, bounded simplification inside OPEN 3.2F; no server/query/schema change; no migration; production unchanged).** Data/isolation PASS (A "1 event joined", B "0 events joined"); Founder found the popup redundant (card already holds the summary). Rule: "This Week is the summary; the Orb is the presence; neither needs a popup." This Week: disclosure button → STATIC summary (not a button/focusable/expandable; testid me-week-summary); Orb unchanged non-interactive presence; weekly popup removed entirely (render, ✕ close, open state, disclosure wiring, controlled plumbing, placement, and the popup-only ?detail=1 fetch + local detail state + session detail cache). Server untouched (weeklyActivity.server.ts, weekly-activity endpoint incl. ?detail=1 retained, owner-scoping, 05:00 window, isolation, anon auth). Inner HEAD `273d9f6b`; staging Worker `8cb85796-571d-4539-82fe-2943b330b9e0` (100%); production UNCHANGED; NO migration. Tests: deleted the 3 popup suites, trimmed shared suites, added focused R4 suite (static summary + joined copy + isolation + Orb inert + NO popup in DOM + no ?detail=1); 33 Me-suite pass; full suite 16 pre-existing/0 new/6051 passed; tsc+cf:build clean; bundle drops all popup trigger/copy paths, keeps summary chip; anon summary+detail both 401. **Device recheck PENDING (R4-A…R4-E) — 3.2F NOT closed.**

**[ ] 3.2F-EVENT-PARTICIPANT-R3 — ME WEEKLY MEANING + DISCLOSURE SEPARATION V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-29, UI-semantics + disclosure-ownership fix inside OPEN 3.2F; no server/query/schema change; no migration; production unchanged).** Two product-meaning failures fixed: (1) copy did not read as the user's own participation; (2) the This Week card and the 7-Orb opened the same popup (duplicate affordances). Lock: "This Week is the record; the Orb is the presence." (A) Card = SOLE popup trigger; the 7-Orb is non-interactive presence (role=img, not a button, not focusable, no aria-expanded/controls); popup gains its own ✕ close control. (B) Copy: "N event(s) joined" (chip), "Events you joined" (section), "Joined <date>" (item) — calm/factual, no mastery/completion/verified/ownership/ranking/XP; canonical title + fail-soft preserved. Inner HEAD `6457400c`; staging Worker `ab125d49-ac59-4064-8952-f97a9b2d3bba` (100%); production UNCHANGED; NO migration. FROZEN: weeklyActivity queries/owner-scoping/window/title resolution, scan, QR, Universal Links, Host Event flow, Orb /start ritual, Today/Learn/Practice, Core XP, schema. Tests: rewrote every suite that encoded the wrong "Orb opens popup" contract to card-owned; new R3 shell-integration suite (joined-copy meaning + card-sole-trigger + non-interactive Orb + close/reopen); 54 Me-suite pass; full suite 16 pre-existing/0 new/6074 passed; tsc+cf:build clean; bundle carries "Events you joined", drops orb-toggle; anon 401. **Device recheck PENDING (R3-A…R3-G) — 3.2F NOT closed.**

**[ ] 3.2F-EVENT-PARTICIPANT-R2 — ME WEEKLY EVENT CONTENT DELIVERY V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-29, on-device visibility/layout fix inside OPEN 3.2F; no migration; production unchanged).** R1-A opening PASS but R1-B Event content FAIL: popup showed only attendance, no Events section/title/date. Traced end-to-end before editing: server PROVEN correct (live probe replicating userDayStartInstant/05:00 → eventsParticipated=[{title:"Test"}], count=1 under the account's real tz America/Los_Angeles AND fallbacks; route spreads ...detail; deployed bundle contains me-week-events render). First break = EVENTS SECTION CLIPPED BELOW VIEWPORT: fixed max-h-[42vh] is unreliable in iOS WKWebView, attendance consumed the height. Fix (MeWeeklyTrace only): dynamic-viewport max-h-[min(70dvh,32rem)] + overscroll-contain + safe-area bottom pad (scrollable contained popup) + Events section surfaced ABOVE the attendance grid (always visible on open). Unified popup preserved; query/window/scan/QR/Orb/schema untouched. Inner HEAD `4e8507c0`; staging Worker `20b08eff-32e1-4c74-b366-3c1ff1381318` (100%); production UNCHANGED; NO migration. Tests: real shell-integration path (fetch ?detail=1 → shell → MeWeeklyTrace → Events) + chip + ordering + scroll-container + account isolation + fail-soft + no-PII; 46 Me-suite pass; full suite 16 pre-existing/0 new/6066 passed; tsc+cf:build clean; anon 401. **Device recheck PENDING (R2-A…R2-F) — 3.2F NOT closed.**

**[ ] 3.2F-EVENT-PARTICIPANT-R1 — ME THIS WEEK DETAIL ENTRY RELIABILITY V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE RECHECK PENDING (2026-07-29, interaction-boundary fix inside OPEN 3.2F; no migration; production unchanged).** Device gate proved the Me "This Week" popup did not open on tap. First break = TAP HANDLER NOT WIRED: the labeled `MeThisWeek` card was display-only; the only trigger was the unlabeled `WeeklyOrb`. Fix: the labeled card is now a semantic disclosure `<button>` (full-card hit target, normal tap, keyboard, aria-expanded/controls) driving ONE shared popup state lifted into the Me shell so the card AND the Orb open the same popup; `MeWeeklyTrace` gains optional controlled `open`/`onOpenChange` (uncontrolled Orb preserved); card inner → phrasing spans for valid button content. Inner HEAD `0c17754d`; staging Worker `343a4db9-4a13-43e7-ba18-c5a94cef9126` (100%); production UNCHANGED; NO migration. FROZEN: weeklyActivity query/owner-scoping/window/title-resolution, scan, QR, Universal Links, Host Event flow, Orb entry, Today, Core XP, nav, schema. Tests: 11 new (first-tap open, content+Events, empty/undefined non-blocking, close/reopen, shared single dialog, other Me controls, controlled/uncontrolled semantics); 36 across Me suites pass; full suite 16 pre-existing/0 new/6056 passed; tsc+cf:build clean; anon endpoint 401. **Device recheck PENDING — 3.2F NOT closed.**

**[ ] 3.2F-EVENT-PARTICIPANT — EVENT PARTICIPATION IN THE ME WEEKLY STORY V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE GATE PENDING (2026-07-28, read-only weekly-projection extension; no migration; production unchanged).** Closes the participant half of the Reality Event loop: scan → recorded → visible in the participant's OWN Me weekly story. Inner HEAD `93143c41`; staging Worker `76c3d480-3686-4bcd-bbf7-8f7e3f2bf671` (100%); production UNCHANGED. Extended canonical `weeklyActivity.server.ts`: `eventsParticipated` count (owner-scoped by user_id, same 7-day/05:00 window, fail-soft) + detail item list `{title,date}` (title via bty_events, newest-first, no event_id/user_id/token/other-participant/XP). UI: MeThisWeek "N events" chip + MeWeeklyTrace popup Events section (own title+date, omit-when-empty). Semantics = verified presence, not completion/mastery/XP; no Orb/Today redesign. FROZEN: scan/QR/idempotency, Host My-events/detail/roster, Foundry, Practice, Program, Field Actions, Today, Orb, schema. Live read-only proof: 3 participants owner-scoped, out-of-window excluded, titles resolve, non-participant→0, anon 401. Tests: server owner-scope/window/boundary/newest-first/zero/fail-soft/no-PII + UI chip/popup; regression 1103; full suite 16 pre-existing/0 new; tsc+cf:build clean. **Device gate PENDING — NOT closed.**

**[x] 3.2E-EVENT-HOST-R2 — PARTICIPANT IDENTITY RESOLUTION + QR ACCESSIBILITY — PASS · CLOSED (2026-07-28, Founder physical-device recheck PASSED; closes the 3.2E-EVENT-HOST Host post-event view: V1 + R1 detail/QR-reopen/roster + R2 identity).** Inner closure HEAD `0f08a137`; staging Worker `11067ff7-0f5d-42da-a10e-a3b79aa678a3` (100%); production UNCHANGED; no migration. Device PASS: card→detail in-shell, QR reopens+scans, owner-only distinguishable roster (name/email) + timestamp, repeat scan idempotent, count/roster no duplicate, relaunch preserves labels, QR token accessibility/privacy engineering-proven. Contract: display_name→full_name→name→email→generic (email as displayName), bounded per-user getUserById (no listUsers), owner 404, payload {displayName,participatedAt} only. **3.2E-EVENT-HOST PASS · CLOSED.**

**[ ] 3.2E-EVENT-HOST-R2 — PARTICIPANT IDENTITY RESOLUTION + QR ACCESSIBILITY — IMPLEMENTED · STAGING DEPLOYED · DEVICE GATE PENDING (2026-07-28, bounded device-gate revision; roster labels were all "Participant"; no migration; production unchanged).** Inner HEAD `0f08a137`; staging Worker `11067ff7-0f5d-42da-a10e-a3b79aa678a3` (100%); production UNCHANGED. Root cause: scan-created participants have arena_profiles.display_name NULL. Locked precedence (server, after owner auth): display_name → auth metadata full_name → name → email → null (client "Participant"). Missing users resolved via BOUNDED per-user admin.auth.admin.getUserById (deduped) — NOT directory-wide listUsers. Payload still { displayName, participatedAt } only (email as displayName when fallback; NO user_id/creator_id/org/token-hash/Core-XP). Owner auth unchanged (non-owner 404, anon 401). QR accessibility: removed the sr-only btyev1 text node (detail + create); QR wrapper role=img aria-label + svg aria-hidden; token only as QR image value. Tests: API precedence/distinct/Korean/deduped-getUserById/no-listUsers/no-PII/owner-404 + component (distinct labels/email-label/no-ids/no-btyev1-in-DOM/QR-aria); regression 570; full suite 16 pre-existing/0 new; tsc+cf:build clean. **Device recheck PENDING — 3.2E-EVENT-HOST NOT closed.**

**[ ] 3.2E-EVENT-HOST-R1 — HOST EVENT DETAIL + QR REOPEN + PARTICIPANT ROSTER — IMPLEMENTED · STAGING DEPLOYED · DEVICE GATE PENDING (2026-07-28, device-gate revision; static cards → tappable owner detail; no migration; production unchanged).** Inner HEAD `dcd95a57`; staging Worker `06bd54e2-4872-4a2e-b79b-3770e03d8e7c` (100%); production UNCHANGED. New owner-scoped `GET /api/bty/events/mine/{eventId}` (nested under mine/ to avoid opennext shadowing; id+creator_id scope → non-owner/unknown 404; anon 401). Payload = event {…,participationCount,qr:{available,payload}} + participants [{displayName, participatedAt}]; identity = arena_profiles.display_name (null → client fallback); NO user_id/email/creator_id/org/token-hash/Core-XP. QR RE-DERIVED (btyev1 is a stateless HMAC, not stored → re-signable; old QR stays valid; no rotation/hash-reversal/migration), offered only while ACTIVE. In-shell EventHostDetail (title/state/time + reopened QR + roster + manual refresh); cards now accessible buttons → detail; foundryView += "event-detail". FROZEN: create/participant auth, btyev1 QR + scan semantics, one-participation/one-award, Core-XP, Program, Today/Orb, schema; GET /mine unchanged. Tests: detail API owner-scope/404/roster/no-PII/QR + component + card-tap + shell contract; regression 563; full suite 16 pre-existing/0 new; tsc+cf:build clean. **Device gates PENDING — 3.2E-EVENT-HOST NOT closed.**

**[ ] 3.2E-EVENT-HOST — REALITY EVENT HOST POST-EVENT PARTICIPATION VIEW V1 — IMPLEMENTED · STAGING DEPLOYED · DEVICE GATE PENDING (2026-07-28, count-only; no migration; production unchanged).** Host now sees "did anyone participate?" for events they opened. Inner HEAD `0f833226`; staging Worker `f3e8634a-0e69-4dfe-90e8-8a1ca27f3a63` (100%); production UNCHANGED. New owner-scoped read `GET /api/bty/events/mine` (creator_id=session user, anon→401, ≤2 reads + in-memory counts, no RPC/migration); payload = eventId+title+state+timestamps+participationCount ONLY (NO participant PII / creator_id / org / QR token / Core-XP internals). In-shell `EventHostList` (My events: loading/empty/success newest-first + honest count copy/error+retry; manual refresh only); Learn "My events" door (Host-gated) + post-create "View my events"; foundryView += "event-list", Back→Learn. FROZEN: create/participant auth, btyev1 QR, scan semantics, one-participation/one-award, Core-XP, Program, Today/Orb, schema. Tests: API owner-scope/counts/empty/no-PII + component + shell contract; regression 555; full suite 16 pre-existing/0 new; tsc+cf:build clean. **Device gates PENDING — NOT closed.**

**[x] 3.2D-EVENT — REALITY EVENT CREATE-TO-VERIFY UI V1 — PASS · CLOSED (2026-07-28, Founder physical-device gates PASSED + read-only data reconciliation; no migration; production unchanged).** End-to-end Reality Event vertical over the pre-built backend (`bty_events`/`bty_event_participation`/`bty_event_scan_award`/`btyev1` QR): leader opens an event (in-shell Host create → canonical QR), any authenticated participant scans → participation recorded once. Inner closure HEAD `6c4c9517`; staging Worker `46f813d5-0546-4ff6-9721-fda774ed3f71` (100%); production UNCHANGED. Device gates PASS: in-shell Host create (never leaves the app), new-unapproved-account first participation, readable result surface + visible Back-to-Today, repeat scan → "Already recorded" (no duplicate award), Action QR regression preserved, no unexplained 500. Data proof: participation 2 rows / 2 distinct pairs / 0 duplicates / 0 orphans; device-test pair = 1 participation, repeat added 0. Sequence:
- [x] 3.2D-EVENT V1 create-to-verify UI — `404b257a`
- [x] R1 In-Shell Host Event Entry Fix (Open-an-event is an in-shell Learn view) — `ad9b43e1`
- [x] R2 Error Surface + Observability (no raw 500; exact error logged; eligibility vs failure copy) — `d6369f2e`
- [x] R3 Event QR Participant Authorization Fix (valid QR + auth + active event → participate; creation still approved+leader-track) + readable navy scan surface — `6c4c9517`

Final contract: creator = approved + leader-track; participant = valid `btyev1` QR + authenticated + active Event; `btyev1` ≠ `aalo1`; participant `user_id` server-derived; XP server-authored; one participation + one award per `(event_id,user_id)`; anonymous → 401; creation authority unchanged; no migration.

**[x] 3.2C-B3A — APP SHELL / REALITY-GROUNDED GUIDED JOURNEY SERIES — CLOSED · COMPLETE (2026-07-27, ledger reconciliation; Founder-confirmed physical-device PASS · staging-only · production unchanged).** Inner closure HEAD `8733a71e`; active staging Worker `3491e8d6-f3d8-46e6-8f4d-53d4c7f4c72d` (100%); production UNCHANGED. Tests: full unit 5976 pass / 16 pre-existing / 7 skip (0 new); app-shell 312; startup Orb + /start 30 (unchanged). Sequence:
- [x] B3A Reality-Grounded Guided Journey — `1c42991d`
- [x] B3A.1 two-door My learning / Create training — `aa3cf1fc`
- [x] B3A.2(1) Practice "Action plans" terminology + Learn hygiene — `cdb222bd`
- [x] B3A.2B Today = Yesterday (canonical counts) — `95195fd1` · R1 canonical Yesterday sources — `91daf7ab`
- [x] B3A.2C Learn compact history + hygiene — `e9ed3a1f` · R1 restore account access behind "Account >" — `900e9597`
- [x] B3A.2D Me reorder + honest weekly summary + Orb touch hygiene — `b1af3884` · R1 dual-interaction + return semantics + root reselect — `6c879b54` · R2 living weekly Orb + inline popup — `e019fb2a` · R3 seven-light + compact popup + stale refresh — `928693d9` · R3.1 restored original 7→1→7 merge-cycle Orb — `8733a71e`
- [x] Weekly popup V1 — PASS · CLOSED (seven dated traces · Activity/No activity · no duplicate weekly totals · no Attendance-forward language · startup Orb unchanged · account-isolated cache)
- [~] B3A.3 detailed trace expansion — DEFERRED · NOT REQUIRED FOR V1 (no per-day Learning/Center/Action labels; no Orb reopen)

**[x] 3.2C-PROGRAM — MINIMAL PROGRAM ROOT / LINEAGE EXTENSION — IMPLEMENTED · VERIFIED · STAGING LIVE (2026-07-27, documentation reality reconciliation; supersedes the earlier "NEXT · HELD" row).** Founder privileged read-only live-schema verification established LIVE SCHEMA MATCHED · HISTORY PRESENT. Migration `20260801000000` (`foundry_programs_root_v1`) present in schema history — no apply, reapply, or repair required. Program Root commits `739b6b66` + `153dcf8c` integrated on inner HEAD `8733a71e`; active Worker `3491e8d6-f3d8-46e6-8f4d-53d4c7f4c72d` (100%); production unchanged. Gates PASS: RPC `bty_foundry_resolve_or_create_program` SECURITY DEFINER + `search_path pg_catalog, public` + org derived server-side (fail-closed) + EXECUTE service-role-only (anon/authenticated denied); nullable `program_id` FKs → foundry_programs ON DELETE SET NULL; org NOT NULL + ON DELETE RESTRICT; title/status CHECKs; indexes; RLS enabled; 5 Programs / 5 linked / 32 legacy-NULL Runs; 0 orphan / 0 cross-org / 0 malformed. No repair or new deploy during reconciliation. Kept distinct from 3.2C-B3A. Legacy null-lineage Guided revision = measured-separate USER-REACHABLE BUT UNUSED candidate (not changed here).

**[x] Slice 3.2B — UNIFIED PROGRAM + LIVE EXPERIENCE CANONICAL CONTRACT V1 — CLOSED · COMPLETE (2026-07-26, DOCUMENTATION-ONLY · no code · no migration · no schema · no data mutation · no deploy).** Converted the closed 3.2A measurement + 10 Founder-ratified decisions into ONE compact canonical contract: `docs/UNIFIED_PROGRAM_LIVE_EXPERIENCE_CONTRACT.md` (no existing doc cleanly held a cross-system Program+Experience contract — Foundry/Arena/Reality-event + net-new Live Experience root). Defines all 25 required terms (Program·Program Version·Module·Quick Program·Program Run·discovery·audience/assignment·participation/completion·Practice·Field Action·Follow-up·Live Experience·Experience Run·Activity·Checkpoint·Invitation/Assignment·Participation·Evidence·QR·XP·Program↔Experience ref·definition-vs-occurrence·versioning/snapshots·current mapping·non-collapse) with product-meaning / current-mapping / future-boundary split, `NOT CURRENTLY PERSISTED` for Live Experience·Experience Run·Activity·Checkpoint, 16 normative non-collapse invariants, and the Founder decisions verbatim. Next boundary recorded: **Slice 3.2C Minimal Program Root / Lineage Extension** (reuse Foundry spine, additive, NO Live Experience tables / NO QR generalization / NO XP change / NO assignment redesign / preserve open-link+assigned-overlay / no destructive backfill). Only ledgers + the one contract doc changed this turn.

**[x] Slice 3.2A — UNIFIED PROGRAM + LIVE EXPERIENCE CONTRACT V1 · REALITY-FIRST DOMAIN MEASUREMENT — CLOSED · COMPLETE (2026-07-26, READ-ONLY · no code · no migration · no data mutation · no deploy).** 6-pass parallel measurement over 188 migrations + service/route tracing. **Verdict C** — Program & Live Experience are separate canonical roots reusing shared infra (Program≈B: Foundry spine already implements most runtime; Live Experience=C: schedule/location/one-to-many Activities/Checkpoints do not exist in any table). Key facts: "Module" = `foundry_module_drafts`+`foundry_event_module` snapshot (not a live object); "Quick Training" as named DOES NOT EXIST (live "quick" = quick-created `foundry_events`; Arena `quick_mode` is unapplied migrations-hold + shell-orphaned); Field Action already rides shared `bty_action_contracts`+`foundry_event_assignments`+review spine (zero XP by design); assignment≠participation proven separate in Foundry (nullable `participant_id` bridge), collapsed-by-design in Arena; 3 hard-coded QR families (aalo1/btyev1/btyfr1, NO polymorphic target); `core_xp_ledger` polymorphic+idempotent → all 6 reward candidates need no new engine; NO event table has location or start/end. Fed directly into 3.2B contract. No mutation.

**[x] APP SHELL + TODAY SIMPLIFICATION V1 — FINAL RECONCILIATION + LEDGER CLOSURE — CLOSED · COMPLETE (2026-07-26, no product-code change · no deploy · no migration · no data mutation this turn).** Read-only reconciliation of the completed slice; every invariant green. Web `inner-main` HEAD **`4ca603e4`** == origin (tree clean); active Worker **`945970fc` (100%)** = build of `4ca603e4`; no production deploy; outer history preserved. Confirmed: four learner tabs Today/Learn/Practice/Me; legacy foundry→learn/arena→practice/center→me aliases compatible; Today always exactly one deterministic CTA (never zero/>1, no AI/random); measured-only yesterday; details collapsed under Show everything; Learn wraps Foundry (host tools when authorized); Practice = unchanged Arena + focused Field Actions (Back→Practice, `?tab=practice&fieldAction=` + backward-compat, Live/QR gated); Me = My Learning + voluntary Recovery/Center + account, forced-recovery middleware unchanged; learner Field Actions use canonical owner-scoped `GET /api/bty/action-contract/mine` (not Today brief); Host review reviewer-authority scoped `/api/arena/action-review-queue`; grouping rejected/submitted/escalated/pending/approved → Needs revision/Awaiting review/Awaiting resolution/Upcoming/Reviewed (dedup exactly-once); no Applied/Observed overclaim; explicit load/empty/error + Host loading; 0 duplicate cards; account isolation + no cross-account flash PASS; rejected correction/resubmit PASS; English-only UI canonical, KO gate N/A (not FAIL). Device gates A–F + additional results PASS. Full suite **5850 pass / 16 pre-existing / 7 skip** (0 new); tsc + cf:build clean. Component commits: `e056abaa` (4-tab) · `12131b95` (empty-state CTA) · `acde4885` (focused surface) · `f0145ce0` (canonical inventory + load states) · `4ca603e4` (escalated separation). **NO discrepancy. APP SHELL + TODAY SIMPLIFICATION V1 CLOSED · COMPLETE.** Only the three ledgers changed this turn.

**[ ] PRACTICE FIELD ACTIONS · ESCALATED SEMANTIC SEPARATION PATCH — DEPLOYED · DEVICE REVISION PENDING (2026-07-26, IA-only · no migration · no schema · no data mutation).** `fieldActionLearnerGroup` conflated submitted+escalated under "Awaiting review" (violated stage contract). Fix (pure grouping + presentation): submitted→Awaiting review, **escalated→Awaiting resolution** (new distinct group; copy "This action is waiting for a resolution." / "이 행동은 해결을 기다리고 있습니다."), never conflated; full order rejected/submitted/escalated/pending/approved. No lifecycle/authorization/schema/records/Host-counts/Today-priority change (Host awaiting-resolution count still from unchanged action-review-queue). Tests: fieldActionGroup (escalated distinct) + FieldActionsFocus (submitted only Awaiting review, escalated only Awaiting resolution, each once, deep-link escalated, EN/KO); full suite **16 pre-existing fail only** (0 new); tsc + cf:build OK. Committed inner-main **`4ca603e4`** (==origin, tree clean); redeployed clean HEAD → Worker **`945970fc` (100% active)**; served-literal probe PASS. **Do NOT claim device PASS. Device revision PENDING; slice NOT closed.**

**[ ] PRACTICE FIELD ACTIONS · CANONICAL LEARNER INVENTORY + LOAD STATES V1 — DEPLOYED · DEVICE REVISION PENDING (2026-07-26, IA-only · no migration · no schema · no data mutation).** Device: learner surface omitted the submitted "배가 고파" (bf5081c6) that IS in the Host queue; also blank during load. Measured root cause: learner state came from `/api/me/today/brief` actionStatus (a Today priority projection, fail-soft + admin-null-omitted), not a canonical inventory. Fix: new READ-ONLY authorized inventory `listMyFieldActions` + `GET /api/bty/action-contract/mine` (requireUser, owner-scoped user_id, column allow-list, non-200 on fail, 401 unauth live-proven) returning every field_action the learner owns across pending/submitted/escalated/rejected/approved, independent of Today ranking/reminder eligibility. FieldActionsFocus now uses this single source (contract-id identity) + pure domain grouping `fieldActionLearnerGroup` + explicit Loading/Empty/Error+retry (read-only) states + Host loading state; non-reviewer → no Host section (honest). Reuses FieldActionForm/HostActionReviewDetail; E3-safe; EN/KO. Tests: fieldActionGroup + myFieldActions.server + rewritten FieldActionsFocus (submitted-absent-from-brief appears once, dedup, load/empty/error/back, Host authorized, non-reviewer, no cross-mount reuse, deep-link, EN/KO); app-shell+action-contract+daily **589 pass/64 files**; tsc clean; full suite **16 pre-existing fail only** (0 new); cf:build OK. Committed inner-main **`f0145ce0`** (==origin, tree clean); redeployed clean HEAD → Worker **`68e882c6` (100% active)**; served-literal + endpoint-401 probe PASS. **Do NOT claim device PASS. Device revision PENDING; slice NOT closed.**

**[ ] PRACTICE FIELD ACTIONS FOCUSED SURFACE V1 — DEPLOYED · DEVICE REVISION PENDING (2026-07-25, IA-only · no migration · no schema · no new API · no data mutation).** Device blocker: Practice → Field Actions dumped to generic top of Today (relevant action hidden behind Show everything). Fix: focused in-shell `FieldActionsFocus` under Practice (landing door + deep link `?tab=practice&fieldAction=<id>`; Back → landing), reusing existing projections ONLY (brief reminders+actionStatus, action-contract/reviewed-plans, arena/action-review-queue) and the canonical FieldActionForm/HostActionReviewDetail — no new table/API, no duplicated lifecycle/authorization. Learner: Needs revision (editable+note)/Awaiting review (read-only)/Upcoming/Reviewed action plans (E3-safe). Host (authority-scoped, empty for non-reviewers, no learner My-Learning leak): Verification pending/Needs revision/Reviewed/Awaiting resolution + queue. Field-action deep link moved today→practice so the Today primary CTA opens the specific focused action (shell backward-accepts old link); additive `actionType` on actionStatus scopes Awaiting review to field_action. EN/KO parity; bans Applied/Observed/Completed/Behavior-changed. Tests: FieldActionsFocus + PracticeLanding + source guards + reminder spec; app-shell+daily **519 pass/53 files**; tsc clean; full suite **16 pre-existing fail only** (0 new); cf:build OK. Committed inner-main **`acde4885`** (==origin, tree clean); redeployed clean HEAD → Worker **`1865e266` (100% active)**; served-literal probe PASS. **Do NOT claim device PASS. Device revision PENDING; slice NOT closed.**

**[ ] APP SHELL + TODAY SIMPLIFICATION V1 · TODAY EMPTY-STATE PRIMARY-ACTION PATCH — DEPLOYED · DEVICE GATE B REVISION PENDING (2026-07-25, IA-only · no migration · no schema · no data mutation).** Device blocker: empty-day Today showed a decorative "Nothing is due" line with NO primary CTA. Root cause: `selectPrimaryAction` returned null → `TodayHome` rendered no CTA. Fix (pure selector + Today projection only): selector returns a discriminated result, NEVER null — reminder tiers 1–5 unchanged and still outrank all, then deterministic fallbacks **6 continue in-progress program** (measured 0<pct<100) · **7 start available Arena practice** (measured `/api/arena/practice` non-empty) · **8 find a program** (always valid). TodayHome reads both signals fail-soft, renders EXACTLY ONE CTA in every state, fallbacks route in-shell (Practice/Learn, no reload); reminder keeps deep-link href. Empty-day copy EN/KO parity (Start practice/연습 시작 · Find a program/프로그램 찾기 · Continue learning/학습 계속하기). No AI/random/fabrication. Tests: selector tiers 1–8 rewrite + new TodayHome empty-state/in-shell/EN-KO; app-shell+daily **507 pass/52 files**; tsc clean; full suite **16 pre-existing fail only** (0 new); cf:build OK. Committed inner-main **`12131b95`** (==origin, tree clean); redeployed clean HEAD → Worker **`eea55f04` (100% active)**; served-literal probe PASS (all 5 CTA literals live). **Do NOT claim device PASS. Device Gate B revision re-test PENDING; slice NOT closed.**

**[ ] APP SHELL + TODAY SIMPLIFICATION V1 — DEPLOYED · COMMANDER DEVICE GATES PENDING (2026-07-25, IA-only · no migration · no schema · no data mutation · code UNCOMMITTED).** Five developer-facing tabs → FOUR learner actions **Today · Learn · Practice · Me** (UI/nav translation only; canonical route/DB/engine names untouched). `AppTabKey`=today|learn|practice|me (grid-cols-4, in-component setTab, no reload, safe-area kept); `resolveInitialAppTab` aliases Foundry→Learn/Arena→Practice/Center→Me so ALL legacy `?tab=` deep links resolve with zero emitter changes. New `TodayHome`: Better-Than-Yesterday header · measured yesterday (daily-trace self-return, never fabricated) · EXACTLY ONE primary action via pure domain selector `todayPrimaryAction.ts` (deterministic, no new engine) routing via canonical deep link · compact attention counts · **Show everything** revealing the UNCHANGED TodayPersonalBrief (collapsed by default). Learn=`LearnHeader` over unchanged Foundry surface + all sub-views; Practice=`PracticeLanding` (Arena practice opens unchanged in-shell ArenaRoom in place · Field Actions→Today · Live/QR gated "Coming next"); Me=account + mirror/orb + `MeEntries` (My Learning · voluntary Recovery/Center · Experiences placeholder). **Forced-reset middleware 307→/center UNCHANGED (guard test).** Tests: todayPrimaryAction/AppTabBar/PracticeLanding/MeEntries/shell-invariants + rewritten initialTab/arena-room-contract/today — app-shell+daily **493 pass/51 files**; tsc clean; full suite **16 pre-existing fail only (stash-proven identical on clean HEAD → 0 regressions)**; terminology baseline unchanged; viewport guard green. ESLint env-broken (pre-existing `next lint`/ajv fault). Deployed cf:build+cf:deploy → `bty-arena-staging` Worker **`afd7d424` (100% active)**, staging only. Code UNCOMMITTED (deployed from working tree; commit on request). **Device gates A–F + EN/KO tab-label decision = Commander. LEDGER NOT CLOSED.**

**[x] Slice 3.1B-3N-5D FINAL RECONCILIATION + LEDGER CLOSURE — CLOSED·COMPLETE (2026-07-25, no migration · no code change · zero DB mutation).** Reviewed Field Action Plan→My Learning + Host stage counts (5D.1) + 5D.1A/1B/1C(+N2/N3/L/R/W)/1D. Web HEAD `d6081ea1` == origin; native `1e30da58` == origin; Worker **`dfaa3c36` (100%)**. Live: ddshanbit 1 active membership; `d5773c18` approved unchanged, `bf5081c6` submitted WHO "Dr. Chi at the office" (S4 revision note preserved in audit, one resubmit); 3 unique session_id. Host counts **1/0/2/0 queue 1 (`bf5081c6`)**; My Learning 2 cards (`d5773c18`/`54b3443c`); approved = E3 only (zero Arena/Applied/Observed). Watchdog real-recovery PROVEN: native **13/13** (blank@T+6 → one purge+one reload, no loop) `1e30da58` + **W-P1–W-P3 device PASS**. Gates A–F/S1–S5/R1–R5 PASS; Orb latency 3/3. Native input-blocker = temp window-level UILongPressGestureRecognizer, removed in `0291b2c5`. Web 5776 pass/16 pre-existing/tsc clean; native build PASS. No prod/schema/migration/data-mutation; latency diagnostics retained. **No discrepancy. 3.1B-3N-5D CLOSED·COMPLETE.** Only the three ledgers changed.

**[x] Slice 3.1B-3N-5C FINAL READ-ONLY RECONCILIATION + LEDGER CLOSURE — 5C.3 CLOSED·COMPLETE · 5C.4 CLOSED·COMPLETE (2026-07-24, no migration · no code change · zero DB mutation).** Reconciled the Action Review lifecycle + iOS viewport fix against live/source/tests. Inner HEAD `80c6cb62` == origin/inner-main; outer main == origin; trees clean (only unrelated untracked `docs/video/`). Active Worker **`f5e01361` (100%)** = build of `80c6cb62`; served==committed (16px + touch-manipulation + revise present; routes 401). Live: approved Field Action = approved/verified_at/completed_at/reviewed_at, run_id NULL; exactly 1 approve audit; **zero Arena effects** (arena_events/core_xp/le_verification = 0); resolver ywamer2022→ddshanbit ALLOWED; queue excludes approved. Tests: 23 targeted suites / 207 pass (lifecycle + viewport + Arena-effects preserved); full suite 16 pre-existing failures only. **No discrepancy → no slice reopened.** Only the three release ledgers changed.

**[x] Slice 3.1B-3N-5C.4 iOS post-action viewport zoom fix — DEPLOYED · iOS ZOOM GATE PENDING (2026-07-24, no migration).** Post-action screen stayed zoomed (submit/resubmit/approve/request-revision) until force-quit. Root cause: editable textareas text-sm (14px<16px) → iOS auto-zoom on focus + retained focus through onBack. Fix (viewport/focus only): 16px (text-base) controls; blurActiveThen() releases focus + 2 rAF before canonical-success navigation (NOT on revise — keeps invalid-field focus); touch-manipulation on Submit/Approve/Request-revision/confirm/cancel/retry (kills double-tap zoom, keeps pinch). No user-scalable=no/maximum-scale; guard test added. Business logic unchanged. tsc/build clean; viewport tests + full suite (16 pre-existing). Deployed inner `80c6cb62` → Worker **`f5e01361` (100%)**; served-code PASS (16px+touch-manipulation present; no restrictive viewport flags). **iOS zoom gate (4 paths) PENDING.**

**[x] Slice 3.1B-3N-5C.3 Gate-A fix (FieldActionForm surfaces Layer-1 'revise') — DEPLOYED · GATE A RE-TEST PENDING (2026-07-24, no migration).** Submitted Field Action was absent from the Host queue because it was status='pending' (not 'submitted'): learner who="My team lead" failed Layer-1 R1 ("Name one person"), submit-validation returned 200 outcome='revise' + rolled back to pending, and FieldActionForm swallowed it as success. Queue/authority/detail are field_action-correct (live: resolver ALLOWED, detail 'Real-world application'). Fix (form only): parse response outcome — revise → stay open + show server signal + focus field + resubmit same contract; onBack only on canonical landing (contract_state awaiting_qr); ambiguous 200 stays open. No validator/queue/schema change. tsc/build clean; form 9 + queue 12 (submitted field_action included, pending excluded) + all suites; full suite 16 pre-existing. Deployed inner `3091a27b` → Worker **`190c6bfb` (100%)**; served-code PASS (revise copy present; no validator/DB leak). Pending contract d5773c18 left for re-test. **Gate A re-test PENDING.**

**[x] Slice 3.1B-3N-5C.3 live-CHECK compatibility fix — DEPLOYED · GATE 0 RE-TEST PENDING (2026-07-24, no migration).** Gate-0 insert failed 23514 (live CHECKs reject le_activation_type='field_application', mode='field', status='draft'/'committed'). Fix: producer inserts pending/foundry/micro_win (action_type='field_action' stays the source discriminator; mode/le inert). Pending field_action ACTION_DUE opens the focused FieldActionForm (not Arena tab). FieldActionForm splits 404 (unavailable) vs 500 (retryable + Retry). tsc/build clean; tests updated; full suite 16 pre-existing. Deployed inner `8ca20acc` → Worker **`aeaacf0e` (100%)**; served-code PASS; LIVE producer confirmed ok+pending (created row deleted, net-zero). **Gate 0 re-test PENDING.**

**[x] Slice 3.1B-3N-5C.3 Gate-0 fix (CTA path + assignment-anchored producer) — DEPLOYED · GATE 0 RE-TEST PENDING (2026-07-24, no migration).** CTA moved from FoundryMyLearning (wrong) to **FoundryCompletionReview** (the device "Review Learning" detail), keyed on assignmentId; old CTA removed → one path. Producer now assignment-anchored (user_id_snapshot → event_id+participant_id → completed progress); no longer requires linked_user_id (NULL for participant/historical completions). Non-arena + source-aware approval unchanged. tsc/build clean; producer 8 (incl. NULL-link accepted) + route 5 + form 6 + CTA 3; full suite 16 pre-existing. Deployed inner `9280878c` → Worker **`3ccdb85c` (100%)**; served-code PASS (401; CTA on completion-review; old my-learning CTA absent; no /bty-arena). **Gate 0 re-test PENDING.**

**[x] Slice 3.1B-3N-5C.3 Field Action producer + source-aware approval V1 — DEPLOYED · GATE 0 + A–F PENDING (2026-07-24, no migration).** In-shell Learning→Action producer + source-aware approval, shipped together. Foundry module → My Learning "Apply this in real life" CTA → Today FieldActionForm (learner authors Who/What/How/When) → submit-validation → Verification Pending → Host review. Source = `action_type='field_action'` (no schema change; server-authored, immutable). Non-arena (run_id/scenario/choice/pattern_family NULL); idempotent per (learner, progress) via UNIQUE(user_id, session_id). Source-aware approve dispatch: arena→full chain; field_action→approve+audit ONLY (zero Level/XP/AIR); unsupported source fails closed pre-mutation. Host detail "Real-world application" label; rejected field_action Needs-revision opens focused form. tsc/build clean; tests producer 7 + dispatch +6 + form 6 + route 5; full suite 16 pre-existing fail. Deployed inner `4f5315fc` → Worker **`d9d2968b` (100%)**; served-code PASS (401s; copy present; 0 leaks). QA edge ACTIVE. **Gate 0 + A–F PENDING.**

**[x] Slice 3.1B-3N-5C Action Review Decisions V1 — DEPLOYED · DEVICE GATES A–F PENDING (2026-07-24).** Migration `20260731000000` APPLIED (SQL Editor) + reconciled (`migration repair` → Local==Remote; no db push). Live PostgREST verify: revision_note/reviewed_at/event_kind cols exist; audit service_role-SELECT-only; anon can't exec either RPC (42501) or read audit (42501). Deployed exact inner `3998db60` → `bty-arena-staging` Worker **`abdd9bb1` (100%)**, staging URL, not production. Served-code PASS: decision+queue unauth→401; app chunk carries all decision + revision-note copy (EN+KO); 0 service_role/authority-table/resolver/reviewer-id/`/bty-arena` in client bundle. Staging-safe tests **112 pass** (14 files); offline full suite 16 pre-existing fail (unrelated). Pilot edges ACTIVE. **Device gates A–F PENDING** (approve · request-revision+note · fresh-authority recheck · cancel · stale/repeat · shell regression).

**[x] Slice 3.1B-3N-5C Action Review Decisions V1 (Approve / Request revision) — IMPLEMENTED·TESTED·BUILT·COMMITTED+PUSHED · MIGRATION-APPLY+DEPLOY = COMMANDER OUT-OF-BAND (migration FIRST) · DEVICE GATES A–F PENDING (2026-07-23).** Two reviewer decisions on the read-only Phase-5B detail as ONE canonical `SECURITY DEFINER` command + Option-2 per-function idempotency hardening (Level+AIR were not idempotent; hardened). Approve (hybrid/link only) → `approved`+`verified_at`+`completed_at`+run `complete_verified`+XP/AIR/Level, gated on an applied transition + scoped to the LEARNER; Request revision → `rejected`+note, no run/XP/AIR/Level. Extended invariant: only canonical verification commands set `verified_at` (QR verify + Host Approve). **Migration `20260731000000` WRITTEN (apply out-of-band BEFORE deploy):** atomic `bty_apply_run_level_verified` RPC + `arena_events` sentinel partial-unique; `le_verification_log.event_kind` + method-independent partial-unique `(contract_id)`; `bty_action_contracts.revision_note`(≤500)+`reviewed_at`; append-only `bty_action_review_decision_audit`; `bty_resolve_action_review` RPC (lock→submitted+verified_at NULL+hybrid/link→DB-derived edge recheck→CAS→audit→snapshot). Code: QR CAS winner gate (zero-row→409, no effects); `onArenaRunCompleteVerified` atomic per (user,run)+`runId`; AIR contract_id+event_kind (23505 benign); `resolveActionReviewDecision` service+POST route; decision UI (confirm+revision sheet, no optimistic terminal); learner revision-note in Today "Needs revision"; resubmission clears revision_note/reviewed_at. tsc/build clean; slice+related **130 pass**; full suite 16 pre-existing fail (identical/unrelated, stash-proven). Inner `3998db60` (0/0). NOT deployed (migration must precede). Pilot edges ACTIVE. **Device gates A–F PENDING.**

**[ ] Slice 3.1B-3N-5B.1 In-Shell Account Switch Simplification V1 — IMPLEMENTED/DEPLOYED · DEVICE GATE PENDING (2026-07-23).** Me → Switch account → Google chooser directly → Today (no "Welcome to bty" interstitial, no 2nd Continue-with-Google tap). New shared `startGoogleOAuth()` (web+native, prompt=select_account); AccountBlock Switch calls it DIRECTLY (next=today, force chooser) — no teardown-first, cancel keeps previous account. `clearAccountScopedStorage()` on successful callback (preserves sb-* + device prefs; no cross-account bleed). Rename "Use another account"→"Switch account"; "Switching account…" overlay + double-tap latch; land on Today. Removed duplicate Foundry switch (same op as Me) — kept read-only account line. login-card byte-unchanged. Fixed latent Phase-5B test stub. No migration; authority/queue/contract/verification/reward/escalation/QR unchanged; Sign out + first-login Welcome unchanged. tsc/build clean; 814 auth/shell/foundry/today suites pass. Worker **`3da4c8eb` (100%)**; inner `db77b36b` (0/0). **Device gate PENDING** (native path device-only).

**[ ] Slice 3.1B-3N Host Action Verification & Revision V1 — PHASE 5B HOST ACTION REVIEW QUEUE V1 — IMPLEMENTED/DEPLOYED · CROSS-HOST QUEUE DEVICE GATE PENDING (2026-07-23).** Read-only Host review queue + in-shell detail (no Approve/Revision). Service narrows candidates by active edges then re-resolves each via Phase 5A resolver (cap 50); eligibility = submitted + verified_at null + hybrid/link (not verification_status); label = arena_profiles.display_name. Routes: `GET /api/arena/action-review-queue` + `/api/arena/action-reviews/[id]` (re-resolves; deny→404). Queue relocated to static segment (opennext dynamic-sibling 404 fix). Today "Action reviews" sibling subsection (top-3/Show all, calm, in-shell anchor, NO decision buttons) + HostActionReviewDetail (Back→Today). tsc/build clean; 1704 related pass; new domain(5)+service(6)+routes(8)+UI(5); live pilot queue proof 9/9; client-bundle scan CLEAN; served 401 + no-store. Worker **`4fafb16d` (100%)**; inner `0af7c32b` (0/0). Pilot edges ACTIVE. **Device gate PENDING (needs 2nd Host).** → 3.1B-3O escalation deferred.

**[x] Slice 3.1B-3N Host Action Verification & Revision V1 — PHASE 1–5A AUTHORITY FOUNDATION + RESOLVER (2026-07-23; no user-reachable route → NO device gate).** Authority migration `20260730000000` (v4 transactional; state+immutable-audit; service_role SELECT-only; `SECURITY DEFINER` RPC assign/revoke, null-safe action, teardown-safe revoke; RESTRICT membership FKs) **applied via Commander SQL Editor + history reconciled** (Local=Remote; authority=0/audit=0). Live gates PASS. Phase 4 pilot (RPC-only): Host A→Learner A + Host B→Learner B, dup→`action_review_already_active` no extra audit, collateral unchanged. Phase 5A `resolveActionReviewAuthority` (learner from `contract.user_id`; default-deny; one-hop edge; hybrid/link; non-self; fail-closed ambiguous) + pure `decideActionReviewAuthority` (24 unit tests); live pilot proof 9/9. tsc clean; build OK; related 1023 pass; schema guard 18 pass; client-bundle security scan CLEAN. NO UI/Approve/Revision yet; escalation untouched (→ 3.1B-3O). Pilot edges left ACTIVE for 5B. [ADR-006]. **PHASE 5A DONE.**

**[x] Slice 3.1B-3M Today Action Hygiene V1 — CLOSED · COMPLETE · COMMANDER DEVICE GATE PASS (2026-07-23).** Device all-PASS: 10 SUBMITTED Actions moved from red overdue → **Verification Pending (count 10)**; collapsed 3 / Show all 10 / Show less device PASS; calm non-red + honest Original deadline + no fabricated submission date; in-shell `?tab=arena` navigation PASS (no `/bty-arena`, no nested nav); Leadership Attention intact. Reconciliation: 0 SUBMITTED in ACTION_DUE + 10 verification_pending (10 distinct IDs); global status distribution unchanged (missed 12 / submitted 12 / approved 91 / pending 12) — **no Action mutation, no status/deadline/audit/XP change, no migration, no lifecycle write surface**. Inner `ae24cc40` (0/0, no re-commit); Worker `b31b7c1d` (100%, prev `9c9c1e95`, no redeploy). Deferred: 3.1B-3M.1 focused `?action=` surface; 3.1B-3M.2 lifecycle & audit. **SLICE DONE.**

**[ ] Slice 3.1B-3M Today Action Hygiene V1 — DEPLOYED · SERVER/BUILD/PRIVACY GATES PASS · COMMANDER DEVICE GATE PENDING (2026-07-23).** Read-only Action projection reclassified by STORED status before deadline: **pending**→ACTION_DUE (only red "Overdue"); **rejected**→ACTION_REVISION/needs_revision (amber); **submitted**→actionStatus.verification_pending; **escalated**→actionStatus.awaiting_resolution; approved/missed/draft/committed→none. New separate `actionStatus[]` on `/api/me/today/brief` (allow-listed, never merged into reminders/hostAttention). Today: DON'T MISS TODAY → VERIFICATION/RESOLUTION (top-3 + Show all N, no dismissal, calm non-red) → LEADERSHIP ATTENTION; full title + honest "Original deadline · <date>" (no fabricated submission date), sourceTitle null in V1. In-shell `?tab=arena` (no `/bty-arena`, no `?action=`). **NO migration, NO Action mutation, NO status/deadline write, NO grouping/merge.** Live: Commander's 10 stale submitted → **0 red overdue + 10 verification_pending** (distinct IDs). Inner `ae24cc40` (0/0). Worker **`b31b7c1d` (100%, prev `9c9c1e95`)**. Full suite 5552 pass / 16 pre-existing fail / 7 skip; served 401 + `private, no-store` + EN/KO markers verified. Deferred: 3.1B-3M.1 focused `?action=` surface; 3.1B-3M.2 lifecycle & audit. **Device gate PENDING.**

**[x] Slice 3.1B-3L Host Leadership Attention V1 — CLOSED · COMPLETE · COMMANDER DEVICE GATE PASS (all 3 categories) (2026-07-23).** SHARED_REVIEW_DUE + FOLLOW_UP_NEEDED + FOLLOW_UP_OVERDUE all physically proven on device, each cleared through its canonical transition; learner **NOT_YET** → obligation `dd71e35b` RESPONDED, audit 1 CREATED + 1 RESPONDED, Host attention auto-removed, Host room "Learner reported: Not yet". No migration, owner-scoped, private bodies excluded, NEW_RESPONSE deferred; XP/completed_at/assignment(`completed`)/review(`ALIGNED`) unchanged; 3K `4dc5f309` untouched; unrelated `124f5430` untouched. Dedicated artifacts: event `ca596ce0` / progress `d661fa70` / followup `dd71e35b` / learner `d0b1af49` / host `ee9d2075`. Staging-only re-date 2026-07-29→2026-07-22 (single guarded row, no reset/delete). Two device hotfixes (no migration): Gate B deep-link (effect-based open-on-arrival) inner `925bbb56` Worker `58d6ec5a`; Action legacy-Arena escape → in-shell `?tab=arena` inner `2e0d6c91` Worker **`9c9c1e95` (ACTIVE 100%)**. V1 inner `1a005eed` Worker `9c11fac3`. Show-all = component tests. Full suite **5528 pass / 16 pre-existing fail / 7 skip**; 52 slice tests; inner-main 0/0. **SLICE DONE.**

**[ ] Slice 3.1B-3L Host Leadership Attention V1 — IMPLEMENTED + DEPLOYED · SERVER/BUILD/PRIVACY GATES PASS · COMMANDER DEVICE GATE PENDING (2026-07-23).** Inner `1a005eed` (from `518e6aa7`; inner-main 0/0). Worker `9c11fac3-3a36-4f13-b809-dde811e261a8` (100%, prev `2f0ec3c9`). **NO migration.** Owner-scoped, read-only `hostAttention[]` on `GET /api/me/today/brief` (SEPARATE from `reminders`) + Today "LEADERSHIP ATTENTION" section (first 3 + Show all N, no dismissal). Eligibility = active Foundry Host + owned Event ONLY (responsibility keys grant nothing). 3 categories: FOLLOW_UP_OVERDUE / FOLLOW_UP_NEEDED / SHARED_REVIEW_DUE (priority in that order → oldest ts → stableId); NEW_RESPONSE deferred. Exact control-room deep links (`event`/`section`/`focus`) scroll+highlight; learner deep links untouched. Private bodies (`response_text`/shared body/`host_review_note`) excluded from Today DTO (served-bundle scan = 0). Full suite **5518 pass / 16 pre-existing fail (identical) / 7 skip**; 42 new slice tests; tsc + build OK; served 401 + `private, no-store` + client markers verified. **Commander device gate PENDING — no device-test data created yet; 3K row `4dc5f309` untouched.**

**[x] Slice 3.1B-3K Follow-up Obligation Engine V1 — CLOSED · COMPLETE · DEVICE GATE PASS (2026-07-22):** Full device flow verified: Today FOLLOW_UP_DUE → in-shell surface (no Room reopen) → submit NOT_YET → success → reminder gone → Host "Learner reported: Not yet". Final obligation `4dc5f309`: 1 row RESPONDED/NOT_YET, responded_at set, completed_at + due_bty_day + owner + assignment intact; audit 1 CREATED + 1 RESPONDED (no dup); XP/completion/assignment/Shared Understanding unchanged; no private text; idempotency (unchanged/already_responded) proven by function body + tests. Staging provenance: natural due 2026-07-29 → Commander-authorized staging-only re-date to 2026-07-22; NOT_YET = device-acceptance evidence, not a natural production follow-up; append-only, no reset/delete. Submit-RPC 42702 hotfix `20260729000000` applied; active Worker `2f0ec3c9` (no redeploy — DB-only hotfix). SLICE DONE.

**[~] Slice 3.1B-3K — MIGRATION APPLIED + DEPLOYED (creation/device gate) row, superseded by CLOSED above (2026-07-22):** Migration `20260728000000` APPLIED (Commander, SQL Editor). LIVE-VERIFIED read-only: both tables 0 rows (zero-backfill), RLS deny (anon table+function → 401/42501), 3 RPCs service_role-only via no-write probes (still 0 rows after). Inner `1594a1fa` + `b400911d` (routes → `private, no-store`) pushed (0/0). Deployed `bty-arena-staging` active **`2f0ec3c9-dd05-498e-ba99-828b5dfef23a`** (100%). Live: learner GET/POST 401+`private,no-store`, Host GET 401 (no 5xx); client chunk has follow-up UI markers; FOLLOW_UP_DUE/RPC names server-bundle only (0 in client). tsc clean; full regression 16 pre-existing (0 new). **PENDING: Commander creates first real obligation + device gate; NOT claimed. No test obligation created.**

**[~] Slice 3.1B-3K — earlier BUILT + HALT-at-gate row (superseded by APPLIED+DEPLOYED above) (2026-07-22):** [FOUNDRY/TODAY/API/UI/MIGRATION] Materializes `module_snapshot.followUpDays` (7/30) into a per-participant dated obligation (NEW `foundry_participant_followups` + append-only audit + 3 service-role SECURITY DEFINER RPCs; RLS deny; UNIQUE(progress_id, follow_up_days)). Created ONCE at authenticated completion (any XP outcome) OR authenticated claim (anonymous→owned); never at anonymous completion. Due = completion BTY day + N days → 05:00-local start instant, fixed at creation (tz via settled ladder), day-granular Today classification. Today `FOLLOW_UP_DUE` (due_today/overdue only, `followup:<id>`, deep link `?tab=foundry&followup=<id>`). Learner `FoundryFollowUpResponse` (4 outcomes APPLIED/PARTLY_APPLIED/NOT_YET/BLOCKED, Host-disclosure, self-reported not verified, all close checkpoint, no invented retry, does not reopen Room). Host `FoundryFollowupStatus` (independent of shared-question gate, learner-reported outcomes, no private text). Separate evidence stage — never XP/completion/assignment/Shared Understanding; no backfill. tsc clean; +64 tests; affected 1562/1562; full regression = 16 pre-existing only (0 new); build PASS; client-chunk scan 0 secrets/RPC. Migration `20260728000000` WRITTEN-not-applied. **HALT: Commander applies migration before deploy. NOT committed(local candidate only)/pushed/deployed.**

**[x] Slice 3.1B-3J.1 — CLOSED · COMPLETE · DEVICE GATE PASS (2026-07-22):** Commander confirmed the physical-device product gate PASSED on active version `05075967-a684-4e30-9432-867d0506eb00` (Today greeting→brief IA + compact consent switch correct on device). Earlier FAIL = stale WKWebView bundle, cleared by restart. Slice DONE — no residual work.

**[x] Slice 3.1B-3J.1 RE-VERIFY + RE-DEPLOY (2026-07-22):** Commander re-reported 3J defects on device after 3J.1 shipped. Measured: HEAD `52aaec76` already correct (greeting→brief, fixed consent row) + on origin; prior deploy `3891a7ef` landed 2 min after the commit → **fix was already live, device FAIL = stale WKWebView bundle.** Re-ran full gate (tsc clean · focused 69/69 · app-shell+center+daily 515/515 · next build PASS · cf:build PASS) and re-deployed from HEAD. Built AND **live-served** app chunk carry 3J.1 markers (`center-consent-track`/`-thumb`, `Today personalization`, `today-personal-brief`, `today-greeting`); served-chunk secret/private-body scan = 0. Active version **`05075967-a684-4e30-9432-867d0506eb00`** (100%); `/en/app`→307, brief→401 `private,no-store`, prefs→401, `/api/version`→200. No new commit, no migration. **Device gate PENDING — Commander must hard-reload / cold-restart native app.**

**[x] Slice 3.1B-3J.1 Today Simplification + Center consent switch hotfix — SHIPPED + DEPLOYED, DEVICE GATE PENDING (2026-07-22):** [TODAY/CENTER/API/UI/DEPLOY] Fixes the 3.1B-3J device-gate FAIL (two competing Today experiences + broken consent switch). Root cause: the Today tab rendered `<TodayPersonalBrief>` ABOVE `<TodaySurface>`, and the greeting lived INSIDE TodaySurface → brief above greeting + the old three-door relationship/commitment ritual (TODAY'S PATH / PROMISE TO CARRY / "living this relationship" / yesterday-trace) as a second Today. FIX: Today tab now renders a dedicated time-aware greeting header → `TodayPersonalBrief` only; the retired surface is unmounted + the shell-local reads that drove it removed (no more side-effecting living-response POSTs); **TodaySurface component + commit/living-response/today-intelligence engines PRESERVED** (presentation removed, records/behavior kept). `/api/me/today/brief` no longer suppresses the blocking Action Contract (that dedup only existed because the old card showed it) → discoverable once in DON'T MISS TODAY (regression test added). Center consent switch → compact setting ROW (top divider, not a card) with fixed-PX 44×24 track + thumb pinned left/top + bounded 20px translate (cannot overflow, immune to iOS Dynamic Type), whole row = one accessible `role=switch` button (no label>button double-fire), `shrink-0`, approved EN/KO copy + Host-non-exposure privacy line, optimistic+revert+in-flight-guard. Privacy unchanged (Today never gets raw response_text; Host still excludes it); consent default OFF; **no migration.** tsc clean; door tests (403) + new switch/regression tests pass; full regression = 16 pre-existing only (0 new); build PASS; 0 new secret/private-body leaks in client bundle. Files: `BtyDailyAppShell.tsx` (+greeting, −dead machinery), `CenterRealityFeed.tsx`, `api/me/today/brief/route.ts`, +5 test files. Inner `52aaec76` (pushed); Worker active `3891a7ef-d491-441e-a8fa-ca9cc409d6c8` (100%); brief unauth→401+private,no-store, prefs→401, `/en/app`→307. **Device gate PENDING (Commander).**

**[x] Slice 3.1B-3G Shared Understanding Evidence V1 — MIGRATION APPLIED + SHIPPED + DEPLOYED, DEVICE GATES PENDING (2026-07-22):** [FOUNDRY/DB/API/UI] Evidence-ladder separation: `response_text`=PRIVATE (untouched, never Host-visible, future Center/Today source) · new `shared_understanding_response`=SHARED (disclosed, Host-reviewable) · Field evidence=future. Migration `20260726000000` applied (SQL Editor) + LIVE-VERIFIED: columns present, `host_review_status` nullable+no-default (NOT NULL=0/21), RPC owner-gated + anon DENIED 42501, audit RLS anon DENIED, legacy invariants shared/status/audit=0, response_text preserved=18. All amendments A–H honored. Surfaces: publish plumbing, completion gate (shared required only when configured; response_text private; idempotency/XP unchanged), Host read projection + review RPC (never response_text), learner UI (both clients, separate disclosed section), Host review UI (4 statuses EN/KO, educational framing), Builder default-proposal. tsc clean; +38 tests; full regression only 16 pre-existing (0 new); build PASS; secret scan 0 leaks. Inner `66a5eefb` (pushed 0/0); Worker `14e2530b` (100%); shared-understanding GET/review POST unauth→401. **Learner gate A–K + Host gate A–J pending (device).**


**[x] Slice 3.1B-3F.1 PDF Room "Back to Foundry" control — BUILT + VERIFIED-TO-BASELINE, DEPLOY+GATE PENDING (2026-07-21):** [FOUNDRY/UI] Assigned PDF Required+durable (restoration PASSED) but the opened PDF learner experience had no visible return control. Root cause: `FoundryDocumentClient` `Frame` never read `?return`/rendered the back control, while the YouTube `FoundryJoinClient` `Frame` already does. FIX (client-only, mirrors video): PDF `Frame` now reads `sanitizeRoomReturn(?return)` → "← Back to Foundry"/"← 파운드리로 돌아가기" (data-testid room-back-to-foundry) every stage, safe-area aware; return=navigation to `/{loc}/app?tab=foundry`, never completes; open-link/unsafe→no control. No schema/API/migration; no change to completion/XP/audience/open-link/YouTube/auth. tsc clean; +5 new tests + 163 f/[token]+UI pass; full suite only 16 pre-existing (0 new); build PASS. Files: `src/app/f/[token]/FoundryDocumentClient.tsx`, `…backToFoundry.test.tsx`. Inner `dfa7214a`. DB no-mutation. Deploy + device gate A–J pending.

**[x] Slice 3.1B-3F Required Learning → Foundry completion loop — BUILT + VERIFIED-TO-BASELINE, DEPLOY+GATE PENDING (2026-07-21):** [FOUNDRY/UI] STEP-0 proved loop already exists (all 8 invariants; do-not-duplicate): mine RPC projects status/completed_at + roomUrl; open≠completion (DB CHECK on completed_at); claimXp/claimDocumentXp gate completed_at → claimAssignmentForParticipant→RPC match by immutable user_id_snapshot; 3-level idempotency; revalidate on mount/visibility/focus. **EXACT GAP: PDF client never connected the assignment for an AUTHENTICATED learner** — `completeDocumentTraining` awards XP inline→`completed_awarded`, but `FoundryDocumentClient` auto-claimed only on `completed_claimable`→claim-xp never fired→card stuck in Required. Video client already reconciled on awarded; PDF lacked parity. **FIX (client-only): auto-claim effect fires one silent claim-xp on EITHER terminal stage** (completed_claimable OR completed_awarded), guarded once. No server/route/schema/migration. tsc clean; +2 new tests (awarded case fails without fix) + 573 foundry/assignment/UI + 232 app-shell/middleware pass; full suite only 16 pre-existing (0 new); build PASS. Files: `src/app/f/[token]/FoundryDocumentClient.tsx`, `…assignmentClaim.test.tsx`. Inner `233b428c`. DB no-mutation. Deploy + device gate A–G pending.

**[x] Slice 3.1B-3E.3 + arena.btydaily.com CANONICAL-DOMAIN CUTOVER — GATE PASS → CLOSED (2026-07-21):** Commander confirmed Safari + installed app show the same clean BtyDailyAppShell (Today/Center/Arena/Foundry/Me). Worker `0ee2d5f8` (100%). Both CLOSED. Keeps (locked): site_url=arena, 18 allowlist entries, workers.dev, native `/start`. Do NOT reopen domain/auth for the Orb.

**[x] TODAY ORB — PLATFORM BOUNDARY = PRODUCT DECISION (CLOSED, 2026-07-21):** Commander decision: **the Orb is intentionally native-app-only.** Installed app keeps the Orb as part of the Today ritual/emotional entry; mobile & desktop web do NOT require it and its absence is **not a parity defect** (functional app-shell experiences remain aligned). **Do NOT open a Safari Orb parity slice; do NOT add a browser fallback or duplicate animation runtime.** Measurement (retained for provenance): visible Orb=`OrbLiving`, rendered ONLY at `/start/page.client.tsx:128` (native launch door), which Safari's post-3E.3 canonical entry bypasses — Class A product boundary, confirmed by this decision. No code change.

**[x] Slice 3.1B-3E.3 Canonical root→app-shell routing — SHIPPED · GATE PASS · CLOSED (2026-07-21):** [AUTH/MIDDLEWARE] Mobile Safari on `arena.btydaily.com` rendered the LEGACY portal instead of BtyDailyAppShell. Root cause: `middleware.ts` `/`→`/en` (hardcoded) + bare `/{locale}` renders `LandingClient` (legacy portal); clean shell only at `/{locale}/app`. FIX (middleware only): `/`→`resolveEntryLocale`(cookie→Accept-Language→en)→`/{locale}/app`; bare `/{locale}`(+slash)→`/{locale}/app`; unauth flows through existing gate→`/{locale}/bty/login?next=/{locale}/app`. Deep routes + `/f/<token>` + `/start` untouched; root `app/page.tsx` `/en`→`/en/app`. Contract C locked (header null on /app; portal nav not in /app tree). tsc clean; +17 new tests (13 canonical-entry + 4 header-purity) + all 60 middleware pass; full suite only the 16 pre-existing (0 new); build PASS (Middleware 83.7 kB). Files: `src/middleware.ts`, `src/app/page.tsx`, `src/middleware.canonical-entry.test.ts`, `src/components/LocaleLayoutHeader.applShellPurity.test.tsx`. Keeps site_url=arena/18 allowlist/workers.dev/`/f/`/switch→foundry/signin→me/native start. DB no-mutation. Deploy + Safari gate A–J + app gate K–O PENDING. Cutover still NOT closed.

**[x] Slices 3.1B-3E / 3E.1 / 3E.2 — DEVICE GATE FULL PASS → CLOSED (2026-07-21):** Commander ran the installed-app gate A–I end-to-end = FULL PASS (Sign out → login → sign in ddshanbit → `/en/app?tab=me` → bottom nav → Me email → Foundry Completed "배가 고파" → switch→Foundry → no `/en/bty`). Worker `eb6a53de` (100%). All three account-identity/return-contract/app-shell-return slices CLOSED. **NEXT [Commander-credential-gated]: canonical-domain cutover — flip Supabase `site_url` workers.dev→arena.btydaily.com, PRESERVE workers.dev `uri_allow_list` entries, keep workers.dev `/f/<token>` alive.** No Management API token in-session (`SUPABASE_ACCESS_TOKEN` absent; ref `mveycersmqfiuddslnrj`) → do in Supabase Dashboard (Auth → URL Configuration) or provide a PAT; then post-flip browser verify + no-mutation recon.

**[x] Slice 3.1B-3E.2 Post-login app-shell return fix (Gate H FAIL) — SHIPPED · DEVICE GATE PASS · CLOSED (2026-07-21):** [AUTH/MIDDLEWARE] Installed-app Sign out→sign in landed on `/en/bty` instead of `/en/app?tab=me`. Measured chain proves `signOutAccount` already emits `next=/app?tab=me` and the shell accepts `me` → defect is the `middleware.ts` authenticated-user-on-`/bty/login` branch DROPPING `next` and hard-redirecting to `/{locale}/bty` (native cookie-race bounces `/app`→login with next preserved → branch discarded it). FIX: branch resolves `next` via the single shared `sanitizeNextForRedirect` and 302s there; `/{locale}/bty` fallback only when next absent/external/protocol-relative/malformed/login-loop. Preserves account-switch→`/app?tab=foundry`. Hosted web only (1 branch) — no signOutAccount/login-card/native/Supabase change → no native rebuild. tsc clean; +7 new middleware tests + all 47 middleware pass; full suite only the 16 pre-existing (0 new, clean-stash proven); build PASS (Middleware 83.6 kB). Files: `src/middleware.ts`, `src/middleware.authed-login-next.test.ts` (new). DB no-mutation (pure routing). Deploy + Gate A–I PENDING; do NOT close 3.1B-3E.1, do NOT flip site_url.

**[x] Slice 3.1B-3E Installed-App Required-Learning Surface — APPLIED + DEPLOYED, DEVICE-GATE PENDING (2026-07-20):** Migration `20260725000000` applied (SQL Editor); RPC live-verified (ANON denied; RPC(Hanbit)→1 completed row for event `5e543327`, no summary). Inner commit **`3c3c1f37`**; Worker version **`278da1d9`** (100%, superseded `7480f07e`); served chunk `page-4906a49df2356ae1.js` (sha256 `52733b09…a360`) carries all learner-surface literals; live route unauth→401; targeted 23/23; assignments unchanged=1 (no new data). Ledger reconcile (SQL Editor) recommended — `supabase_migrations` is out-of-band. STOP at Commander Gates A–D (device).

**[~] Slice 3.1B-3E — BUILT/HALTED (superseded by the APPLIED+DEPLOYED row above) (2026-07-20):** [FOUNDRY/DB/API/UI] Learner reads their OWN Foundry assignments inside the installed app (Required/Completed inside the Foundry tab — no 6th tab). New service-role-only SECURITY DEFINER RPC `bty_foundry_list_my_assignments` (migration `20260725000000`), route `GET /api/bty/foundry/assignments/mine` (401 unauth, server-derived id only, no client user_id, `summary` omitted — no column), UI `FoundryRequiredLearning` (same-origin in-WebView Room open, refresh on mount/visibility/focus, no optimistic completion). tsc clean; 23 targeted pass incl. real Foundry-tab render; full suite only the 16 pre-existing (proven on clean-stash baseline, 0 new); build PASS (route = dynamic fn). Read-only live recon: assignments total=1 (event `5e543327`/completed/user_snap `d0b1af49`), new RPC ABSENT (pending). **NOT committed/applied/deployed** — HALT: no DDL path this session + must not deploy before the RPC exists (Gate A). Next: Commander applies migration → deploy → Gates A–D. Result: BUILD COMPLETE, DEPLOY GATED.

**[x] Arena Professional Identity Curation — FINAL REVIEW PASSED · APPLIED · DEPLOYED (Slice 3.1A-3, 2026-07-20):** [ARENA/DB/API/UI] Founder approved the final pre-deploy review incl. the **audit deletion-policy reversal**. (1) **Single-primary concurrency — no new index**: reused `bty_org_membership_one_active_primary` (unique `(user_id) where status='active' and is_primary=true`); the `for update` does not serialize same-user/different-org curations (disjoint rows) — the index does; RPC now conflict-aware via `get stacked diagnostics constraint_name` → `primary_membership_conflict`, full rollback. (2) **Active-target**: status judged AFTER lock, not filtered in WHERE (missing ≠ inactive); `organization_membership_inactive` raises before any write → target byte-equivalent, no audit row, no demotion. (3) **Audit FKs RESTRICT → `ON DELETE SET NULL` + NOT NULL snapshots** (measured: `bty_org_memberships.user_id` is already CASCADE to auth.users, `/api/admin/users` DELETE + e2e fixtures delete users every run; RESTRICT would block all three and deadlock the cascade). No CASCADE anywhere. (4) `search_path = pg_catalog, public`; EXECUTE service_role only; browser addresses a membership, never a user/actor. tsc clean; **74 focused**; arena+admin **910/910**; full suite only 16 pre-existing (identical set+signatures); build PASS. Commit inner-main **`295519ba`** (pushed). Migration **APPLIED** (only pending one; recorded in `schema_migrations`). Live-verified: `role_started_on`=date · index predicate · FKs SET NULL ×3 · snapshots NOT NULL / live FKs nullable · EXECUTE=postgres+service_role only · `search_path=pg_catalog, public` + secdef · RPC rejects inactive, no org_id change, no membership INSERT/DELETE · compat CHECK · audit RLS on, 0 client grants. Data: 24/24, multi-primary **0**, audit 0, legacy 3 & 2 UNCHANGED. Deploy PASS Worker **`7e439488-5f34-42d8-b5aa-53360dbc5fd7`** (100%; was `9561e5f1`); curate POST+GET unauth→401, org-memberships→401, page→307, `/api/version` 200, no secret VALUE (an initial GET 404 was a ~3s-post-deploy propagation artifact; re-probe 401). **GATE REVISED (Founder):** `change_set_id` / two-row audit requirement **STRUCK** — absent repo-wide; the RPC writes exactly 1 audit row (promoted target; demoted row unaudited). Deferred to **Slice 3.1A-3.1 Primary Membership Audit Completeness**; this Slice is not reopened. **Gate B (A→B primary switch) STRUCTURALLY UNREACHABLE — DEFERRED, NOT FAILED:** measured — all 24 members have exactly 1 membership (0 users with 2+) and `bty_organizations` has exactly 1 row (`BTY_LEGACY`; `BTY_DSO` is an enterprise, not an org), so there is no dual-membership account and no second org to switch to. Re-fires when a 2nd org + real dual-membership member exist; not manufactured for the gate. Gate A (curation persist-after-reload + Date-unknown) and Gate C (no permission/access/Foundry/Learning-Path/XP/history change) are **human/device gates the agent cannot execute**. Gate C baseline captured pre-gate (legacy 3/2 · approved 24 · arena_profiles 31 · core_xp 1662 · runs 626 · foundry 19/15 · office_assignments 2 · audit 0 · curated 0). Verdict **APPLIED + DEPLOYED — GATE A/C PENDING (human), GATE B DEFERRED**. outer = 이 ledger only.

**[x] Arena Professional Identity Curation — PRE-DEPLOY CORRECTION (Slice 3.1A-3, 2026-07-19):** [ARENA/DB/API/UI] 4 contract fixes on the initial build (same session), still gated/uncommitted: (1) RPC is now **membership-preserving** — keyed `(p_user_id, p_organization_id)`, curates the user's EXISTING membership in the selected org + (re)designates primary (demote-other/promote-target, scoped), **never changes organization_id / never insert-delete-repoint**; user with no membership in the selected org → `organization_membership_missing`. (2) **`role_started_on DATE`** column added (calendar, no TZ); timestamptz-midnight cast removed; read/write/audit repointed. (3) **`grant execute ... to service_role`** added after the revoke. (4) Audit FKs **`ON DELETE RESTRICT`** (was CASCADE) + audit gains `role_started_on date`/`is_primary` before-after. is_primary flip leaderboard-safe (leaderboard is_primary = `office_assignments`). tsc clean; **91 focused pass**; arena+admin 887/887; full suite only 16 pre-existing; build PASS. **NOT applied/deployed/committed — report under review.**

**[x] Arena Professional Identity Curation (Slice 3.1A-3, 2026-07-19):** [ARENA/DB/API/UI] Admin WRITE/curation path on top of 3.1A-1/3.1A-2. Migration `20260719000000_bty_org_membership_identity_curation_v1.sql` (**FILE-ONLY, not applied — apply is Commander-gated**): append-only `bty_org_membership_identity_audit` (before/after + changed_by + changed_at; client-deny), DB-level `bty_org_membership_family_role_compat` CHECK (incompatible pair unwritable), atomic SECURITY-DEFINER RPC `bty_curate_membership_identity` (row-lock → validate → UPDATE + audit INSERT one tx; `identity_source='admin_curated'`; EXECUTE revoked from public/anon/authenticated). Domain (pure): `validateIdentityCuration`/`roleStartDateError`/`isCalendarDateString` (role-requires-family, incompatible, future-date; today injected). Service: `resolveManageableOrganizations` (single scope seam) + `curateMembershipIdentity` (domain → org-manageable → target-in-scope → RPC; never trusts UI). API: `POST /api/admin/arena/org-memberships/curate` + `GET` (orgs+taxonomy); collection GET gains `organizationId`. UI: `/admin/arena-identity` dependent editor (Org/Family/Role, family-change invalidates role, Role Start Date + Date-unknown→null) + Complete/Needs-curation/Role-date-unknown status. HELD: no XP/AIR/Arena/Reflection/readiness read-or-write, no Learning-Path/module/privilege side effect, no legacy `memberships`/access-gate change, canonical IDs only, unknown never inferred, memberships never merged. `tsc` clean; **+47 focused tests**; arena+admin regression 883/883; full suite only **16 pre-existing** failures (ZERO new — proven with changes stashed out); `npm run build` PASS (curate route + page in manifest). Verdict **CODE COMPLETE — MIGRATION APPLY + DEPLOY + COMMANDER DEVICE GATE PENDING**. Supersedes the 3.1A-2 read-only-contract test. Not yet committed.

**[x] Arena Canonical Member Identity — Admin Read + Observability (Slice 3.1A-2, 2026-07-19):** [ARENA/ADMIN/UI] Read-only admin surface over the 3.1A-1 canonical foundation. New service `listCanonicalMembershipsForAdmin` (reads bty_org_memberships + bty_organizations + arena_membership_requests only; reconciliation + drift summary; NO writes; never touches legacy memberships/organizations); route `GET /api/admin/arena/org-memberships` (requireAdminEmail; server-side name+label resolution; strips raw user_id; GET-only); page `[locale]/admin/arena-identity` + AdminNav "Member Identity" (summary cards, aligned/drift banner, member list with "Not set" for unknown identity, Refresh only — no edit/curation controls); approval page surfaces the existing canonicalMembership write-through result non-blockingly (ok / reconciliation_pending + link; approval always succeeds). requireApprovedMembership + Arena access UNCHANGED; no curation/onboarding/auth/dual-read/cutover/audience-targeting. tsc clean; +19 tests; full suite only 16 pre-existing failures (ZERO new); build + cf:build PASS. (Deploy initially blocked by expired wrangler OAuth — no code defect; resumed from identical tree after `wrangler login`.) Commit inner-main `5354e6c0`. Deploy PASS Worker `9561e5f1-a4cb-4c58-a133-31e6e837b398` (100%; was `217de6cf`); org-memberships unauth→401, arena-identity unauth→307, /api/version 200; no client secret VALUE. Live: 24 approved / 24 active canonical / 24 unknown family+role → aligned; legacy memberships=3 & organizations=2 unchanged. Verdict **DEPLOYED — COMMANDER ADMIN UI GATE PENDING**. ADMIN WRITE-THROUGH gate still DEFERRED (needs first real pending request). 3.0A/3.0B/3.0C-1 unchanged. outer = 이 ledger only.

---

**[x] Arena Canonical Organization Membership Foundation (Slice 3.1A-1, 2026-07-18):** [ARENA/DB/MIGRATION] Prerequisite identity core for future audience targeting. STEP-0 STOP (plain `organizations` occupied by a live out-of-band admin table) → Founder namespaced the new tables `bty_*`. Migration `20260718000000_bty_org_membership_foundation_v1.sql` APPLIED to staging: `bty_enterprises→bty_organizations→bty_org_memberships` (client-deny RLS + revoke, service-role writes only; stable-key CHECK taxonomy NULL=unknown; one-active-primary partial unique index; bigint FK to bigserial request id). Seeded BTY_DSO/BTY_LEGACY; backfilled **24 approved members** as active-primary BTY_LEGACY with **job_family/role/dates NULL (never guessed)** from legacy leader/staff free text. Domain `orgIdentity` (7 families/11 roles; Partner/CD/Trainer/Lead NOT roles); service `organizationMembershipService` (idempotent, curation-safe, never a 2nd active primary, never writes legacy `memberships`); approve route write-through best-effort/non-atomic (reconciliation_pending on miss, never blocks approval). **Access UNCHANGED** — `requireApprovedMembership` untouched, canonical membership NOT read for access (Phase 1). Post-apply verified: 24 backfill reconciles, roles_leaked=0, dup=0, multi-primary=0, legacy memberships=3 & organizations=2 UNCHANGED, RLS×3/policies=0/write-grants=0. +34 tests; full suite only 16 pre-existing failures (ZERO new); tsc/build/cf:build PASS. Commit inner-main `55ad9f3d`. Deploy PASS Worker `217de6cf-a7f6-4d07-837d-95161b4fdfdf` (100%; was `cf0f72f3`); approve route unauth→401; no client secret VALUE. Verdict **MIGRATION APPLIED + DEPLOYED**. **ADMIN WRITE-THROUGH PRODUCT GATE — DEFERRED, NOT FAILED (2026-07-19):** gate verification (read-only) found staging UNCHANGED since backfill (approved 24, pending 0, `membership_approval`=0) → no pending request existed to approve, so write-through couldn't fire (re-approve of an approved member = 404 no-op); NO code defect, all automated/DB gates green. Commander confirmed the native app has no logout/account-switch/new-login path (reinstall restores session) → manual new-account approval test unreachable; adding logout/Gmail solely for the test is unjustified. Re-fires on the first real pending request OR when a canonical member-administration flow exists. No dual-read/cutover/audience-targeting/role-inference; 3.0A/3.0B/3.0C-1 unchanged; `requireAdminEmail` hardening deferred. Next: measure identity-administration increment. outer = 이 ledger only.

---

**[x] Foundry Adaptive Clarification (Slice 2.4C, 2026-07-17):** [FOUNDRY/DOMAIN/AI] Deterministic sufficiency gate between 2.4A direction-selection and the 2.4B Module Draft Copilot. Tapping "Draft the rest of this training" first consults a PURE domain verdict `assessClarification(ctx,state)`: sufficient (common) → straight into the EXISTING 2.4B generate→review→apply, ZERO questions; under-specified → the SMALLEST clarification, ONE question at a time, reassessed after each answer, stopping the instant it's sufficient, hard ceiling 3, "Draft with what I have" always available (authorship preserved). Detectors conservative + priority-ordered by downstream impact (v1 active: `observable_behavior` reusing `observableBehaviorWarning`, `success_evidence` <4 words; 7 dimensions modeled/extensible). Persistence UNCHANGED = `answers` jsonb; clarification rides a NAMESPACED `answers.clarification` key, excluded from `SNAPSHOT_ANSWER_KEYS` (never publishes), never overwrites a canonical field → NO migration, no second source of truth, resume-safe (answered questions never repeat after refresh). AI never decides progression — clarifications only enrich the draft prompt (server reads them from the draft; not in the stale fingerprint). No display strings in domain (UI copy localizes by dimension/choice key; evidence chips = seen/heard/recorded/confirmed + custom). Files: domain `clarification.ts` (+ `.test.ts`), `module-builder.ts` (+`clarification?` field + `validateDraftPatch` bounds), `moduleDraftCopilotService.ts` (clarifications arg), route `module-draft/route.ts` (pass-through), `ModuleDraftCopilot.tsx` (new `clarifying` phase), `ModuleBuilderShell.tsx` (assess + persist wiring), `moduleBuilderCopy.ts` (en+ko clarification copy). SCOPE HONORED: no Arena scenario/Practice-Pack generation, no publish change, no canonical-schema change beyond the namespaced key, 2.4A/2.4B behavior unchanged, "Create Arena practice" stays a handoff. tsc clean; +21 tests (domain 11/persistence 3/component 6/route 1); Foundry+Arena suite 892; full suite only 16 pre-existing failures (ZERO new); zero new terminology violations (28=28 A/B); build OK. Deploy PASS Worker Version `cf0f72f3-0b98-479a-8db7-6ee83f1bd4e7` (100% active, tail of deployments list, Created 2026-07-18T05:31:15Z; was `2aa43a64`); worker `/api/version` 200. Verdict **DEPLOYED — COMMANDER iPhone PRODUCT GATE PENDING** (served version ≠ product proof). 2.4B CLOSED (PASS). outer = 이 ledger only.

---

**[x] Foundry Direction-to-Module Draft Copilot (Slice 2.4B, 2026-07-17):** [FOUNDRY/API/AI] After a 2.4A direction is applied + audience picked, step 5 offers an OPTIONAL "Draft the rest of this training": BTY proposes ONE structured draft; the Host reviews each section (keep current / use suggestion / skip; edits the completion question); only on "Apply reviewed draft" are approved fields written. Applied canonical fields = learningNeeds, completionPrompt, arenaRecommended, followUpDays (all whitelisted, existing answers jsonb → NO migration); no canonical title/follow-up-guidance field exists → rationales/material-guidance/assumptions/warnings are advisory-only. Mirrors 2.4A security/generation: domain `module-draft-copilot.ts` (context reconstruct via Builder steps 1–4 gate + fingerprint stale-guard + fail-closed validator: enums/length/markup/completion-quality/evidence-honesty/material-honesty/distinctness); server service (timeout + one bounded retry, no fallback); route `POST /modules/[id]/module-draft` (requireManager + owner-scope + editable + server-reconstructed context + 409 stale + 422 insufficient + KV rate-limit; zero mutation, no raw output); UI ModuleDraftCopilot (idle→loading→review→applied/error; empty→Use, existing→Keep-current never silently replaced; stale blocks Apply; mounted step 5; canonical patchAnswers; failure never blocks manual). Not Dr. Chi voice. Publish/participant/Arena/QR/XP/PDF/YouTube/target-roles untouched; no auto-approve/publish. tsc clean; +54 tests (domain 24/service 6/route 9/UI 12/shell 3); Foundry+Arena suite 907; full suite only 16 pre-existing failures (ZERO new); build OK; no client secret leak. Commit inner-main `8109e0c2..33f0aa7e` (pushed). Deploy PASS Worker Version `2aa43a64-0ec2-4822-9ab9-93701a6b0ce3` (100%; was `68f7e1ee`); served-code verified (route unauth POST 401 not 404; chunk `page-1055748286d906f9.js` 200 carries EN+KO module-draft literals). Verdict **DEPLOYED — COMMANDER MODULE-DRAFT COPILOT GATE PENDING** (served literals ≠ product proof). 2.4C not started. outer = 이 ledger only.

---

**[x] Arena Practice Choice Ghost-Capsule Fix (Slice 2.4A.4, 2026-07-17):** [ARENA/UI] Commander device: the first choice on the three-choice DECIDE screen showed an unexplained empty right-side capsule (others didn't) → looked preselected/broken. Measured: `ArenaPracticePlayer.ChoiceCard` rendered a right-side pill from a `tag` prop; only the ACTION-DECISION phase passed `tag = isActionCommitment ? "action"/"행동" : undefined`. `isActionCommitment` is an INTERNAL authoring classification (real-action vs waiting/deferring; contract guarantees ≥1, usually first) → lone pill on card #1, and it LEAKS which option is the preferred action (directive-forbidden). No learner purpose → removed the pill entirely: ChoiceCard is now label-only, identical structure every phase; deleted the `tag` prop, capsule markup, and unused `actionTag` copy (en/ko). No preselection, no new selection state, no index special-casing; selection = phase advance. Accessibility preserved (native buttons, label = accessible name). Scenario JSON, choice-submission API, reducer/progression, scoring, XP, QR, pattern, persistence untouched. +11 rendered tests (reproduction failing-pre-fix; no-pill; equivalent-structure/no-preselect; pill-free primary+tradeoff; no hidden score/correctness/XP/commitment; accessible-name; full-width; first/second-selection-identical; full-progression+complete-once; test-mode-no-completion; KO pill-free). tsc clean; Arena+Foundry suite 853; full suite only 16 pre-existing failures (ZERO new); build OK; no migration. Commit inner-main `4f731123..8109e0c2` (pushed). Deploy PASS Worker Version `68f7e1ee-4df0-4c86-adbc-0ad9e90a35d2` (100%; was `f325f850`); served-code verified (player chunk `page-1f4ac291ed83fafb.js` 200 retains player strings = correct build; `actionTag` key — present ONLY for the removed pill — absent from served bundle + all deployed chunks). Verdict **DEPLOYED — COMMANDER ARENA CHOICE VISUAL GATE PENDING** (rule: an unselected choice must never look preselected or contain a control with no visible meaning). No new slice started. outer = 이 ledger only.

---

**[x] Foundry Review Completion Gate Reconciliation (Slice 2.4A.3, 2026-07-17):** [FOUNDRY/UI/DOMAIN] Commander device: Approve disabled + "Complete the highlighted sections first" but NO row highlighted → Host blocked with no visible corrective action. Measured root cause: Approve gated by `builderApprovalErrors` (steps 1–7 + youtube-url; server re-enforces the SAME gate — no client/server drift) while the Review highlight was a separate incomplete mapping (only behavior+material rows). Every other blocking field (problem/audience/evidence/learning/follow-up/youtube-url/pdf) blocked with zero highlight; Follow-up even rendered "No follow-up" when unset (masked missing as done). Copilot binding verified correct (behavior/evidence write the exact canonical keys). Fix (UI/domain/copy only, NO migration, no new required fields, server authority unchanged): new domain `reviewMissingSections` = single source mapping every blocking code (+ service `material_pdf_required`) → {section, step}, de-duped/ordered, with an `ALL_BLOCKING_CODES` totality invariant test; Review rows derive missing-state from it (gold rail + Required badge + actionable reason + Edit); explicit "N section(s) need attention" named/tappable summary at top AND beside Approve; Approve disabled iff missing>0; follow-up/material show honest "Not added yet"; removed dead `publishNotReady`/`needsAttention` copy. +13 tests (domain mapping/whitespace/ordering/optional-capability/Copilot-values/PDF-fold/invariant; shell REPRODUCTION failing-pre-fix + edit→complete→Approve-enables + complete-clean + Copilot-recognized; 3 stale tests updated). tsc clean; Foundry suite 818; full suite only 16 pre-existing failures (ZERO new); build OK. Commit inner-main `f435586a..4f731123` (pushed). Deploy PASS Worker Version `f325f850-6098-4e52-a0bb-f8397087ae52` (100%; was `7e739f8a`); served-code verified (chunk `page-9d12e94ea15f205e.js` 200 carries new EN+KO summary/Required copy; old "Complete the highlighted sections first" GONE EN+KO; route 401). Verdict **DEPLOYED — COMMANDER REVIEW COMPLETION GATE PENDING** (rule: never block a Host without showing the exact action). 2.4B not started. outer = 이 ledger only.

---

**[x] Foundry Copilot Entry Discoverability Polish (Slice 2.4A.2, 2026-07-17):** [FOUNDRY/UI] Commander device: 2.4A works but the Host missed the small inline Copilot trigger and continued manually — discoverability/mobile-hierarchy polish (NOT a functional failure). UI/copy ONLY (4 files; NO generation/API/persistence/validation/Builder-architecture change — git-diff-name confirmed). (1) Idle trigger → a clearly-separated OPTIONAL assistive block under the problem input: eyebrow "Not sure how to turn this into training?" + full-width action "Show me three possible directions" (iPhone touch target) + support "…review and edit before anything is applied."; restrained gold/navy, secondary to the primary Next, no auto-open/auto-generate; EN+KO. (2) Card density (measured: Commander reported slow 3-card comparison) — title+Capability+Draft-behavior+Use always visible; Why/Evidence/Assumption collapsed behind "View details/Hide details" (aria-expanded/controls), default collapsed; no info removed, contract unchanged, Use works collapsed+expanded. All 2.4A functionality preserved. tsc clean; +6 tests (assistive block EN/KO, collapse/expand, select-from-both, shell block-above-footer + hidden-for-short-input; rendered discoverability, not literal-only); Foundry suite 805; full suite only 16 pre-existing failures (ZERO new); build OK; no migration. Commit inner-main `da9f4754..f435586a` (pushed). Deploy PASS Worker Version `7e739f8a-0c6b-4fe5-a528-efdaa49077b9` (100%; was `fb8b693b`); served-code verified (chunk `page-ac9f2e04abdf4831.js` 200 carries EN+KO block + View/Hide details; route 401 unchanged; no secret leak). Verdict **DEPLOYED — COMMANDER DISCOVERABILITY GATE PENDING** ("would a first-time Host notice this without being told?"). 2.4B not started. outer = 이 ledger only.

---

**[x] Foundry Intent-to-Module Direction Copilot (Slice 2.4A, 2026-07-17):** [FOUNDRY/API/AI] Assistive AI direction-selection on the Guided Module Builder problem step: Host writes one problem → "Show me three directions" → exactly 3 meaningfully-different directions (Capability/why/behavior/evidence/assumption) → Use → review+edit → Apply → only capability+behavior+evidence written to canonical Builder fields via the existing autosave path. Model proposes, Host reviews/edits/confirms; nothing changes until Apply; no hidden generation, no silent overwrite, no target-role application. Fail-closed pure validator (`direction-copilot.ts`: exactly-three, distinctness, observability, evidence-honesty, unsafe-markup, unknown-fields-dropped, server-assigned ids); server service (bounded timeout + ONE bounded retry, NO deterministic fallback); Host-gated route `POST /modules/[id]/directions` (owner-scope + editable-draft + stale-problem guard + KV rate-limit; never mutates draft/returns raw output/logs the problem); UI state machine (idle→loading→results→review→applied/error; stale results block Apply; failure never blocks the manual Builder). NO migration — advisory `capabilityCandidate` rides `answers` jsonb; capability applied+editable via a Step-3 input + review row, restores on reload. NOT Dr. Chi voice. Publish/QR/XP/PDF/YouTube/completion-question/participant flow untouched. tsc clean; +53 tests (validator 27/route 11/service 6/UI 9/shell 2); full Foundry suite 799 green; full suite only 16 pre-existing failures (ZERO new); build OK. Commit inner-main `5bf76fe8..da9f4754` (pushed). Deploy PASS Worker Version `fb8b693b-2c60-43c5-b1d4-3cdee130b44a` (100%; was `aee37a55`); served-code verified (new route unauth POST 401 not 404; staging chunk `page-470883e03588098d.js` 200 carries copilot en+ko literals; no secret leak). Verdict **DEPLOYED — COMMANDER HOST-DEVICE GATE PENDING** (served literals/routes ≠ product proof). 2.4B not started. outer = 이 ledger only.

---

**[x] Foundry Native In-Shell Arena Practice Player (Slice 3.0C-1, 2026-07-17):** [FOUNDRY/ARENA/NATIVE] The whole Arena Practice journey (list→start→play→complete→list) now runs INSIDE BtyDailyAppShell's arena tab as LOCAL state — ZERO route navigation (the 3.0B.3 regression was router.push ejecting to standalone web). New `ArenaRoom` (app-shell) replaces the arena LockedRoom; reuses `ArenaPracticePlayer` unchanged (callbacks→setView, no router — enforced by a source-contract test). New `arenaRoomActions` (Node-testable, injected fetch): START CONTRACT corrected (snapshot→validate→POST start→run id; play only on full success, else error+Retry, no false success); completion honest (POST+await; failure preserves local state + Retry/Back, no false persisted completion); only /api/arena/practice* (no /api/arena/run, no XP). Renders as flow content in shell <main> (no overlay/nested-scroll/portal/second-nav); AppTabBar unchanged. Foundry publish-link safety: removed router.push "Open in Arena" → non-navigating "Open the Arena tab to start this practice." + Back to editor. Today/Center/Foundry/Me + canonical Arena untouched. tsc clean; +23 tests; full suite only 16 pre-existing failures (ZERO new); build OK; no migration. Commit inner-main `5bf76fe8` (pushed). Deploy PASS Worker Version `aee37a55` (100%); served-code verified (ArenaRoom literals present, 3.0B.3 bridge gone, publish-safety copy present, no secret leak). Verdict **DEPLOYED — COMMANDER iPhone PRODUCT GATE PENDING** (served literals ≠ product proof). outer = 이 ledger only.

---

**[!] EMERGENCY ROLLBACK — Slice 3.0B.3 native app-shell regression (2026-07-17):** [FOUNDRY/ARENA/NATIVE] Real iPhone regression: Start practice unresponsive; "Enter Arena training" + Center/Foundry bottom tabs navigated OUT to standalone web routes instead of staying in-app (BtyDailyAppShell's in-component tabs deliberately don't navigate; 3.0B.3's router.push broke the shell). Commander authorized rollback of 3.0B.3 ONLY. DONE: restored Worker Version `984625c8` (3.0B.2 build) 100% active; reverted `ba7a5c5b` via normal revert commit `fd8d8f80` (no force-push; pushed); tree identical to 3.0B.2 (empty diff), ArenaTabRoom deleted + arena LockedRoom restored. NO migration reverted, NO data deleted — all 3 tables + the published practice `48065992` intact. Served-code proof (rebuilt reverted tree, local verify only): ArenaTabRoom/"Enter Arena training"/"Preparing your decision training" = 0 in the /app chunk; 3.0B.2 features preserved. **Native app-shell tab/nav architecture MUST be re-measured before any further Arena-tab attempt** (Practice play must live INSIDE the shell without router.push). Verdict **ROLLED BACK to 3.0B.2 — COMMANDER iPhone RESTORATION GATE PENDING**. outer = 이 ledger only.

**[~] Foundry Native App-Shell Arena Tab = Practice Room (Slice 3.0B.3, 2026-07-17) — ROLLED BACK (see entry above):** [FOUNDRY/ARENA] The Commander's "being prepared" screen was BtyDailyAppShell's static `<LockedRoom>` for the arena tab (line 1594). DECISIVE: the native app is mounted at `/[locale]/app` = BtyDailyAppShell, which owns tabs via in-component state (AppTabBar/setTab) — NOT the /bty-arena route. So all prior 3.0B.x changes were on a surface the native Arena tab never renders; the arena tab was a permanent locked stub (no request/bootstrap/discovery). Fix (UI only, no migration, 2 files): new `ArenaTabRoom` replaces the arena LockedRoom — loads published practices INDEPENDENTLY (decoupled by construction) with a bounded 10s timeout so it never stays on "preparing"; states loading→list/empty(calm)/error+Retry; refetch on mount/focus/visibility; Start deep-links the play route; subtle "Enter Arena training" keeps canonical reachable. Canonical Arena untouched. tsc clean; build OK; full suite only 16 pre-existing failures (ZERO new). Commit inner-main `ba7a5c5b` (pushed). Deploy PASS Worker Version `59e44d68` (100%); served-code verified (/app shell chunk carries ArenaTabRoom literals; no secret leak). Verdict **DEPLOYED — COMMANDER iPhone GATE PENDING** (gate = the Practice card visibly appearing in the real iPhone Arena tab; not declared on served literals/API 200s). outer = 이 ledger only.

---

**[x] Foundry Real Native Arena-Tab Discovery + Publish State (Slice 3.0B.2, 2026-07-17):** [FOUNDRY/ARENA] 3.0B.1 didn't fix the product (list never showed in the real Arena tab). Traced actual path: bottom-nav Arena href resolves via useArenaEntryResolution → /[locale]/bty-arena (ArenaEntryClient), layout=ArenaLayoutShell (force-dynamic). Root cause: the list was rendered only INSIDE ArenaEntryClient branches → lost to state-specific early returns / route cache. Fix (NO migration): ONE shared `ArenaPracticeDiscovery` rendered by ArenaLayoutShell ABOVE the page (lobby-gated), visible states (loading/list/empty/error+Retry), refetch on mount/route/focus/visibility; removed the duplicate from ArenaEntryClient. Publish already-live banner + [Open in Arena]/[Back to editor] success (getPublishedForCurrentRevision); Create/Continue/Manage control-room label (hasPublishedForEvent). tsc clean; 57 arena-lib tests (+2) pass; full suite only 16 pre-existing failures (ZERO new); build OK. Commit inner-main `d0090246` (pushed). Deploy PASS Worker Version `984625c8` (100%); served-code verified (routes 401, discovery chunk 200 w/ literals, no secret leak). Verdict **DEPLOYED — COMMANDER iPhone PRODUCT GATE PENDING** (served literals/routes/DB = supporting evidence only; not claimed). outer = 이 ledger only.

---

**[x] Foundry Arena Practice Visibility + Test Entry Reconciliation (Slice 3.0B.1, 2026-07-17):** [FOUNDRY/ARENA] Fix for 3.0B device failure. MEASURED LIVE: the Commander's publish DID create a row (48065992 "Practicing Gratitude in Leadership"); failures were client/authz/placement. Root causes: (1) discovery list only in ArenaEntryClient default branch (active users land on NEXT_SCENARIO_READY) + fetch-once-on-mount staleness; (2) Test in Arena was a subtle header link (scrolled past); (3) publish confirmation was only a button-label change. Fixes (NO migration — schema already has published_by): creator visibility (own published always + all_members if approved; requireArenaAccess non-fatal membership; canAccessPractice creator-or-approved); prominent Test (disabled+"Save this draft before testing" when dirty); publish success screen + [Open in Arena] deep-link + [Return to training]; discovery rendered on next-ready surface too + refetch on mount/route/focus/visibility + loading/list/empty/error(retry) states. tsc clean; 85 arena tests (+3) pass; full suite only 16 pre-existing failures (ZERO new); build OK. Commit inner-main `09014828` (pushed). Deploy PASS Worker Version `7a01fe92` (100%); verified routes 401, LIVE-DB creator query returns own row, served new-UI literals, no secret leak. Verdict **DEPLOYED — COMMANDER iPhone END-TO-END DEVICE GATE PENDING** (served literals/routes ≠ product proof; not claimed). outer = 이 ledger only.

---

**[x] Foundry Arena Practice Publish + Playable (Slice 3.0B, 2026-07-17):** [FOUNDRY/ARENA] Makes a saved 3.0A draft testable, publishable, and playable in the real Arena. Root cause of "not visible": 3.0A draft had no publish/runtime-store/run/discovery (not a rendering bug). Runtime: a dedicated practice loader + surface reusing Arena presentation (`EliteArenaStep2Context`) + canonical phase semantics PRIMARY→TRADEOFF→ACTION DECISION — NOT the canonical fixed-cardinality/branched/XP-bound binding (would fabricate content + touch invariant zones), NOT the legacy 1+1 system. Two client-deny tables with NO XP columns: `foundry_published_arena_practices` (immutable snapshot + lineage; UNIQUE(source_draft_id,revision) idempotency, new revision = new row) + `foundry_arena_practice_runs` (isolated from arena_runs → zero-XP, never complete_verified, no user_scenario_history). Test in Arena = ephemeral (no run/XP/record); Publish = exact saved revision (stale→409, invalid→422, dup→same row); Arena tab discovery + `/bty-arena/practice/[id]` play route records zero-XP start/complete. Migration `20260717020000` APPLIED+VERIFIED (13/6 cols no-xp, UNIQUE, FKs never arena_runs, RLS on, grants NONE). tsc clean; **82 arena tests** (+24) pass; full suite only 16 pre-existing failures (ZERO new); build OK. Commit inner-main `c29f4985` (pushed). Deploy PASS Worker Version `ed090b38` (100%); served-code verified (5 routes 401 not 404; Test/Publish + discovery + play literals in served chunks; no secret leak). Verdict **DEPLOYED — COMMANDER iPhone TEST/PUBLISH/PLAY DEVICE GATE PENDING** (not headlessly executable; not claimed). outer = 이 ledger only.

---

**[x] Foundry Guided Arena Builder V1 — single Arena scenario DRAFT from a published module (Slice 3.0A, 2026-07-17):** [FOUNDRY] Host opens a published module → "Create Arena practice" → 2 guided questions → AI-generated three-phase scenario draft (PRIMARY/TRADEOFF/ACTION DECISION, ≥1 action-commitment) → edit → save → reopen → preview. Extends existing seams only (NO parallel Arena runtime; live Arena scenario contract untouched). New client-deny table `foundry_arena_scenario_drafts` binds the EXACT immutable `foundry_event_module` version (event+version+source_draft_id, never "latest"). AI via existing `llm/client` + deterministic domain validator + template fallback (works with no key). Auth = `requireManager` + owner-scoped + owner-of-source check. Source-summary route at TOP-LEVEL `arena-source/[eventId]` (dodges opennext dynamic-sibling shadow). UI iPhone-first, one-question-per-screen, in-app hub view + control-room button; non-playable self-contained preview (honest boundary vs Arena's cost-invariant components). Failure honesty throughout (provider fail keeps answers→template; save fail visible; invalid edit 422; missing/foreign/retired source honest). tsc clean; **58 new tests** pass; full suite only the 16 pre-existing failures (ZERO new); build OK (4 routes compiled); terminology net-clean. Migration `20260717010000` **APPLIED+VERIFIED** (mgmt API → info_schema: 12 cols, 4 CHECKs, 3 FKs incl. source_draft_id NO-cascade, RLS on, client grants NONE). Commit inner-main **`fab062d1`** (pushed); outer docs `cae5d0a6`. Deploy PASS `bty-arena-staging` Worker Version **`d9df4123`** (100% active). Served-code verified: 4 new routes 401 (gated, NOT 404 → no shadow); chunk `page-59998880587b4ce2.js` (200) carries "Create Arena practice"/"아레나 연습 만들기"/Q1; no secret leak. Verdict **DEPLOYED — COMMANDER HOST iPhone DEVICE GATE PENDING** (not headlessly executable; not claimed). outer = 이 ledger only.

---

**[x] Foundry Immersive Player V1 — BTY owns immersive, drop YouTube/native fullscreen dependency (2026-07-15):** [FOUNDRY/UI] Commander retest of the capability-based fix = FAIL: on iPhone Safari the native/webkit request only enlarged the iframe while Safari address+tab bars stayed — intermediate broken state. **Product decision: BTY owns the immersive experience; YouTube is only the video renderer.** Tapping the control now ALWAYS enters BTY Immersive Mode immediately — NO browser Fullscreen API, NO capability branching → identical on every platform. Container: `fixed inset-0`, `100vw×100dvh`, `z-[2147483647]` above all app chrome, black bg, safe-area exit control, reversible body scroll-lock; player NEVER remounted (playback/wake-lock/resume/checkpoints/reflection preserved). Removed `src/lib/native/fullscreen.ts` + test (false-success assumption gone). Button renamed enter/exit "immersive mode". Only the Foundry player changed. tsc clean; 249 suite pass; build OK. Deployed `bty-arena-staging` Version `a867b54b-da99-41a3-a3a1-bf41038a73ae` (100%), chunk `page-9b6877a4fa758674.js`; served markers present (data-immersive/100dvh/z-[2147483647]/"immersive mode") + CSS `z-index:2147483647` generated; native markers (requestFullscreen/webkit*/native_fullscreen) all GONE (0); zero secret leak. Inner `27d7960e`→inner-main. **COMMANDER IMMERSIVE RETEST — PENDING.** outer = 이 ledger only.

---

**[~] Foundry Fullscreen Repair — capability-based + immersive fallback (2026-07-15, SUPERSEDED same day by Immersive Player V1):** [FOUNDRY/UI] Commander device evidence on Immersive V1: screen-awake/resume/checkpoint/reflection PASS, **fullscreen entry FAILED**. Root cause: iPhone Safari / installed Capacitor WKWebView do NOT implement `Element.requestFullscreen` (only `HTMLVideoElement.webkitEnterFullscreen`, unreachable for a cross-origin YT iframe) → the custom button silently no-op'd (prior code: single `el.requestFullscreen` guarded by `if`, errors swallowed). Fix (fullscreen path ONLY): new capability helper `src/lib/native/fullscreen.ts` — `enterFullscreen()` picks standard→webkit→`onFallback`, initiated synchronously in the tap gesture, errors surfaced dev-only (not swallowed); `exitNativeFullscreen`/`currentFullscreenElement`. `YouTubePlayer.tsx`: on no-native, activate in-app immersive fallback (container `fixed inset-0` `100vw×100dvh` z-60 covering page, safe-area exit control, reversible body scroll-lock) WITHOUT remounting the player (playback/wake-lock/resume preserved); ensures iframe `allowfullscreen`; listens `fullscreenchange`+`webkitfullscreenchange`; never claims OS fullscreen when only immersive. +13 focused tests (helper 8 + component 5); nothing else touched. tsc clean; 257 suite pass; build OK. Deployed `bty-arena-staging` Version `4afb3f09-1773-41c5-8afc-cd4fa1a23336` (100%), chunk `page-4927b4db17626dc9.js`; served markers present (immersive_fallback/native_fullscreen/webkitRequestFullscreen/webkitfullscreenchange/100dvh/data-immersive), zero secret leak, only `/f/[token]` chunk changed. Inner `c25774d8`→inner-main. **COMMANDER FULLSCREEN RETEST — PENDING.** outer = 이 ledger only.

---

**[x] Foundry Immersive Learning + AI Reflection V1 (2026-07-14):** [FOUNDRY] New arc after the (CLOSED) Embeddability Gate. Deeply immersive learning on the participant watch surface `/f/[token]` (standalone, shell-free). **Hybrid persistence per Commander decision: telemetry EPHEMERAL, meaning PERSISTENT.** (1) **Watch Integrity Engine** — pure client-side reducer `domain/foundry/watch-integrity.ts` (playback ticks→WatchCoverage/ForwardSeekCount→CompletionState pass|review|incomplete); raw telemetry never persisted, never shown; never gates XP. (2) **Immersive video** — one-tap Fullscreen, Screen Wake Lock API + native `window.Capacitor.Plugins.KeepAwake` bridge (no `@capacitor/*` dep) via `lib/native/keepAwake.ts`, sessionStorage playback-resume, immersive UI hide while playing. (3) **Anti-summary prompts** — personal-reflection questions (never "main point"), expression layer. (4) **AI Living Reflection V1** — 4 sections (What Emerged / Where You Stretched / Living Sentence / Next Invitation). Pipeline **Reality→Rule Engine(domain decides meaning)→Context Builder→LLM expresses→Validator→persist**; LLM (`getLlmClient`, OpenAI-compat) with deterministic template fallback = SAME meaning; Validator rejects leaked metrics/score/homework. (5) **Checkpoints** — optional 0–2 pauses, never mandatory. Files: `domain/foundry/{watch-integrity,living-reflection}.ts`, `lib/bty/foundry/events/{reflectionExpression,livingReflectionService}.ts`, `lib/native/keepAwake.ts`, `app/f/[token]/{YouTubePlayer,FoundryJoinClient}.tsx`, new route `api/bty/foundry/public/[token]/reflection`, additive migration `20260715000000_foundry_living_reflection_v1.sql` (4 nullable cols on `foundry_event_training_progress`; no XP/response/join/creation/gate/routing change). tsc clean; 243 foundry+native unit tests pass (+59 new); build OK. **DEPLOYED 2026-07-14:** migration applied+verified (REST types+CHECK behavioral); inner `872eb033`→inner-main + outer docs→main (remote==local); staging Version `82c7da77` (100%, BUILD_ID `ofKNMoM7BT_Pqeq-edKCU`); served markers present, zero secret leak; live smoke PASS (real OpenAI provider path ≠ template, meaning-only persistence, idempotent, controlled fallback, disposable data cleaned). Residual: staging-worker HTTP LLM call + native KeepAwake presence → device gate. **COMMANDER DEVICE GATE — PENDING.** outer = 이 ledger only.

---

**[x] Yesterday Memory — Arrival Order Patch V1 (2026-07-13):** [TODAY/PRESENTATION] Commander HOLD: memory arrived AFTER the Today surface. Root cause: doors rise off MOUNT clock (btyRise @300-500ms) while memory was gated behind its async fetch (mounted late). Presentation-only fix, inner `e6391f8a` (`BtyDailyAppShell.tsx` + test, +92/−25; no resolver/API/DB/copy): shell holds arrival behind EXISTING hydration gate until memory settles (`todayHydrated && !memoryPending`, bounded `MEMORY_ARRIVAL_CAP_MS=900`, late-read lockout to avoid swap) → memory present at first paint, rides trace beat @200ms AHEAD of doors → Greeting → Memory → doors. Trace decoupled from intel when memory exists. Doors/affordance choreography + interaction untouched. No-memory = unchanged, no pause; tab re-entry = at rest, no replay. tsc clean; today 37→40; full suite 4042 pass (16 pre-existing). Deploy `bty-arena-staging` Version `d8b331c2-6d94-46d0-b71d-bd806d36b831`; new chunk `page-44c6c92bbc85665b.js` carries `data-today-memory` + fetch URL, prior chunk 404 (rotated), route 401. **COMMANDER DEVICE GATE — PENDING.** outer = 이 ledger only.

**[x] Yesterday → Today Memory Bridge V1 (2026-07-13):** [TODAY/DAILY] STEP 0: existing arrival trace (`data-today-status`) was generic Arena-intel, not the user's real prior choice; evidence + resolver (`readYesterdayContext` over `today_relationship_commitments`) already existed (hidden generation context only). Promoted the trace to evidence-backed memory, **migration-free**. Inner `e84c27c3` (6 files): PURE `domain/daily/yesterdayMemory.ts` (committed relationship → remembered CHOICE, null on absent/degraded, no judgment/completion language) + loader + GET `/api/me/today/yesterday-memory` (read-only, English-only) + shell wiring (trace holds until intel+memory settle → no swap/no layout shift/doors non-blocking; `data-today-memory` marker). No-evidence Today byte-identical. tsc clean; resolver 7/7, today 33→37; 16 full-suite failures pre-existing (stash-verified unrelated). Deploy `bty-arena-staging` Version `7abcaedd-5cc3-48fc-842a-6200ed43c0b1`; client chunk `page-d39409976442efc5.js` carries `api/me/today/yesterday-memory`×2 + `data-today-memory`; route → 401 auth-gated. Boundary: Arena/Foundry/XP/Orb/Center-UX/schema/legacy-`/today` untouched. **COMMANDER DEVICE GATE — PENDING.** outer = 이 ledger only.

**[x] Center Return V1 — the return settle (2026-07-13):** [CENTER/UI] STEP 0 audit → RETURN read as instantly-swapped content (hard loading→held cut, structurally = Today's Held-in-Center). Narrowest seam = entry rhythm. Inner `3fdfb72f` (`CenterKeepRoom.tsx` + test ONLY, +19/−1): `btyCenterReturn` keyframe (opacity+3px, 560ms, no-delay) on the held block **only when `returned`**; SAVE path stays instant → return≠save at motion level. reduced-motion = opacity-only. No copy/DB/API/AI/engine; Today untouched. tsc clean · 145/145 green. Deploy `bty-arena-staging` Version `4ccd5fa9-c739-4c89-a126-1a75020c1c62`; served chunk `page-efc1ce42b917222c.js` carries `btyCenterReturn`×5. **COMMANDER DEVICE GATE — PENDING.** outer = 이 ledger only.

---


**[x] Architecture Freeze Rule — Product-Driven phase declared (2026-06-25):** [DOCS/CULTURE] Architecture Phase **CLOSED**. `BTY_IMPLEMENTATION_RULES.md` §0 Architecture Freeze Rule — frozen stack, default=code-only, Think→ADR→Code→Evidence, product evidence metrics. Project State phase flag. Onboarding+Constitution+`.cursorrules`+`CLAUDE.md` sync. No new `*_CANON`/`*_ARCHITECTURE`/`*_v2` without Commander. outer = 이 ledger only.

---

**[x] BTY doc OS — Last Resort Principle locked (2026-06-25):** [DOCS] 문서 아키텍처 동결 선언. Onboarding+Constitution+Implementation Rules+decisions/README에 **"새 문서는 마지막 수단"** 운영법 고정(1질문1문서 표, History→Knowledge→Code, Learning=코드만/제품전환=ADR만). Roadmap에 "증거=실행 앱" 한 줄. **이후 확장=ADR append+State/Roadmap update만; Canon/Boot/Rule 신규 금지.** outer = 이 ledger only.

---

**[x] BTY AI OS v1.2 — ADR layer + 60-second Self Test gate (2026-06-25):** [DOCS] `docs/decisions/` ADR-001~005(why: Core XP root, Event 200-benign duplicate scan, Learning verify-first, SECURITY DEFINER revoke, Evidence≠Verification). 읽기 스택에 ADR 삽입(Rules→ADR→Code). Onboarding Part D 60초 OS 테스트. Constitution 문서법에 ADR 추가. outer = 이 ledger only.

---

**[x] BTY AI OS v1.1 — Constitution-first order + Roadmap split + Self Test LOCK (2026-06-25):** [DOCS] 읽기 순서 재정렬(Onboarding→Constitution→Boot→State→Roadmap→Rules). `docs/BTY_PRODUCT_ROADMAP.md` 신설(State와 분리). Constitution·Boot **LOCKED BIOS** 표기. Onboarding Self Test 8오해+canonical 3문장+파괴방지 목적. `CLAUDE.md`·`.cursorrules` 동기화. outer = 이 ledger only.

---

**[x] BTY AI Onboarding System — BOOT + Constitution + Project State + Implementation Rules (2026-06-25):** [DOCS] AI BIOS 5종 신설 — `docs/BTY_AI_BOOT.md`(철학·사고 12원칙), `docs/BTY_PRODUCT_CONSTITUTION.md`(영구 제품 헌법 11조), `docs/BTY_PROJECT_STATE.md`(현재 단계·엔진·우선순위), `docs/BTY_IMPLEMENTATION_RULES.md`(레이어·ledger·migration·auth·deploy), `docs/BTY_AI_ONBOARDING.md`(읽기 순서·문서 분류법). 기능 설명 배제·"왜"만. `CLAUDE.md`·`.cursorrules` 진입점 연결. outer = 이 ledger only.

---

**[x] BUILD 6 — Reality Event Engine Slice 2a — Event creation route — COMMITTED+PUSHED, no deploy (2026-06-24):** [API/ARENA] Event creation route **`POST /api/bty/events`** — **leader-track gate** + **approved-member gate** + **`bty_events` insert (service-role after gates)** + **Event QR token signing (`btyev1` family)**. Gate order: `requireUser`(401)→`requireApprovedMembership`(403)→`isLeaderTrack`(403)→validation(400)→service-role insert→sign. Validation: title/event_type non-empty, xp_value int 10–100 (mirrors DB check), valid_until parseable+future. Response `{event,token,qrUrl}`. **3 new files:** `event-qr-token.ts` (sign+verify, btyev1), `isLeaderTrack.ts` (reusable leader read, fails-closed service-role), `app/api/bty/events/route.ts`. **Event token separate (`btyev1`); Action QR (`aalo1`) UNTOUCHED;** crypto ~30L **intentionally duplicated**, shared-primitive extraction **deferred** (Commander-accepted). **Scope: create only** — NO scan / NO participation / NO XP / NO activity_xp_events / NO UI / NO org-TII / NO schema migration / NO deploy. `verifyEventQrToken` defined unconsumed. **qrUrl = forward-reference** (scan = Slice 2b, not built). **Gates:** tsc **0** / terminology **13/baseline 13/+0**. **Commit:** inner-main `3135f300→e2f1f796` (3 files). **Push:** `origin inner-main:inner-main` FF `3135f300..e2f1f796` (no force, remote==local). **Slice 1 tables already applied to prod** (`mveycersmqfiuddslnrj`); route writes `bty_events` but **unshipped (no deploy)**. outer = 이 ledger only.

---

**[x] BUILD 5 — Event QR Slice 1 migration FILE committed+pushed — SCHEMA NOT APPLIED (2026-06-24):** [DB/ARENA] Event QR Slice 1 migration file committed (`20260624000000_bty_events_slice1.sql`): **`bty_events`** + **`bty_event_participation`** — `xp_value` **10–100 DB check**, `status` **active/cancelled** check, **unique(event_id, user_id)**, `xp_awarded` snapshot, `activity_xp_event_id` **FK→activity_xp_events** (on delete set null); indexes (creator+created / user+scanned / event_id); **RLS** (events `select using(true)` / participation `select own`; **write=service-role** route gate). **⚠️ SCHEMA NOT APPLIED** — file only; apply = **separate Commander-GO dispatch** (production-effective single shared Supabase; snapshot before/after). **Slice 1 = NO org/TII** (later nullable ALTER ADD), **NO routes yet** (later slices). **Event QR = second official QR family** (validate invariant expanded, separate Event validate route — NOT Action `/qr/validate` overload); **core XP/avatar only, no band progression**. **Commit:** inner-main `9dc320cf→3135f300` (1 file). **Push:** `origin inner-main:inner-main` FF `9dc320cf..3135f300` (no force, remote==local). **Apply (NOT run):** `npm run db:push`. **Deploy:** none. outer = 이 ledger only.

---

**[x] BUILD 4 — PHASE II ring shrink (collision fix) — COMMITTED+PUSHED, no deploy (2026-06-24):** [UI/ARENA] PHASE II ring **56→40px** (+ `text-xs`→`text-[10px]`) to resolve collision with the "Today's growth" label on `/bty` hub. `PhaseIIRing.tsx` only — inline `width/height 56→40`, className text-size `text-xs→text-[10px]`; all other props (RING_COLOR/borderColor/opacity/rounded-full/border-2/uppercase/tracking/title/aria/"Phase II"/comment) unchanged. **Signal preserved** (Second Awakening, `HEALING_COACHING_SPEC_V3 §9` — shrunk not removed, still under `{phase==="II" && …}`); `EmotionalStatsPhrases` `gap-2` unchanged. **Pre-existing defect** (PhaseIIRing `fa0b86d6`/EmotionalStatsPhrases `a746c07a` predate Slice A — not deploy-introduced). EmotionalStatsPhrases/PendingActionList/My Page/Comeback/IA untouched. Pixel-fit (40 vs 36/44, text-[10px] inside 40px) pending visual post-deploy. **Gates:** tsc **0** / terminology **13/baseline 13/+0**; 1 file, 2 lines. **Commit:** inner-main `f9a63046→9dc320cf`. **Push:** `origin inner-main:inner-main` FF `f9a63046..9dc320cf` (no force, remote==local). **Deploy:** NOT performed — staging still `82e22c34` (BUILD 1+2+3 live). outer = 이 ledger only.

---

**[x] BUILD 3 — pending list home-actionable filter — COMMITTED+PUSHED, no deploy (2026-06-24):** [UI/ARENA] `/bty` home Pending list now shows actionable items only. Frontend display-only filter, `PendingActionList.tsx` only. **Predicate:** `status !== "missed" AND deadline_at >= now` (on top of endpoint `verified_at IS NULL`); **`rejected` INCLUDED**, only `missed` excluded by status; drops recorded-missed + past-deadline (fix for the 13 "Expires in expired" cards). Filter after loading guard; `count`/EmptyState/map all from filtered `actionable` array. **Display-only — zombies remain:** expired/missed contracts NOT swept (no sweep/status-mutation/cron) — they stay in DB, just hidden; DB lifecycle cleanup = separate future track. Deadline copy `{expiresIn} {formatDeadline}` kept (future rows correct; standalone "Expired" moot, not added); `formatDeadline` left as-is; ISO→epoch-ms vs Date.now() (UTC-safe). Endpoint/My Page/`ActionContractHub`/`AwaitingQrList`/DB/PHASE II untouched. **Gates:** tsc **0** / terminology **13/baseline 13/+0**; 1 file +12/−2. **Commit:** inner-main `9bdb35a1→f9a63046`. **Push:** `origin inner-main:inner-main` FF `9bdb35a1..f9a63046` (no force, remote==local). **Deploy:** NOT performed — staging still `6a5bb4ef`. outer = 이 ledger only.

---

**[x] BUILD 2 — pending action list on /bty hub (first visible UI slice) — COMMITTED+PUSHED, no deploy (2026-06-24):** [UI/ARENA] First visible UI of the action-centric home. NEW `src/components/bty/PendingActionList.tsx` — self-fetching client component (mirrors `EmotionalStatsPhrases`: useEffect/useParams, loading/error/empty) consuming `GET /api/arena/action-contracts/pending` (endpoint `a9431f97`); renders `action_text` + status badge + `expiresIn`/`formatDeadline`. **Cards display-only** (no chevron/onClick/Link — nav = later slice). **Badge mapping** (frontend-only, no `display_state`): QR needed (`verification_type∈{qr,hybrid,qr_peer,qr_system,qr_location}` regardless of status) · Awaiting verification (non-QR + status∈{submitted,approved,escalated}) · In progress (else, incl. draft/committed/pending/rejected/missed). "재노출 예정"+expired/redo **deferred** (no data source/later). **Mount:** `bty/(protected)/page.client.tsx` addition above `EmotionalStatsPhrases`; My Page surfaces (`AwaitingQrList`/`ActionContractHub`) untouched (dup = later IA track). **i18n:** 6 keys ×3 in `actionContract` (title KO "내가 해야 할 행동"/EN "My actions to complete"); card chrome `--arena-*` vars, status accents raw-Tailwind (amber/blue/emerald). **Gates:** tsc **0** / terminology **13/baseline 13/+0**; 3 files, +176. **Commit:** inner-main `a9431f97→9bdb35a1` (incl. 2b refinement). **Push:** `origin inner-main:inner-main` FF `a9431f97..9bdb35a1` (no force, remote==local). **Deploy:** NOT performed — staging still `b84fab2a`; endpoint+UI both unshipped, next deploy bundles both. outer = 이 ledger only.

---

**[x] BUILD 1 — pending action contracts endpoint — COMMITTED+PUSHED, no deploy (2026-06-24):** [API/ARENA] First mutation after extended read-only IA/action-loop/aggregation-shape measurement. New `GET /api/arena/action-contracts/pending` — user-wide, **side-effect-free** read (pure select; no status transition, no session/next, no expired→missed write); filter `user_id=caller AND verified_at IS NULL`; **user-scoped client, NO service-role** (RLS + user_id filter). Response `{ contracts: [...] }`, 7 fields each: `id, status, action_text`(alias of `contract_description`)`, deadline_at, verification_type, session_id, created_at` — exposes canonical `verification_type` (not `_mode`); `session_id`=run id (no separate `run_id` col); order `deadline_at ASC, created_at DESC`. Purpose: foundational data layer for action-centric "Today" home (slice A) — **user-wide pending list confirmed ABSENT prior** (existing sources were per-session lookups / POST-create). **Gates:** tsc **0** / terminology **13 / baseline 13 / +0**; single new file (+65, 0 existing modified). **Commit:** inner-main `00e9bad1→a9431f97`. **Push:** `origin inner-main:inner-main` FF `00e9bad1..a9431f97` (no force, remote==local). **Deploy:** NOT performed — staging still `b84fab2a` (Phase 1.1+2A). outer = 이 ledger only.

---

**[x] Phase 2A header glyph (4-slot) — CLOSED, COMMITTED+PUSHED, no deploy (2026-06-24):** [BRAND/UI] transparent-navy BTY knot glyph added to 4 header wordmark slots; text wordmark preserved (NOT replaced). Slots: `ArenaLayoutShell.tsx` · `CenterLayoutShell.tsx` · `my-page/layout.tsx` · `LandingClient.tsx` (Landing TOPBAR, NOT hero). Each: Link += `inline-flex items-center gap-1.5`, first child `<img src="/brand/bty-knot-transparent-navy.svg" alt="" aria-hidden width=20 height=20 className="h-5 w-5 shrink-0">` before byte-identical `<span fontWeight:400>bty</span>ARENA`. Decorative aria-hidden (text = accessible name); light/cream header bg → navy glyph contrasts. footer/landing hero/metadataBase/favicon/icon/OG/auth 전부 무접촉; asset 미재생성; no BrandWordmark component. **Verify:** classifier recovered (transient outage가 gate 보류시켰음) → tsc **0** / terminology **13 / baseline 13 / +0**. diff = 4 files, +8/−3. **Commit:** inner-main `79e001ab→00e9bad1` (4 files). **Push:** `origin inner-main:inner-main` FF `79e001ab..00e9bad1` (no force, remote==local). **Deploy:** NOT performed (별개 go) — Phase 2A + Phase 1.1 차기 deploy 동시 선적. **Note:** `CURSOR_TASK_BOARD.md` 파일명 의도적 미변경 (rename = 별개 governance track). outer = 이 ledger only.

---

**[x] Phase 1.1 login glyph polish (transparent-gold) — COMMITTED+PUSHED, no deploy (2026-06-23):** [BRAND/UI] E1 (b): login card knot gold-on-navy → floating transparent-gold. ONE src mutation in `login-card.tsx`: `/brand/bty-knot-gold.svg` → `/brand/bty-knot-transparent-gold.svg` (staged Phase 1 asset, verified transparent — `rect fill="none"`, 0× `#0B1F3A`, 3× gold `#C9A66B`); width/height/class(`rounded-2xl`)/alt/aria-hidden 불변 → knot floats on slate-950 card, navy tile 제거. metadataBase/layout/favicon/icon/OG/auth 전부 무접촉 (diff empty 확인). **Verify:** tsc 0 / terminology 13(+0) / diff = 단일 라인 (blob `96aef59e→3252848b`). **Commit:** inner-main `50257189→79e001ab` (1 file). **Push:** `origin inner-main:inner-main` FF `50257189..79e001ab` (no force). **Deploy:** 미수행 (별개 go) — Phase 1은 기배포 (Version `ef50077a`, 현재 LIVE = 아직 navy-tile glyph); 1.1 transparent-gold는 차기 deploy서 supersede. **E2:** metadataBase 미설정 유지 (결정) → OG/twitter 메타 `localhost:3000` base live; custom-domain 결정으로 deferred, 미수정. outer = 이 ledger only.

---

**[x] Phase 1 BTY knot brand assets (inner web) — COMMITTED+PUSHED, no deploy (2026-06-23):** [BRAND/UI] knot favicon/icon/OG + login glyph wired into inner-main. Exact bytes from `/Users/hanbit/Dev/_bty_phase1_assets/` (12 files, sizes + SHA-256 verified, no re-rasterize): `public/favicon.ico` replaced (multi-res 16=white/32-48=gold) + Next15 app-convention `src/app/{icon,apple-icon,opengraph-image,twitter-image}.png` + `public/brand/bty-knot-gold.svg` (login glyph) + 4 variant SVG staged (Phase 2, unwired). Only code change = `login-card.tsx` +9 (gold knot `<img>` above headline, additive/decorative `aria-hidden`; OAuth/form/auth 무변경); `layout.tsx` 무변경 (convention auto-wires metadata, title/desc 보존). **Verify:** tsc 0 / terminology 13(+0) / `npm run build` PASS — static routes `/icon.png`·`/apple-icon.png`·`/opengraph-image.png`·`/twitter-image.png` + `.meta` emitted. eslint=ignoreDuringBuilds(next.config; env Node24 vs nvmrc20 ajv crash, pre-existing). **Commit:** inner-main `61e1d40e→50257189` (11 files, +79). **Push:** `origin inner-main:inner-main` FF `61e1d40e..50257189` (no force). **Deploy:** 미수행 (별개 go). **HOLD:** metadataBase unset(pre-existing) → OG 절대URL deploy origin 기준; canonical origin 후속 설정 권고. outer = 이 ledger only (코드 미러 아님; 코드는 inner-main).

---

**[◐] B-w3 wrapper plugin fix + native OAuth E2E — INIT PASS / RETURN HOLD (2026-06-22):** [NATIVE/WRAPPER] Wrapper-only (inner UNTOUCHED 61e1d40e, deploy 06af1777 live). Fixed B-w2 inventory error (assumed `@capacitor/browser` in shell — live runtime showed undefined): `~/Dev/bty-native-app` `npm install @capacitor/app@8.1.0 @capacitor/browser@8.0.3` + `npx cap sync ios` + rebuild → live LLDB proof `Plugins.App=object`+`Plugins.Browser=object`. **Inner B-w2 code verdict: CORRECT+live** — captured `Browser.open` url `redirect_to=btyarena://auth/callback?next=/protected`, verifier cookie minted, `/api/auth/initiate` absent. **Init half PASS:** system browser (SFSafariViewController, not embedded WebView) → Google consent (mveycersmqfiuddslnrj.supabase.co). **Return leg HOLD:** post-login system browser landed on https Site URL (workers.dev), not `btyarena://` → Supabase fell back → `appUrlOpen` never fired → `/api/auth/callback` never hit (tail-confirmed) → app stayed `/login` unauth. **Next = 1 return-leg re-verify** (allow-list now shows `btyarena://auth/callback**` wildcard; unsure if present at run time); if fallback persists → ASWebAuthenticationSession plugin swap. E2E driven via LLDB evaluateJavaScript (sim tap synth blocked); Commander completed Google login. outer = this ledger only (no inner, no Supabase/deploy).

---

**[x] B-w2 native-aware OAuth branch (inner) — COMMITTED+PUSHED, runtime HELD (2026-06-22):** [AUTH/UI] Capacitor-shell OAuth 분기를 inner-main에 배선, `isNative()` 가드로 web byte-unchanged. 신규 `src/lib/native/isNative.ts`(`window.Capacitor.isNativePlatform()` ‖ `BTYNative` UA, @capacitor 의존 0) + `src/components/native/CapacitorAuthBridge.tsx`(appUrlOpen→`/api/auth/callback`, web=null), 가드편집 `login-card.tsx`(native redirectTo `btyarena://auth/callback`/`skipBrowserRedirect:isNative()`/시스템브라우저 `Browser.open`) + `[locale]/layout.tsx`(bridge 마운트). `/api/auth/callback` 무변경. **Verify:** tsc 0 / terminology 13(+0) / web build PASS. **Commit:** inner-main `61e1d40e`(4 files, +108/−3). **Push:** `origin inner-main:inner-main` FF `dafa1f9f..61e1d40e`(no force) — ★ 이 FF가 직전 push-HELD였던 `e0cb6e04`(C2 day-page deploy commit, 조상)도 함께 publish. **Deploy:** 미수행(별개 go). **Decision-1**(Supabase btyarena:// allow-list) 미적용 시 native OAuth=EXPECTED-FAIL. outer = 이 커밋(ledger only, 코드 미러 아님).

---

**[x] C2-재진입 링크 day-page 재지정 + allHref 제거 — DEPLOYED Version c78fb10d + 3-way PASS (2026-06-20):** [UI] `TrainProgressCard`([CenterPageClient.tsx](../bty-app/src/app/[locale]/center/CenterPageClient.tsx)) 재진입 링크 3건을 28days 허브 대신 day 페이지로 재지정. (Task4) `done` "28일 다시 둘러보기/Review your 28 days" → `/${locale}/train/day/1`(인라인 리터럴, allHref 공유 회피). (Task5) `!done` "전체 보기/View all" `href={allHref}`→`href={href}`(기존 :240 `/${locale}/train/day/${n}` 재사용 = "Day N 하러 가기" 버튼과 동일 목적지, 중복 허용·지시대로) + `const allHref`(:241) **제거**(미사용화). 제거 전후 `grep allHref` 잔여 **0** 확인. 결과: CenterPageClient는 `train/28days` 무참조 — 잔존 28days 진입점은 `/app` 버튼·Awakening 링크·서버 redirect·i18n 주석 4곳뿐(스코프 외, 미접촉). 라벨·element(`<a>`)·className 전부 불변, locale-full 유지. render-only·게이트/state 무변경. **Verify:** tsc 0 / eslint clean(allHref unused-var 0) / lint:terminology 13(+0, 편집파일 0). **Commit:** inner-main `e0cb6e04`(1 file) + outer main(이 커밋: 미러 + ledger). **Deploy:** `rm -rf .open-next && npm run deploy` exit 0 → Worker **bty-arena-staging** Version **`c78fb10d-8269-4b3c-8e49-e9373f386568`**. **3-way PASS:** inner HEAD e0cb6e04 / deploy Version c78fb10d / live center chunk `page-666b44b7ec23f4c4.js` HTTP 200 — `train/28days` **0건**(allHref 제거 라이브 확증, local build와 일치) + `train/day/1`·"Review your 28 days" 존재. **육안 미수행**(auth-gated /center 307→OAuth, done=lcd>=28 사용자 필요): 코드 라이브 청크 실증, 카드 렌더는 Commander 로그인 육안. **Push:** 이번 dispatch 미지시 → inner/outer **HELD**(Commander 인가 대기; inner-main `dafa1f9f..e0cb6e04` 미push). docs/video 미스테이지(별개 standing).

---

**[x] C2-완료카드 재진입 링크 — DEPLOYED Version d11aa9e7 + 3-way PASS (2026-06-20):** [UI] 28일 완료 후 `TrainProgressCard`([CenterPageClient.tsx](../bty-app/src/app/[locale]/center/CenterPageClient.tsx)) 갭 닫음 — `done`(lcd>=28) 시 `{!done && …}` CTA 블록이 "Day N 하러 가기" 버튼·"전체 보기" 링크를 둘 다 숨겨 축하 텍스트만 남고 프로그램 복귀 경로 부재였음(인벤토리 확인: 프로그램 자체는 미잠금 — todayUnlockedDay=clampDay(29)=28, 전 셀 클릭 가능·직접 URL OK; 막힌 건 카드의 진입구뿐). 조치: `{!done && …}` **미접촉**, 직후 `{done && …}` 블록 1개 추가 — 기존 :269-271 "View all" `<a>` 패턴 그대로 미러(element `<a>`·className `text-xs text-dear-charcoal-soft underline`·href `allHref`=`/${locale}/train/28days` 동일 목적지·locale-full), 라벨 "28일 다시 둘러보기"/"Review your 28 days" + aria "28일 프로그램 다시 둘러보기"/"Review your 28 days", 래퍼 `<div className="mt-2">`(!done 간격 일관). 프로그램 잠금·게이트·state 무변경(render-only). **Verify:** tsc 0 / lint:terminology 13(+0, 편집파일 경고 0) / UI render-only. **Commit:** inner-main `dafa1f9f`(1 file) + outer main(이 커밋: 미러 + ledger). **Deploy:** `rm -rf .open-next && npm run deploy` exit 0 → Worker **bty-arena-staging** Version **`d11aa9e7-1934-4521-bc6e-7c165d4bfa4b`**. **3-way PASS:** inner HEAD dafa1f9f / deploy Version d11aa9e7 / live center chunk `page-3fd6abe60070191e.js` HTTP 200 + 신규 리터럴("Review your 28 days"·"다시 둘러보기"·"train/28days", local build와 해시·리터럴 일치). **육안 미수행**(auth-gated /center 307→OAuth, 완료상태=lcd>=28 사용자 필요): 코드 라이브는 청크 리터럴 실증, 완료 카드 렌더는 Commander 로그인 육안. **Push:** inner-main `c6fec2b8..dafa1f9f` + outer main push(아래 동기화). outer docs/video 미스테이지(별개 standing).

---

**[x] C2-5+B-2 "Review past reflections" 재배치 + Letters 프로그램 복귀 링크 — DEPLOYED Version 7b460a5e + 3-way PASS (2026-06-20):** [UI] Train Day 28일 화면의 "Review past reflections" 링크를 LEFT 사이드바(28일 그리드 헤더)에서 제거하고, MIDDLE 컬럼 `TrainDayReflectionSet`의 "오늘의 Dear Me 성찰" 버튼 옆 flex row로 **이전**(href `/${locale}/center/letters?day=${day}` 불변 = ?day deep-link 보존, `locale==="ko"`→`isKo` 통일, `marginBottom:16` 제거·flex 정렬). 역방향 보강: Letters History 헤더(`LettersClient.tsx`)에 **프로그램 복귀 링크** 추가 — `dayParam && /^\d+$/.test(dayParam)`일 때만 `← Day N`(`href /${locale}/train/day/${dayParam}`, Center와 동일 dear-sage pill, 좌측=주 복귀, aria isKo분기), 비-deep-link 진입 시 미렌더. 기존 그리드 셀 locale-less `/train/day/${d}` 불일치는 별개 파킹(미접촉). **Verify:** tsc 0 / lint:terminology 13(+0, 편집 3파일 금지어 0) / UI render-only(측정·XP·순서·완료게이트 무변경). **Commit:** inner-main `c6fec2b8`(3 files) + outer main(이 커밋: 같은 3 미러 + ledger). **Deploy:** `rm -rf .open-next && npm run deploy` exit 0 → Worker **bty-arena-staging** Version **`7b460a5e-0351-49c5-ad80-1be4413cd855`**. **3-way PASS:** inner HEAD c6fec2b8 / deploy Version 7b460a5e / live 이번 빌드 고유 해시 chunk(letters `page-4465e4d30272d661`·train/day `page-c9d884b8f486ea36`) HTTP 200 + 신규 리터럴 실증(letters "← Day"/"Back to Day"/"train/day/" · train "Review past reflections"/"center/letters?day="/"flexWrap"). ※`wrangler versions/deployments list`는 API lag로 stale 표시(deploy 정상, 신규 해시 serving이 활성 전이 증명). **육안 미수행**(auth-gated /train·/center/letters 307→OAuth): 코드 라이브는 청크 리터럴로 실증, 화면 렌더(버튼 옆 링크 정렬·← Day 조건부)는 Commander 로그인 육안 필요. **Push:** 미지시 → inner/outer 양쪽 **HELD**(Commander 인가 대기). outer 무관 standing 미러랙·docs/video 미스테이지(별개).

---

**[x] MIRROR_PAIR_CANON — CLOSED (2026-06-15):** [DOCS] 첫 순수 의미-저작 canon 생성 — `docs/MIRROR_PAIR_CANON.md` (Axis Relationship Layer, explanation-only, ≠ Axis Definition). 본문 = Mirror Pair 01~04 **Commander verbatim** (01 Ownership↔Accountability / 02 Repair↔Conflict / 03 Authority↔Control / 04 Authority↔Visibility); 의미 저작 = Commander 100%, 이 chat = Arbiter(정합 검증 only). **검증:** STEP 0 PASS(HEAD==origin/main dc7e1c2 / BTY_AXIS_CANON v1.0·v1.1 byte-identical) → C-1 모순 0·C-2 substrate-재정의 0·대비-호명 유지·Authority §3 단일정의↔03/04 일관 drift 0. **Pair 03 개정 1회:** 초안 Control="실행 역량"이 §3 Control(self-protection→trust) 정의역과 C-1 불일치 → HALT → Commander revise(붙듦/내려놓음/신뢰 framing) → 재대조 PASS. net-new(락 상속 0), `AXIS_CANON_BODY_CLOSE.md` §3 이관 트랙 충족, canon/lock 본문 무수정·새 조항 0. **Gate:** lint:terminology 13(+0) / forbidden-token 0 / doc-only / 런타임 무변경(3-way 불요). **Commit:** outer docs 단독, **자가 push 금지 준수 → push HELD**(Commander/인가 런타임 이관). inner 무접촉.

---

**[x] RANKING-#7a Live-ranking XP 클리핑 — DEPLOYED 1dada5f6 + 육안 PASS (2026-06-14):** [UI] live-ranking 사이드바 행 우측 XP 그룹 통째 미표시 — 긴 codeName-userDisplayName이 행을 카드 overflow:hidden 밖으로 밀어 XP 은닉. flexbox-truncation 4링크 폐쇄: 외곽 행(grid item) minWidth:0 + 좌측 그룹 minWidth:0 + 이름 라인 overflow/ellipsis/nowrap + XP 그룹 flexShrink:0. **교훈:** flex:1은 오버플로에 inert(기각), 외곽 행 minWidth:0이 실제 필요(육안 확정). **Verify:** tsc 0 / lint:terminology 13(+0) / UI-render-only(XP·순위 무변). **Deploy:** npm run deploy → Worker bty-arena-staging Version 1dada5f6-6595-4cda-9f06-77146d56ba43, 3-way PASS(active==1dada5f6 / 청크 1220-a577aaa2 textOverflow / mtime fresh). **육안:** Incognito+Disable cache — XP 전 행 표시·이름 ellipsis·isMe 보더 무손상. **Commit:** inner-main cdf028ff(단일 파일 +5/-4), push HOLD(launch 런웨이). outer mirror 미스테이지(bty-app/ 25 divergence).

---

**[x] C2-3-Done-Vacuous — DONE (2026-06-12):** [DOCS] Train Day Reflection History branching 이미 구현·배포. LettersClient.tsx day_reflection 분기, 4 confirmations 코드 해소(Q/A·Final·빈답변·title). 메모리 drift 정정: L331→L391, deploy-held=거짓(이미 배포), branching=Center letters History. code 0 mutation. inner 4831a62.
  - BACKLOG: TrainDayCapture.tsx orphan prune (별도 source dispatch) / legacy reflection rows 무해.

---

**[x] B1-Decision2-Vacuous — DONE (2026-06-12):** [DOCS] IA Decision 2 이미 구현. Current State card(StageContextCard inline + HealingPhaseTracker) live-compose, Train first-position+filled CTA로 시각1순위. 근거정정: stage=phantom 아님, wrong-name grep miss(B3 실제부재와 구별). code 0 mutation. inner 6a2ce4e IA_CANONICAL_REALITY.md 기록.
  - BACKLOG: explicit Train hero / double-shell de-nest (optional polish, parked).

---

**[x] B3-IA-Phantom-Cleanup + OUTER-RECOVERY — DONE (2026-06-12):** [DOCS] removal lane 폐기→doc-correction. D1/D4 wrong-tree supersede, GROWTH stale banner(both branches), 신규 IA_CANONICAL_REALITY.md. production 0 mutation. features/growth 보호, Comeback KEEP-parked. editable outer 확보(/Dev/btytrainingcenter-OUTER), bty-website-clean WIP→wip/clean-rescue-20260612 @ bdd2ced9 보존. inner 1f11b81 + outer fa685762.
  - BACKLOG: Comeback micro-decision / WIP remote push(auth 선결) / committer identity as-is / outer ledger flow 복구 완료.

---

**[x] A. Awakening 임계 수치 노출 중립화 — DEPLOYED Version 872ed80f + 3교차 PASS (2026-06-08):** (Push: inner `344ffc70..4b1553af`/outer `884b482..82d820c` synced 0/0. Deploy Worker bty-arena-staging Version `872ed80f-4b07-469b-b5d8-74cc7a96bd48`. 3교차: active==872ed80f / HEAD 4b1553af / live literal[awakening 청크 "여정을 더 쌓으면 이 의식이 열려요"·"as you continue your journey"·"당신의 성장을 돌아보는 의식" + 30/10/userDay 부재, healing 청크 {day}/{sessions}/30/10 부재, 404 EN]. ※ritual act1 "세션 수:N"(page:217)은 자격통과자 의식 콘텐츠로 스코프 외 잔존. 육안 미수행=auth-gated+OAuth, Commander 로그인 필요.) [UI/카피] (다-중립 후속) Awakening 게이트 임계(30일/10세션)·진행수치(userDay/sessionCount)를 **모든 UI 표면에서 제거**, 게이트 존재만 알림. **카피만 — 게이트 로직(secondAwakening.ts/completeAwakeningAct.ts) 절대 미접촉(② 보류)**, REQUIRED_DAY/MIN_SESSIONS 불변 = measurement freeze-safe. (1) `healing/awakening/page.client.tsx`: 해금조건("30일 훈련과 최소 10회 세션…"→"여정을 더 쌓으면 이 의식이 열려요."/"This ritual opens as you continue your journey."), **진행수치 `<p>`(현재 X일/Y세션) 블록 삭제**, 제목("30일, …"→"당신의 성장을 돌아보는 의식"/"A ritual to reflect on your growth."). (2) `i18n.ts` awakeningActsTriggerLine ko(2942)/en(4760): "{day}일·세션 {sessions}회 이후…"→"여정을 더 쌓으면 의식이 해금돼요."/"The ritual unlocks as you progress." + `AwakeningActsTrack.tsx` 치환(.replace {day}/{sessions}) 제거 + 미사용 `day`/`sessions` 상수 정리. Center intro(i18n:2918)는 숫자 미포함 → 미접촉. **Verify:** tsc 0 / lint:terminology 13(+0, 금지어 0) / 편집표면 30·10·userDay·sessionCount·{day}·{sessions} 노출 0. **Commit:** inner-main `4b1553af`(3 files +6/-16) + outer main(이 커밋: 같은 3 미러 + ledger). **미배포·미push** (빌드타임 정적 import·컴포넌트 → deploy 전 staging 미반영). outer stale backlog 미스테이지.

---
**※ 이번 freeze 사이클 종료 (2026-06-08):** measurement freeze 하 무해 텍스트/카피/presentation·UI 가드만 처리(A-1 시나리오 텍스트, B-1 404 EN, D1 journey 리스트, D2 train 접기, Awakening eligibility 가드 + 임계 중립화). **D1=CLOSED** (low-traffic surface — journey 다줄 body→ul/li, 배포 1c843f24 이전 2c60409b 포함, 추가 작업 불요). **freeze 후 별도 착수 트랙(②포함, 미착수):** ① Awakening 게이트 **재설계**(달력일+챗세션 → 프로그램완료 기반 (나), train/journey 완료신호 출처 결정 + secondAwakening.ts:78-79 + completeAwakeningAct.ts:54 동반) · ② **IA 4겹 정리**(train 28일 / growth journey / Center HealingPhaseTracker 4단계 / Awakening 3막 — "Day/단계/치유여정" 명칭·중복 화면 충돌) · ③ **Growth 허브 위치**(BottomNav·HubTopNav 양쪽 부재 = 고아, 정규 진입점 부여) · ④ 온보딩 흐름 · ⑤ 2b raw→sections splitter(마일스톤 4일 서사 보존 선결) · ⑥ Phase 5(시나리오 뉘앙스 reword: core_13-EN/core_03-EN HALT 2건 포함).
---

**[x] Awakening eligibility 버튼 가드 + D2 KO 접기 임계 — DEPLOYED Version 1c843f24 + 3교차 PASS (2026-06-08):** [UI+API] 라이브 버그 2건 후속. **(1) Awakening Acts "저장 실패" 오해** (STEP0 판정 (a) NOT_ELIGIBLE 403 — day30+10세션 게이트가 generic "저장에 실패했어요·다시 시도" 토스트로 표출): **Cycle 1** GET `/api/bty/awakening`에 `eligible: boolean` 노출(`getSecondAwakening` 위임 = POST 게이트 `completeAwakeningAct.ts:54`와 동일 권위 소스, drift 방지; userDay/sessionCount **수치 비노출** = 다-중립). **Cycle 2** `AwakeningActsTrack.tsx` 비자격 버튼 가드 — `recordNext`/`disabled`에 `data?.eligible === false`(undefined=구버전 GET→**기존 동작 유지**, 전원 비활성 회피), 중립 안내(ko "아직 이 단계에 도달하지 않았어요"/en "You haven't reached this stage yet.", 임계·수치 미노출), 403 도달 사전 차단(진짜 500/네트워크 토스트는 보존, i18n.ts 미접촉). **(2) D2 KO 접기 미표시** (STEP0 판정 (d) 단일 800자 임계 × 한글 ~2배 밀도 → KO raw 24일치 <800 숨김): `train/day/[day]/page.client.tsx` 임계 `locale==="ko"?450:800` locale-aware + cut 하한 `Math.round(LESSON_COLLAPSE_AT*0.25)`(EN 200 불변/KO 113, 0방지). **KO 회복**: >450인 24일 토글 노출, 최단 4일(Day1=442·2=422·5=420·6=410)만 정당 숨김. **EN 회귀 0**(27/28 보임, Day5=748 숨김 불변). render-only 준수(클라 자격 재계산 0, 서버 boolean 소비), 측정/XP/순서 무변경. **Verify:** tsc 0 / lint:terminology 13(+0, 금지어 0) / 경계 day 결정적 확인(KO Day4=572>450 보임). **Commit:** inner-main `344ffc70`(3 files +21/-5) + outer main(같은 3 미러 + ledger). **Push:** inner-main `7228bf15..344ffc70` / outer main `64271ed..358cd65` (synced 0/0). **Deploy:** `rm -rf .open-next && npm run deploy` (check-env 하드게이트→opennext build→deploy, exit 0) → Worker **bty-arena-staging** Version **`1c843f24-fa58-4bea-8048-f4e7ab46ed16`**. **3교차 PASS:** active 100%==1c843f24 / inner HEAD 344ffc70 / live literal(train 청크 `"ko"===y?450` 삼항+450/800, healing 청크 "아직 이 단계에 도달하지 않았어요"+"You haven't reached this stage yet."+eligible, 404 EN). **육안 미수행(auth-gated+OAuth-only):** 코드 라이브는 청크 리터럴 실증, 화면 렌더(비자격 버튼 disabled+안내·KO Day4 토글)는 Commander 로그인 육안 필요. Cycle 1 배포됨→Cycle 2 가드 `eligible===false` 작동. outer stale backlog 10개 미스테이지. ※디스패치 verify "KO Day5=748"은 EN 길이 혼동(실 KO Day5=420)으로 정정.

**[x] A-1+B-1+D1+D2 시나리오 텍스트 정리 + 루트404 EN + journey 리스트 + train 가독성 — DEPLOYED Version 2c60409b (2026-06-08):** (정정: 직전 "COMMITTED, HALT-before-deploy" 표기는 stale — 실제 Worker 2c60409b로 배포 완료, B-1 404 EN 라이브 실증됨; D1/D2 육안은 OAuth 필요) [UI+DATA+DOC] A-1 시나리오 무해 텍스트 6건 중 **4건 적용**(측정 frozen 불변): core_22 ko `pressure` citation 잔재(`[oai_citation:0‡…](sediment://…)`) 제거 / core_11 ko·en `action_decision` 라벨 trailing period **16건** 정규화(primary·second_choices 무마침표 관례 통일, KO AD2 중복 유지) / core_01 ko eb.D.AD1 "own해야 할"→"책임져야 할" 비문 교정(doctor·support할 하우스 스타일 유지) / core_03 ko eb.B.AD1 미번역 "two-part " 제거(reset 도메인어 유지). **2건 HALT**(편집 안 함, 후보 α/β/γ 제시): core_13-EN 과단축(ko 대칭 하우스 스타일·telegraph 강화 위험), core_03-EN "two-part reset"(정상 영어·reset 도메인어 dilution 위험) — Commander 결정 대기. **B-1:** not-found.tsx 루트 404 EN화(metadata/h1/본문/"Log in"/"Dashboard"; "홈 (KO)" 언어셀렉터 라벨 의도 유지, [locale]/not-found.tsx 미접촉). **D1:** JourneyDayStep body 다줄→`<ul>/<li>` 분기(1줄→`<p>` 폴백, testid 유지). **D2:** train day page.client raw 가독성(renderParagraphs 문단여백 + 800자 `<details>` 더보기/접기 + ✅완료/🔒잠김 셀라벨 + eyebrow/h1 계층) — presentation-only, raw blob 유지. **B-2 폐기:** BtyAuthGuard.tsx(orphan — 두 사본 다 미마운트, 실 auth gate=middleware) 편집 `git checkout` 되돌림. **Verify:** tsc 0 / lint:terminology 13(+0, 금지어 try-again·retry·error·invalid 도입 0) / scenario .json 5개 JSON.parse valid / **frozen 필드(pattern_family·direction·axis·intensity·is_action_commitment) diff 0**. **Commit:** inner-main `7228bf15`(8 files +101/-33) + outer main(이 커밋: 같은 8 미러 + ledger 2). **미배포·미push 명시:** 시나리오 JSON·컴포넌트는 빌드타임 정적 import → `npm run deploy` 전 staging 런타임 미반영, 육안검증은 배포 후 staging URL에서만. D1/D2 auth-gated(/train·/growth/journey 307→login) 육안은 배포+OAuth 필요. push/deploy=Commander 승인 후 별 dispatch. outer 무관 backlog 10개(center/healing/i18n 등 선행 inner 미미러분) **미스테이지**(WIP 혼입 차단).

**[x] G-DC-24 Center letter 복구 + 28d 모바일 fix + BTY canonical + Awakening 게이트 + letter 이력 — DEPLOYED + 3-way PASS (2026-06-07):** [UI+API+DOC+DEPLOY] Center dead-CTA 추적: href 오타 추정 → STEP0 인벤토리로 반증, 실제는 /dear-me letter composer가 HK7 phantom(llm.ts) collateral로 05140389 삭제됨 + energy "+Log"는 태생 dead-by-design. 조치: letter composer를 CenterPageClient 모달로 인라인 복구(phantom-safe, API-only reply via getDearMeReplyTemplate), "+Log" 제거 후 카드 display-only. 28일 프로그램 모바일 깨짐: train/day/[day]/page.client.tsx:166 인라인 px-grid(360px 1fr 420px) → 반응형 className(grid-cols-1 md:grid-cols-[360px_1fr_420px]). BTY canonical 모순 해소: PHILOSOPHY_LOCK_V1.md "Better Than Myself" stale → lock 문서 :3/:55 + i18n.ts:4023 태그라인("Gratitude. Integrity & Better Than Yesterday.") 정정, lint 13 복귀. btyARENA 메타데이터(layout.tsx) 커밋. Second Awakening 서버 게이트: completeAwakeningAct.ts:53-55 day30+sessions10 재검증(NOT_ELIGIBLE 403, forward-only) — UI-only 게이트 갭 닫음. letter 이력 화면: 경량 calendar endpoint(/api/dear-me/letters/calendar, body 제외) + LetterCalendar 컴포넌트 + /center/letters(자동 보호) + Center 카드 "이력 보기" 링크. **Verify:** 매 commit tsc clean + lint:terminology 13. K 게이트 41 unit(403/200). **Deploy:** 3회 — Worker 15544f49(6-commit: a0f6ea95→fc8ee82f) / 1554a8af(K게이트 d6f07a92 + M1/M2 동반) / 0103ed21(M3/M4 ff6d398b). **3-way PASS:** active 100% 각 Version / git HEAD 일치 / 라이브 리터럴(btyARENA—Better Than Yesterday title, NOT_ELIGIBLE 번들, /center/letters 307 protected). inner-main만 push(origin/main 미push, ~88 ahead #9). Commander 폰 확인: Center letter 모달·"+Log" 제거·28d 단일컬럼·letter 이력 캘린더 1-4 전부 PASS.
  - **B grandfather note:** user 85bd8f1f completed awakening acts 1/2/3 before eligibility (0 sessions, day 0, 15s span, 2026-04-30). Rows grandfathered/inert. Server-side gate added forward-only. No production cleanup performed in this lane. Rehearsal accounts(52e543cc/9587a44e) unaffected (0 rows).
  - **Backlog (non-blocker):** (1) /letters limit 20 vs calendar 전체 점 — 21+ 편지 시 점 있으나 본문 없는 갭(Free 플랜 한참 뒤). (2) AwakeningActsTrack.tsx:127 COOLDOWN_ACTIVE dead-code + :117-122 blocked 카피 day/session 미반영. (3) /api/version BTY_DEPLOY_VERSION 하드코딩(2026-04-27) → real build-time stamp 배선. (4) energy "+Log" 진짜 composer = post-launch 신규.
  - **미확정 플래그:** K 403 e2e = 번들 inclusion + unauth 401 경로로 active 정황 확인, authed eligible=false→403 행동 확증은 실기기/통합테스트 대기.

**[x] G-DC-23 full_name on membership form + admin display — DEPLOYED + 3-way PASS (2026-06-03):** [UI+API+MIGRATION+DEPLOY] Real name (본명, admin-identification) added end-to-end, distinct from `display_name` (public nickname). Membership form ([TeamMembershipForm.tsx](../bty-app/src/components/bty/membership/TeamMembershipForm.tsx)) gains a required `full_name` text input (max 120, validated by new pure helper [profileFullName.ts](../bty-app/src/lib/bty/arena/profileFullName.ts) `validateFullName`); POST [membership-request/route.ts](../bty-app/src/app/api/arena/membership-request/route.ts) writes it to BOTH `arena_membership_requests` (audit snapshot) and `arena_profiles` (authoritative — `ensure_arena_profile` RPC then UPDATE; 422 on invalid). Admin 3 screens (리더십지표 AIR/Stage/MWD · 멤버십 · 멘토신청) display `full_name` via shared [fullNameMap.server.ts](../bty-app/src/lib/bty/arena/fullNameMap.server.ts) `fetchFullNameMap` (`arena_profiles` `IN(user_ids)` lookup), falling back to email/uuid when unset (font-mono dropped for names). Migration `bty-app/supabase/migrations/20260603000000_arena_full_name.sql` (`arena_profiles.full_name` + `arena_membership_requests.full_name`, both `ADD COLUMN IF NOT EXISTS`). **Verify:** tsc clean / vitest 3445 (=baseline; mentor route mock +`.in()`, form success/5xx tests +Full-name input). **DB:** `migration repair --status applied 20260603000000` → `migration list` local+remote synced (no db push). **Deploy:** `rm -rf .open-next` + cf:build + cf:deploy → Worker **`5deb597c-c53a-4dee-85b3-23bd6346684b`**. **3-way PASS:** active 100% == 5deb597c / inner `a021e846` + outer `03959b5` / live admin APIs HTTP 401 (auth-gate, not 500 = route-load healthy). **Note:** column-firing runtime proof (authed membership-submit → admin name) = Commander live-verify; `membership-request/route.test` has no happy-path 200 case (pre-existing gap).

**[x] G-DC-22 code badges on identity cards + outfit lock — DEPLOYED + 3-way PASS (2026-06-03):** [UI+ASSET+DEPLOY] Code badge (7 codes forge/pulse/frame/ascend/nova/architect/codeless) renders on the CODE IDENTITY card ([ProfileClient.tsx](../bty-app/src/app/[locale]/bty/(protected)/profile/ProfileClient.tsx), 64px beside codeName chip) and the dashboard IDENTITY card ([page.client.tsx](../bty-app/src/app/[locale]/bty/(protected)/dashboard/page.client.tsx), 56px beside avatar), via new [codeBadge.ts](../bty-app/src/lib/bty/arena/codeBadge.ts) `codeBadgeSrcByName` (codeName→`/badge/<name>.png`, null→omit). Legacy `badge_*_icon.png` ×5 removed; `public/badge/` normalized to lowercase canonical names (HALT-gated twice: missing pulse/nova, then `Ascend.png`/`cordless.png` mis-spell → Commander re-supplied 7 correct PNGs). Outfit selection globally locked for MVP ([AvatarSettingsClient.tsx](../bty-app/src/app/[locale]/bty/(protected)/profile/avatar/AvatarSettingsClient.tsx) `OUTFIT_SELECTION_OPEN=false` → all tiles disabled, save path never called) + inline "곧 제공 / Coming soon — selection opens later". **Verify:** tsc clean / vitest 3445. **Deploy:** clean cf:build + cf:deploy → Worker **`1bc056f3-c49f-4d4d-b8d6-4bc7f2c64bdb`**. **3-way PASS:** active 100% == 1bc056f3 / inner `ab88a43d` + outer `fcbf524` / 7 badge assets live HTTP 200 (incl. renamed ascend/codeless), legacy `badge_forge_icon.png` 404.

**[x] G-DC-21 password-reset UI removal — DEPLOYED (2026-06-03):** [UI+DEPLOY] Account SECURITY card (reset-email UI) removed — Google OAuth only, no site password. `send-reset-email/route.ts` left as dead code (follow-up cleanup, non-blocking). Inner `4251e6b1` (git-ancestor of today's deployed workers `1bc056f3` / `5deb597c` → live). **Live (Commander-reported):** SECURITY card absent.

**[x] G-DC-19 leaderboard display_name + hide weekly tier + per-code rename gate — DEPLOYED (2026-06-03):** [UI+MIGRATION+DEPLOY] Leaderboard name line shows `CODE-displayName`; weekly tier (Bronze/Silver/Gold) hidden (computation retained for revert). `display_name` change gated to once per code (migration `bty-app/supabase/migrations/20260602223038_arena_display_name_code_gate.sql` — `arena_profiles.display_name_changed_at_code_index`); sub-name rename UI closed (`SUBNAME_RENAME_ENABLED=false`). Inner `99129251` (git-ancestor of today's deployed workers → live). **Live (Commander-reported):** display-name change verified, 3-way PASS.

**[x] Bug 3 — avatar no-selection guard (unselected→initials, not scrubs) — DEPLOYED + smoke PASS (2026-06-02):** [UI-LOGIC+DEPLOY] 4 files (inner `f4f371c9` / outer pending this push). `resolveDisplayAvatarUrl` ([avatarOutfits.ts](../bty-app/src/lib/bty/arena/avatarOutfits.ts)) lacked the Lane 7 no-selection guard its sibling `resolveDisplayAvatarLayers` already had → unselected users (row exists; char/outfit/theme all null) got the level-based clothed default (basic clinic scrubs) instead of initials. FIX: mirror the guard (no explicit choice → `return null`); [core-xp/route.ts:208](../bty-app/src/app/api/arena/core-xp/route.ts) gates the `avatarUrl` outfit fallback on `hasAvatarSelection` (vars are DB-row `?? null`, so unselected=null). Tests: edges:112 `not.toBeNull`→`toBeNull` (matches its own "returns null" name); +5 `resolveDisplayAvatarUrl` no-selection cases. **Verify:** tsc 0 / terminology 13(+0) / vitest no-selection 10·edges 15·core-xp 13·leaderboard 9 green. **Deploy:** `rm -rf .open-next && npm run deploy` (check-env prebuild + opennext) → Worker **`3f9a1f02-24e5-4737-9ebc-d68f87672a7a`** (deployments list active 100% 21:36:33Z = deploy stdout; worker.js mtime 21:36:12Z fresh; prior `b2a4abc8`). **Smoke 4/4 PASS** (Commander browser): (1) 미선택 row-exists → dashboard/leaderboard/ArenaHeader 이니셜 (2) 선택 유저 → 옷 그대로 (3) fresh !row → 이니셜 (4) 3-surface 일관. Bug 3 봉쇄 실증. **Bug 2** (fresh-user empty equip) 동반 해소 — 같은 미선택 경로가 이제 fail-safe 이니셜.

**[x] SECURITY REVOKE anon EXECUTE on 6 SECURITY DEFINER functions — advisor WARN — APPLIED to prod (2026-06-02):** [MIGRATION+DEPLOY] Migration `bty-app/supabase/migrations/20260602000002_revoke_anon_execute_definer_funcs.sql` (6 × `DO $$` to_regprocedure-guarded GRANT-then-REVOKE). **Hole closed:** DEFINER funcs ran with owner privs bypassing caller RLS; anon EXECUTE (direct + PUBLIC) let unauthenticated callers invoke XP-mutating / profile / season RPCs = privilege escalation. **Caller analysis (verified vs `bty-app/src`, all call sites auth-gated):** AUTHENTICATED (anon-key+session→`authenticated`, getUser/requireUser→401) = `increment_arena_xp`, `increment_weekly_xp`, `ensure_arena_profile`, `consume_lab_attempt` (lab/complete:26 gate); SERVICE_ROLE-only = `run_season_carryover` (activeLeague:62), `get_leaderboard_profiles` (leaderboardService admin). **Fix shape:** 4 auth funcs → `GRANT EXECUTE TO authenticated, service_role` then `REVOKE FROM anon, PUBLIC`; 2 service funcs → `GRANT TO service_role` then `REVOKE FROM anon, authenticated, PUBLIC`. **★ GRANT-then-REVOKE rationale:** observed ACL access was via PUBLIC (anon=no explicit grant) → bare `REVOKE FROM PUBLIC` would also strip authenticated/service_role; explicit GRANT-first makes it safe under any ACL structure. Grant-only change — **no function body / XP logic touched** (Core vs Weekly XP separation + leaderboard ranking invariants preserved). Signatures observation-confirmed: `increment_arena_xp(uuid,uuid,integer)` / `increment_weekly_xp(uuid,uuid,integer)` / `ensure_arena_profile()` / `consume_lab_attempt(uuid)` / `run_season_carryover()` / `get_leaderboard_profiles(uuid[])` (all out-of-band — no in-repo definition, same drift family as RLS 16 + leaderboard view). **Verify:** db push `Finished` (0 error) / `migration list` 20260602000002 local+remote synced / ACL-after observable = pg_proc proacl re-query handed to Commander (expect anon gone on all 6, authenticated retained on 4, service_role-only on 2). **PENDING (deferred, merged with RLS smoke):** core-loop smoke — authenticated user action→contract→XP still works = REVOKE + RLS-active-3 both no-impact. **NOTE:** `arena_profiles_leaderboard_public` security_definer_view = already DROPPED by Commander (view_count=0); not a migration file.

**[x] SECURITY RLS enable — advisor `rls_disabled_in_public` ERROR 16 tables — APPLIED to prod (2026-06-02):** [MIGRATION+DEPLOY] Migration `bty-app/supabase/migrations/20260602000000_rls_enable_advisor_error_16_tables.sql` (16 × `ALTER TABLE IF EXISTS … ENABLE ROW LEVEL SECURITY`, no policies — RLS-only default-deny; ACTIVE 3 = service_role bypass so app unaffected, ORPHAN 13 = 0 code refs). **Classification (verified vs `bty-app/src`):** ACTIVE 3 (`bty_action_contract_escalations`/`_validator_evaluations`/`scenario_pool_health_snapshots` — all getSupabaseAdmin); ORPHAN 13 (`qr_tokens`, `bty_action_contract_escalation_resolutions`, `arena_choices`, `choice_events`, `pattern_axis_states`, `action_requests`, `action_approvals`, `reexposure_links`, `action_execution_log`, `user_integrity_state`, `arena_scenarios`, `pattern_shift_results`, `user_state_snapshots`). **Migration-history 장부 정비 (db push가 divergence로 차단됨):** remote-only 3 signup migrations (`20260525000000/001/002`, parked in `migrations-reverted/`) → `repair --status reverted`; local-only QR v1 5 (`20260527010000~010400`) + le_pulse (`20260531000000`) were applied out-of-band (untracked) → observation-gated `repair --status applied` (Commander dashboard confirmed A=10 cols ∧ B=4 constraints ∧ C=0 old-constraint; le_pulse_log table observed via `inspect db table-stats`, 4 rows) → then `db push` applied ONLY `20260602000000`. **★ OUT-OF-BAND DRIFT FINDING:** 14/16 advisor tables had NO creation migration in repo = schema created outside migration history (dashboard/manual); migration-history table was diverged in both directions until this reconciliation. **★ `qr_tokens` was live with 1 row of real data + seq scans** (RLS-off exposure of an actual token table the app code never references — closed). **Verify:** db push `Finished` (no error) / `migration list` 20260602000000 now local+remote synced / advisor-after observable = pg_class.relrowsecurity query handed to Commander (expect 16 × true). **PENDING (deferred):** core-loop smoke (action→contract→validation→XP) to empirically confirm ACTIVE 3 unaffected by RLS — Commander to fold into next core-loop check (theory: service_role bypass → no impact). **NEXT:** `security_definer_view` (`arena_profiles_leaderboard_public`, 0 code refs — INVOKER conversion or drop) + WARN (anon `increment_xp` execute) — separate items.

**[x] HOTFIX Center 28-day assessment gate + public result locale CTA — DEPLOYED (2026-06-02):** [UI+DEPLOY] Bug 1 — `TrainProgressCard` now requires a completed 50Q assessment (`hasAssessment={submissions.length>0}`, already-fetched signal, 0 new fetch); no submission → "먼저 50문항 진단 완료" + `/${locale}/assessment` card instead of the false "program ready" card (fail-safe: empty→prompt). Bug 1b — `(public)/assessment/result` CTA `/en/train/start` → `` `/${lang}/train/start` `` (public group has no locale param; `lang` is the in-scope client state; localized `ResultClient.tsx:390` already correct, untouched). **Verify HARD:** tsc 0 / terminology 13 (+0) / inner commit `9825a1aa` / active Version `b2a4abc8` (deployments list newest 100%) / 3-way PASS (active ↔ HEAD 9825a1aa ↔ worker.js mtime 16:34:46Z) / live-worker grep both http 200 (result `/en/train/start`=0 + lang-concat present; center gate string present). **Verify PENDING (executor attest):** runtime browser 4-item (new=50Q card / existing=28-day unchanged / KO result=/ko/train/start / 9-step regression) — code live-verified, authed/localStorage browser observation not run (headless auth N/A); diff is Center + (public) result only → no arena 9-step flow code touched → regression risk ~0 by inspection. **Not pushed** (origin push not requested). Bug 2 (fresh-user avatar empty equip) = backlog, untouched.

## SESSION CLOSURE 2026-06-02 (launch-eve) — closed + post-launch backlog

**Closed this session (all observed, not inferred):**
- Bug 1+1b (Center 28-day gate + public result locale CTA) — DEPLOYED Worker `b2a4abc8`.
- Bug 3 (avatar no-selection → initials) + Bug 2 (fresh-user empty equip, same path) — DEPLOYED Worker `3f9a1f02`, 4/4 browser smoke.
- Security closure: RLS `rls_disabled_in_public` ERROR 16 tables (`20260602000000`, incl. `qr_tokens` live-data exposure) + `security_definer_view` `arena_profiles_leaderboard_public` DROP (0-ref) + anon EXECUTE REVOKE on 6 SECURITY DEFINER funcs (`20260602000002`, XP-inflation/season-RPC escalation). All applied to prod via `supabase db push`.
- not-a-defect ×3 confirmed (no fix needed): queue #2 / payload / CRON (per prior triage).
- Migration-history drift reconciled: `repair --status reverted` 3 signup (`20260525*`, parked in `migrations-reverted/`) + `repair --status applied` 6 out-of-band (QR v1 5 + le_pulse) — observation-gated (dashboard A=10/B=4/C=0 + table-stats). See memory `project_migration_history_out_of_band_drift`.

**Post-launch backlog (non-blocking):**
- Season clock: weekly/30-day UI conflation + `season_ended` misnomer; `arena_seasons` UUID lifecycle never rotated (2 items below in SEASON/XP section).
- Scenario translation (EN coverage); avatar upgrade (asset expansion); vestigial cleanup (dead orphan tables/routes).
- **Out-of-band schema 14/16** — advisor RLS tables + leaderboard view + 6 DEFINER funcs have no in-repo creation migration; backfill canonical definitions or `db pull` to capture.
- CRON secret rotation; Deferred-Queue #3 release-gate; orphan route audit.
- **Security WARN 잔여**: `function_search_path_mutable` (~15 funcs — set fixed `search_path`); `btree_gist` extension in public; `avatars` storage bucket policy; `leaked_password` protection (Supabase Pro feature — paid).

## SEASON/XP CLOCK BACKLOG (post-launch, non-blocking) — logged 2026-06-02

**Context:** Season-period question RESOLVED — `arena_leagues` is a fixed 30-day auto-grid (`activeLeague.ts`: `EPOCH 2026-01-25`, `PERIOD_DAYS 30`, lazy auto-rollover on first read; name `Season YYYY-MM-DD` = window start). Today 2026-06-02 → index 4 → `Season 2026-05-25` (window 2026-05-25 → 2026-06-24). Normal, launch-independent. Two follow-ups parked below; both **post-launch**.

- **(a) ★ Weekly/30-day UI clock conflation + `season_ended` misnomer** [post-launch] — `triggerWeeklyReset` ([`bty-app/src/engine/xp/weekly-xp-reset.service.ts`](../bty-app/src/engine/xp/weekly-xp-reset.service.ts)) emits an event literally named `season_ended` but fires **weekly** (Mon 00:00 UTC), not at the 30-day league boundary. Three overlapping "season" cadences (30-day league window / `arena_seasons` lifecycle / weekly reset) wear the same label. **Action:** trace which clock the UI `arenaHubSeasonEndsLabel` countdown ("시즌 종료까지" / "Season ends in") actually reads from (league `end_at` vs next-Monday weekly reset), confirm whether they're conflated, then clean up naming. Trace is post-launch (UI-confusion confirmation only, non-blocking).
- **(b) ★ `arena_seasons` UUID lifecycle never rotated in code** [post-launch] — `arena_seasons` is declared the "single source of truth for season lifecycle" (`20260301000000_arena_league_season_schema.sql`: `season_id uuid`, `season_number` monotonic, `status active|ended`) but **no `src/` code computes/rotates/inserts its rows** — only `getActiveSeasonId` reads it. Declared-authority table left dormant while the real period math lives only in `activeLeague.ts` (`arena_leagues`). **Action:** audit features depending on `arena_seasons` (e.g. `system-health-dashboard.service.ts` reads `season_number`; `season_id` stamped into `weekly_xp_history`) — confirm they degrade gracefully against the unpopulated/un-rotated table or wire rotation.

---

## PHASE 3 GOVERNANCE TRANSITION

**PHASE3-GATE-1 CLOSED (2026-05-15)**
Residency validation procedure codified: [`docs/RESIDENCY_VALIDATION_PROCEDURE.md`](RESIDENCY_VALIDATION_PROCEDURE.md) (commit `9e21fb5`)

Phase 3 mutation governance transitioned from:
- freeze-based protection

to:
- procedure-based constitutional validation

12-Axis runtime semantics review remains active as a parallel architecture track, not a Phase 3 blocker.

Sprint family closure (5 sprints, 6 commits, code mutation 0):
- α-1c Task A (`a658259`): row #12 reclass
- α-1c Task B (`2a11af07` + `1e20629`): accountability drift NOTE
- HK2-R2 audit (mutation 0): systemic finding 26 LIVE + 9 AXIS
- α-1c-B (`96fc888`): 26 LIVE batched reclassification
- 12-Axis NOTE (`ef74926d` + `5c9618a`): 13 axis-drift rows deferred
- PHASE3-GATE-1 (`9e21fb5`): residency procedure codified

## Deferred Queue

Commander-confirmed order. Item 1 closed 2026-05-17; items 2–6 are forward-planning lanes.

1. ~~result_origin closure authoring~~ — **CLOSED 2026-05-17** (STEP 0 / 0.5 / 1; spec [`docs/RESULT_ORIGIN_CLOSURE_SPEC.md`](RESULT_ORIGIN_CLOSURE_SPEC.md), outer `8512f52`).
2. ~~reinforcement + AIR semantics~~ — **CLOSED 2026-05-17** (lane #2 complete; spec §5 / §7 / §8 / §9)
   - ~~reinforcement delay policy~~ — **RESOLVED 2026-05-17** (`RESULT_ORIGIN_CLOSURE_SPEC.md` §9 NORMATIVE — bounded reinforcement, legitimate)
   - ~~AIR footprint intent vs defect~~ — **RESOLVED 2026-05-17** (`RESULT_ORIGIN_CLOSURE_SPEC.md` §5 NORMATIVE — semantic over-collapse)
   - ~~insufficient_signal escalation semantics~~ — **RESOLVED 2026-05-17** (`RESULT_ORIGIN_CLOSURE_SPEC.md` §7 NORMATIVE — (a) partially legitimate / indiscriminate / gated, (b) latent governance hazard)
   - ~~loop containment ↔ integrity-metrics boundary~~ — **RESOLVED 2026-05-17** (`RESULT_ORIGIN_CLOSURE_SPEC.md` §8 NORMATIVE — functionally decoupled; governance-isolation guarantee)
3. ~~runtime label terminology~~ — **CLOSED 2026-05-17** (STEP 0 / 0.1 / 1; spec [`docs/RUNTIME_LABEL_TERMINOLOGY_SPEC.md`](RUNTIME_LABEL_TERMINOLOGY_SPEC.md), outer `b43edfd`).
4. 12-Axis drift surfaces
   — **CLOSED 2026-05-17 — substance complete; addressed by the prior CLOSED [`docs/12_AXIS_ARCHITECTURE_REVIEW.md`](12_AXIS_ARCHITECTURE_REVIEW.md) (Verdict phase, 2026-05-16).** queue #4 STEP 0 confirmed the four drift surfaces (WS-1/2/4/5) are all measured and Verdict-deferred there; currency check passed — no surface file drifted since the review baseline `44c9d6f`. FINGERPRINT_VERSION freeze intact; the 24-row ↔ 10-family gap remains WS-1-intended (no reinterpretation authorized); no deferred surface activated. **Carry-forward residue:** the WS-1 24-row relabel (`bty-app/docs/BTY_12_CORE_AXIS.md`) is runtime-neutral representational hygiene, Verdict-sanctioned, intentionally deferred — an inner-involved residue, topology-dependent on #6 (inner push topology); not actioned here.
5. line 148 numeric reconciliation (depends on #3 — **now unblocked**, #3 CLOSED 2026-05-17)
   — **RETIRED 2026-05-17 — signal-only / uncorroborated / no target surface found.** Introduced in `76099e0` forward-planning ledger transcription; never code/doc-corroborated. queue #5 STEP 0 confirmed "line 148" resolves to no file — no numeric reconciliation surface exists in repo state. Retired without a mutation target; **not** resolved by inference. (Original item text above preserved verbatim.)
6. inner push topology (Platform/Infra Mode)
   — **CLOSED 2026-05-17 — disposition (a) policy doc-only closure.** queue #6 STEP 0 measured the topology: inner content is already published to the shared remote via the outer `bty-app/` co-track on `origin/main` (full blob parity — 0 same-path mismatches); the `inner-main` ref is intentionally manual-push-only (upstream unset — estimate); the `origin/inner-main` lag (`a916c66f`, 15 behind inner HEAD `7ff03ced`) is content-safe. Policy articulated as [`docs/DUAL_REPO_TOPOLOGY_RISK.md`](DUAL_REPO_TOPOLOGY_RISK.md) §8 (NORMATIVE, 7 clauses). No inner push / upstream / remote mutation performed. **⚠️ Deferred Queue now fully complete — #1–#6 all CLOSED/RETIRED.** Carry-forward beyond the queue: **§5.3 representation-collapse remediation — CLOSED 2026-05-17** (outer `0b82187` / inner `71f01839`); **WS-1 24-row relabel residue — CLOSED + PUBLISHED 2026-05-17** (outer `53b8d0f` / inner `9f8942a0`); **§7.4 dormant-(b)-ingress governance handling — CLOSED + PUBLISHED 2026-05-17** (carry-forward 3/3 complete; B-1b route containment + §7.6 record, outer `f202782` / inner `35cba550`).

---

## D-4 — QR VERIFICATION SESSION 안2-B (multi-QR, dormant) + #1 (scanner banner, active) · CLOSED

**Status:** CLOSED — #1 active, 안2-B dormant-by-data (forward-ready) · **Date:** 2026-05-29 (D-4) · **Class:** code mutation (7 lib/UI/route + 1 test), inner + outer published + deployed · **Launch:** 2026-06-02 (D-0)
**Header:** [D-4 안2-B + #1 CLOSE 2026-05-29] Owner multi-QR awaiting list + logged-out scanner confirmation banner. #1 active (witness gap closed); 안2-B list dormant by data (≤1 unverified contract, no manager producer). inner `702a9e3d` (parent `4cd22ca4`) / outer `b3479b8` (parent `32298aa`) / Cloudflare active `5d0624d9-701e-401b-84cd-4842295395cb`.

**What shipped (8 files, +313/-3, additive):**
- plural `fetchAwaitingVerificationContractsForMyPage` (no limit; same awaiting filters as singular) + state-route `awaiting_verification_contracts[]` (type/payload/assembly).
- `AwaitingQrList` (NEW): per-item action_text/deadline/source + personal|manager tier label + per-item Show QR; dedups the Hub contract; empty → null.
- `handleRequestQrForContract(contractId)` → shared hoisted `ActionLoopQrPanel` (last-click-wins).
- logged-out scanner banner (✓verified / already / failed) + 6 bilingual i18n keys.

**Verify (Claude-verified):** `tsc` exit 0; vitest 3400 passed / 0 failed / 6 skipped (+5 plural-fetch regression).

**#1 scanner banner — ACTIVE (Commander-verified):** logged-out re-scan → "already verified" banner (was blank page); ✓/already/failed all supported.

**안2-B multi-list — DORMANT BY DATA (not a defect):** singular + plural pull the same awaiting set; ≤1 unverified contract at a time (`blocked_by_open_contract` invariant blocks concurrent personal; no `manager_only` producer — 4 creation sites all `mvp_open`). Dedup empties the 1-item list → null. Correct + forward-ready; 2+ concurrent unverified is structurally unreachable today. Singular Hub / 안2-A untouched; regression 0.

**Launch decision:** single-contract path (안2-A Hub + single QR + 안1 + P1) suffices; 안2-B auto-activates on manager issuance.

**Closure verdict:** 안2-B CLOSE (dormant-by-data) + #1 CLOSE (active).

**Carry-forward:**
- **★ Manager QR issuance** (`manager_only` producer) = activates 안2-B multi-list. Post-launch.
- **★ Owner realtime ✓** (My Page auto-refresh on scan). Post-launch.
- verified-history / analytics / `arena_level_records` repo-migration realignment / EN T3.

---

## D-4 — arena_level_records DRIFT · RESOLVED DB-SIDE (column-name mismatch, no repo commit)

**Status:** RESOLVED DB-side (runtime green); repo-migration realignment deferred · **Date:** 2026-05-29 (D-4) · **Class:** DB-only Commander SQL hotfix (no code commit, no redeploy) · **Launch:** 2026-06-02 (D-0)
**Header:** [D-4 arena_level_records DRIFT RESOLVED DB-SIDE 2026-05-29] qr/validate level update no longer fails; band progression unblocked. Live table diverged from CREATE TABLE migration (real col `last_evaluated_at` vs code `last_evaluation_at`). Worker `44751b0b` unchanged (DB caught up to code). Staged STEP-1 migration discarded.

**Symptom:** `qr/validate` level record update failed at runtime ("column does not exist") → `consecutive_verified_completions` never incremented = band progression silently stalled; verify/run-done/XP/AIR unaffected.

**Root cause:** live `arena_level_records` created **outside the migration path** — diverges from CREATE TABLE `20260431240000`: real column `last_evaluated_at` vs code-expected `last_evaluation_at` (name mismatch, not absence); also PK `id` vs `user_id`, `integer` vs `smallint`, default `'L'` vs `'mid'`.

**Fix (Commander SQL hotfix, no redeploy — worker already expects code names):**
1. ADD COLUMN `last_band_change_at` (genuinely absent).
2. Combined `ADD COLUMN IF NOT EXISTS` attempt **created** `last_evaluation_at` new → duplicate with `last_evaluated_at`.
3. Diagnosed name mismatch; `last_evaluated_at` all-NULL confirmed → `DROP COLUMN last_evaluated_at`.
4. Column names now match code; `information_schema` verified.

**Verified (Commander tail 9:08:45):** qr/validate `awaitingVerification:true` → `arena_run_done_after_contract_verify`; "level record update failed" gone.

**Lesson:** prescribing "add missing column" without `information_schema` ground-truth nearly turned a column-NAME mismatch into a duplicate column (`ADD COLUMN IF NOT EXISTS` can't match a differently-named column). Verify-real-schema-then-mutate.

**Closure verdict:** RESOLVED DB-side; repo-migration realignment = separate post-launch.

**Carry-forward:**
- **★ arena_level_records repo-migration realignment** [post-launch] — reconcile repo migration to actual DB (names/PK/types/defaults); forward alignment migration only, NOT editing `20260431240000`. Discarded STEP-1 file unusable as base.
- **★ 안2-B multi-QR session surface** [launch] + **★ #1 scan-confirmation UI** (bundle).
- EN T3 (28일 train body KO, score.ts, root layout lang, not-found, train/28days, auth callback:168).

---

## D-4 — QR VERIFICATION SESSION 안2-A (SURFACE) + 안2-A2 (PLACEMENT) · CLOSED (single-QR normalized)

**Status:** CLOSED (안2-A + 안2-A2) · **Date:** 2026-05-29 (D-4) · **Class:** code mutation (2 lib/UI files + 1 test), inner + outer published + deployed · **Launch:** 2026-06-02 (D-0)
**Header:** [D-4 안2-A SURFACE + 안2-A2 PLACEMENT CLOSE 2026-05-29] Canonical submitted contracts now surface on the awaiting card (resurrecting the 안1 Complete-by-QR button); QR panel hoisted under the button + auto-scroll. 안2-A inner `ec360b6c`/outer `559fb9e`; 안2-A2 inner `4cd22ca4`/outer `8c4ee32`; Cloudflare active `44751b0b-6ad7-4ddf-aff2-2102d94c8385`.

**What shipped:**
- **안2-A surface fix** (`openActionContractForMyPage.ts`, 2 lines): awaiting select + expiry sub-branch `.eq(status,approved)` → `.in(status,[approved,submitted])`. Gate (`validation_approved_at` not null, `verified_at` null) preserved; terminal query untouched; display unchanged. + regression test (3 cases).
- **안2-A2 placement fix** (`MyPageLeadershipConsole.tsx`, +16/-8): hoist `ActionLoopQrPanel` to directly under `ActionContractHub` (wrapped in ref) + `useEffect` `scrollIntoView` on open (optional-chained for jsdom safety). Sibling order + mint/validate/panel logic unchanged.

**Root cause (안2-A):** submit-validation canonical path → `status=submitted`+validation_approved+unverified; awaiting query matched `approved` only → submitted invisible → stale terminal "Execution recorded/Next scenario unlocked" surfaced + 안1 button inert.

**Verify (Claude-verified):** `tsc --noEmit` exit 0; vitest 3395 passed / 0 failed / 6 skipped (+3 regression).

**Probe (Commander-verified):** awaiting card + "Complete by QR" shows (안2-A); click → QR under button + auto-scroll (안2-A2); stale "Execution recorded" removed.

**Corrections recorded:**
- `action_completed` = canonical QR-eligible type (~all arena contracts); button gate reads `verification_mode`="hybrid" → not a type mismatch. The "hide QR for action_completed" direction was wrong (would break QR flow) — avoided.
- 안1 (awaiting-card QR button, `6ea159e7`/`8416ba48`) was inert alone (awaiting query dropped `submitted`); 안2-A makes it effective.

**Closure verdict:** 안2-A + 안2-A2 CLOSE.

**Carry-forward:**
- **★ 안2-B multi-QR session surface** [Commander launch target] — plural unverified-QR list + personal/manager (`verification_tier`) split + per-item QR. New plural fetch + state-route field + list component.
- **★ #1 scan-confirmation UI** — logged-out scanner validate-result block; bundle with 안2-B.
- **★ `arena_level_records.last_band_change_at` drift** — qr/validate level update runtime fail (verify/run-done OK); migration-first.
- EN T3 (28일 train body KO, score.ts, root layout lang, not-found, train/28days, auth callback:168).

---

## D-4 — QR RE-EXPOSURE 안1 + P1 FULL-LOOP · CLOSED (post-approve re-exposure + external-scan closure)

**Status:** CLOSED (안1 + P1 동시) · **Date:** 2026-05-29 (D-4) · **Class:** code mutation (1 UI file, additive), inner + outer published + deployed · **Launch:** 2026-06-02 (D-0)
**Header:** [D-4 QR RE-EXPOSURE 안1 + P1 CLOSE 2026-05-29] ActionContractHub awaiting-verification card gained a QR re-exposure button (post-approve dead-end fixed); P1 full external-scan loop runtime-proven incl. logged-out bypass. inner `6ea159e7` (parent `c11ee4b8`) / outer `53062f8` (parent `5436efe`) / Cloudflare active `8416ba48-5256-454a-bd36-a0875cc9e603` (supersedes Scanner `fc03cbb5`).

**What shipped (안1, STEP 1 atomic 1-file):**
- **ActionContractHub `action_awaiting_verification` branch** (`src/components/bty/my-page/ActionContractHub.tsx`, +15 additive): QR button gated `verification_type ∈ {qr, hybrid}`, `onClick → onRequestQr()` (reuses existing prop — no new wiring) → `handleRequestQr` → mint `approvedAwaiting` branch → `ActionLoopQrPanel` re-opens. On-demand re-mint (token stateless, contract row is source; no URL persistence). Reused existing i18n `btnQr`. Existing "Awaiting Verification" copy kept (additive only).

**Verify (Claude-verified):** `tsc --noEmit` exit 0; vitest 3392 passed / 0 failed / 6 skipped.

**Runtime (Claude-verified via wrangler tail + Commander-observed UI):**
- **P-안1 (Commander-observed):** "Complete by QR" button renders on the awaiting card (absent pre-deploy).
- **P1 Scan A (Claude tail, 5:25/5:29, logged-in):** `qr/validate` → `awaitingVerification: true` → `arena_run_done_after_contract_verify` (verified_at write path).
- **P1 Scan B (Claude tail, 5:37, LOGGED-OUT):** deep-link `/my-page?arena_action_loop=commit&aalo=… Ok` while plain my-page/center/bty-arena/foundry/leaderboard/my-page-subpaths all → `login?next=` at the same instant → logged-out middleware isolation definitively proven; `qr/validate` reached; contract `19b508d0` `approved`/`awaitingVerification:false` → re-write skipped (double-verify gate).

**Closure verdict:** 안1 CLOSE + P1 CLOSE (동시).

**Carry-forward:**
- **★ Scan-confirmation UI absent [NEW, launch UX]:** logged-out scanner gets no verification-result feedback on `/my-page` (page blank — logged-out data correctly empty). Needs scan success / already-verified / failure screen.
- **★ `arena_level_records.last_band_change_at` absent [NEW, migration drift]:** `qr/validate` level update failed (`column … does not exist`, Claude tail 5:25/5:29/5:37); verify/run-done OK, level/band partial fail. Migration-first.
- 안2 multi-QR (concurrent unverified contracts surface) — post-launch.
- EN T3 backlog (28일 train body KO, score.ts domain KO, root layout lang, not-found, train/28days, auth callback:168).

---

## D-4 — SCANNER PUBLIC ACCESS FIX · CLOSED (middleware exception · A)

**Status:** CLOSED (middleware exception lane — A) · **Date:** 2026-05-29 (D-4) · **Class:** code mutation (1 prod file + 1 test), inner + outer published · **Launch:** 2026-06-02 (D-0)
**Header:** [D-4 SCANNER PUBLIC ACCESS FIX CLOSE 2026-05-29] Restores mvp_open principle 5 (scanner identification: anyone / optional auth). A logged-out scanner reaching `/{locale}/my-page?arena_action_loop=commit&aalo=<token>` is allowed through ONLY when 3 conditions hold (path == my-page AND `arena_action_loop=commit` AND `aalo` present); `isPublicPath`/auth/consent/matcher untouched. inner `c11ee4b8` (parent `2bf81b5e`) / outer `d0844f9` (parent `5fd52b0`) / Cloudflare Version `fc03cbb5-a87b-492b-a4d0-cd11a5459c07`.

**What shipped (STEP 3 atomic 2-file commit):**
- **middleware narrow exception** (`src/middleware.ts`, +9/-0): after `isPublicPath`, a 3-AND guard (`/^\/(en|ko)\/my-page$/` anchored + `arena_action_loop=commit` + `aalo` present) returns `NextResponse.next()`. Anchored regex → subpaths do not leak the bypass.
- **test** (NEW `src/middleware.aalo-public-scan.test.ts`, 6 assertions): 3-condition allow, general my-page wall, each missing-condition wall, anchored-subpath wall, ko parity. Logged-out simulated via `getUser → user:null` mock (mirrors existing membership-gate test infra).

**Verify (Claude-verified):** `tsc --noEmit` exit 0; vitest 3392 passed / 0 failed / 6 skipped (+1 test file); targeted middleware unit 6/6.

**Probe (Commander-verified, no Claude DB/deploy access):** **P2 (release safety) RUNTIME GREEN** — logged-out general `/my-page` → 307 login wall held (tail evidence). Bypass correctness corroborated by: unit 6/6 + provenance URL-format match (mint route `action-loop-token/route.ts:172`) + secret `ARENA_ACTION_LOOP_QR_SECRET` present + mint Ok (tail 4:45:16) + `validation_approved_at` SET (tail 4:45:11).

**Closure verdict:** middleware exception lane CLOSE (A).

**Carry-forward (separate lanes):**
- **P1 full-loop end-to-end** (`qr/validate` → `verified_at`) = validate-route domain; split out.
- **★ QR Verification Session gap = launch blocker (new lane):** post-`approve_action_contract` the contract maps to `action_awaiting_verification` (`toDisplayState` `approved`→awaiting), which `ActionContractHub` renders as a button-less card — no Request/Show-QR action; the QR panel is ephemeral (`qrPanelOpen`/`qrUrl` reset on mount) → no re-exposure path. Multi-QR (re-issue) also unsupported.

---

## D-5 — CLIENT QR RENDER FIX · CLOSED (UI invariant)

**Status:** CLOSED (UI-side invariant achieved; URL-hide micro + L4 queued) · **Date:** 2026-05-28 (D-5) · **Class:** code mutation (4 prod files + 2 test), inner + outer published + deployed · **Launch:** 2026-06-02 (D-0)
**Header:** [D-5 CLIENT QR RENDER FIX CLOSE 2026-05-28] External-witness QR now renders instead of self-navigating the actor's browser to the commit deep-link. One shared `<ActionLoopQrPanel>` consumed by Arena resolve + My Page = UI system invariant. Combined with L5+L6 C5 server invariant, Spec v2 §3.5 is fully realized at both layers. inner `5a0174b4` / outer `f767e04` / Cloudflare Version `9df62778-5afa-4777-8a2a-2b1e30b8a194`.

**Authority:** Spec [`docs/QR_VERIFICATION_ARCHITECTURE_V1.md`](QR_VERIFICATION_ARCHITECTURE_V1.md) (Locked v2) §3.5 @ `d07a47ba` · Plan [`docs/CLIENT_QR_RENDER_FIX_PLAN.md`](CLIENT_QR_RENDER_FIX_PLAN.md) (Locked v1.2, 301 lines) @ `9e4313c3`.

**What shipped (4-stage, atomic 6-file commit):**
- **1A — shared `<ActionLoopQrPanel>`** (NEW `src/components/arena/ActionLoopQrPanel.tsx`, 41 lines): pure render component, props `(url, onDismiss, locale)`. `locale` added per v1.1 H4 amendment (i18n parity for `getMessages(locale).actionContract.dismiss` "닫기"/"Close"; presentation-layer param, NOT business coupling). Verbatim render parity with the MyPage block it replaces.
- **1B — producer migration** (`useArenaSession.startPendingContractQrFlow`): removed the single `window.location.assign(json.url)` self-nav at L2122; replaced with state exposure (`pendingContractQrUrl` / `pendingContractQrOpen` / `setPendingContractQrOpen`). 409 terminal + error branches UNCHANGED.
- **1C — Consumer A** (`ArenaResolveClient`): consumes new state + imports shared component + conditional render adjacent to `ArenaPendingContractGate`. Gate UNCHANGED (presentational button only).
- **1D — Consumer B DRY** (`MyPageLeadershipConsole`): removed inline `QRCodeSVG` + `qrcode.react` import; consumes shared component. `handleRequestQr` / aalo useEffect / qr state lifecycle UNCHANGED.

**UI system invariant achieved:** both awaiting-QR surfaces (Arena resolve + My Page) route through one shared `<ActionLoopQrPanel>` = one render contract. With L5+L6 C5 (one shared blocking helper for GET+POST server invariant), Spec v2 §3.5 progression model is now realized at BOTH server and UI layers. The only `window.location.assign` self-nav vector is removed.

**Tests:** 3386 passed / 0 failed / 6 skipped (+8 from baseline: 5 ActionLoopQrPanel + 3 ArenaResolveClient). No rewrites needed — the MyPage module-level `qrcode.react` mock + `qr-debug-value` assertions are transparent through the shared component (proof 1D preserved semantics). `tsc --noEmit` clean; eslint ajv crash pre-existing (backlog, not a gate).

**Probes (Commander active test 2026-05-28, Cloudflare `9df62778`):**
- **★ Probe 1 (CLOSURE GATE) GREEN:** actor clicks "complete by QR" → QR image renders on screen; browser URL stays on `/bty-arena/play/resolve/` (no self-navigate to `/my-page?...aalo=`). Directly resolves Commander's original "QR 안보임" symptom.
- **★ Probe 2 (CLOSURE GATE) GREEN:** fresh-session canonical contract render leaves `verified_at` NULL (render ≠ commit). [Pre-fresh-session DB row `9651cf9b` showed `verified_at` SET ~99s post-validation — a STALE BUNDLE still running the old `window.location.assign` before hard reload, NOT `9df62778` behavior. Confirmed by Commander hard-reset + fresh-session re-run = green.]
- **Probe 3 (external witness) GREEN:** Commander confirmed the external-scan path completes the loop.

**Cross-cycle provenance lessons (6 recorded this lane):**
1. plan amendment dispatch must carry inline body or surgical patch — not instructions only (L5+L6 v0.3, this lane v1.1/v1.2).
2. surgical patch = fenced **Old/New** blocks (unambiguous bytes, not description format).
3. predictions are layout-independent — string presence ✓, line count = derived metric.
4. line-count mismatch resolved by Commander, never by padding (self-authoring forbidden); content = authority (v1.2 ratified 301 lines).
5. plan §3.1 file table anchors to STEP 0 verbatim paths; inferred directories forbidden — mark "TBD verified in STEP 1 PART A".
6. (NEW) deploy-probe stale-bundle pattern — Cloudflare Workers update immediately but client tabs hold old JS; probes need hard reload / fresh session. Probe timestamps suspiciously close to deploy = stale-bundle suspect.

**★ OPEN ITEMS (post-lane, separate work):**
- **(i) qr-debug-value URL exposure** — selectable commit URL below the QR; intentional §1.4 testability retention through STEP 1. Disposition: micro-task to hide (sr-only / dev-only flag), NEXT immediately after this ledger.
- **(ii) Manual self-vector** — actor opening the commit URL in their own browser still auto-commits (pre-L4 baseline; Probe 6 documented intentional). Disposition: **L4** server-side tier-aware self-scan hardening + `verification_confidence` write to `le_verification_log` (spec §6.1).

**Decisions baked (Commander 2026-05-28):** (A) shared component · (b) separate "Client QR Render Fix" lane · (I) L4 sequential · v1.1 H4 amendment (locale prop) · v1.2 §3.1 path reconciliation. Core principle: "BTY는 '선택'이 아니라 '행동 완료'로 닫히는 시스템이고, QR은 진행 제한의 실행 게이트다." — now realized at both server and UI layers. Memory ref: #15 ledger topology, #26 spec v2, #27 sequencing.

**Next:** qr-debug-value URL-hide micro-task → L4 STEP 0 inventory → L4 lane (tier-aware `qr/validate` self-scan hardening + `verification_confidence` write).

---

## D-5 — L5+L6 QR ISSUANCE ALIGNMENT · CLOSED (server invariant)

**Status:** CLOSED (server-side complete; client follow-up lane queued) · **Date:** 2026-05-28 (D-5) · **Class:** code mutation (3 prod files + 2 test), inner + outer published + deployed · **Launch:** 2026-06-02 (D-0)
**Header:** [D-5 L5+L6 CLOSE 2026-05-28] QR scan = sole progression gate. Canonical `mvp_open` self-report auto-approve removed; Layer 2 advisory (X-2); binding surface collapsed to the shared blocking helper. Server invariant achieved across submit-validation + GET + POST surfaces. inner `f214cdcc` / outer `c6b1c4a` / Cloudflare Version `d6ab7835-275d-4b81-a927-577e5e38615d`.

**Authority:** Spec [`docs/QR_VERIFICATION_ARCHITECTURE_V1.md`](QR_VERIFICATION_ARCHITECTURE_V1.md) (Locked v2) §3.5 @ inner `d07a47ba` · Plan [`docs/L5_L6_QR_ISSUANCE_ALIGNMENT_PLAN.md`](L5_L6_QR_ISSUANCE_ALIGNMENT_PLAN.md) (Locked v0.3) @ `2b7b6aa4`.

**What shipped (3 components):**
- **C1 — canonical auto-approve removed** (`submit-validation/route.ts`): canonical `mvp_open` no longer auto-approves; lands `submitted` + `validation_approved_at`, `verified_at` NULL → QR offered → `qr/validate` scan sets `verified_at`. Gate renamed `canSelfReportAutoApprove`→`canLegacyAutoApprove`; legacy `legacy_self_attest` OR **retained** (decision 4A, `TODO[L8-cleanup]`); INVARIANT-3 inline completion gated to legacy.
- **C4 — Layer 2 advisory (X-2)**: `escalate`/`reject` no longer write blocking statuses — all three Layer 2 outcomes collapse to the `submitted` progression class. `bty_action_contract_escalations` audit row **kept** (Q2, non-blocking). Layer 2 semantic confidence (approve→high / escalate→medium / reject→low) is **outcome-derived server-side, NOT in the response** (spec §5 no-rationale). Structurally removes the escalate dead-end (Lane 7 H1).
- **C5 — binding surface** (`buildArenaBindingSnapshotResponse.server.ts`): `action_decision` status-branching collapsed to a single `snapshotForBlockedContract(blocking)` — GET + POST surfaces now consume the **same** blocking helper. submitted/escalated block at `ACTION_SUBMITTED`/`ACTION_ESCALATED` until QR scan. "QR = sole gate" is a system invariant across surfaces.

**Server invariant achieved:** canonical contracts land `submitted` + `verified_at` NULL; `qr/validate` is the sole `bty_action_contracts.verified_at` writer; binding + GET surfaces consume the same blocking helper. Constraints honored: `ACTION_ESCALATED` enum retained, H3 breaker kept, writer-only collapse, `qr/validate` untouched (scan-side = L4), legacy OR preserved.

**Tests:** 3378 passed / 0 failed / 6 skipped (9 rewritten + 2 new: escalation audit Q2, D1 boundary). `tsc --noEmit` clean. Pre-existing eslint ajv crash — backlog (not a gate).

**★ OPEN ITEM (post-deploy Probe 1 finding — separate forward lane, NO rollback):** Probe 1 showed canonical contract `87d92b73` reached `status='approved'` + `verified_at` SET with **no QR rendered**. Read-only re-inventory verdict: **L5+L6 server is correct** (canonical → awaiting_qr; `verified_at` set by the legitimate `qr/validate`). Root cause is a **pre-existing client self-completion path** that auto-approve had been masking — `startPendingContractQrFlow` (`useArenaSession.ts:2122`) does `window.location.assign(commitURL)` → self-navigates the actor to `/my-page?arena_action_loop=commit&aalo=…` → `MyPageLeadershipConsole.tsx:225-237` auto-POSTs the actor's own token to `qr/validate` → `verified_at` set, no QR shown. Disposition: **forward fix in a new Client QR Render Fix lane** (render the QR for an external scan instead of self-navigating); `qr/validate` self-scan-hole closure deferred to **L4** (tier-aware hardening). **No rollback of `f214cdcc`** (would restore the auto-approve bypass). `verification_confidence` scan-side write also deferred to L4 (spec §6.1 met at L4).

**Decisions baked (Commander 2026-05-28):** a/b/A · Q1/Q2/Q3 · (X-2) MVP advisory / X-3 post-launch content gate · "QR sole gate = system invariant". Memory ref: #15 ledger topology, #26 spec v2, #27 lock-and-dispatch sequencing.

**Next:** Client QR Render Fix — STEP 0 inventory → STEP 1-3 → then L4 (tier-aware validate route + self-scan hole + `verification_confidence` write).

---

## D-6 — QR VERIFICATION ARCHITECTURE v1 · L1 DB MIGRATION CLOSE

**Status:** CLOSED (5 migrations applied + verified + pushed) · **Date:** 2026-05-27 (D-6) · **Class:** schema migration, inner + outer published · **Launch:** 2026-06-02 (D-0)
**Header:** [D-6 L1 CLOSE 2026-05-27] QR Verification Architecture v1 Layer 1 DB migration applied to production + committed/pushed inner `d9443b84` / outer `b21c47f0`. 5 migration files (column add, log extension, legacy stamp, CHECK constraints, type-CHECK correction). All STEP 2 (9 tests) + §3.4 FK VALIDATE PASS. Schema ready for L2.

**Migration files (5, 010000–010400):**
- `010000` `bty_action_contracts` — +5 columns + 2 indexes
- `010100` `le_verification_log` — +7 columns + FK NOT VALID + 2 indexes
- `010200` legacy stamp UPDATE — 133 rows → `legacy_self_attest` (drift-safe `WHERE tier IS NULL AND confidence IS NULL`)
- `010300` 6 CHECK constraints — 4 on contracts + 2 on log (DO $$ idempotent wrappers)
- `010400` drop stale prefixed `bty_action_contracts_verification_type_check` (L1 correction patch)

**Production state (post-migration):**
- `bty_action_contracts`: 133 rows, all stamped `legacy_self_attest` (Commander D2 preservation — XP/AIR preserved, no rollback); +3 new CHECK (tier / status / confidence)
- `verification_type` CHECK replaced & expanded 8 → 11 values (3 new canonical + 8 legacy; old prefixed constraint dropped via `010400`)
- `le_verification_log`: 51 rows, +7 columns, FK VALIDATED + enforcing (`convalidated=true`), +2 indexes, +2 CHECK

**Verification:** STEP 1A / 1B / 1C / 1D / 1D-corr ALL PASS · STEP 2 (9 tests) PASS · §3.4 FK VALIDATE PASS (convalidated=true, enforcing confirmed).

**Commits (pushed):** inner `d9443b84` → origin/inner-main · outer `b21c47f0` → origin/main (mirror). 6 files each (5 migrations + .gitignore), 297 insertions.

**Authority:** Spec [`docs/QR_VERIFICATION_ARCHITECTURE_V1.md`](QR_VERIFICATION_ARCHITECTURE_V1.md) (Locked v1) · Plan [`docs/L1_MIGRATION_PLAN.md`](L1_MIGRATION_PLAN.md) (Locked v1.3). Memory ref: #26 bty_qr_arch_v1_LOCKED, #27 lock-and-dispatch sequencing.

**Deferred (separate dispatches):** Plan §5.1 footnote (DROP referenced unprefixed `verification_type_check`; actual prod name was prefixed — patched via `010400`, doc footnote pending) · migration-file LF copy-friendly-header housekeeping.

**Next:** L2 contract creation rewrite (4 WRITE sites).

---

## D-7 LANE 7 — QR GATE REGRESSION FIX (H1 + H3)

**Status:** CLOSED (code fix + SQL hotfix complete; push HELD pre-D-1 = 6/01) · **Date:** 2026-05-26 (D-7, post-Lane-6) · **Class:** launch-blocker resolved · **Launch shift:** 5/30 → 6/02 (5/30 presentation only)
**Header:** [D-7 Lane 7 CLOSED 2026-05-26] QR-GATE-REGRESSION incident — two independent layers (H1 WRITE regression + H3 runtime collapse) fixed across inner + outer; SQL hotfix unblocked the triggering user. Outer-only ledger closure (no inner change).

**Trigger:** user-facing deadlock on contract `fe71287c-5875-4f66-b576-b5bbed7f4c03`.

**Diagnostic:** QR-GATE-DIAG (Phase 1 row inspection, Phase 2 Track A+B, Track C code trace 2026-05-26).

**Root cause — two independent layers:**
- **H1 — WRITE regression (`baf5f210`, 2026-05-22, STAB-07-P0 L1):** 3 creation sites changed `verification_type` `self_attest`→`qr`, but `submit-validation` route.ts:267 4-AND gate first condition still requires `verification_type==='self_attest'`. Auto-approve path permanently dead → every contract falls through to non-deterministic Layer 2. The `baf5f210` commit message + the prior Release Gate entry claimed "consumption pipeline pre-wired (submit-validation gate, …)" — Track C confirmed this was **false for the gate** (commit-message corroboration gap).
- **H3 — runtime collapse:** `arenaRuntimeSnapshot.server.ts` collapsed `escalated`→`ACTION_SUBMITTED` and `qrAllowedForContract` had no escalated branch → escalated contracts had every gate false, no forward path. Independent of H1: production never auto-approves (`BTY_ENV` gate, submit-validation L246-247), so this deadlock hit every escalation regardless of `verification_type`.

**Decisions (Commander):**
- **Layer 1 (A2):** revert 3 WRITE sites `qr`→`self_attest`. Universal QR remains a deferred direction; consumption side (qr token mint, validate route, escalated recovery) requires separate verification before re-introduction.
- **Layer 2 (B2+B1, fully wired):** distinct `ACTION_ESCALATED` runtime state across all consumer surfaces (union + blocking-array + guard + priority 96 + mapping + qrAllowed + destination router + parse-allow + ArenaBlockedSurface + ArenaPendingContractGate + ArenaResolveClient `isResolveState`) + `qrAllowed` escalated case (`escalated AND verified_at IS NULL → true`).
- **SQL hotfix (Commander-run):** contract `fe71287c` → `status=approved` (+ validation_approved_at / verified_at / completed_at); escalation row → `status=resolved`, `resolution=secondary_approve`; audit breadcrumbs in `details.hotfix` + `escalations.reviewer_notes`. (Recorded as Commander action; not Claude-verified — DB read was access-blocked.)

**Commits (push HELD):**
- Layer 2: inner `fee5d29d` → `ba89565a` / outer `56adb8b` → `ae60617`. Files: `arenaRuntimeSnapshot.types.ts`, `arenaRuntimeSnapshot.server.ts`, `arenaRuntimeSnapshot.server.test.ts` (NEW, 8 cases), `arenaRuntimeDestination.ts`, `arenaSessionRouterClient.ts`, `ArenaBlockedSurface.tsx`, `ArenaPendingContractGate.tsx`, `src/app/[locale]/bty-arena/play/resolve/ArenaResolveClient.tsx`, `ArenaResolveClient.escalated-revise.test.tsx`.
- Layer 1: inner `ba89565a` → `90e5c13a` / outer `ae60617` → `5fa80b8`. Files: `action-contracts/route.ts`, `ensureActionContract.ts`, `eliteBindingActionCommitment.server.ts`, `action-contracts/route.test.ts`.

**Baseline:** 3364/0/6 → 3372/0/6 (Layer 2 +8 tests; Layer 1 in-place test update, count unchanged). **tsc:** PASS (clean exit 0).

**Push status:** HELD pre-D-1 (6/01). **Worker:** `47dca7a4` (regression still live until D-1 deploy).

**D-1 deploy mandate:** Layer 2 = primary launch-safety dependency (production never auto-approves; Layer 2 is the only forward exit for escalated state). Layer 1 restores normal staging posture but does not change production semantics. Release Gate entry added (QR/verification gate touched per release-safety.md).

**Production effect when deployed (D-1 6/01):** staging → self_attest + 4-AND gate → auto-approve resumes; production → BTY_ENV gate skips auto-approve → Layer 2 → escalate possible → `ACTION_ESCALATED` + `qr_allowed=true` → forward QR exit. No 72h deadlock, no silent breakage.

**Lessons:** (1) commit-message / Release-Gate claim ≠ code reality — require producer+consumer+gate three-way verification before release confidence; (2) `\s`-based git-grep can return false-empty — use fixed-string fallback; (3) smallest-diff vs domain-correctness tension — full ACTION_ESCALATED wiring (Option B) chosen over the 1-line concession.

**VERIFICATION (post-deploy + live, 2026-05-26 evening) — incident FULLY CLOSED:**
- **Released early (D-7, not the planned D-1):** Commander explicit decision due to active production deadlock on the organic-user path. Process gates all passed (baseline 3372/0/6 · tsc clean · Release Gate entry · rollback anchors); release-safety is process-enforced, not calendar-enforced.
- **Deploy (Claude-verified, 3-way):** worker `47dca7a4` → `20f15258-a2e6-465e-8b39-450eaf47f6fe` — deploy stdout + `wrangler versions list` + live `/api/version` probe all agree.
- **Push (Claude-verified):** inner `f71c0616..90e5c13a` → origin/inner-main; outer `ed2fed3..3f92e66` → origin/main (full 25-commit held aggregate, incl. Lane 3 auth surface + LRI/Certified spec doc `2faa66d`).
- **Browser flow (Commander-reported, not Claude-verified):** fresh contract `ea20f335-be9c-4fd6-9f1b-0e18601d3354` (hanbitchi, `core_07_repair_conversation`) → `verification_type='self_attest'`, `status='approved'`, `validation_approved_at` 15:44:10, auto-approved in ~41s; no escalation / no QR step / no deadlock.
- **SQL hotfix (Commander-run, not Claude-verified):** `fe71287c` (chihanbit7) resolved `secondary_approve` 14:29:05; `b76b1da3` (hanbitchi) resolved 15:38:47 (2 escalation rows both resolved).
- **Runtime-model correction:** the D-1 prediction lines above ("production never auto-approves") are **superseded by live reality** — there is no separate production worker; the single live worker `bty-arena-staging` is staging-configured (`BTY_ENV="staging"` + `SELF_REPORT_AUTO_APPROVE="true"`, confirmed in deploy binding output), so the 4-AND auto-approve gate **fires** and Layer 1's self_attest revert resolves the deadlock at source. Layer 2 `ACTION_ESCALATED` is the safety net for genuine escalations. Claude memory model updated (`bty_prod_worker_absent_single_db`). Production-tier worker separation = deferred architectural item, not current state.

**D-6 cleanup sweep (2026-05-26 evening, post-LANE-7-VERIFIED-UPDATE) — Commander-run + reported, NOT Claude-verified (no DB access):**
- Six pre-deploy QR-gate-regression artifacts manually approved and unblocked: `e4632681` (STAB-08 smoke seed), `1ba8b194`, `c52628f0`, `9df071f9`, `38d9e485`, `aaa3a010`. Four related escalation rows resolved (38d9e485, 9df071f9 [duplicate-pair], aaa3a010); the remaining three are orphan-pattern with no escalation child row.
- No external organic users affected — affected rows were Commander variant accounts (`52e543cc`, `38ce28d2`, `ee9d2075`, `9587a44e`), the STAB-08 smoke seed, or known internal test IDs only. Executed row-by-row in Supabase SQL Editor; results paste-verified by Commander.
- **Operational note (Commander-reported):** a bulk `UPDATE … WHERE id IN (…)` inside a BEGIN/COMMIT block silently failed (verify SELECT showed pre-update state; constraint/trigger dumps showed no obstruction); row-by-row single-statement UPDATEs succeeded for all 6. Recorded as a hotfix-discipline backlog item (CURRENT_TASK #7).

---

## D-4 LANE 6 — HANDBOOK ADDENDUM v2 FINAL DRAFT (Commander lane)

**Status:** CLOSED (artifact persisted, awaiting Lawyer Input #2) · **Date:** 2026-05-26 (D-4 evening) · **Class:** legal artifact, outer-ledger closure, docs-only · **Launch:** ~2026-05-30
**Header:** [D-4 Lane 6 CLOSED 2026-05-26] btyARENA employee handbook addendum v2 FINAL DRAFT persisted at [`docs/legal/HANDBOOK_ADDENDUM_2026-05-v2.md`](legal/HANDBOOK_ADDENDUM_2026-05-v2.md) (Lawyer Input #2 source). Outer-only ledger closure — no inner commit per ledger cotrack symlink topology.

**Decisions locked this chat:**
- Q1 (BTY data in performance review): role-definition framing — BTY informs, organization decides; not a sole or automatic determinant.
- Q2/Q3 (allowed aggregate surfaces / min team size): disposed — not in handbook scope; belongs in internal data governance doc.
- Q4 (Manager QR authority): A — handbook discloses scope ("authorized team-development, QR, or behavioral reinforcement workflows"), mechanics suppressed.
- Q5 (persist in repo): YES — this commit.

**Key architecture:**
- §3.2 three-layer role (Measurement / Evidence / Decision).
- §3.3 sole-or-automatic-determinant exclusion (anchor sentence).
- §3.4 three-layer visibility (Participant / Manager / Enterprise-Admin).
- §3.5 role-based visibility framing (replaces v1 "minimize re-identification" privacy posture).

**Disclosure posture:** aligned with Lane 5 consent doc `2026-05-v1` (active-truth).

**Next:** Lawyer Input #2 cover note (Commander side, separate channel).

- **Push:** HELD pre-D-1 (inner ahead 19 / outer ahead 21; this ledger commit → outer ahead 22). Lane 3 + Lane 6 both pending.
- **Baseline:** `3364/0/6` (unchanged — docs-only commit, no code).
- **Deploy:** NOT EXECUTED — worker live `47dca7a4` unchanged.

---

## D-4 LANE 3 — ONE CLEAN PUBLIC ENTRY DOOR (executed early)

**Status:** CLOSED (push/deploy HELD) · **Date:** 2026-05-26 (D-4; pulled forward from D-1 runway) · **Class:** launch surface, push-held, outer-ledger closure · **Launch:** ~2026-05-30
**Header:** [D-4 Lane 3 CLOSED 2026-05-26] OAuth single public entry door — AuthGate inline Google CTA + login-card provider feature flag. Implementation already landed (inner `fee5d29d` / outer `8179606`); this is the outer-ledger closure block (no inner commit).

**Why pulled forward:** today's auth-surface diagnostic surfaced `/api/auth/register` was LIVE (not vestigial) and the landing AuthGate still rendered an email/pw login form, so the D-1 "one clean entry door" work was executed same day. Commander locked the decisions 2026-05-26 (D-4 morning); push/deploy held per launch_runway.

**Closed:**
- `AuthGate.tsx`: email/pw form removed → inline **"Continue with Google"** CTA (ko: "Google로 계속하기") → `/[locale]/bty/login?next=/[locale]/bty`.
- `login-card.tsx` (real path `src/components/auth/login-card.tsx`): `NEXT_PUBLIC_BTY_AUTH_PROVIDERS` feature flag (default `google`); Microsoft + Phone OTP code retained, hidden by default; `signInWithOAuth`/`signInWithOtp`/`verifyOtp` paths intact.
- `AuthGate.cta.test.tsx` new (5 tests).
- `login-card.oauth-prompt.test.tsx` +1 scenario (env-driven provider visibility).
- `LAUNCH_OPERATIONS.md` production-users table: `aidencool0929` (`e9eded1c`) row added — Commander Google variant, dormant single-session 2026-04-06.

**Commander-locked decisions:**
- Inline CTA (not auto-redirect) — agency + zero-flash.
- Post-login `next` default `/${locale}/bty` (middleware convention).
- `/api/auth/login` retained for `/admin/login` backend (untouched).
- Google-only public provider for launch; feature flag allows future expansion.
- Microsoft + Phone OTP gated, not deleted.

**Commit chain:**
- Inner: `8822e4e9` → `fee5d29d` (4 files, +199/−117).
- Outer: `7124183` → `8179606` (5 files = 4 co-track + LAUNCH_OPERATIONS, +200/−117).
- Outer this commit: `8179606` → (Lane 3 closure ledger).

**Verification:** baseline `3358/0/6` → `3364/0/6` (+6 new tests); tsc clean.

- **Push:** HELD (inner ahead 19 / outer ahead 20; this ledger commit → outer ahead 21).
- **Deploy:** NOT EXECUTED — worker live `47dca7a4` unchanged.

**Provenance / Claude Code disclosed deviations (4):**
- Path drift surfaced: `src/components/auth/login-card.tsx` (not `src/components/login-card.tsx`) — memory #21 reinforced.
- Per-render provider flag (not module-scope const) for vitest testability — prod-identical (Next inlines `NEXT_PUBLIC_*` literals at build).
- CTA copy ko-localized to match login-card convention.
- Ledger closure intentionally authored by C3 (per memory #25 — implementation dispatch does not silently author its own closure).

**Deviations from Commander-locked plan:** none beyond the 4 disclosed above.

---

## D-4 AFTERNOON — LAUNCH-OPS HARDENING (auth surface · LAUNCH_OPS · verify chain)

**Status:** CLOSED · **Date:** 2026-05-26 (D-4 afternoon) · **Class:** launch-ops hardening, push-held, outer-ledger-only · **Launch:** ~2026-05-30
**Header:** [D-4 afternoon CLOSED 2026-05-26] OAuth-only auth surface enforcement + LAUNCH_OPERATIONS.md + admin/email-confirm verify chain.

**Closed lanes:**
- Launch model corrected (memory #16): organic **OAuth + admin-approve**, NOT a 20-user pre-planned invite cohort.
- **Auth surface 3-layer defense**: AuthGate UI toggle (env flag `NEXT_PUBLIC_BTY_ALLOW_SELF_REGISTER`) + `/api/auth/register` backend 410 guard (`BTY_ALLOW_SELF_REGISTER`) + Supabase email confirmation ON.
- **`BTY_ADMIN_EMAILS` verify**: repo (`authz.ts` + `wrangler.toml` `[vars]` since `fa0b86d6`) + runtime (Cloudflare Dashboard).
- **Supabase email confirmation**: ON verified (Dashboard).
- **Locale operational protocol**: EN default; Commander shares `/ko/*` links for KO audience.
- **`LAUNCH_OPERATIONS.md`** created + provenance upgrade.
- **`LEGAL_FOLLOWUPS.md`** date drift fixed (D-4 = 2026-05-26).

**Diagnostic findings** _(DB-side items per Commander Dashboard / query, 2026-05-26; not Claude-witnessed)_:
- chihanbit7 5 IN_PROGRESS runs = intentional inspection artifacts.
- 16 `auth.users` in DB; 3 approved (all Commander variants); 0 external approved.
- hanbitchi `tier=27` = cumulative DONE-runs counter (valid; not a discrete enum).
- LLM provider: OpenAI only (Anthropic 0 paths; Gemini wired but keyless; self-hosted inactive).
- 7+ schema drifts surfaced and corrected (memory #22).

**Commit chain (outer-only this afternoon):**
- `884a261`: auth surface hardening co-track (mirrors inner `8822e4e9`).
- `d86fcd7`: `LAUNCH_OPERATIONS.md` create.
- `59e6a7e`: `LAUNCH_OPERATIONS.md` provenance upgrade.
- (this commit): closure micros — LEGAL date drift + this ledger block.

**Inner commit chain:** `8822e4e9` — auth surface hardening (AuthGate + register guard). No further inner commits this afternoon.

- **Push:** HELD (outer ahead 19 after this commit; inner `inner-main` manual-push-only).
- **Deploy:** NOT EXECUTED — worker live `47dca7a4` (unchanged).

**Memory edits this session:** #6 (9 canonical runtime states) · #16 (D-4 afternoon launch model corrected) · #19 (wrangler ASC sort) · #22 (db_cleanup_discipline — 7 schema drifts + hanbitchi/chihanbit7 PRESERVE).

---

## D-4 MORNING — LAUNCH-PREP CLOSURE (Lane 5 consent · #26 · #23 · Phase 5)

**Status:** CLOSED · **Date:** 2026-05-26 (D-4 morning session; underlying commits landed 2026-05-25 ~22:00 PDT / D-5 evening) · **Class:** launch prep, push-held, outer-ledger-only · **Launch:** ~2026-05-30
**Header:** [D-4 morning CLOSED 2026-05-26] Lane 5 EN/KO consent + CONSENT_VERSION bump + #26 contrast + #23 LangSwitch dedup + Phase 5 cleanup.

D-4 morning launch-prep lanes closed; push + deploy HELD per the launch_runway freeze. The code already landed as the Phase 2 / Phase 2.5 co-track pairs (see chain below); this is the outer ledger closure only — no inner commit (`docs/CURSOR_TASK_BOARD.md` ledger topology, outer real file; the `bty-app/docs/` symlink is untouched).

**Closed lanes:**
- **Lane 5 EN consent** — active-truth vendor disclosure (Cloudflare / Supabase / OpenAI; Anthropic excluded per Phase 1 Provider Verify).
- **Lane 5 KO consent** — legal-equivalent Korean; Commander-locked 4 edits (진료실 팀 / PHI+identifiable / 전송 wording / 동의합니다).
- **CONSENT_VERSION** — `2026-05-pending-v1` → `2026-05-v1`.
- **#26** — PatternSignaturePanel light-theme contrast fix (1.04:1 → 14.5:1, AA pass).
- **#23** — LocaleLayoutHeader `isAdminArea` guard (LangSwitch dedup on `/admin/*`).
- **Phase 5 cleanup** — smoke-step2d (`51a162ff`) DELETE (cascade 0 rows — pure auth artifact); preserve cohort verified (hanbitchi `52e543cc`, chihanbit7 `9587a44e`, ywamer2022 `ee9d2075`).
- **LEGAL_FOLLOWUPS.md** created (cross-border transfer flag).

**Diagnostic findings (closure):**
- chihanbit7 classification: `likely_real_user` (Commander variant); 5 IN_PROGRESS runs = intentional inspection artifacts, NOT a launch blocker.
- arena_runs schema validated: 7 column-name drifts surfaced (memory #22 reinforced with specifics).
- Provider Verify: BTY runtime = OpenAI only (Anthropic 0 code paths; Gemini wired but keyless; self-hosted inactive).

**Commit chain:**
- Phase 2 (Lane 5 + #26 + #23): inner `7afd272a` / outer `a6790c53` — 2026-05-25 22:02 PDT.
- Phase 2.5 (KO consent + CONSENT_VERSION bump): inner `82c7d59f` / outer `c57f8ca7` — 2026-05-25 22:14 PDT.
- Phase 5: DB DELETE only (no commit).
- **Push:** HELD (launch_runway D-3/D-2 final-push window).
- **Deploy:** NOT EXECUTED — worker live remains `47dca7a4` (the D-5 closure deploy; unchanged at D-4).

**Memory edits this session:** #6 (two-layer state machine — 9 canonical states locked) · #16 (D-5→D-4 transition, closed lanes + queue) · #19 (wrangler 4.85.0+ `versions list` ASC sort) · #22 (db_cleanup_discipline — 7 schema drifts + cascade trace + preserve defaults).

---

## PUSH AUTHORITY MODEL — LOCKED

**Status**: GOVERNANCE MODEL LOCKED · **Date**: 2026-05-24 · **Class**: release governance, time-anchored · **Lifetime**: D-9 → D-0 freeze window · **Governance record**: `docs/PUSH_AUTHORITY_MODEL_D9.md`

Push Authority Model authored, corroborated against Stage A inventory, and verbatim locked. Defines read-only-only commit class, fixture seed/clean DB-mutation classification, and per-anchor authority gates; current 5-ahead state remains HOLD under the locked D-9 freeze model.

---

## Q-GEN STEP 0 — CONSTITUTIONAL CLOSURE

**Status**: FORMALLY CLOSED · **Date**: 2026-05-24 · **Lane class**: forensic, mutation-zero · **Constitutional record**: `docs/Q-GEN_STEP_0_CLOSURE.md`

Five-cycle corroborated forensic lane formally closed with mutation 0. Lever α/β/γ remains HOLD; constitutional authority resides in the closure document and no implementation authority follows without a separately opened lane.

---

## STAB-07-P0 — Universal QR Lane 1: CLOSED

**Status:** CLOSED (Phase 0C v2, LIVE branch) — pending launch-eve verification gate (Stage 8 spec)
**Date:** 2026-05-22
**Header:** [stab_07_p0 CLOSED 2026-05-22] L1 universal-QR swap shipped.

Universal-QR Lane 1 shipped: `verification_type` `self_attest`→`qr` on all 3 contract-creation paths — `route.ts:64` (json-dev), `ensureActionContract.ts:280`, `eliteBindingActionCommitment.server.ts:201`. Branch **LIVE** per Commander decision (json-dev is configuration-live via `ARENA_PIPELINE_DEFAULT="new"`; universal-QR α = all 27 core uniform). Creation-side only — existing rows preserved.

- **Commits:** inner code `baf5f210` + tests `35013b74`; outer co-track code `ee0edb18` + tests `25f0af02`; inventory sheet restore `9e53574` (Phase 0B). Code blobs byte-identical to the original `7ca96ae7` Lane 1 attempt.
- **Tests:** baseline 3310→**3314** (+4 qr-flow cases: json-dev emits qr; submit-validation no-auto-approve / Layer 2 pass→awaiting_qr / fail→escalated), `tsc` clean.
- **Deploy:** staging Worker Version `6528ecf2-f0e0-4a8c-8996-f2b58bcd4b45` (2026-05-23T01:28Z). Rollback targets preserved: `5a544379` (prev) / anchor `a27781f5` (UNTOUCHED).
- **Live smoke:** test-user arena run → new contract `verification_type=qr`, `status=escalated` (Layer 2 ran; NOT auto-approved); Scope C escalated-revise UI rendered ("sent for review / revise and resubmit"). DB immutability verified — pre-existing `self_attest` count unchanged (28); only new row emits qr.
- **Consumption pre-wired:** submit-validation auto-approve gate keys on `verification_type==='self_attest'` (qr bypasses → Layer 2); `qr/validate` witness route; 72h escalation cron (escalated→pending SLA); Scope C revise UI (ArenaResolveClient L54/170/177-184).
- **Phase chain:** 0A retro (root cause = missing escalated UI, fixed by Scope C) → 0B inventory restore (α confirmed) → 0C v1 AMBIGUOUS-STOP (json-dev live-but-minimal: 4 contracts/1 user) → 0C v2 LIVE.
- **Pending:** launch-eve verification gate (D-1/D-0). **Rollback:** `wrangler rollback` to `5a544379` (fast) or anchor `a27781f5` (deep) + `git revert ee0edb18`/`baf5f210`; Scope C and sheet `9e53574` stay.
- **Stale-by-design comments** (optional follow-up): route.ts:60-63 + route.test.ts:87-91 still describe old `self_attest`.

---

## STAB-08 Scope C — Escalated Contract Revise UI: CLOSED

**Status:** CLOSED (Scope C only; Scope A/B remain backlog)
**Date:** 2026-05-22
**Header:** [stab_08 Scope C CLOSED 2026-05-22; Scope A/B backlog]

STAB-08 Scope C surfaces the escalated-contract revise path on the Arena Resolve
surface. An escalated contract maps to runtime_state="ACTION_SUBMITTED"
(runtimeStateFromBlockingContract) with qr_allowed=false, so ArenaResolveClient
previously rendered only ArenaPendingContractGate — no revise CTA — even though
submit-validation/route.ts:189-200 already accepts an escalated resubmit. Scope C
adds a client-side branch keyed on the server-emitted action_contract.status==="escalated"
that reuses ArenaActionValidationForm verbatim, preceded by a distinct escalation
notice. Pure UI surfacing — no snapshot, API, Scope A/B, cron, or form-component change.

CORRECTION to the ROLLBACK reading: the "permanently stuck" framing was a UI misread,
not a runtime failure. The server already supported escalated resubmit; Scope C surfaces
the form backing it. Layer 1/2 `revise` responses are expected validation outcomes, not
dead-end regressions. The Scope C launch-blocker noted in STAB-07-P0 ROLLBACK is cleared.

File: src/app/[locale]/bty-arena/play/resolve/ArenaResolveClient.tsx (single file —
  L54 derived `escalatedReviseAllowed`; L170 branch condition; L177-184 escalation notice).
Inner: `3e63a5da` on inner-main (local; co-track mirror authoritative on origin/main).
Outer co-track: `c9ce8c2` on main (bty-app/ leak-integration of the code).
Worker: Version `5a544379-3c15-44c5-a5d4-b0eaf4685562`, staging (supersedes `844990c0`).
Baseline: vitest 3307 → 3310 / 0 / 6 (+3 ArenaResolveClient.escalated-revise tests).
  lint PASS (tsc --noEmit clean).
Smoke: seed contract `e4632681` (hanbitchi) → escalated; DB-verified transition
  escalated → submitted → pending after Commander browser resubmit (×2); revise notice
  + validation form render confirmed by Commander; submit-validation accepted the escalated
  resubmit (verification_type=qr → no staging auto-approve; Layer 1 revise (R1/R2/R4
  validation on insufficient input — expected) → re-edit → Layer 2 → "sent for review"
  → pending (re-editable)). Worker Version 5a544379.

Canonical anchor UNCHANGED: `a27781f5-e709-4660-bd07-1d11a72d60d7` remains the rollback-safe
stabilization anchor. STAB-08 Scope C is forward progress, NOT a new rollback anchor; 5a544379
is the current staging version, not a rollback target.

Inventory artifact: docs/STAB-08-SCOPE-C-INVENTORY.md (committed in this closure as
  historical inspection artifact).

Backlog (NOT closed by Scope C):
- Scope A — My Page action-state blind spot (openActionContractForMyPage excludes
  escalated/submitted) — post-launch backlog.
- Scope B — Escalation recovery cron not scheduled (wrangler.toml no [triggers]; open
  bty_action_contract_escalations row never closed on self-resolve) — post-launch backlog.
- Cleanup endpoint hardening — /api/test/cleanup-action-contracts has no Cloudflare-effective
  env guard (VERCEL_ENV check inert on Workers); add BTY_ENV positive assertion — post-launch.

Forward hook: STAB-07-P0 redispatch now UNBLOCKED (escalated revise UI shipped); follows as a
separate lane. STAB-07-P0 scenario-classification inventory (STAB-07-P0-SCENARIO-INVENTORY.md,
recoverable at c6159ab) restoration deferred to that lane.

---

## STAB-07-P0 ROLLBACK

[stab_07_p0 ROLLED_BACK 2026-05-22]

STAB-07-P0 Lane 1 (universal QR verification, inner `7ca96ae7` + outer `c6159ab`) was reverted after staging smoke surfaced a pre-existing UX gap that blocks user recovery.

Gap discovered (verified at inner HEAD `7ca96ae7`):
- Lane 1 stopped staging auto-approve, so real Layer 2 ran on every submit.
- Layer 2 returned `escalate` for the Commander's first smoke statement → contract `status="escalated"`.
- Escalated contracts have NO user-facing revise surface: `ArenaResolveClient.tsx:163-191` renders `ArenaActionValidationForm` only when `runtime_state="ACTION_REQUIRED" && !validationApproved`; escalated maps to `runtime_state="ACTION_SUBMITTED"` (`runtimeStateFromBlockingContract`) → falls through to `ArenaPendingContractGate`. And `qrAllowedForContract` returns false for escalated (`validation_approved_at` null) → the gate shows **neither a revise form NOR a QR button**.
- Cron auto-reset is unscheduled (T1: `wrangler.toml` has no `[triggers]`/crons) → no background recovery.
- Escalated is blocking (T2: `BLOCKING_STATUSES` includes "escalated") → user cannot start a new scenario.
- Result: hanbitchi was permanently stuck on staging.

Rollback restores the staging auto-approve path (`BTY_ENV="staging"` 4-AND gate condition 1 matches "self_attest" again). The rollback does not change production behavior (auto-approve is staging-only). **NOTE:** the escalate→no-revise-UI gap is **latent in production independent of Lane 1** — production self_attest contracts already route through Layer 2 (no auto-approve in prod) and can escalate into the same dead-end. STAB-08 Scope C is therefore launch-blocking for any production launch where contracts hit Layer 2.

Reverted commits:
  Inner: `7ca96ae7` (Lane 1) reverted via `f71c0616` (pushed to origin/inner-main)
  Outer: `c6159ab` (closure) reverted via `bae3322` — also deleted `docs/STAB-07-P0-SCENARIO-INVENTORY.md` (recoverable: `git show c6159ab:docs/STAB-07-P0-SCENARIO-INVENTORY.md`)
Worker redeployed: Version `844990c0-5be3-4235-9a02-e6acb541d99f` (staging, reverted code). lint PASS, vitest 3307/0/6.

STAB-07-P0 RE-OPENED. Re-design must include the escalated revise UI as part of the universal QR ship.

> Annotation (2026-05-22): Scope C resolved — see STAB-08 Scope C CLOSED entry; STAB-07-P0 redispatch unblocked.

---

## STAB-08 — My Page Action State + Escalation Recovery (SCOPE EXPANDED)

[stab_08 OPEN 2026-05-22, scope expanded; LAUNCH-BLOCKING for STAB-07-P0 re-attempt] → Scope C CLOSED 2026-05-22 (see top); Scope A/B remain backlog.

Scope A — My Page blind spot for escalated/submitted contracts (surfaced by STAB-07-P0 smoke triage):
  `openActionContractForMyPage` queries `pending` / `approved`+awaiting_qr / `in(approved,completed,missed)` only; escalated/submitted match none → My Page falls back to history and surfaces a stale completed banner + that contract's XP, masking the current state.

Scope B — Escalation recovery cron not scheduled (surfaced by retry-prep):
  `action-contract-escalation-expire/route.ts` resets escalated→pending after `expires_at`, but `wrangler.toml` has no `[triggers]`/crons → not auto-invoked. (Also: the open `bty_action_contract_escalations` row is never closed on self-resolve — no UPDATE in submit-validation/qr-validate/lifecycle.)

Scope C — Escalated contract revise UI missing (surfaced by this rollback; VERIFIED):
  `ArenaResolveClient.tsx:163-191` renders the form only for `runtime_state="ACTION_REQUIRED"`; escalated→`"ACTION_SUBMITTED"`→`ArenaPendingContractGate`, and `qr_allowed=false` for escalated → no form, no QR. Server-side resubmit IS allowed (`submit-validation/route.ts:189-200`) but the client provides no surface to invoke it.

STAB-08 is now a launch-blocking dependency for the STAB-07-P0 re-attempt.

---

## C3 INVENTORY MISS (post-mortem note)

The STAB-06-FIX-03 traces and the STAB-07-P0 traces verified the SERVER-SIDE resubmit allowance for escalated (`submit-validation/route.ts:189-200`) but did NOT inspect `ArenaResolveClient`'s render branches for `runtime_state="ACTION_SUBMITTED"` + `status="escalated"`. Server acceptance and UI exposure are independent properties.

Discipline reinforcement: when verifying a recovery path is "code-allowed," also verify the user-facing UI provides a surface to invoke it. A path the server accepts but the client never renders is functionally a dead-end.

---

## STAB-06-FIX-03 — Self-Attest Completion UX Certified

**Status:** CLOSED
**Date:** 2026-05-21
**Header:** [stab_06_fix_03 CLOSED 2026-05-21]

STAB-06-FIX-03 restored honest self-attest completion flow: the system now shows completion explicitly, suppresses stale QR gates, and resumes progression only through a user-visible Next Scenario CTA.

STAB-06 also surfaced a higher-order verification-mode architecture gap: all current action contracts are hardcoded as self_attest with auto-approve seeded true. This is not closed in STAB-06 and is promoted to STAB-07.

Inner: `4ae97ea8` (Track B) on top of `ae76092b` (Track A), pushed to origin/inner-main.
Worker: Version `4bf3ba18-89e5-4e6a-b34c-934ba963943f`, staging URL bty-arena-staging.ywamer2022.workers.dev.
Track A scope: U4 (verified_at + validation_approved_at wire propagation through BlockingArenaContractRow + select lists + ArenaPendingContractPayload + parsePendingContract) + U5 (qr_allowed terminal-state gating: gatesForBlockedContract now row-arg, qrAllowedForContract helper enforces pending OR submitted/approved+awaiting-QR-only) + U6 (action-loop-token 409 enrichment: contract_state discriminator + verified_at).
Track B scope: U1 (submit-validation response contract_state: terminal | awaiting_qr) + U2 (form onApproved info-arg + ArenaResolveClient hook-owned actionTerminalCompletion + redirect-OUT effect gating) + U3 (new ArenaActionCompleted component + 3 i18n keys en/ko: arenaActionCompletedTitle/Lead/NextCta) + U7 (FIX-02 auto-retryArenaSession removed; hook-exported clearPendingContractAndReload as user-CTA trigger; single source of truth = hook).

BTY principle restored: "I acted → recognized → I choose to continue" flow on self_attest completion.
Smoke: 3-scenario hanbitchi browser run PASS — all 3 returned contract_state:"terminal", completion screen rendered, no blank QR window, explicit Next CTA observed.
Baseline: vitest 3307/0/6 (3303 baseline + 4 new ArenaActionCompleted tests). lint PASS (tsc --noEmit clean).

Doctrine carryforward: hook-owned single source of truth for completion state (prevents stale-surface duplication); redirect-OUT effects must gate on terminal completion to prevent premature snapshot transitions; defense-in-depth (server qr_allowed conditionalization + client completion-screen routing) shipped together via Track split.

---

## STAB-07-P0 — Verification Mode Integrity: Hardcoded Subset Bridge

**Status:** OPEN (BACKLOG → ACTIVE)
**Date:** 2026-05-21
**Header:** [stab_07_p0 OPEN 2026-05-21]

Surfaced by STAB-06-FIX-03 closure smoke (3 scenarios, all returned contract_state:"terminal"). STAB-07-P0 pre-audit (read-only) confirmed VERDICT: ABSENT.

Three contract-creation paths hardcode verification_type:"self_attest" + details.self_report_auto_approve:true unconditionally:
- ensureActionContract.ts:280 (run-completion path)
- eliteBindingActionCommitment.server.ts:201 (Elite AD1-commit path)
- action-contracts/route.ts:64 (json_dev_runtime path)

No solo/relational classification in scenario metadata. No DB CHECK on verification_type (text NOT NULL only).
Existing verification-method fragments (UI type "qr"|"link"|"hybrid"|"self_report", DB verification_mode CHECK) classify HOW to verify, not WHETHER action is solo or relational.

Commander direction:
1. Classify canonical scenarios solo/self_attest vs relational/QR-witness (Commander content judgment).
2. Apply temporary hardcoded scenario-ID classification at the 3 paths (minimal code patch).
3. Defer architectural scenario-metadata/schema work to post-launch STAB-07 phases.

Anchors: prior STEP 0/0.5/1 EXIT REPORTs (in conversation log) document mechanism + gap details.

---

## STAB-06-P0D — Outer Leak-Integration: scenario-selector

**Status:** CLOSED / OUTER MIRROR RECONCILED
**Date:** 2026-05-21
**Header:** [stab_06_p0d outer leak-integration 2026-05-21]

scenario-selector.service.ts brought into outer mirror. Inner blob 2ab5779a now reflected in outer. Selector seed update `${userId}:${UTC date}:${served.length}` certified live both repos.

**Technical anchors:**
- Inner closure commits: `88c15287` (STAB-06-P0D-A: djb2Hash + tieBreakSeed `${userId}:${UTC date}`), `b2d3f08a` (STAB-06-P0D-A2: seed += `:${served.length}`, GET rotation).
- Worker live: `110fa9aa` → `db0dbfa1` (P0D-A2 deploy, smoke 3x PASS at prior closure).
- Outer leak duration: bridge between inner P0D closure and STAB-06-FIX-03 outer closure phase.
- Outer integration: outer `bty-app/` prefix sync, blob `6af2eb6d` → `2ab5779a` (this commit).

---

## STAB-05-P0[ABCD] — Runtime Completion Integrity Audit: Inventory Certified

**Status:** CLOSED / INVENTORY CERTIFIED
**Date:** 2026-05-21 (D-9 from 2026-05-30 launch)
**Anchor:** inner c3f933c6 / outer post-STAB-05 closure commit (see git log on main) / staging Version a27781f5-e709-4660-bd07-1d11a72d60d7 (canonical anchor preserved)
**Baseline:** 3303 passed / 0 failed / 6 skipped (unchanged — no code mutation occurred)

### Doctrine

STAB-05 did not identify a single root cause by design.

It completed relational topology inventory and preserved all hypotheses without selection.

No mutation, no code inspection, no fix proposal.

### Archive Summary (top-of-mind, 5 findings)

1. arena_runs completion topology is not table-local only.
2. status/completion_state divergence exists and repeats as a dual-field pattern (also present in arena_pending_outcomes status × consumed_at).
3. User lineage is material: orphan / test / synthetic / admin-pattern users are present in the data.
4. arena_events is a real progression surface with monotonic event/run gradient (IN_PROGRESS 1.78 → ABANDONED 3.00 → DONE 4.71).
5. arena_pending_outcomes is a separate lifecycle surface, not joined to arena_events via source_event_id.

### Composite Topology Shape (Commander-locked finding)

> Runtime completion integrity issue is composite:
> table-local + user-lineage-local + event-surface distributed.

These three shapes are not mutually exclusive. The data supports surfaces of all three. No single shape explains all observations.

### Phase Sequence

- **P0A — DB Status Census:** 5 hypotheses enumerated (H1-H5). 828 arena_runs / 3 status values (IN_PROGRESS 492, DONE 287, ABANDONED 49).
- **P0B — Extended DB Census:** dual-field divergence localized (83 rows DONE×in_progress + 1 row inverse), pipeline all legacy (828/828), hanbitchi 9 temporal clusters identified. D1-D4 added.
- **P0C — Final DB Census:** orphan user `85bd8f1f-...` (49 ABANDONED + 31 divergence rows, identity-table absent). 23 arena-related tables inventoried. arena_events 2.87x event-per-run ratio. D5-D8 added.
- **P0D — Orphan + Event Surface Census:** 11 distinct user_ids total in 828 runs; 3 synthetic UUIDs (b0000000-...-e201/202/203); FK on arena_runs.user_id confirmed; arena_events events/run monotonic gradient; arena_pending_outcomes lifecycle surface with self-divergence (status × consumed_at). D9-D12 added. H6, H7 emerged.

### Hypotheses Preserved (No Selection)

- H1: abandonment without ABANDONED status (narrowed to single orphan user / 21-day window)
- H2: parallel run permission (mechanism uninspected)
- H3a: pipeline column divergence in arena_runs (narrowed: all production rows legacy)
- H3b: parallel runtime materialization in arena_events (narrowed: surface monotonic with progression)
- H4: multi-field status divergence (narrowed: pattern is table-systemic, repeats across arena_runs + arena_pending_outcomes)
- H5: test/dev/synthetic accumulation (narrowed: 4+ test-pattern users in cohort)
- H6: outcome-driven divergence (alive with connection topology re-questioned)
- H7: reinforcement loop chain divergence (alive from schema inventory)

### Distinction Surfaces Accumulated

D1-D12 across 4 phases. Full enumeration in conversation history. Surfaces materially narrowed without selection.

### Structural Findings (S1-S11)

Including: FK constraint presence, CHECK constraints on completion_state and pipeline, RUN_COMPLETED_APPLIED count (239) ≠ DONE count (287), arena_pending_outcomes not connected to arena_events via source_event_id, public.users contains password column (security inventory observation).

### Decision Rationale (D-9 closure framing)

STAB-05 is not a "fixed" phase — it is a "topology map established" phase. Further inspection (code paths, root cause selection, fix proposal) deferred. Closure at this depth was Commander decision:

- Sufficient signal for next backlog item (dashboard dual-surface branching) without further STAB-05 depth
- Deeper inspection would shift from inventory to fix temptation
- D-9 launch posture requires observation-grade artifacts, not implementation-grade fixes for non-blocker findings

### Stabilization Chain (narrative continuation)

- STAB-01-P1: SELF_REPORT_AUTO_APPROVE 4-AND gate
- STAB-02-P1: core_xp_ledger ARENA insert
- STAB-03-A-P1: snapshot column-mapping correction
- STAB-04-P0: rollback survivability certification (PARTIALLY CERTIFIED, governance success)
- **STAB-05-P0[ABCD]: runtime completion topology audit (CLOSED, INVENTORY CERTIFIED, no code touched)**

D-9 chain extended. Launch posture: strong + topology-mapped.

### Operational Lessons (ledger archive only, memory deferred per Commander)

1. Inventory-first doctrine prevents premature causality lock — 4-phase audit preserved 7 alive hypotheses without scoring.
2. 4-tier response structure (Observed / Possible / Distinguishing surface / Not yet inspected) maintained across 32+ queries; constraint not violated.
3. SQL provisional queries based on assumed column names must NOT execute before schema discovery — multiple Wave 1 errors in P0D from this pattern.
4. Composite topology shapes are valid findings; "single root cause" is not a required output.
5. DB census without auth schema reads (PII-restricted) successfully produced identity inventory via `profiles`, `bty_profiles`, `arena_profiles`, `users` (public schema).
6. arena_pending_outcomes is structurally a separate lifecycle table from arena_events — bridge hypothesis (source_event_id) does NOT join in current data.

### Memory consolidation status: DEFERRED

Per Commander 2026-05-21 directive: context entropy management priority over memory accumulation. STAB-05 findings preserved in this ledger entry only.

Candidate for future memory promotion (currently deferred): "Runtime completion topology is composite, not single-table."

---

## STAB-04-P0 — Semantic Rollback Integrity Certification

**Status:** PARTIALLY CERTIFIED — governance success (not incomplete execution)
**Date:** 2026-05-21 (D-9 from 2026-05-30 launch)
**Anchor:** inner c3f933c6 / outer post-STAB-04-P0 closure commit (see git log on main) / staging Version a27781f5-e709-4660-bd07-1d11a72d60d7 (canonical anchor restored)
**Baseline:** 3303 passed / 0 failed / 6 skipped (unchanged across dispatch)

### Doctrine

STAB-04 proved that rollback boundaries preserve semantic runtime continuity across worker deployments. This is the foundation of launch-and-beyond incident response doctrine.

Canonical binding integrity takes precedence over smoke throughput.

Post-closure operational anchor: `a27781f5-e709-4660-bd07-1d11a72d60d7` = canonical rollback-safe stabilization anchor.

### R1 — Worker Rollback Round Trip: CERTIFIED

- pre_version_id: `a27781f5-e709-4660-bd07-1d11a72d60d7`
- rollback_target: `8ed84aaa-3577-4587-8ebb-458cc416e63d` (STAB-02-P1)
- restore_target: `a27781f5-e709-4660-bd07-1d11a72d60d7` (canonical anchor)
- T_pure_rollback_roundtrip: < 1 minute (operational feasibility verified)

**Semantic continuity evidence:**
- Smoke A at 8ed84aaa: verification label `"hybrid"` (legacy `verification_mode` column)
- Smoke B at a27781f5: verification label `"self_attest"` (canonical `verification_type` column)
- Label diff `"hybrid"` → `"self_attest"` proves legacy/canonical mapping paths both alive at runtime; STAB-03-A-P1 binding integrity preserved across rollback boundary.

**Gates:**
- R1-G1 smoke completion both states: PASS
- R1-G2 XP increment both states (STAB-02-P1 boundary survival): PASS
- R1-G3 label diff legacy→canonical: PASS (verbatim evidence above)
- R1-G4 operational feasibility <5min: PASS
- R1-G5 inner repo baseline re-confirmation 3303/0/6: PASS

### R2 — DB Constraint Round Trip: EXECUTION DEFERRED

DB constraint round-trip execution deferred due to shared production substrate topology.

**Discovery (mid-dispatch):** Supabase project topology is single-project (`dentistchi's Project`, AWS us-east-1, NANO tier). No staging/production DB separation exists. `core_xp_ledger_user_source_uq` DROP/CREATE operates on production substrate.

**Governance decision:** STAB-04 frozen mutation rule ("No schema expansion during STAB-04. Only rollback integrity validation allowed.") plus the role of `core_xp_ledger` as XP identity continuity substrate makes single-project DROP/CREATE rehearsal a governance violation. Defer to post-launch hardening when staging DB is provisioned.

**Procedure (documented for future execution):**
- R2.1 BEFORE snapshot: arena_count, source_type distribution, duplicate count, indexdef verbatim
- R2.2 `DROP INDEX IF EXISTS public.core_xp_ledger_user_source_uq`
- R2.3 mid-state verification (index absent in pg_indexes)
- R2.4 `CREATE UNIQUE INDEX IF NOT EXISTS core_xp_ledger_user_source_uq ON public.core_xp_ledger (user_id, source_type, source_id) WHERE source_id IS NOT NULL`
- R2.5 AFTER snapshot (re-run Q1-Q4 verbatim)
- Gates: arena_count delta 0, source_type distribution row-identical, duplicate_count after=0, indexdef predicate `WHERE source_id IS NOT NULL` preserved

### R3 — Combined Disaster: PROCEDURE DOCUMENTED, NOT EXECUTED

Scenario: Worker = latest, DB = pre-STAB-02-P1 state.

Procedure:
1. Detection: `23505` violation on ARENA insert OR no ARENA rows landing in core_xp_ledger.
2. Decision tree: DB drift → re-apply STAB-02-P1 migration then verify worker; Worker drift → rollback worker then verify DB constraint compatibility.
3. Recovery anchor: `a27781f5` + STAB-02-P1 migration applied = canonical pair.
4. Never destroy data to "match" a rolled-back code state. Forward-fix only.

Filed to post-launch hardening backlog alongside R2.

### Canonical Anchor Restored

staging Version after dispatch: `a27781f5-e709-4660-bd07-1d11a72d60d7`

### Operational Lessons (memory consolidation candidates)

1. wrangler rollback requires full UUID; short ID rejected by Cloudflare API.
2. `wrangler deployments list` outputs oldest-first; use `tail` or `jq` for re-runnable scripts.
3. classifier shared-staging block is governance, not failure; Commander manual surface is designed escape valve.
4. **Worker staging ≠ DB staging.** Supabase project topology is single-project; all DB-touching dispatches must treat Supabase mutations as production-effective regardless of Worker environment target.
5. R1-G5 vitest gate is "inner repo baseline re-confirmation," not "post-rollback staging code verification" — dispatch wording must distinguish.
6. Dispatch shell blocks for Commander paste must omit zsh-incompatible comments (`#`).
7. Claude Code context after classifier veto does not include subsequent Commander manual surface actions; C3 must inject "previous turn progress summary" in follow-up dispatches.
8. Smoke verification step instruction should prioritize UI label observation over DevTools when label is visibly rendered.
9. Ledger closure dispatch must inspect actual ledger files first to learn existing entry conventions; dispatch formatting is subordinate to ledger continuity.

### Stabilization Chain (narrative completion)

- STAB-01-P1: SELF_REPORT_AUTO_APPROVE 4-AND gate (self_attest stabilization)
- STAB-02-P1: core_xp_ledger ARENA insert (XP boundary)
- STAB-03-A-P1: snapshot column-mapping correction (canonical mapping)
- **STAB-04-P0: rollback survivability certification (this entry)**

D-9 chain complete. Launch posture: strong.

---

**[x] STAB-03-A-P1 snapshot column-mapping correction landed (2026-05-21):** [SNAPSHOT+UI-DISPLAY+DEPLOY] STAB-03-P0 inventory verdict (iii) — UI-render-bug, not routing. DB has `verification_type='self_attest'` for all demo contracts since 2026-05-19 `c727284a` (MVP-FIX-ACTION-DEMO-03 A' series); UI label "hybrid" came from snapshot/payload fields named `verification_type` that read the LEGACY column `verification_mode` (CHECK workaround, always `'hybrid'`). Engine correctness unaffected — fix = rename source column at snapshot mapping layer. **Mutations** (MUT-1/2/3/4): (1) `src/lib/bty/arena/blockingArenaActionContract.ts` — `BlockingArenaContractRow` type gains `verification_type: string` (line 9); both SELECT clauses (`fetchBlockingArenaContractForSession` line 30, `fetchBlockingContractRowByContractId` line 62) include `verification_type`. (2) `src/lib/bty/arena/arenaRuntimeSnapshot.server.ts:61` `actionContractSnapshotFromBlocking` sources `verification_type: row.verification_type` (was `row.verification_mode`) + 5-line STAB-03-A-P1 rationale comment. (3) `src/lib/bty/arena/arenaSessionNextCore.ts:66` 409 body composer sources `verification_type: blocking.verification_type` + same comment. (4) `src/app/api/arena/session/next/route.test.ts:122,143` fixture adds `verification_type: "self_attest"` stub; assertion updated to expect `pending.verification_type`. **D3 preserved:** no new UI, no new selector, no schema changes, no migration, no new runtime states, no new verification_type values. **D2 preserved:** arena gate path only — `openActionContractForMyPage.ts` parallel surface UNTOUCHED (STAB-03-A.2 if needed post-rehearsal). **Baseline:** 3303 → 3303 passed / 0 failed / 6 skipped (±0). `tsc --noEmit` clean. **PMG-3 staging DB scan** (Commander 2026-05-21 SQL paste): all 5 post-`c727284a` contracts have `verification_type='self_attest'`; 5/19 hybrid trio (`d92a91fa`/`fc379c38`/`c814ba6f`) + `2b89c045` are pre-demo-fix historical rows. No post-fix hybrid rows; A2 informational only, no backfill required. **Staging deploy:** `bty-arena-staging` Version **`a27781f5-e709-4660-bd07-1d11a72d60d7`**. /api/version → `{"app":"bty-arena","env":"staging","worker":"bty-arena-staging"}` confirms runtime alive on the new Version. **VG-5 rehearsal PASS** (Commander hanbitchi uid `52e543cc-fa69-44e1-a33c-0389f5d52bff`, 2026-05-21): (a) "Verification: self_attest" label confirmed via image capture; (b) contract `a44af95c` `status='approved'` with all 3 timestamps NOT NULL, `escalated_at=null`; (c) `core_xp_ledger` row id=8 `delta_xp=10` `source_type='ARENA'` `source_id` matches run_id `87ab2b4d`; (d) subsequent `/api/arena/n/session` returns 200 with new scenario (not 409). STAB-01-P1 + STAB-02-P1 + STAB-03-A-P1 all working in single user cycle. **VG-6 DB cross-check PASS** (Commander 2026-05-21): rehearsal contract written with `verification_type='self_attest'` + `verification_mode='hybrid'` confirming canonical column population intact. **Commits:** inner `c3f933c6`, outer leak-integration `2c53cf7`, outer ledger this commit. **D4 reclassification:** refresh-bypass behavior (binding-snapshot vs session-router status-filter asymmetry for approved rows) reclassified as expected progression behavior per Commander 2026-05-21; STAB-03-B deferred unless post-α rehearsal shows user confusion sufficient to threaten deterministic demo flow. **Rollback:** `wrangler rollback 8ed84aaa-3577-4587-8ebb-458cc416e63d` (prior = STAB-02-P1) + `git revert c3f933c6` (inner) + `git revert 2c53cf7` (outer leak-integration) + this ledger revert. No DB changes to roll back. **Next:** STAB-03-A.2 (My Page parallel surface) if needed; STAB-03-B (binding-snapshot approved arm) if D4 reverses.

---

**[x] STAB-02-P1 core_xp_ledger ARENA insert landed (2026-05-21):** [API+DEPLOY+MIGRATION] STAB-01-P1 VG-5 PARTIAL PASS surfaced pre-existing XP-sync defect — auto-approve path now reliably reaches verified-completion, but `applyArenaRunRewardsOnVerifiedCompletion` updated `arena_profiles.core_xp_total` (via `applyDirectCoreXp`) without writing `core_xp_ledger`, leaving `getCoreXPBreakdown` to compute 0% ARENA contribution despite `mapSourceTypeToBucket` (core-xp-history.service.ts:44–60) being designed to receive ARENA / RUN / QUEST / CARRYOVER / SEASON source rows. **Fix** at `src/lib/bty/arena/reflectionRewards.server.ts` L134+ — caller-side ledger insert immediately after `applyDirectCoreXp` returns success; `applyDirectCoreXp`/`applyCoreXp.ts`/`awardLabXP`/`lab-xp.service.ts` UNTOUCHED (D1/D2/G1 — Lab path retains its own ledger row at lab-xp.service.ts:77). Insert payload: `{user_id, delta_xp: arenaCoreXp, source_type: 'ARENA' (D1 literal), source_id: run.run_id (D4 — one run = one ledger event)}`. Insert gated by `arenaCoreXp > 0` (MUT-2.2). Non-23505 errors logged but non-fatal — `arena_profiles.core_xp_total` is canonical, ledger is audit-only (MUT-2.1). **Migration** `bty-app/supabase/migrations/20260521000000_core_xp_ledger_uniq_user_source.sql` — partial unique index `core_xp_ledger_user_source_uq` on `(user_id, source_type, source_id) WHERE source_id IS NOT NULL`. PMG-3 staging DB scan returned 0 existing rows; no pre-constraint cleanup needed. **Baseline:** 3299 → 3303 passed / 0 failed / 6 skipped (+4 cases: positive insert, zero-gain skip, 23505 idempotency benign, non-23505 non-fatal warn). `tsc --noEmit` clean. **Staging deploy:** `bty-arena-staging` Version **`8ed84aaa-3577-4587-8ebb-458cc416e63d`**. **VG-2 migration apply — deferred to Commander Studio** (DDL on shared staging blocked by auto-mode classifier per `discipline_classifier_shared_staging_block`; app code is safe without the constraint, idempotency merely weaker until applied). **VG-6 demo-lifecycle smoke — deferred to Commander** (state-changing curl on shared staging same classifier discipline, identical pattern to STAB-01-P1 VG-5). **Commits:** inner `b4a0f4ea`, outer leak-integration `babf028`, outer ledger this commit. **D3 preserved:** arena_profiles.weekly_xp, arena_profiles.lifetime_xp, arena_runs.total_xp paths untouched. **D4 preserved:** weekly-reset cron stays dormant. **Next:** Commander applies migration via Supabase Studio → runs VG-6 smoke → pushes origin inner-main + origin main.

---

**[x] STAB-01-P1 self-report auto-approve 4-AND gate landed (2026-05-20):** [API+DEPLOY] submit-validation route gate extended from 2-AND (`verification_type='self_attest' AND details.self_report_auto_approve=true`) to 4-AND by AND-ing two worker-level env conditions (`process.env.SELF_REPORT_AUTO_APPROVE='true' AND process.env.BTY_ENV='staging'` after `.trim().toLowerCase()`). Demo lifecycle now demo-safe on staging without Layer 2 non-determinism; production worker (BTY_ENV='production') and any other env never auto-approve. `wrangler.toml` `[vars]` gains `SELF_REPORT_AUTO_APPROVE = "true"`; the file already binds staging-only via `name = "bty-arena-staging"` at L1 — Commander 2026-05-20 A2 option α (D5): no `[env.X]` restructure, code-level BTY_ENV AND-term provides defense-in-depth. **D3 preserved:** Layer 2 architecture untouched, Action contract creation untouched, QR/action lifecycle untouched. **D4 preserved:** blocking-fetch (MVP-FIX-ACTION-DEMO-01 B-1) unchanged. **Baseline:** 3296 → 3299 passed / 0 failed / 6 skipped (+3 negative cases: env-unset, BTY_ENV=production, verification_type=external_witness; existing positive case extended in-place). `tsc --noEmit` clean. **Staging deploy:** `bty-arena-staging` Version **`84ba771b-fb74-458e-ba54-7f2b94043245`**, binding manifest confirms `env.SELF_REPORT_AUTO_APPROVE ("true")`. **Verification:** VG-1/2/3/4 PASS. **VG-5 demo-lifecycle smoke — classifier-deferred** (auto-mode blocks state-changing curl on shared staging per `discipline_classifier_shared_staging_block`; Commander runs the smoke interactively or grants per-call permission). **VG-6 production leak check — DEFERRED** (production worker config external to this repo's wrangler.toml; deploy went only to `bty-arena-staging`; `bty-arena-prod.ywamer2022.workers.dev` and `bty-arena.ywamer2022.workers.dev` both return 404 — no public production endpoint reachable for read-only diff). **Commits:** inner `72a38bf2`, outer leak-integration `0b12c9b`, outer ledger this commit. **Next:** Commander VG-5 smoke; then push origin/main + origin/inner-main.

---

**[x] Stabilization + multi-user verification sprint CLOSED — 5/30 MVP no blockers (2026-05-18):** Pre-launch stabilization and verification pass; operating-doc sync (this entry) is the first of three remaining 5/30 items. **Completed work (5):** (1) **Migration reconciliation** — the `db push` workflow settled; **15 applied / 17 hold**, established as the migration baseline. (2) **cron `DEPLOY_URL` fix** — corrected the cron deploy target URL. (3) **D-12 compression test PASS** — Forced Reset activation confirmed live; the TII output matched the code prediction to 4 decimal places. (4) **`weekly_xp` lost-write hotfix** — the 4 XP write paths converted to an atomic increment RPC, closing the read-modify-write lost-write window; deployed **Version `60865c9d`**, origin push complete; baseline inner `16ce6b81` / outer `2d4f92e`. (5) **Multi-user verification closed** — RLS `anon` blocking empirically demonstrated; concurrency closed at the code level; recall banner verdict reached. **Recall banner verdict:** mechanism = **PASS** (`recallPrompt` generation proven live, banner JSX correct); live render is **inactive** — Arena is the intended canonical Elite-only vertical slice, so no non-elite scenario passes the `!eliteSetup` gate. Not a bug; not a launch blocker. non-elite wiring is a post-launch track. **Post-launch debt (explicitly post-launch — distinct from launch-safe):** non-elite loader fragility · migration cleanup 5 items (scenarios · Memory Engine · `action_contracts` · escalations RLS · SSO DDL) · localhost URL baked-in · Core XP RMW · cross-user RLS empirical demonstration · recall lost-prompt pattern. **MVP status:** 5/30 launch blocker = **none**. Remaining: (a) operating-doc sync (= this entry) · (b) 20 account creation + core-loop smoke · (c) launch prep + production smoke. **Commit:** this ledger commit — outer single commit; the `bty-app/docs/CURSOR_TASK_BOARD.md` symlink blob is unchanged, so no inner commit. **Next:** 20-account creation + core-loop smoke.

---

**[x] Stage 2 UI sequence lane — STEP 0 corroboration CLOSED + lane split PUBLISHED (2026-05-17):** Standalone entry — realizes the forward `Next:` note in the UI Authority Clarification Lane entry below ("Stage 2 UI sequence … under the contract"). Deferred Queue #1–#6 and the closed Stage 2 series (2026-05-14) are untouched. **STEP 0** = read-only corroboration (mutation 0, working tree untouched); baseline 6/6 matched, ABORT 0, fabrication 0. **Lane split** — the single lane is split into two sub-lanes. **(1) UI-surface sub-lane — status OPEN**, immediately dispatchable; prerequisite document = [`docs/BTY_ARENA_UI_AUTHORITY_CONTRACT.md`](BTY_ARENA_UI_AUTHORITY_CONTRACT.md) v1 (already frozen); scope = render work against the frozen contract (§3 + §5.1); allowed-path candidates `src/components/bty-arena/**`, `globals.css`, `tailwind.config.ts`, surface JSX — runtime spine excluded; runtime spine non-contact. **(2) runtime-semantics sub-lane — status BLOCKED**, prerequisite governance decision unresolved; scope = FD-6 Resolve navigation effect + binding-layer authority resolution; prerequisites = (a) whether Binding Layer Spec v3 is promoted to NORMATIVE or a replacement source-of-truth is designated, (b) whether the §5.1 frozen-invariant amendment authority is opened — both separate Commander decisions. **STEP 0 classification (signal→state)**: **FD-6 Action Gate = MIXED** — render + runtime-state-driven navigation; the coupling is consumption (reads the server-authoritative snapshot), not authoring (no transition-route call, no snapshot mutation); evidence `ArenaResolveClient.tsx:40,44-50,55-68`. **Binding Layer Spec v3 = DRAFT_SIGNAL** — the file exists (`docs/BTY JSON ↔ Supabase Binding Layer Spec v3.md`, blob `879da028`, unchanged since `fa0b86d` initial commit), carries no NORMATIVE marker, and is absent from the authoritative-docs list — **not usable as a dispatch premise**; separately, binding-validation runtime code (`buildArenaBindingSnapshotResponse.server.ts`) exists independently of the spec, so the runtime spine is real. **ArenaBlockedSurface precedence = FROZEN_INVARIANT** — `BTY_ARENA_UI_AUTHORITY_CONTRACT.md` §3 + §5.1; enforcement code `useArenaSession.ts:1054-1067` + `arenaBindingReducer.ts:54-60`. **Commit**: this ledger commit — ledger-only, code/runtime/UI mutation 0. **Next**: UI-surface sub-lane dispatch (immediately available); runtime-semantics sub-lane blocked pending the two governance decisions.

---

**[x] UI Authority Clarification Lane — contract v1 PUBLISHED (2026-05-17):** New lane — **not** a carry-forward item; the carry-forward queue 3/3 closure is untouched. Runtime authority preservation lane with UI clarification scope (UI = subject, deterministic runtime authority = protected target). **STEP 0** (read-only corroboration) + **STEP 0.1** (read-only behavioral characterization of the R7 6-site surface) + **STEP 1** (documentation-level freeze) + **STEP 2** (this ledger link). **R7 6-site classification**: **class A = 0, class F = 0** — no client-side true runtime-authority violation; distribution B×3 (#1/#2/#4 bounded optimistic UI) / C×3 (#3/#5/#6 render-only / non-contacting derivation). The entry-shell precedence invariant — server `ACTION_*` / `FORCED_RESET_PENDING` / `REEXPOSURE_DUE` / `NEXT_SCENARIO_READY` deterministically override client binding/optimistic snapshots — is the measured existing behavior, **frozen, not changed**. **Deliverable**: [`docs/BTY_ARENA_UI_AUTHORITY_CONTRACT.md`](BTY_ARENA_UI_AUTHORITY_CONTRACT.md) v1 (6 sections: Authority Map / Surface Taxonomy / Precedence Matrix / Queue Ownership Model / Rendering Contract / Out-of-scope), outer `80e7eb2`. **This contract is a prerequisite for the next UI / mockup / design-token work** (Stage 2 UI sequence: Resolve → Play → Center → Foundry → Hub). **Code mutation 0** — documentation-level freeze; `tsc` clean; vitest 1 pre-existing unrelated red maintained (no variation). **Two unresolved branches recorded, NOT closed in this lane**: (1) **Foundry self-authorization gap** — "Foundry cannot self-authorize progression" is NOT IMPLEMENTED (`program-progress/route.ts:74-78`); contract §3 records the gap → separate runtime-behavior lane. (2) **Identity Signal Authority Review** — R7 #6 client-computed trait weight is persisted via `/api/bty/arena/signals` without server recomputation; contract §6 → separate review lane. **Commit**: outer `80e7eb2` (contract) + this ledger commit. **Next**: Stage 2 UI sequence step 2 (Resolve) under the contract; Foundry-gap lane + Identity-Signal-Review lane uninitiated.

---

**[x] §7.4 dormant-(b)-ingress governance handling CLOSED + PUBLISHED (2026-05-17) — carry-forward lane 3/3, FINAL:** Closes the sole remaining carry-forward lane (Deferred Queue #6 residue). Commander disposition **C = A + B-1b** (same-lane immediate closure). **STEP 0** corroboration (read-only, mutation 0): re-confirmed the §7.2(b) "dormant" measurement — `air_below_threshold` registered in `VALID_CONTEXTS` + the domain `StageTransitionContext` union, `getNextStage` STAGE_3→4 mapping present, `POST /transition` route ingress present, `AIR_THRESHOLD_STAGE_ESCALATION = 0.50` defined with no runtime comparison site; **prod caller 0** (only `.test.ts` files supply the context as a transition argument), while the authenticated `POST /transition` path is nonetheless client-reachable. **B-1b** (route-level containment): `transition/route.ts` gains an explicit reject branch — `context === "air_below_threshold"` → deterministic `403 CONTEXT_DORMANT_INGRESS_CLOSED`. `air_below_threshold` is **deliberately retained** in `VALID_CONTEXTS` and the domain `StageTransitionContext` union (allowlist NOT pruned — B-1a rejected; the domain↔route asymmetry is made explicit in code); `getNextStage` and `air.ts` constants **untouched** → escalation runtime-numeric impact **0**, no AIR comparison site added. **A**: `RESULT_ORIGIN_CLOSURE_SPEC.md` §7.6 containment record appended. **Not a §7.4 governance-triggering mutation** — it closes the route to the ingress, does not activate it. **B-2** (an AIR comparison gate that would give `0.50` a runtime comparison site) is **explicitly out of scope** — separate governance review required, lane not initiated. **Verification**: §7.4-scoped verification green — `transition/route.test.ts` 6/6 (incl. the new `403` reject case), `stages.test.ts` 11/11, `state-service.test.ts` 15/15, `tsc --noEmit` clean. **Baseline language**: Full fresh-tree baseline is not green. Clean tree `71f01839` reproduces 1 unrelated pre-existing red in `re-exposure/validate` caused by `reinforcementLoopSchedule.server` mock missing `reinforcementCapReached` export. This predates the §7.4 B-1b mutation and is deferred to a separate baseline-maintenance lane. §7.4 scoped verification is green. **Commit**: outer `f202782` (route co-track + spec §7.6) / inner `35cba550` (logically identical route co-track pair) + this ledger commit. **⚠️ Carry-forward queue now 3/3 CLOSED + PUBLISHED** — §5.3 representation-collapse, WS-1 24-row relabel, and §7.4 dormant-(b)-ingress all closed; no open carry-forward lane remains. inner baseline `35cba550`, upstream unset (manual-push-only, §8) — not pushed; inner content published via the outer `bty-app/` co-track. **Next**: separate baseline-maintenance lane for the `reinforcementLoopSchedule.server` mock (Commander-designated); B-2 governance lane remains uninitiated.

---

**[x] §5.3 representation-collapse remediation CLOSED + LEDGERED (2026-05-17) — carry-forward lane 2:** Fixes the `RESULT_ORIGIN_CLOSURE_SPEC.md` §5.3 representation defect — `le_activation_log` had no `result_origin` column, so the AIR graph processed every `insufficient_signal` fallback activation as one identical penalty footprint. **Remediation A** (Commander-selected): add the column, tag at insert time. **STEP structure**: STEP 0 corroboration (5/6 handoff anchors verified; the nonexistent "§3 S1–S4 verification design" anchor rescinded) → PHASE 1–5 mutation dispatch. **Changes** (3 files, co-tracked): new migration `20260517000000_le_activation_log_result_origin.sql` — `add column if not exists result_origin text` (nullable, CHECK `computed|insufficient_signal`) + partial index on fallback rows; **additive / insert-time only — no UPDATE backfill** (pre-§5.3 rows keep NULL); `reflectionRewards.server.ts` writes `result_origin` into the activation insert (origin preserved, fallback now distinguishable from a genuine micro_win); `reflectionRewards.server.test.ts` +2 assertions. **AIR/escalation impact: numeric 0** — `computeAIR` unchanged, `weightedChosen`/`weightedCompleted` unchanged, escalation thresholds (`0.80`/`0.50`) unchanged, no AIR carve-out; pure S-A representation fix (crosses §8.4, impact-reviewed). FINGERPRINT_VERSION untouched; §7.4 `air_below_threshold` ingress untouched. Tests 40/40 green; `tsc --noEmit` clean. **Commit**: outer `0b82187` / inner `71f01839` (logically identical co-track pair; blob 3/3 MATCH) + this ledger commit. **Next**: §7.4 dormant-(b)-ingress governance handling — sole remaining carry-forward lane.

---

**[x] Queue #3 (runtime label terminology) CLOSED (2026-05-17):** Runtime label terminology judged and articulated as the new normative spec [`docs/RUNTIME_LABEL_TERMINOLOGY_SPEC.md`](RUNTIME_LABEL_TERMINOLOGY_SPEC.md). **STEP 0 (Layer A)**: server `ArenaRuntimeStateId` (9 labels, all per-request derived; `runtime_state` not a DB column) + client `RuntimeFlowState` (6 labels, in-memory FSM); 4 identical-string labels across two unshared types; "state" naming drift on the type/field surface. **STEP 0.1 (Layer B)**: code routing vocabulary (`route` union, `catalog`, "Elite v2 chain allowlist" vs "canonical allowlist" variance, `useLegacyRunStepApi`/`isCanonicalJsonRuntimeScenario`); 4 dispatch signal-paths code-absent → signal-only; cross-layer "runtime" overload (moderate). **STEP 1**: §1–§8 NORMATIVE — every candidate classified **doc-articulation sufficient**; **rename NOT required**, 0 code identifiers changed; signal-only vocabulary recorded as code-uncorroborated. Terminology closed; §8 conditional escape only. doc-only — runtime/schema/test 0, code-identifier change 0. **Commit**: outer `b43edfd` (spec) + this ledger commit. **Next**: Deferred Queue #4 — 12-Axis drift surfaces; #5 (line 148 reconciliation) now unblocked.

---

**[x] Lane #2 (reinforcement + AIR semantics) / reinforcement delay policy CLOSED (2026-05-17) — ⚠️ lane #2 fully complete:** Reinforcement cadence legitimacy judged and articulated as `RESULT_ORIGIN_CLOSURE_SPEC.md` §9 (NORMATIVE). **STEP 0** (read-only corroboration, commit 0): cadence topology — delay band-driven (`no_change` 3d / `unstable` 5d), iteration cosmetic-only, cap = 3 per chain, retry pull-based (`getDueOutcomes`, not cron). **STEP 1**: §9 NORMATIVE — **bounded reinforcement, legitimate**, three-layer — (per-chain) structurally bounded (cap = 3 + ≥3d/≥5d spacing + 5 prevention guards); (aggregate) user-initiated, not system-coercive; (abandonment) zero new pressure. bounded reinforcement CORROBORATED; coercive recurrence / unbounded retry REBUTTED; diminishing cadence CONTRADICTED. §9.1–§9.5 appended; §1–§8 untouched. doc-only, runtime/schema/test 0. **Commit**: outer `b1bb130` (spec §9) + this ledger commit. **⚠️ lane #2 fully CLOSED** — §5/§7/§8/§9 all resolved. **Publication held** — push not performed; outer `ahead=2` pending Commander review. **Next**: Deferred Queue #3 — runtime label terminology.

---

**[x] Lane #2 (reinforcement + AIR semantics) / loop containment ↔ integrity-metrics boundary CLOSED (2026-05-17):** Canonicalizes the functional decoupling from lane #2 STEP 0 PHASE 5 as `RESULT_ORIGIN_CLOSURE_SPEC.md` §8 (NORMATIVE). No separate corroboration STEP — PHASE 5 reused under an escape-hatch check (`1235124..HEAD` touched zero non-`.md` files → corroboration authoritative). **§8**: reinforcement loop containment and integrity metrics (AIR/forced-reset) are **functionally decoupled** — 0 shared mutable state (disjoint persistence), the cap does not gate the `le_activation_log` AIR footprint emit, shared points are only a common trigger and a shared `payload.validation_result` read. **§8.3 governance-isolation guarantee**: a reinforcement-containment mutation does not implicitly change AIR/escalation semantics → containment tracks proceed without reopening §5/§7, subject to the §8.4 boundary self-check. §8.1–§8.5 appended; §1–§7 untouched. doc-only, runtime/schema/test 0. **Commit**: outer `f8d5097` (spec §8) + this ledger commit. **Next**: lane #2 remaining sub-item — *reinforcement delay policy*.

---

**[x] Lane #2 (reinforcement + AIR semantics) / insufficient_signal escalation semantics CLOSED (2026-05-17):** Escalation legitimacy judged and articulated as `RESULT_ORIGIN_CLOSURE_SPEC.md` §7 (NORMATIVE). **STEP 0 / 0.1** (read-only corroboration, commit 0): Stage-3 entry trigger, forced-reset 4-input aggregation (`stage3SelectedCountIn14d` hardcoded `0` → effective 2-of-3), escalation-local attribution survivability (no `result_origin` field anywhere in the escalation path), and the second Stage-4 ingress `getNextStage(STAGE_3,"air_below_threshold")` via `POST /transition` — measured **dormant** (no in-repo caller; the `0.50` constant has no runtime comparison site). **STEP 1**: §7 NORMATIVE — **(a)** active ingress (`evaluateForcedReset`, 2-of-3, `0.80`) = partially legitimate / structurally indiscriminate / aggregation-gated (over-broad, not catastrophic, never single-trigger); **(b)** dormant ingress (`air_below_threshold`, single-signal, `0.50`) = **latent governance hazard** (not a defect today; activation changes escalation authority semantics → governance review REQUIRED before activation). §7.1–§7.6 appended; §1–§6 untouched. doc-only, runtime/schema/test 0. **Commit**: outer `3e6dc6f` (spec §7) + this ledger commit. **Next**: lane #2 remaining sub-items.

---

**[x] Lane #2 (reinforcement + AIR semantics) / STEP 1 — AIR footprint intent/defect judgment CLOSED (2026-05-17):** Closes `RESULT_ORIGIN_CLOSURE_SPEC.md` §5 `[OPEN]` with a NORMATIVE judgment. **STEP 0** (read-only corroboration, commit 0): 4-trigger upstream attribution + reinforcement/AIR 1-hop adjacency + drag-propagation chain — the four `insufficient_signal` triggers have heterogeneous attribution (user-action / runtime-sequencing / pipeline-data / registry-mixed), `result_origin` lost at hop 1 (`le_activation_log` has no `result_origin` column), forced-reset 2-of-4 contributory (never sole, Stage-3 gated), loop-cap ↔ AIR decoupled. **STEP 1**: §5 NORMATIVE judgment — the AIR drag is **"semantic over-collapse"**, partially legitimate (behavioral absence) / partially over-broad (system absence), **not a catastrophic punitive defect**; §5.1–§5.6 added; §1–§4 untouched; §4 invariant cited not redefined; doc-only, runtime/schema/test 0. **Commit**: outer `78587c6` (spec) + this ledger commit. **Next**: lane #2 remaining sub-items; §5.3 representation-collapse remediation = separate runtime/schema track.

---

**[x] Track 1-C / result_origin closure authoring (STEP 0 / 0.5 / 1) CLOSED (2026-05-17):** `result_origin` (`computed` | `insufficient_signal`) XP/verified closure semantics fixed as a normative spec. **STEP 0** — re-baselined corroboration; measured the result_origin code path (5 set-sites / 2 files — `reexposureValidation.server.ts:250/283/316/361` + `route.ts:189`), re-confirmed no prior spec (MISSING, not CONTRADICT); read-only, commit 0. **STEP 0.5** — AIR footprint semantic classification; VERDICT: the unconditional `le_activation_log` `micro_win` activation emitted for `insufficient_signal` is **grade D (conditional)** — for LE Stage-3 users a contributory (2-of-4, never sole) forced-reset escalation input, and it dilutes AIR/LRI/TII downward; sign is anti-reward (AIR drag), **not a reward leak**; read-only, commit 0. **STEP 1** — [`docs/RESULT_ORIGIN_CLOSURE_SPEC.md`](RESULT_ORIGIN_CLOSURE_SPEC.md) authored (normative); closure invariant `insufficient_signal ⟹ coreXp=0 ∧ weeklyXp=0 ∧ verified=false` (total XP zero = distinguishing invariant; `verified=false` necessary-not-sufficient — computed `no_change` is also `verified=false`); single enforcement point `reflectionRewards.server.ts:185-186`; doc-only. **Commit**: outer `8512f52` (UNPUBLISHED — `origin/main` at `39b545f`). **Carried [OPEN — separate track]**: (a) AIR footprint intent-vs-defect (spec §5) → *reinforcement + AIR semantics* deferred lane (queue #2); (b) `PatternSignatureEvent.result_origin` optional-field absent-as-`computed` legality (spec §6). **Next**: publication (separate track); see Deferred Queue.

---

**[x] Track 1-C / STEP 4 — Reinforcement Loop Iteration Cap CLOSED (committed); outer UNPUBLISHED (2026-05-17) — retroactively ledgered, doc-only reconciliation:** The reinforcement re-exposure loop previously rescheduled `unstable`/`no_change` follow-ups indefinitely — only a `changed` band ended the loop. **Change**: `REINFORCEMENT_LOOP_ITERATION_CAP=3` (`bty-app/src/lib/bty/arena/reinforcementLoopSchedule.server.ts:19`) + `reinforcementCapReached()` predicate (`:63`); at the cap iteration `unstable`/`no_change` ends the loop (`loop_reason "loop_ended_iteration_cap"`, no follow-up scheduled, `next_runtime_state` terminal `NEXT_SCENARIO_READY` instead of `REEXPOSURE_DUE`). 4 files (`reinforcementLoopSchedule.server.ts` + re-exposure `validate/route.ts` + their two test files), **SQL/migration 0, config 0**. **`result_origin` surface NON-CONTACT** — STEP 4 diff touches no `result_origin`/`insufficient_signal`/`verified`/XP line (STEP 0 corroboration REPORT R4). Tests 14/14 green on fresh tree (R7). **Commit**: outer `5e9362e` / inner `7ff03ced` (logically identical co-track pair). **⚠️ outer `5e9362e` is UNPUBLISHED** — `origin/main` still at `39b545f`, outer `ahead=1`; publication is a **separate track**, not decided by this ledger-backfill step. STEP 4 was committed without a ledger entry at commit time — this is the retroactive backfill (sanctioned commit, Commander-approved). **Next**: publication decision for `5e9362e` (separate track).

---

**[x] Track 1-C / STEP 3 — Validation Semantics Documentation Alignment CLOSED + PUBLISHED (2026-05-17) — retroactively ledgered, doc-only reconciliation:** Doc-only alignment of re-exposure validation semantics: forced-reset linkage correction, derived-label framing, v1 supersede note. **Doc-only — code/test/sql/config 0** (corroborated via `git show --stat`): outer `39b545f` touched 3 `.md` files (`BTY JSON ↔ Supabase Binding Layer Spec v3.md`, `BTY_ARENA_SEMANTIC_LOCKING_TABLE_v1.1.md`, `BTY_ARENA_SEMANTIC_LOCKING_TABLE_v1.md`); inner `03322ca1` touched 1 `.md` file (`BTY JSON ↔ Supabase Binding Layer Spec v3.md` — the co-tracked subset; the two `SEMANTIC_LOCKING_TABLE` docs are outer-root-only). **Commit**: outer `39b545f` (PUBLISHED — `origin/main` is at `39b545f`) / inner `03322ca1` (local mirror). STEP 3 was committed without a ledger entry at commit time — this is the retroactive backfill (sanctioned commit, Commander-approved).

---

**[x] Track 1-C / STEP 2 — Fallback Insufficient-Signal Metadata (Route B) CLOSED + PUBLISHED (2026-05-16):** Runtime Evolution Session governance mutation — separates operational `unstable` (a measured pattern-shift band) from input-absent fallback collapse, which previously both produced the same `unstable` value. **Change**: `validation_payload.result_origin` JSONB metadata (`computed` | `insufficient_signal`) tags every re-exposure validation result; the four fallback collapse sites in `bty-app/src/lib/bty/arena/reexposureValidation.server.ts` are tagged `insufficient_signal` (discarded `elite_axis_missing` preserved as `insufficient_signal_reason`). `insufficient_signal` is excluded from analytics confidence/repeat evidence (`patternSignatureAggregation` holds the aggregate — no confidence gain, no repeat increment) and from verified XP (`reflectionRewards` → `verified:false`, XP 0). Band values (`changed`/`unstable`/`no_change`) unchanged; `PatternShiftBand` remains 3 values; **DB migration 0** (additive JSONB key, no CHECK constraint change). 11-file co-track commit (6 source + 5 test), lint + 36 tests green. **Baseline**: outer `998ba8d4` (= `origin/main`) / inner `65902fac` (local-only, `inner-main` upstream unset). **Publication corroboration**: the formal POST-PUSH REPORT for the STEP 2 outer push was missing from the C3 tracking path; `origin/main = 998ba8d4` is corroborated by Commander direct measurement (`git rev-parse origin/main`) — recorded here so publication provenance is not re-doubted next session. **Deferred**: reinforcement delay policy change, UI surfacing of `result_origin`. **Next**: Track 1-C / STEP 3 — re-exposure evolution (or Commander-designated track); STEP 3 not started in this step. **[SUPERSEDED 2026-05-17 — ledger backfill]**: the "STEP 3 not started" clause reflects the 2026-05-16 STEP 2 closure moment only; STEP 3 (`39b545f` / `03322ca1`) and STEP 4 (`5e9362e` / `7ff03ced`) were subsequently committed and are recorded retroactively in the Track 1-C / STEP 3 and STEP 4 entries above.

---

**[x] HK10–HK17 namespace CLOSED (2026-05-16) — NON-CANONICAL DEBRIS, carry-forward 금지:** see [`docs/closures/HK_NONCANONICAL_DEBRIS_CLOSURE.md`](closures/HK_NONCANONICAL_DEBRIS_CLOSURE.md)

---

**[x] Stage 2 Foundry CLOSED (2026-05-14) — VERIFICATION-ONLY; v1.1.1 §5.4 compliant; FORCED_RESET secondary block enforced via Center step; 3 deferred backlog items recorded:** Closure doc [`docs/closures/STAGE2_FOUNDRY_CLOSURE.md`](closures/STAGE2_FOUNDRY_CLOSURE.md) (new). **Inner refs unchanged**: HEAD `a1800737` (Center 2D); no inner commit this sub-phase. **Phase 1 inventory** (≥3-signal cross-check, Center-style discipline) mapped `/[locale]/bty/(protected)/foundry/` route (302 lines = 8 tracked files: hub `page.tsx` 21 + `page.client.tsx` 94 + `FoundryHubLoadingShell` 17 + `loading.tsx` 6 + `dojo-micro/` 49 + `program/[programId]/` 115) + `src/components/foundry/` 2269 lines (6 components: DojoAssessmentShell 485, EliteSpecWidget 306, LearningPathWidget 418, MentorChatShell 313, ProgramProgressShell 352, ProgramRecommenderWidget 395) + domain/lib/engine/api foundry dirs. **All 8 route files TRACKED + CLEAN** (verified via `git ls-files` + `git status`); not in the 30-entry sync-debt set; not in untracked-import problem set. **v1.1.1 §5.4 compliance VERIFIED**: live `/bty/foundry` is lifecycle-external (0 `ArenaRuntimeStateId` rendering — grep across foundry route files: 0 hits for PRIMARY_CHOICE/TRADEOFF/ACTION_DECISION/REEXPOSURE/FORCED_RESET/playUiSegment; the few "arena" matches are CSS variable names `--arena-accent`/`--arena-text` for theming); analysis-flavored hub (ProgramRecommender pattern-driven via `foundry_recommendations` + Supabase broadcast `foundry_program_assign` + `foundry_unlock` scenario tokens; Dr. Chi Mentor / Dashboard / Elite card links + Leaderboard nav); no in-scenario interaction (no primary_choice / forced_tradeoff / action_decision UI; no `useArenaSession` import in foundry/ tree). **v1.1.1 §2**: no row maps to Foundry — correct per Stage 1 §3.4 lifecycle-external. **§8 PASS evidence**: §8-1/3/4/5/6/8 n/a (Foundry is lifecycle-external, no runtime states, no LOCKED states); §8-2 PASS (no Play/Resolve state rendering); §8-7 **ENFORCED** via three-layer Center-step inheritance — URL-level `bty-app/src/middleware.ts:351-372` source scope matches `/[locale]/bty/foundry/*` → 307 to `/[locale]/center` on `userHasForcedResetPending`, before contract block (HARD LOCKED > LOCKED precedence) [2C-1 `d0d763c7`]; UI-level `HubTopNav.tsx` gates `{hubPill(foundry, "Foundry", "foundry")}` inside `forcedResetActive ? null : (<>...</>)` both `arena` and `dear` theme branches [2C-2 `0f160e54`]; test-pinned by `middleware.forced-reset-redirect.test.ts` test #3 `/en/bty/foundry/insights` → 307 `/en/center` [2D `a1800737`]. **Foundry step 5 classification: VERIFICATION-ONLY** — same pattern as Play step 3 (legitimate closure class; mirrors Resolve 2D-1 absorbing §8-2 → Play step 3 verification-only; here Center step absorbed §8-7 → Foundry step 5 verification-only). NOT a skipped step. **Plan A chosen** over Plan B.1 (VISUAL_BEHAVIOR_SPEC §1.3 reconciliation — older non-locking design doc, no §8 urgency, would require inner commit due to `bty-app/docs/` path) and Plan C (build VISUAL_BEHAVIOR_SPEC §1.3 prescribed elements as on-page content — v1.1.1 doesn't require, greenfield scope unjustified). **3 deferred backlog items recorded** per Commander's Plan A condition (closure §6): (1) `bty-app/docs/BTY_ARENA_VISUAL_BEHAVIOR_SPEC.md` §1.3 ↔ live-code content-design gap — older design doc prescribes Pattern Summary / Decision Replay / Hidden Stats / Trend Graph / Insights Card Stack elements that current `/bty/foundry` hub lacks (some analysis elements may live on `/bty/dashboard` or `/growth/history`); NOT in v1.1.1 locking authority chain; content-design backlog, NOT §8 gap; (2) **Foundry vs Growth route coexistence documentation gap** — live product has BOTH `/[locale]/bty/foundry` (3-card hub: Mentor/Dashboard/Elite + ProgramRecommender) AND `/[locale]/growth` (5-card hub: Dojo/Integrity/Guidance/Journey/History at `src/app/[locale]/growth/page.tsx`); mirrors Center's route-lineage pattern (older `BTY_CURSOR_MASTER_PROMPT.md` + `BTY_MASTER_BUILD_V1.md` use "Growth" name + `/growth/*` routes; v1.1.1 + VISUAL_BEHAVIOR_SPEC use "Foundry"); both tracked + clean + lifecycle-external — documentation gap not entanglement; future v1.1.2 outer-doc route-lineage note could formalize (parallel to v1.1.1 §5.5.1 Center pattern); (3) v1.1.1 §9 D2 Foundry FORCED_RESET access-block UI representation — infra enforcement landed (2C-1 + 2C-2), but explicit user-facing "you were redirected because Foundry is blocked" notice NOT built; user IS protected (HARD LOCKED enforced), just no explanatory toast/notice; non-§8-blocker UX polish; previously deferred by Center 2E closure §6.2, re-confirmed deferred here. **4-check gate PASS upfront**: (a) outer `0 0` vs `origin/main` at `c4b395d`; (b) **EXACTLY 30 sync-debt entries** measured; identical set to Center 2E §8.1 (18 prior + 3 from 2C-1 + 4 from 2C-2 + 5 from 2D); 0 anomalies; (c) HK6 untouched; (d) explicit-path staging — 3 doc files only. **Outer sync-debt unchanged at 30 entries** (Foundry step adds 0 — outer-doc-only Plan A; no inner commits). **Stage 2 LOCKED order: step 5/6 complete** (Lobby ✓ → Resolve ✓ → Play ✓ → Center ✓ → **Foundry ✓** → Hub). **Tests**: 17/3255/6 — exact baseline preserved (no new tests in Foundry step; §8-7 tests landed in 2D). Unpushed pending Commander review. **Next**: Stage 2 step 6 = **Hub** (final Stage 2 step; inherits §8-Open #1 from Play closure §5.1 — NEXT_SCENARIO_READY rendered in Play at `BtyArenaRunPageClient.tsx:1067-1167`; ownership requires resolving D1 Lobby↔Hub merge — Hub step's scope cannot be verification-only if D1 must be decided).

**[x] Stage 2 Center CLOSED (2026-05-14) — v1.1.1 §5.5 correction + FORCED_RESET enforcement (middleware redirect + nav suppression + 30 tests); §8-Open #2 RESOLVED:** Closure doc [`docs/closures/STAGE2_CENTER_CLOSURE.md`](closures/STAGE2_CENTER_CLOSURE.md) (new). **Sub-phase chain**: 2A (Phase 1 inventory + blocker-class finding) → 2A→β (Commander reframe: spec-vs-spec inconsistency, Option β) → 2B `540aaa7` outer (v1.1 → v1.1.1 spec correction; §5.5 split into §5.5.1 default recovery surface + §5.5.2 FORCED_RESET override sub-mode; 4-doc consensus citation + route lineage note) → 2C-1 `d0d763c7` inner (Platform/Infra Mode: middleware FORCED_RESET redirect for `/bty-arena/*` + `/bty/foundry/*` → `/center`; `userHasForcedResetPending` helper + 4 unit tests; HARD LOCKED > LOCKED precedence over blocking-contract block) → 2C-2 `0f160e54` inner (UI Mode: `useForcedResetActive` hook with 60s TTL + in-flight singleton dedup; `Nav.tsx`/`HubTopNav.tsx`/`BottomNav.tsx` suppression — Center+LogoutButton stay, Foundry+Arena+sub-pills+divider hidden; loading default NOT-suppressed) → 2D `a1800737` inner (30 tests across 5 new files: middleware redirect 8, hook 8, Nav 4, HubTopNav 5 both themes, BottomNav 5; 0 prod bugs surfaced) → 2E (this closure). **Blocker-class finding + resolution**: Phase 1 first read = spec-vs-product contradiction; Commander reframe = spec-vs-spec inconsistency (v1.1 §5.5 outlier vs `BTY_ARENA_VISUAL_BEHAVIOR_SPEC.md` Screens 9-13 + `BTY_CURSOR_MASTER_PROMPT.md` Recovery layer + `BTY_MASTER_BUILD_V1.md` §6 + `LEADERSHIP_ENGINE_SPEC.md` §5). Option β: correct v1.1, keep shipped product. Route lineage nuance: 2/4 docs route Recovery at `/growth/recovery` (older architecture); live product consolidated at `/center` via `bty-app/src/middleware.ts:133-146` 301 alias — documented in v1.1.1 §5.5.1. **v1.1.1 changes**: title marker `v1.1` → `v1.1.1` (filename unchanged per `ba1d375` lesson); §5.5 rewritten; §6 scope clarifier + §6.2/§6.3 header reframe; §8-5 sub-mode-scoped; §0 Changelog v1.1.1 entry; §12 Memory block v1.1.1 line; Footer Status. **Unchanged**: FD-1~6, §2 row 8, §3 FD-5, §8-1/2/3/4/6/7/8 semantics, §11 LOCKED order. **§8-Open #2 RESOLVED**: gap (a) URL-level (2C-1 middleware), gap (b) UI-level (2C-2 nav suppression), test-guarded (2D 30 tests). **Mode A (recovery hub) status**: confirmed compliant with v1.1.1 §5.5.1 — zero code changes needed (Phase 1's Plan B prediction held; all 2C-1/2C-2/2D inner code was FORCED_RESET sub-mode hardening only). **Tests**: 17/3255/6 — baseline 17 failures preserved across all 4 inner commits (same 7 files); +34 new tests passing (4 helper from 2C-1 + 30 from 2D); 0 prod bugs surfaced by tests. **4-check gate PASS upfront** with **measurement-over-estimate discipline** (new in 2E per 2D's inconsistent estimate "18→25" vs "18+4+5+5"): (a) outer `0 0` vs `origin/main` at `540aaa7`, (b) **EXACTLY 30 sync-debt entries measured** by `git status --short \| wc -l`; classified 18 prior + 3 from 2C-1 + 4 from 2C-2 + 5 from 2D = 30 (matches); 0 anomalies, (c) HK6 untouched by 2E, (d) explicit-path staging — 3 doc files only. **Outer sync-debt now 30 entries** per HK8 closure clause 4 — pending post-Stage-2 leak-integration sprint. **Stage 2 LOCKED order: step 4/6 complete** (Lobby ✓ → Resolve ✓ → Play ✓ → **Center ✓** → Foundry → Hub). **Deferred backlog (Center-originated, 5 items)**: (1) `BtyArenaRunPageClient.tsx:1031-1065` gate-page removal — intentionally retained (coexistence safety, Resolve 2B pattern), unreachable in practice after 2C-1 middleware; removal post-deploy verification; (2) v1.1.1 §9 D2 (Foundry block UI representation) — infra enforcement landed, explicit UX notice not built, non-§8-blocker; (3) v1.1.1 §9 D5 (48h timer UI) — **CLOSED**, `ForcedResetUX.tsx` already has countdown L181-189; (4) v1.1.1 §6.3 "reset 사유 명시 (pattern_family / axis)" — content gap, ForcedResetUX checklist is generic, not §8-2 structural; (5) JSON-engine ACTION_REQUIRED relocation — inherited from Resolve 2D-2 backlog (independent). §8-Open #1 (NEXT_SCENARIO_READY in Play → Hub step ownership) untouched by Center step. Unpushed pending Commander review. **Next**: Stage 2 step 5 = **Foundry** (analysis surface, lifecycle-external; v1.1.1 §5.4 secondary block already enforced by 2C-1+2C-2).

**[x] Stage 2 Center sub-phase 2B — v1.1 → v1.1.1 §5.5/§8-5/§6 scope correction (2026-05-14):** Edit to [`docs/BTY_ARENA_SEMANTIC_LOCKING_TABLE_v1.1.md`](BTY_ARENA_SEMANTIC_LOCKING_TABLE_v1.1.md) (filename unchanged per ba1d375 stability lesson; in-document version marker v1.1 → v1.1.1). **Trigger**: Phase 1 inventory found Center = fully-built recovery hub (Dear-Me letters + Resilience log + Self-esteem Assessment + Healing Phase Tracker) + ForcedResetUX sub-mode, but v1.1 §5.5 framed Center entirely as "system interrupt surface (FD-5) / safe room 아님" — outlier vs 4 FINAL LOCKED docs. Commander confirmed: v1.1 §5.5 is the correction target, not the live product. **Correction applied (5 sections edited)**: (1) §5.5 split into §5.5.1 default recovery surface (Safe Mirror / Small Wins / Self-esteem / Healing Phase Tracker / Dear Me letters; calm/warm tone; voluntary nav allowed) + §5.5.2 FORCED_RESET override sub-mode (full redirect / HARD LOCKED / compliance task surface / 48h window). §5.5.1 cites 4-doc consensus: `BTY_ARENA_VISUAL_BEHAVIOR_SPEC.md` §1.4 + §1.5 Screens 9-13 + §3 tone (primary evidence); `BTY_CURSOR_MASTER_PROMPT.md` Recovery layer + tone; `BTY_MASTER_BUILD_V1.md` §6 Screen Roles; `LEADERSHIP_ENGINE_SPEC.md` §5 Stage4 deterministic engine. (2) **§5.5.1 Route lineage note** (Commander addition): older specs (CURSOR_MP, MASTER_BUILD_V1) routed Recovery at `/growth/recovery`, live product consolidated Recovery + Dear-Me at `/center` via `bty-app/src/middleware.ts:133-146` 301 alias — `/center` is canonical recovery route, `/growth/recovery` references are historical lineage. (3) §6 reframed: top-level scope clarifier ("§6 전체는 §5.5의 FORCED_RESET sub-mode에 적용"); §6.2 header "Center 내 UI 금지" → "FORCED_RESET sub-mode 내 UI 금지"; §6.3 header reframe symmetric. (4) §8-5 rewritten to sub-mode scope ("FORCED_RESET sub-mode 활성 시 Center를 menu/dashboard/safe-room 톤으로 표현 금지; default recovery mode는 §5.5.1에 따라 menu/dashboard pattern + calm·warm 톤 허용"). (5) §0 Changelog: new "v1.1 → v1.1.1 (2026-05-14)" sub-section recording the §5.5/§8-5 scope correction + change ledger. §12 Memory block: new v1.1.1 entry. Footer Status: "v1.1.1 frozen". **Unchanged (per 2A confirmation)**: FD-1~FD-6 frozen decisions, §2 row 8 (`FORCED_RESET_PENDING → Center, HARD LOCKED`), §3 FD-5 ("Center hard interrupt, full redirect, modal 아님"), §8-1/2/3/4/6/7/8 prohibition semantics, §11 Stage 2 LOCKED order. Document title (line 1) header marker: `v1.1` → `v1.1.1`. **Filename intentionally NOT renamed** (`BTY_ARENA_SEMANTIC_LOCKING_TABLE_v1.1.md`) per ba1d375 Stage 0 lesson — renaming breaks every cross-doc reference. **4-check gate PASS upfront**: (a) outer `0 0` vs `origin/main` at `c9e8dfa` (Play closure pushed between turns); (b) 18 sync-debt entries IDENTICAL to Play closure §7.1 (0 anomalies); (c) HK6 untouched; (d) explicit-path staging 3 doc files only. **Outer sync-debt unchanged at 18 entries**. **2A → 2B done; next: 2C (Mode B FD-5 hardening — middleware redirect for FORCED_RESET_PENDING + HARD LOCKED nav suppression in Nav.tsx/HubTopNav.tsx/BottomNav.tsx; **Platform/Infra Mode dispatch needed** for `src/middleware.ts` touch; addresses §8-Open #2 inherited from Play closure)**, then 2D (tests for middleware redirect + nav suppression), then 2E (Center closure doc). Unpushed pending Commander review.

**[x] Stage 2 Play close (2026-05-14) — VERIFICATION-ONLY; Play surface already v1.1 §8-compliant post-2D-1; 2 §8-Open tensions backlogged with named ownership:** Closure doc [`docs/closures/STAGE2_PLAY_CLOSURE.md`](closures/STAGE2_PLAY_CLOSURE.md) (new). **Inner refs unchanged**: HEAD `b92bd0d9` (2D-1); no inner commit this sub-phase. **Phase 1 read-only inventory** mapped `BtyArenaRunPageClient.tsx` (1440 lines post-2D-1) to 15 return paths; **only #7 (L985-1010) + #15 (L1244-1439) render Play-domain content** the surface owns per v1.1 §2. **v1.1 §2 home rows VERIFIED**: row 1 `PRIMARY_CHOICE_ACTIVE` → `<ChoiceList variant="elite">` via `s.playUiSegment === "primary_choice"` ✓; row 2 `TRADEOFF_ACTIVE` → `<EliteArenaPostChoiceBlock>` ✓; row 3 `ACTION_DECISION_ACTIVE` → `<EliteActionDecisionStep>` ✓; row 7 `REEXPOSURE_DUE` → `<ArenaReexposurePanel>` mounted inside Play `<ScreenShell>` (testid `arena-play-snapshot-reexposure`) — two-layer model preserved (server-triggered via `effectiveArenaSnapshot.runtime_state`, client-rendered) ✓. **§8 PASS evidence**: §8-1 (all gate reads server-derived; no client `runtime_state` writes); §8-2 production-path (Resolve render branch removed by 2D-1 + navigation useEffect; JSON-engine inline ACTION_REQUIRED at L643-844 is already counted as the deferred 2D-2 Resolve backlog item, NOT re-scoped here); §8-6 FD-4 (REEXPOSURE_DUE rendered as Play mode flag, not separate surface/overlay/modal). **D4 (re-exposure header phrasing) CLOSED**: already shipped in `src/lib/i18n.ts` (KO `:2928-2940` "재노출 라운드", EN `:4594-4604` "Re-exposure round"); §7.2 abstract context (no spoiler) + §7.3 no "retry"/"skip" framing — verified compliant by Phase 1 inspection. **§8-Open backlog (2 tensions, NAMED OWNERSHIP per Commander condition)**: (1) `NEXT_SCENARIO_READY` rendered in Play at `BtyArenaRunPageClient.tsx:1067-1167` (3 sub-branches incl. `arena-next-scenario-continue` CTA); v1.1 §2 row 9 says Surface = Lobby or Hub; test-pinned by snapshot-gates P5 E (4 tests). **OWNERSHIP: Stage 2 step 6 (Hub)** — Lobby refactor `7e4b33ca` did route separation but did NOT resolve D1's surface-ownership for NEXT_SCENARIO_READY; Hub step must decide whether NEXT_SCENARIO_READY moves to Hub or Lobby. (2) `FORCED_RESET_PENDING` rendered as Play gate-page with manual `<Link href={/center}>` at `BtyArenaRunPageClient.tsx:1031-1065`; v1.1 FD-5 requires full redirect (Center hard interrupt) not a Play-surface gate; test-pinned by snapshot-gates P5 B. **OWNERSHIP: Stage 2 step 4 (Center)** — middleware `src/middleware.ts:351-368` already redirects for `userHasBlockingArenaActionContract` but NOT FORCED_RESET_PENDING; Center step adds the FD-5 enforcement. Both tensions pre-existed Stage 2; neither introduced by Resolve or Play; surfaced under named ownership so they don't become silent debt. **Plan A (verification-only)** chosen over Plan B (D4 polish — already compliant) and Plan C (NEXT_SCENARIO_READY/FORCED_RESET fixes — D1-blocked / Center-scope). **4-check gate PASS upfront**: (a) outer `0 0` vs `origin/main` at `4406ced` (Commander pushed 2E between turns), (b) 18 sync-debt entries — IDENTICAL SET to 2E Resolve closure §8.1 (7 prior HK6/HK7/HK8/HK9/Lobby + 11 Resolve-added 2B/2C/2D-1); 0 anomalies, (c) HK6 untouched by this task, (d) explicit-path staging — 3 doc files only. **Outer sync-debt unchanged at 18 entries** (no inner commit this sub-phase) per HK8 closure clause 4 — pending post-Stage-2 leak-integration sprint per [INNER_PUSH_POLICY §5](INNER_PUSH_POLICY.md). **Stage 2 LOCKED order: step 3/6 complete** (Lobby ✓ → Resolve ✓ → **Play ✓** → Center → Foundry → Hub). **Verification-only as legitimate closure class**: not a skipped step — 2D-1's production Resolve removal + existing Play-domain code/copy absorbed what would have been Play step 3 refactor work; inventing inner work to "earn" a commit would have pushed into D1-blocked (Hub) or pre-committed Center architecture territory, both correctly rejected. Unpushed pending Commander review. **Next**: Stage 2 step 4 = **Center** (inherits §8-Open #2 — FD-5 full-redirect enforcement for FORCED_RESET_PENDING).

**[x] Stage 2 Resolve close (2026-05-14) — v1.1 §8-2 production-path RESOLVED via route separation; JSON-engine relocation DEFERRED (Commander Option B):** Closure doc [`docs/closures/STAGE2_RESOLVE_CLOSURE.md`](closures/STAGE2_RESOLVE_CLOSURE.md) (new). **Inner commit chain** `7e4b33ca` (Lobby close) → `9dc9076c` (2B) → `d51bfb4c` (2C) → `b92bd0d9` (2D-1); explicit `inner-main` ff-sync verified `main == inner-main == b92bd0d9` (2D-1 lesson: never implicit). **Sub-phase chain**: 2A (read-only inventory: L988-1066 production block + L618+ JSON-engine block + ITEM 2 EmptyState edge case) · 2B `9dc9076c` (`ArenaResolveClient` production path created at `/play/resolve` + `useArenaSession` SavedArenaState schema slots for `jsonFlow`-family; both paths coexist) · 2C `d51bfb4c` (test guardrail BEFORE 2D surgery: snapshot-gates P5 A/D migrated component-level, arena-guards `/play/resolve` FLIPPED non-deprecated, +14 NEW tests across 3 files, ITEM 2 EmptyState edge case **test-pinned**) · 2D-1 `b92bd0d9` (production Resolve branch L987-1066 removed from `BtyArenaRunPageClient.tsx`; `useRouter` + transition useEffect → `router.push('/play/resolve')` on `s.arenaActionBlocking` false→true; action-decision-503 test re-pointed to assert navigation; json-reexposure got next/navigation mock as infra-only fix) · 2D-2 **DEFERRED (Option B)**. **§8-2 status**: PRODUCTION path **RESOLVED** (BtyArenaRunPageClient renders zero production Resolve states; ArenaResolveClient owns /play/resolve; user-facing surface boundary enforced); JSON-engine path explicitly **OUT OF §8 USER-FACING SCOPE** (non-production, jsonCatalogDevMode-gated dev tool, zero real-user exposure) — OPEN as documented backlog, not blocking compliance. **2D-2 deferral rationale**: full state lift into useArenaSession.ts (2400-line state machine) for a dev-only path doesn't justify the risk peak under the deadline; payoff (§8 closure for dev-only path) ≪ risk (state machine surgery). **ITEM 2 EmptyState edge case**: L988-1066's null-snapshot EmptyState fallback (`arena-play-action-block-no-contract-payload`) was removed — Commander-approved intentional behavior change. ArenaResolveClient now redirects to /play for null-snapshot case (was logically impossible hook state; redirect is v1.1 §4.3 correct). Pinned by `ArenaResolveClient.empty-state-edge-case.test.tsx` so the removal is never mistaken for accidental drop. **2B schema-slot correction**: 2B added 7 `jsonFlow`-family fields to `SavedArenaState` shape but the 2D-2 dispatch context wrongly claimed "These persist across route navigation already" — VERIFIED INACCURATE (0 saveState writes, 0 loadState reads, 0 hook exposure; all JSON-engine state is `React.useState` in BtyArenaRunPageClient.tsx:85-104). Recorded in closure §5; future sub-phases must verify wires, not rely on type declarations. **Tests**: baseline **17/3221/6** preserved at 2D-1 close (identical failure set to pre-Resolve baseline); +14 NEW tests; 6 migrated/re-pointed; 0 new failures. **4-check gate PASS upfront**: (a) outer 0/0 vs origin/main at `108a280`, (b) 18 sync-debt entries classified (7 prior + 11 Resolve-added: 4 from 2B, 5 from 2C, 3 from 2D-1) — 0 anomalies, (c) HK6 canonical file untouched by 2E, (d) explicit-path staging (3 doc files only). **Outer sync-debt set now 18 entries** per HK8 closure clause 4 — pending post-Stage-2 leak-integration sprint. **Stage 2 LOCKED order: step 2/6 complete** (Lobby ✓ → **Resolve ✓** → Play → Center → Foundry → Hub). **Deferred backlog (Resolve-originated)**: 2D-2 JSON-engine relocation + state lift · orphan `ArenaResolveScreen.tsx` cleanup (§8-4-violating, never rendered) · `AWAITING_VERIFICATION` vs `ACTION_AWAITING_VERIFICATION` naming → v1.2 · v1.1 §9 D3/D6 deferred items (non-§8-blockers, confirmed at Resolve code-time) · borderline JSON-engine framing cleanup. Unpushed pending Commander review. **Next**: Stage 2 step 3 = **Play**.

**[x] Stage 2 Lobby refactor complete (2026-05-14) — v1.1 §8-2 violation resolved (route separation):** Inner commit `7e4b33ca` "refactor(arena): separate Lobby entry from Play surface (v1.1 §8-2 compliance)" pushed to `origin/inner-main` (`6f8f4dec` → `7e4b33ca`). **2 files refactored** (+9/-13): `ArenaEntryClient.tsx` (mode-toggle removed; `router.push` to `/bty-arena/play` replaces in-component state switch; `localStorage.removeItem("btyArenaState:v1")` preserved for fresh-step-2 entry); `play/page.tsx` (redirect replaced with `BtyArenaRunPageClient` mount per v1.1 §5.2). **Tests at baseline**: 17/3207/6 — no regression (0 tests required updating). **Design tokens**: `--arena-*` preserved; no new tokens. **Stage 2 LOCKED order** (Lobby → Resolve → Play → Center → Foundry → Hub per v1.1 §11) — **step 1/6 complete**. **Outer sync-debt set now 7 entries** (5 HK8 + 2 Lobby refactor: ArenaEntryClient.tsx, play/page.tsx) per HK8 closure clause 4 + Commander Option 1 — pending post-Stage-2 leak-integration sprint per [INNER_PUSH_POLICY §5](INNER_PUSH_POLICY.md). **Gate (b) now parametric** (known sync-debt set, not hardcoded count). **Out of scope (preserved)**: `lobby/` redirect alias (dead, cleanup deferred), `BtyArenaRunPageClient.tsx` internals, `hub/`, `wip-v2`, AL-2-D-P1 freeze. **frontend-design SKILL.md** path-resolution: dispatch's `/mnt/skills/...` absent on Hanbit machine; used local copy at `~/.claude/plugins/marketplaces/claude-plugins-official/plugins/frontend-design/skills/frontend-design/SKILL.md` (verified same content). Unpushed pending Commander review. **Next**: Stage 2 step 2 = Resolve (FD-6 Action Gate; highest §8-prohibition violation risk per v1.1 §11).

**[x] Inner History Rewrite Sprint Stage B complete (2026-05-14) — 207MB openwebui tar removed from inner history; .gitignore secrets-safety fix integrated to outer:** Boundary doc [`docs/closures/INNER_HISTORY_REWRITE_2026-05-14.md`](closures/INNER_HISTORY_REWRITE_2026-05-14.md) (new). **Trigger**: inner-main first push rejected by GitHub 100MB limit — `openwebui-backup-20260426.tar.gz` (207.46MB) at inner root commit. **Stage A**: filesystem backup at `/Users/hanbit/Dev/bty-app-PRE-REWRITE-BACKUP-2026-05-14` (4.4GB) + git safety branch. **Stage B**: `git filter-repo --path openwebui-backup-20260426.tar.gz --invert-paths --force` (Commander manual run; auto-mode classifier blocked the agent's attempt — same pattern as [[discipline_classifier_shared_staging_block]] for force-push-class operations). **Verification (16 checks PASS)**: tar gone from all history, 72 commits preserved, **tests 17/3207/6 = EXACTLY baseline** (no regression), .git 1.3GB → 329MB. **Mapping**: inner-main `2626f9a7` → `6f8f4dec`; 3 al-launch-* tags re-pointed; full commit-map at `bty-app/.git/filter-repo/commit-map`. **Doc cascade**: pre-rewrite inner-hash citations in outer docs are HISTORICAL-ONLY (no mass-update). **Option 2 integration**: `bty-app/.gitignore` env-coverage fix folded into outer in same commit for secrets-safety parity (5 HK8 leaks remain pending separate sprint). **Anomalies (not blocking)**: prior filter-repo run on 2026-05-01 (.git/filter-repo/ mtime), ref-map main old hash mismatch with Stage A. **Recovery**: filesystem backup at `/Users/hanbit/Dev/bty-app-PRE-REWRITE-BACKUP-2026-05-14` (preserve until Stage C confirmed). **Mutation outer**: 4 files (boundary doc + .gitignore + 2 board updates); 5 HK8 leaks preserved unstaged. Unpushed pending Commander review. **Next**: Commander review → push approval → Stage C (inner-main re-push without 207MB blocker) → leak-integration sprint for 5 HK8 leaks.

**[x] Inner Push Policy Sprint complete (2026-05-14) — HK8 Option D operationalized as Scope 1-B (disciplined coexistence):** Policy doc [`docs/INNER_PUSH_POLICY.md`](INNER_PUSH_POLICY.md) + pre-flight inventory [`docs/INNER_PUSH_POLICY_PREFLIGHT_INVENTORY.md`](INNER_PUSH_POLICY_PREFLIGHT_INVENTORY.md) committed. **Scope locked 1-B** (disciplined coexistence; 1-A true history unification deferred to post-deadline backlog). **inner branch = `inner-main`** (parallel to `origin/main`; inner forbidden from pushing to `origin/main`). **Push gate**: 5-check (HK8 4-check + outer-HEAD-recorded for traceability). **Leak integration pattern** defined (outer fetches inner-main → cherry-pick → 4-check → push). **Stash probe (Part 1)**: 82-file stash NOT C7/E3/F2/G5 cluster; confirmed AL-1.9 era WIP; page.tsx hunk SUPERSEDED by `66af5946`; BtyArenaRunPageClient/useArenaSession hunks = REVERTED-WORK backup of `fb9d700b` (reverted by `a92e531f`). **Stage 2 Lobby blocking: NO** (stash orthogonal). Disposition recommended: commit-and-name `wip/stash-2026-05-12-al19-era-snapshot` (Commander decides). **C7/E3/F2/G5 scope: UNKNOWN** (deferred to WIP-triage probe). **4-check gate PASS upfront**. Mutation = 4 outer docs (2 new + this row + CURRENT_TASK row); bty-app/ untouched; AL-2-D-P1 freeze preserved; 5 HK8 leaks intact. Unpushed pending Commander review. **Next**: Commander review → push approval → optional follow-up sprints.

**[x] Stage 1 Figma Frame Mapping doc authored (2026-05-14) — Stage 0 → Stage 1 transition complete for BTY Arena Cursor pipeline:** Mapping doc [`docs/BTY_ARENA_STAGE1_FRAME_MAPPING_REPORT.md`](BTY_ARENA_STAGE1_FRAME_MAPPING_REPORT.md) per v1.1 authority (`db97c55`, superseding `ba1d375` placeholder + `6fc83bf` v1). **7-row master table** (6 design surfaces + `ios-frame.jsx` wrapper) using v1.1 2-column Trigger/Render authority model (C-A5+C-A9). Per-surface detail blocks; **Stage 2 LOCKED order** Lobby → Resolve → Play → Center → Foundry → Hub (C-A4); A1-A9 ambiguity register; Stage 2 entry checklist (§8 lint rules + `ios-frame.jsx` HARD LOCKED constraint + AL-2-D-P1 freeze + HK8 5-leak preservation). **D1 KEPT DEFERRED** per Commander Q-A — Hub identity at Stage 2 Lobby code-time. **A2 HK6 isolation verified** (no Stage 1 impact). **4-check gate PASS upfront** before mutation. Mutation = 3 outer docs (1 new + this row + CURRENT_TASK row); bty-app/ untouched; AL-2-D-P1 freeze preserved; 5 HK8 leaks intact. Unpushed pending Commander review per Q-C. **Next**: Commander review → push approval → Stage 2 Cursor dispatch (Lobby first).

**[x] AL-2-D-P1 Reproducibility Reconciliation Step A family STRUCTURALLY COMPLETE (2026-05-10) — BTY reproducibility governance first forensic sprint:** Inner=`50317b8 → f0b4b70` (Step A.3 commit `fix(archetype): restore tracked fingerprint dependency closure`, +1282/-0 across 13 files), outer chain `ce8390b → this commit`, worker `e9e179ed-38a7-40ae-8f97-13cfb09191b7` (untouched). **AL-2-D-P1 V=1 freeze invariants 5/5 PRESERVED** (FINGERPRINT_VERSION=1 / alias 59 / Lock 7 raw passthrough / Lock 4 active = QUIETFLAME 1 / R3.5.2 closure). **4 step outcomes**: A.1 archetype/ inventory (16 = 3 tracked + 13 untracked, broken import graph at HEAD 50317b8) · A.2 `getMyPageIdentityState.ts` M qualification → **UNRELATED_WIP** (`arena_profiles.core_xp_total` → `arena_memberships.core_xp` DB schema migration consumer 변경, AL-1.9-C 역방향) → HK6 candidate · A.3 archetype/ tracked commit `f0b4b70` (13 files, vitest 61/61) · A.4 fresh worktree verification via `git worktree add --detach` 7/7 signals PASS. **Core distinction confirmed**: Semantic freeze ≠ Repository reproducibility freeze (분리 정의 establish). **Phantom dependency threat class identified**: tracked authority + untracked target = reproducibility threat — Variant 1 (inner-tracked + inner-untracked) closed for archetype/, OPEN for `@/lib/llm` (HK7); Variant 2 (outer-untracked + inner-tracked) identified at closure (HK8). **Errata (2)**: ERRATUM_A4_G5 (pnpm → npm ci substitution ACCEPTED) · ERRATUM_A_CL_PATH (root → `docs/` path substitution ACCEPTED + `discipline_dispatch_environment_inventory` self-validation). **Backlog additions**: HK6 schema migration · HK7 `@/lib/llm` phantom · HK8 outer-inner sync · HK9 orphan inventory docs. **Inner working tree preserved**: M `getMyPageIdentityState.ts` 보존 (Commander Q2). **Outer pre-existing dirty state**: 7 entries preserved untouched per G-CL-16. Tests 66/66 + archetype 61/61 PASS (carry-forward + fresh worktree). Deploy 0 / inner src/ mutation 0 / runtime impact 0.

**[x] AL-2-E sprint family CLEAN CLOSE — Phase 1 / Step 2 / Phase 2 종결, AL-2 STRUCTURALLY COMPLETE (2026-05-10):** Inner=`50317b8`, Outer chain=`d896de7 → f9515d7 → d513c6e → aff7628 → this commit`, Worker=`e9e179ed`. **AL-2-D-P1 freeze invariants 5/5 PRESERVED**. **Phase 2 4 components**: Mutation Eligibility Matrix (243 cells: 81 FORBIDDEN / 135 RISKY / 27 CONDITIONAL) · Semantic Fingerprint Integrity (135 cells: INTACT 100 / DRIFT 35 / VIOLATION 0; i2/i4/i5 = 100% INTACT) · Commander Escalation Queue (3 substantive: Q1.1 core_01 OUTLIER + Q1.2 26 standard + Q4.1 phantom overlap) · Drift Density Heatmap (15 literals × 3/5/7 confidence + F1 phantom lineage 20 rows / 3 baseline users). **Reconciliation R1-R6 + P1-P4** at [docs/AL-2-E-PHASE2-RECONCILIATION-APPENDIX.md](AL-2-E-PHASE2-RECONCILIATION-APPENDIX.md). **Sub-sprints (priority-ordered)**: 🔼 P1 HK4-F1 merged forensic / 🔼 P2 HK5 axis layer cleanup / P3 HK2 pattern_family layer. **Mutation roadmap**: R6 production-weighted formula. **BTY architecture maturity**: Stage 1-4 COMPLETE / Stage 5 IN PROGRESS / Stage 6 ENTERING. **17/17 Hard Guards PASS**. Tests 66/66 PASS (carry-forward). Deploy 0 / src/ mutation 0.

**[x] AL-2-E Ψ-1 Step 2 CLEAN CLOSE — Lock 5 spec 보강 완료 (Phase 2 entry condition MET) (2026-05-10):** Outer `d896de7 → f9515d7 (Lock 5 spec + guidelines append) → this commit (operating docs)`, inner `50317b8` (unchanged), worker `e9e179ed-38a7-40ae-8f97-13cfb09191b7` (unchanged). **Lock 5 spec**: [docs/LOCK_5_SEMANTIC_BOUNDARY_SPEC.md](LOCK_5_SEMANTIC_BOUNDARY_SPEC.md) (NEW; citation source: Commander decision — semantic anchor `lock5_semantic_boundary`). **4-tier classification**: 3 FORBIDDEN (action_decision text / `dbChoiceId` / `next_map`) · 5 RISKY (primary choice / escalation / second-choice / `bty_tension_axis` phrasing / propagation) · 1 CONDITIONAL (title-body: pressure shift → RISKY, else SAFE) · SAFE (cross-cutting edit class). **5-invariants pre-mutation check**: `FINGERPRINT_VERSION=1` / alias 59 / Lock 7 raw passthrough / Lock 4 active = QUIETFLAME 1 / R3.5.2 closure. **Guidelines update**: [SCENARIO_CONTENT_GUIDELINES.md](SCENARIO_CONTENT_GUIDELINES.md) 4-tier 요약표 + 5-invariants + spec link append-only (sha256 verification: original 86 lines `9c37640...` unchanged; +49 lines appended; 0 deletions). **9 [DEFERRED_NO_CITATION] categories from Phase 1 Area 4**: 모두 resolved per Commander decision. **AL-2-D-P1 freeze invariants ALL PRESERVED** (5/5). **Phase 2 entry condition MET**. **Backlog updated**: AL-2-E Phase 2 🔼 우선순위 격상; AL-2-HK HK4 + F1 phantom 합병 후순위 유지. Tests 66/66 PASS (carry-forward, no run). Deploy 0 / src/ mutation 0 / runtime impact 0.

**[x] AL-2-E R3 Phase 1 CLEAN CLOSE — snapshot lock acquired (Ψ-1 Step 1 of 3) (2026-05-10):** Outer `4f19421 → ba7eade (10 docs commit) → this commit (operating docs)`, inner `50317b8` (unchanged), worker `e9e179ed-38a7-40ae-8f97-13cfb09191b7` (unchanged). **Phase 1 deliverable**: 10 docs (5 Area 0 + 4 Audit Areas + 1 reconciliation appendix). Reconciliation: [docs/AL-2-E-R3-PHASE1-RECONCILIATION-APPENDIX.md](AL-2-E-R3-PHASE1-RECONCILIATION-APPENDIX.md). **Critical findings (4)**: F1 phantom `patient-complaint-revised-estimate` (20 DB / 0 JSON) · F2 axis system-wide drift Reputation + 14 non-canonical (runtime impact 0) · F3 elite cohort 0 production signals (Path 2 95-row 0 hit) · F4 Lock 5 9 categories [DEFERRED_NO_CITATION]. **Markers**: 2 [SEMANTIC_DRIFT_DETECTED] · 9 [PHASE_2_DEFERRED] · 9 [DEFERRED_NO_CITATION] · 2 `<C5 inventory에서 확인>`. **17/17 Hard Guards PASS** (base 13 + E1-E4). **AL-2-D-P1 freeze invariants ALL preserved** (FINGERPRINT_VERSION = 1 / alias 59 / Lock 7 / Lock 4 active = QUIETFLAME 1 / R3.5.2). **Pending**: Step 2 Lock 5 spec 보강 (Commander session) / Step 3 Phase 2 audit (Path 1 only). Tests 66/66 PASS (carry-forward). Deploy 0 / src/ mutation 0.

**[x] AL-2-E Phase 2 CLEAN CLOSE (2026-05-10, outer `aff7628`) — Path 1 27-scenario semantic mutation eligibility audit + Commander reconciliation:** 4 components + 1 reconciliation appendix delivered. **Components**: Mutation Eligibility Matrix (243 cells) · Semantic Fingerprint Integrity (135 cells, VIOLATION 0) · Commander Escalation Queue (3 substantive) · Drift Density Heatmap (15 literals + F1 phantom lineage). **Commander R1-R6 + P1-P4** at [AL-2-E-PHASE2-RECONCILIATION-APPENDIX.md](AL-2-E-PHASE2-RECONCILIATION-APPENDIX.md). **AL-2-D-P1 freeze invariants 5/5 PRESERVED**. **17/17 Hard Guards PASS**.

**🔼 Priority 1: HK4-F1 merged forensic sub-sprint (R3 승인, registered 2026-05-10, ENTRY CONDITION MET via Phase 2 close `aff7628`):** **Mode**: forensic-only (P4 원칙 적용). **Allowed**: lineage reconstruction · semantic authority trace · engine generation mapping. **Forbidden**: cleanup · deletion (20 phantom rows 보존) · rewrite (scenario_id rename / re-tag). **Inputs**: 5 baseline UUID enumeration (Q15/Q16) + 20 `patient-complaint-revised-estimate` phantom rows + 3 phantom user overlap with baseline cohort (`3c732192` 9 / `2322beb7` 7 / `38ce28d2` 4 = QUIETFLAME holder). **Forensic targets**: (a) pre-AL-2 phantom era → AL-2-A Council session migration boundary (10-day silent gap 2026-04-18 → 2026-04-28), (b) `patient-complaint-revised-estimate` semantic provenance, (c) longitudinal identity continuity assessment. **Cross-reference**: P4 forensic-only pattern (memory #29 detect first mutate later). **Entry**: 본 Phase 2 commit 완료.

**🔼 Priority 2: HK5 axis layer cleanup sprint (R4 + R5, registered 2026-05-10):** **Mode**: alias recommendation only (runtime rewrite 금지, automatic migration 금지). **Scope (HIGH 3 only per R5)**: (1) `Axis 1 — Ownership` format prefix → canonical `Ownership`, (2) `Axis 2 — Time` format prefix → canonical `Time`, (3) `Reputation → Visibility` per AL-2-C R3.3.2 visibility-axis. **Defer**: MEDIUM 5 candidates (`system_identity → Identity` / `Image → Visibility` / `Comfort → Control` / `correction → Repair` / `Support → Repair`) + LOW 7 candidates (`belonging` / `System` / `Documentation` / `transferability` / `scalability` / `consistency` / `self_correction`). **Cross-reference**: P2 similarity ≠ canonical equivalence (memory #25 isomorphic). **Entry condition**: HK4-F1 sprint 완료 후.

**Priority 3: HK2 pattern_family layer sprint (registered 2026-05-10, R4 후순위):** **Scope**: 37 DEPRECATE LOW row 정책 결정 (per [HK-deprecate-low-row-status.md](AL-2-D-P1-R3-HK-deprecate-low-row-status.md)) + 16 i1-drift literals overlap. **Mode**: TBD (deeper rewrite 가능; Lock 7 raw passthrough 흡수 영역 cleanup은 input_hash 영향). **Entry condition**: HK5 완료 후.

**Phase 3 mutation phase (registered 2026-05-10, pending Commander 승인):** **Mode**: scenario JSON mutation per Lock 5 boundary tier. **Priority formula (R6)**: `priority_score = (production_exposure × 0.5) + (semantic_risk × 0.3) + (drift_density × 0.2)`. **Entry pre-condition**: priority_score 산정 per scenario × per category + Commander 승인 + 5-invariants pre-mutation check (Lock 5 spec §3) 통과 + AL-2-D-P1 freeze invariants 5/5 preservation 검증. **Phase 3 dispatch는 별도** (Guard 11 — C5 자체 dispatch 발행 금지).

**[x] AL-2-E Step 2 CLEAN CLOSE (2026-05-10, outer `f9515d7`) — Lock 5 spec 보강 완료:** 9 [DEFERRED_NO_CITATION] field categories Commander 결정 (semantic anchor `lock5_semantic_boundary`)으로 resolved. **Output**: [LOCK_5_SEMANTIC_BOUNDARY_SPEC.md](LOCK_5_SEMANTIC_BOUNDARY_SPEC.md) NEW + [SCENARIO_CONTENT_GUIDELINES.md](SCENARIO_CONTENT_GUIDELINES.md) append-only. **4-tier 분류**: 3 FORBIDDEN (action_decision text / `dbChoiceId` / `next_map`) · 5 RISKY (primary choice / escalation / second-choice / `bty_tension_axis` phrasing / propagation) · 1 CONDITIONAL (title-body) · SAFE cross-cutting. **5-invariants pre-mutation check** 명시. **Phase 2 entry condition MET**.

**[x] AL-2-HK HK4 + F1 phantom forensic 합병 권고 → R3 승인됨 (2026-05-10):** Phase 2 reconciliation R3에서 합병 sub-sprint 정식 승인. forensic-only mode lock 적용. 상세 sprint scope = Priority 1 entry 위 참조.

**[x] AL-2-D-P1 CLEAN CLOSE — no-bump V=1 freeze lock (Path P4, Guard 11 PASS) (2026-05-10):** Outer `3b1eb39 → 0b0db1d (audit 7 + appendix) → this commit (operating docs)`, inner `50317b8` (unchanged), worker `e9e179ed-38a7-40ae-8f97-13cfb09191b7` (unchanged). **24h observe** (window 2026-05-09T04:17:18Z → 2026-05-10T04:17:18Z): scenario α confirmed — T1/T2/T3/T4 ALL UNFIRED. Reconciliation appendix [docs/AL-2-D-P1-R3-RECONCILIATION-APPENDIX.md](AL-2-D-P1-R3-RECONCILIATION-APPENDIX.md) (8 sections). **Markers**: 14/14 [REQUIRES_P0_RECONCILIATION] resolved; 16/26 `<C5 inventory에서 확인>` resolved + 10 deferred → AL-2-HK. **Lock 4 baseline 정정**: active = QUIETFLAME 1 (38ce28d2); STILLWATER (85bd8f1f) historical, superseded 2026-05-02 (pre-AL-2-A by 6d). **Locked invariants**: FINGERPRINT_VERSION = 1 (Lock 6 carry-forward), R3.5.2 closure preserved, alias dictionary 59 entries 활성화 유지, Lock 7 raw passthrough preserved. **이전 'AL-2 sprint family clean closed' (2026-05-09) entry의 'Deferred outer-scope items remain' 종료** — outer-scope items (audit 7 docs + reconciliation appendix + operating docs 2개) 모두 commit. AL-1.9-D-r3-inventory.md은 별도 sprint scope (Guard 9). **AL-2 sprint family final state**: AL-2-A CLOSED / AL-2-B CLOSED / AL-2-C CLEAN CLOSE / AL-2-D-P0 CLEAN CLOSE / AL-2-D-P1 CLEAN CLOSE. Tests 66/66 PASS (carry-forward, no run). Deploy 0 / src/ mutation 0 / runtime impact 0.

**AL-2-HK (entry candidate, registered 2026-05-10, P0-independent) — Cleanup sprint:** AL-2-D-P1 R3 audit 7 docs + reconciliation appendix carry-forward + Step A family closure additions. **Candidates (9)**:
- HK1: `patternFamilyCompatibilityMap` 코드 deletion (`bty-app/src/data/scenario/index.ts:542-556`) — 13-entry dead artifact, 0 imports across entire repo, replacement authority = `PATTERN_FAMILY_ALIAS`. Source: [HK-compat-map-deletion-trace.md](AL-2-D-P1-R3-HK-compat-map-deletion-trace.md) HK A1 (deletion_blocker = none).
- HK2: 37 DEPRECATE LOW row 정책 결정 — axisVector 영향 0, determinism violation 0, 처리 정책 결정 필요. Source: [HK-deprecate-low-row-status.md](AL-2-D-P1-R3-HK-deprecate-low-row-status.md) HK A2; [AL-2-B-low-confidence-deferred.md §3.3](AL-2-B-low-confidence-deferred.md).
- HK3: 3 dead enum arms 처리 — selector classification: fallback / ai_assisted / cached_match-write-side. Source: [HK-orphan-and-dead-branch-inventory.md](AL-2-D-P1-R3-HK-orphan-and-dead-branch-inventory.md) HK A3.8.
- HK4: 5 baseline UUID 정밀 식별 — Q15/Q16 미실행 영역 (user_pattern_signatures oldest 5 + bty_arena_signals oldest 5 + cross-validation). Source: [AL-2-D-P1-R3-RECONCILIATION-APPENDIX.md §3.3 + §4](AL-2-D-P1-R3-RECONCILIATION-APPENDIX.md).
- HK5: axis term 자유 텍스트 정책 검토 — "Reputation" ∉ canonical 12 axes; schema `user_pattern_signatures.axis text NOT NULL` (no enum). Source: [AL-2-D-P1-R3-RECONCILIATION-APPENDIX.md §5](AL-2-D-P1-R3-RECONCILIATION-APPENDIX.md).
- HK6 (Step A.2 deferred): `arena_profiles → arena_memberships` schema migration consumer reflection — file `bty-app/src/lib/bty/identity/getMyPageIdentityState.ts` (currently M in inner working tree at `f0b4b70`), semantic `arena_profiles.core_xp_total` → `arena_memberships.core_xp` (AL-1.9-C `2026-05-06` 정정의 역방향, 즉 `arena_memberships` 테이블이 새 schema에 등장). Production reachability YES via `/api/bty/my-page/state`. Sprint identity: DB schema migration reconciliation (NOT reproducibility, NOT semantic freeze). Dependency: DB schema 마이그레이션 사전 확인 + 기타 `arena_profiles.core_xp_total` consumer 식별 + production deploy timing.
- HK7 (Step A.4 identified): `@/lib/llm` phantom dependency closure — untracked source `bty-app/src/lib/llm.ts`, tracked consumers `bty-app/src/lib/bty/center/letterService.ts` + `bty-app/src/lib/bty/validator/layer2Semantic.ts`. Pattern: phantom_dependency_pattern Variant 1 (inner-tracked authority + inner-untracked target). Sprint identity: repository reproducibility defect closure (sibling of Step A family). Priority depends on letterService / layer2Semantic production reachability inventory (별도 inventory sprint 권고).
- HK8 (Step A closure identified): outer-to-inner repo sync — outer `ce8390b`가 inner의 `archetype/buildFingerprintInput.test.ts` + `tensionAxisToAxisVector.ts`를 ?? (untracked)로 보지만 inner는 동일 파일을 tracked. Pattern: phantom_dependency_pattern Variant 2 (outer-untracked target + inner-tracked source). Governance question: outer가 inner state를 어느 시점에 sync해야 하는지 discipline 부재. Outer-dirty entries at closure (5): `M bty-app/src/domain/pattern-family.ts` · `M bty-app/src/lib/bty/archetype/buildFingerprintInput.ts` · `M bty-app/tsconfig.tsbuildinfo` · `?? bty-app/src/lib/bty/archetype/buildFingerprintInput.test.ts` · `?? bty-app/src/lib/bty/archetype/tensionAxisToAxisVector.ts`. Sprint identity distinction: NOT inner reproducibility, NOT semantic.
- HK9 (Step A closure identified): orphan inventory docs cleanup — `docs/AL-1.9-D-r3-inventory.md` + `docs/AL-2-C5-24h-observe-inventory.md` (untracked in outer, prior sprint artifacts). Sprint identity: documentation hygiene (lowest priority). Action: lineage 확인 후 archive 또는 정식 등재.

**HK6/HK7/HK8 sprint identity distinction reminder:** HK6 (DB semantic schema migration) ≠ HK7 (inner-scope repository reproducibility defect) ≠ HK8 (outer-inner repository sync). 같은 HK family이나 sprint scope 분리 유지.

**Blocker**: Commander 진행 결정 시 즉시 가능. **Out of scope (deferred to AL-2-D fingerprint sprint)**: FINGERPRINT_VERSION bump · courage / identity pen() axis · patternRequires field architecture · anchor swap-A re-evaluation.

### ERRATA (Step A family closure)

**ERRATUM_A4_G5 (2026-05-10):** Step A.4 dispatch G-A4-5 letter (`pnpm install --frozen-lockfile`) was Commander-side authoring error. Inner repo is npm-based (`package-lock.json` present, `pnpm` binary absent, no `pnpm-lock.yaml`). C3 substituted with `npm ci --no-audit --no-fund` preserving frozen-lockfile spirit (uses `package-lock.json`, no lockfile regeneration). Substitution **ACCEPTED**. Root cause: pre-dispatch package manager inventory omitted. Discipline addition: `discipline_dispatch_environment_inventory` — dependency-install command 발행 전 4-signal inventory (lockfile presence / binary availability / `packageManager` field / CI install command) mandatory.

**ERRATUM_A_CL_PATH (2026-05-10):** Step A Family Closure dispatch §4 specified `CURSOR_TASK_BOARD.md` at outer root path `/Users/hanbit/Dev/btytrainingcenter/CURSOR_TASK_BOARD.md`. Actual canonical path: `docs/CURSOR_TASK_BOARD.md` (CLAUDE.md "Operating docs single source: ... live in repo root `docs/`" 명시). Outer HEAD `ce8390b` precedent commit ("docs(operations): AL-2-E sprint family clean close + sub-sprint roadmap") already uses `docs/` path. C3 HALT pre-mutation + spirit-preserving substitution → **ACCEPTED**. Self-validation: `discipline_dispatch_environment_inventory` (minted in same dispatch §3) would have caught this had it been applied to §4 — confirms discipline validity through immediate self-test. Root cause: Commander dispatch authoring inventory omitted for outer path despite memory `Key documents` 리스트에 명시되어 있었음 (single-signal drift between memory accuracy and dispatch authoring).

**AL-2-E (scope TBD, registered 2026-05-10) — Scenario JSON re-tag sprint candidate:** Hanbit scope 정의 선행 후 dispatch. 현재 표기 "AL-2-E (scope TBD)". **Inventory-first 원칙**으로 scope 미정의 상태 진입 금지. **Candidate inputs** (per [AL-2_SPRINT_CLOSURE.md §5.3](AL-2_SPRINT_CLOSURE.md)): scenario JSON re-tag (Lock 5 deferral), bty_tension_axis literal alignment, 12 Type 4 OUTSIDE literal rewrite (Phase 2 enum null entries). **Blocker**: scope 미정의 — Hanbit Commander session 선행.

**[x] AL-2 sprint family clean closed (2026-05-09):** AL-2-A → AL-2-B (P0/P1/P2/P3) → AL-2-C (R3 + mutation) → AL-2-D-P0 cumulative closure. Inner=`50317b8`, outer=`3b1eb39`, worker=`e9e179ed-38a7-40ae-8f97-13cfb09191b7` (bty-arena-staging, no redeploy from C5). Coverage 14.3% → ~83% inventory; 59-entry alias dictionary; 10/12 axes pen()-wired (courage/identity → AL-2-D); activePatterns Set normalized (R3.5.2 closure). Closure snapshot: [docs/AL-2_SPRINT_CLOSURE.md](AL-2_SPRINT_CLOSURE.md). **Deferred outer-scope items remain**: CURRENT_TASK, CURSOR_TASK_BOARD, AL-1.9-D-r3-inventory.

**[x] AL-2-A CLOSED — Vocabulary Audit & Semantic Governance Bootstrap (2026-05-08):** Sprint goal = decision infrastructure 구축 (audit phase = full scope). **Reframe**: vocabulary mismatch fix sprint 아닌 **normalization inexistence discovery sprint**. T5 critical finding 으로 root issue 가 "vocabulary mismatch" 가 아닌 **semantic authority 부재** 임을 입증 (normalization wiring 이 존재한다고 가정했으나 actual = dead artifact). **Deliverables (4 file 신설 + 1 verify)**: (1) [docs/AL-2-A-vocabulary-inventory.csv](AL-2-A-vocabulary-inventory.csv) 110 row × 10 col full enumeration · (2) [docs/AL-2-A-mapping-decision-template.csv](AL-2-A-mapping-decision-template.csv) 110 row × 8 col, decision = TBD default · (3) [docs/AL-2-A-vocabulary-lineage.md](AL-2-A-vocabulary-lineage.md) 4 axis layer + 4 pattern layer + cross-layer gap · (4) [docs/AL-2-A-runtime-path-trace.md](AL-2-A-runtime-path-trace.md) 6 path × vocabulary authority + translation gap · (5) T5 verify: `patternFamilyCompatibilityMap` (data/scenario/index.ts:542-556) = **dead artifact** (declared only, 0 imports across entire repo, normalization wiring 0). **R3 phase 5 carry-over**: 110 unique families / 748 occurrences / canonical 5 coverage 14.3%. **Code mutation 0** (canonical / scenario data / compatibility map / buildFingerprintInput / spec / rules.ts 모두 unchanged). **Spec edit 0**. **Mapping decision deferred** to BTY Semantic Council session (Hanbit 영역). **Frame anchor**: "Semantic similarity ≠ semantic equivalence" — canonical preservation bias 회피, 110 family forced compress 회피. **다음**: BTY Semantic Council session → mapping decision → AL-2-B entry.

**AL-2-B (entry candidate, registered 2026-05-08, post-Council) — Normalization wiring + patternRequires recalculation:** AL-2-A 가 입증한 normalization inexistence 영역 의 actual fix sprint. **Predecessor**: BTY Semantic Council mapping decision (input 형식: AL-2-A-mapping-decision-template.csv 의 decision/candidate_canonical/semantic_rationale columns 채움). **Scope candidates** (Council decision 의존): (a) compatibility map revival or rewrite (vocabulary normalization wiring INSERT path 안 적용), (b) buildFingerprintInput penalty wiring 의 vocabulary alignment, (c) patternSignatureUpsert.server.ts:13-19 의 normalizeFamilyKey 확장 (canonical normalization 적용), (d) spec § 5.1 L261 wording 정정 + § 0 L17 ±0.05 envelope 의존. **Blocker**: Council decision 부재 시 entry 0. **Out of scope (deferred to AL-2-C/D)**: archetype 의미/서사 재설계, FINGERPRINT_VERSION bump, specificity 매트릭스 재계산.

**[x] AL-1.9-D NO-FIX CLOSE — STILLWATER strict matching scope verify (2026-05-08):** R3 inventory 4 phase 결과 = STILLWATER cutoff 충족 영역이 vocabulary unification (구조 변경) 영역으로 확정. Spec § 0 L17 "구조 변경 금지" 위반 회피 → no-fix close + AL-2 escalate. **R3 evidence**: (1) production user_pattern_signatures 5 rows / 3 users / 4 distinct pattern_family, 0 canonical match (delegation_deflection 0 → conflict cutoff 충족 0); (2) bty_arena_signals 5 users avg_meta = 0.484~0.544 cluster (spec § 10 L580 baseline 0.50 ± 0.06 운영 입증); (3) penalty 0 시 5/5 users fail STILLWATER conflict ≤ 0.40; (4) 3-vocabulary mismatch (canonical 5 / compatibility map 13 / scenario data ~) overlap 0; (5) spec drift § 5.1 L261 `conflict_avoidance` ↔ code L33 `delegation_deflection`. **Closure doc**: [docs/AL-1.9-D-r3-inventory.md](AL-1.9-D-r3-inventory.md) (R3 evidence + AL-2 entry context carry-over). **Code mutation 0** (canonical / scenario data / compatibility map / buildFingerprintInput / spec 모두 unchanged). **AL-2 entry candidate added** (vocabulary unification scope, axis 구조 재설계 + patternRequires 재정의 + § 5.1 L261 wording 정정 + compatibility map 정리). **다음**: AL-1.9-B-2 verify · AL-2 entry decision (Commander 영역) · DB schema audit · 별 housekeeping.

**AL-2 (entry candidate, registered 2026-05-08) — Vocabulary unification + archetype product-level 재정의:** spec § 0 L18 reservation. Scope: (a) scenario pattern_family → canonical 5 mapping (또는 canonical 5 expand to cover scenario vocabulary), (b) bty_tension_axis → canonical AxisVector 12 dim mapping, (c) compatibility map (data/scenario/index.ts:542-556) 정리, (d) spec § 5.1 L261 wording 정정 (`conflict_avoidance` → actual canonical), (e) FINGERPRINT_VERSION bump, (f) §3.3.2 specificity 매트릭스 재계산. **Predecessor**: AL-1.9-D R3 inventory carry-over ([docs/AL-1.9-D-r3-inventory.md](AL-1.9-D-r3-inventory.md)). **Decision points (Commander)**: vocabulary direction (compress vs expand), compatibility map role, forward-only vs backfill. **Blocker**: archetype 의미/서사/Foundry path 재설계 dependency.

**[x] AL-1.9-E-P1.1-A LIVE — Suppression coverage helper extraction + 3 caller wiring + test safety net + staging deploy (2026-05-06 → 2026-05-07 deploy `964c3911`):** Predecessor: AL-1.9-E P1.1+P1.2 inventory + P1.3 closure (defer resolved, fix scope 2-axis split — refactor + suppression coverage). 3 sub-sprint × 2 commit + 1 follow-up = 7-row chain. **D-sub1 ✅ refactor axis (inner `aea73d2` / outer `affb20f`)**: `fetchRecentServedScenarioIds` helper을 `arenaSessionNextCore.ts:103-109` 에서 추출. silent-error swallow → `[]` 보존, identical query shape. 5/5 helper test + 5/5 regression test (특히 `.in([DONE,IN_PROGRESS,ABANDONED])` + `.gte(24h-ago)` query-shape regression) PASS. **D-sub2 ✅ suppression coverage axis (inner `91ba61a` / outer `56591f5`)**: 3 router caller (`src/engine/integration/`) 에 helper wiring. `post-session-router.ts:84` (`client` non-null), `foundry-arena-return.ts:156` (admin nullable, L153 pattern), `recovery-loop-router.ts:145` (scenario_retry 한정, L159 pattern). +9 insertions, 4/4 verify gate. **D-sub3 ✅ test + closure axis (test inner `b91d893` / closure outer `6a458b0`)**: 3 caller integration test (`*.served-suppression.test.ts`) 추가, 8 cases all PASS (post-session 2 + foundry 3 incl admin null + recovery 3 incl admin null). Mock pattern: `vi.mock` module-level, `expect.objectContaining` forward-compat. Closure doc [`AL-1.9-E-P1.1-A-closure.md`](AL-1.9-E-P1.1-A-closure.md) — commit chain table + verify gate evidence + defer items cross-link (recovery-loop scenario_retry semantics resolved via `824a494`). Test 0 safety gap → resolved. **Staging deploy LIVE (2026-05-07T12:56:20Z)**: worker `964c3911-3610-4bc7-ab2d-b4fe8eda7881` (replaces `5aebbe79`). 4-signal verify ✅ (version ID + worker live HTTP 200 + bundle grep handler.mjs 4× servedArenaScenarioIds + DB baseline ywamer Q3 = memory L484 exact match, P5-A.2 cold-start archive 보존). `.env.local` cleanup skipped per (a-modified) — middleware NODE_ENV guard tree-shake BYPASS_AUTH branch in production build, prior 4 deploy empirical evidence. Runtime trace (Q1+Q2 post-deploy 0 rows) deferred to organic user activity, P1.1-A coverage 확장이라 deploy gate 아님. **다음 entry candidates**: quickModeService router-bypass axis (별 sprint) · AL-1.9-B-2 verify (Cluster A 자연 해소 후속) · AL-1.9-D (archetype cutoff) · DB schema audit (Incidental C+D) · 별 housekeeping (memory + inner commit + frame violation 정정)

**[x] AL-1.8-E full LIVE — Secure link auto-commit visibility banner (2026-05-04):** AL-1.8-E partial 후 잔여 P1 backlog 처리. Inventory 결과 handler 인프라 (`MyPageLeadershipConsole.tsx:225-274` useEffect + qr/validate endpoint + cross-tab dispatch + PostCompletionSheet)는 이미 완전 구현, **user-perceivable feedback** (failure silent, success modal 일시 표시, pending 0) 만 결손. `33e8283` 1 commit 3 files +235 lines: validationStatus 5-state machine + handler HTTP status 분기 (401→unauthenticated/422→expired/!ok→error/ok+success→success) + inline banner JSX (role/aria-live/color-coded/spinner/dismiss) + i18n 5 keys ko/en + tests 5건. **Worker version**: `1ca9f98b-8482-4b56-b910-3246ef035897`. **Live verification (4/4 PASS)**: V1 success banner + modal screenshot ✅, V1.5 SQL `status='submitted', submitted_at=21:21:25` (tail transition 시각 일치) ✅, V2 invalid token → unauthenticated banner ✅, V3 3-signal (deploy ID + HEAD + tail) ✅. Test 5/5 신규 PASS, 1건 WIP-induced fail은 AL-1.8-F backlog. **다음**: AL-1.8-F WIP test mock interference 또는 AL-1.8-C dead column cleanup

**[x] AL-1.8-E partial LIVE — My Page UI contrast + layout reorder + overflow fix (2026-05-04):** Hanbit 보고 4건 처리. (1) `5c3fbf1` secure link `<a target="_blank">` + light-mode contrast + dismiss button contrast. (2) `6ce36e1` JSX block reorder — QR/secure link panels를 ActionContractHub 직후로 이동, PatternSignaturePanel을 metric area로 이동. (3) `d9e6fff` PatternSignaturePanel 전체 light/dark dual variant (23 lines 1:1 replacement). (4) `834d582` Identity Hero + AIR/TII/RHYTHM `min-w-0` + `break-words` + grid breakpoint `sm` → `md` (좁은 viewport 깨짐 + 박스 overflow 해결). **Final live worker**: `55fd3759-e021-4064-acbf-f40306991a9c` (4 commits 모두 live, 누적 deploy `600e919a` → `73a88260` → `c89d6eab` → `55fd3759`). **Test**: 14/14 PASS, 1 WIP-induced 회귀는 AL-1.8-F backlog. **Full 잔여**: secure link 새 탭 commit flow (`?arena_action_loop=commit&aalo=...`) token validation + UI feedback 디버깅. **다음**: AL-1.8-E full 또는 AL-1.8-F

**[x] AL-1.8-F COMPLETE — 9 test fails 일괄 해결 (2026-05-05):** Self-check 후 base 사실 검증으로 sprint 진입 보고 정정 (1 fail 가정 → 9 fails 실제). 3 sub-sprint 분할 진행. **F.1 (Healing API mock)**: production code (`src/app/api/bty/healing/route.ts:40-44`)가 `.maybeSingle()` 사용하지만 3 test files (healing/route.test, q3-healing-awakening, q241-healing-awakening-get.smoke)의 supabase mock chain에 `maybeSingle` 누락 → 6 fails. 표준 패턴 (archetype/integration.test.ts:146 이미 사용)을 3 files에 적용 → 6 fails 해결. **F.2 (Arena UI ESCALATION ack)**: WIP가 elite flow에 ESCALATION step 도입 (primary → step 3 ESCALATION → `acknowledgeEscalation()` Continue 클릭 → step 4 FORCED_TRADEOFF). `BtyArenaRunPageClient.action-decision-503.integration` + `BtyArenaRunPageClient.continue-next` 2 tests가 primary 직후 forced-tradeoff 검색 → fail. 2 tests에 escalation ack step (`elite-legacy-escalation` waitFor + 내부 button 클릭) 삽입 → 2 fails 해결. **F.3 (MyPage URL-based mock)**: 진단 코드 트레이스 결과 3 fetches (`/api/arena/session/next?locale=en` via `useArenaEntryResolution` in PremiumMyPageIdentityScreen + `/api/arena/core-xp` via AL-1.8-E + `/api/bty/my-page/state`) 발생. Test의 2 mockResolvedValueOnce 슬롯이 session/next + core-xp에 잠식 → state fetch는 undefined → throws → setServerPack(null) → archetype default ("STILLWATER") 렌더. mockImplementation URL switch 패턴 (이미 동 file 4+ tests 사용)으로 전환 + stateCallCount로 retry 검증 → 1 fail 해결. **Total**: 6 test files, ~47 lines, production code 변경 0, 회귀 0. Final vitest **3159 passed | 0 failed | 6 skipped** (3165). 3 commits 분리 권장 (sub-sprint 별 독립). F.3 진단 중 별도 production 이슈 발견 (NOVA vs QUIETFLAME — 같은 계정 두 화면 다른 codename) → AL-1.8-G backlog 등록. **다음**: AL-1.8-G inventory 또는 AL-1.8-C dead column cleanup

**[x] AL-1.8-G RESOLVED — Identity facet labels 명료화 (2026-05-05):** Inventory 결과 single source 통일이 아닌 **의도된 triple identity facets** 확정. (1) Stage codeName (`/api/arena/core-xp` `codeName` 필드) — FORGE/PULSE/FRAME/ASCEND/NOVA/ARCHITECT/CODELESS, dashboard 화면, 700+ XP에서 hide. (2) Sub-name (`/api/arena/core-xp` `subName` 필드, renameable) — 사용자 자가 식별자, dashboard codeName 아래. (3) Archetype codeName (`/api/bty/my-page/state` → `resolveArchetypeForUser` → `computeLeadershipState`) — QUIETFLAME 등, my-page IdentityHero. 3 facet 모두 다른 의미: "어디까지 왔는가" + "내가 부르는 이름" + "내 결정이 보여주는 나". **Spec 단서 3건** confirm dual identity by-design: `BTY_ARENA_DOMAIN_SPEC.md:28` (Stage 700+ hide), `MY_PAGE_IDENTITY_CONSOLE_V1.md:19` (codename+stage 둘 다 표시), `GATE_REPORT_LATEST.md:59` (Stage는 core-xp 전용). **이전 가설 정정**: F.3 trace 시 "useArenaEntryResolution이 codename source"로 추정했으나 실제는 **navigation routing only** (contract.href). **Fix (Option A — label 명료화)**: 5 files, logic/API 변경 0. (a) i18n.ts: `leadershipIdentityEyebrow` → `leadershipArchetypeEyebrow` rename + 값 "LEADERSHIP ARCHETYPE"/"리더십 아키타입" + new `leadershipArchetypeSubtext` ("The pattern your decisions reveal."/"당신의 결정이 보여주는 리더십 패턴입니다."). (b) PremiumMyPageIdentityScreen.tsx: IdentityHero에 `identitySubtext` prop + render. (c) dashboard/page.client.tsx: "Identity"/"정체성" → "Stage Identity"/"단계 정체성" + subtext "Your milestone in Core XP progression."/"Core XP 여정에서의 현재 단계입니다." Vitest 19/19 MyPage tests PASS, full suite 회귀 0. **다음**: AL-1.8-C dead column cleanup

**[x] AL-1.8-D LIVE — Reinforcement choice_type filter expansion (2026-05-04):** `delayed-outcome-trigger.service.ts:663` 1줄 fix (`.eq("choice_type", "no_change_reexposure")` → `.or("choice_type.eq.no_change_reexposure,choice_type.like.reinforcement*")`). `1c91674` HEAD, worker version `d56e6cb7-6706-42ad-a247-6c37d3e82cca`. **Test**: 10/10 trigger.service (5 신규 + 5 기존) + 1/1 e2e (mock builder `.or()` 추가) + 8 files 55/55 회귀 PASS. **Live verification**: SQL hack으로 iter3 reinforcement pending을 due로 당긴 후 test user 접속 → UI "Re-exposure round" + "재노출 후속 · 축 점검 (3차)" 셸 진입 + SQL row가 trigger picker에 잡힘 확인. 운영 pending 3건 (iter2 × 2 + iter3 × 1)은 5월 9일 trigger cycle에서 자동 consume 예정. **다음**: AL-1.8-E (QR display)

**[x] AL-1.7 + AL-1.8-A LIVE — G1~G7 PASS (2026-05-04):** AL-1.7 hold 해제 후 staging 검증 완료. `cf240c4` (AL-1.7 Phase 1 BINDING_V1_SECOND meta 수정) → live `27a8f394` 11:55 PT → AL-1.8-A 추가 (`885ded1` G6 RC2 wiring fix: SecondChoice axis type + choice/route.ts meta 주입 + reexposureValidation chain registry fallback) → live `256e2184` 14:13 PT. **이중 사용자/시나리오 검증**: User A (`38ce28d2`)/`core_03` truth_naming + User B (`2322beb7`)/`core_02` integrity_compromise 모두 G6 PASS. 마이그레이션 `20260410120000_arena_pending_outcomes_reinforcement_loop.sql` 직접 적용 (supabase db push 미사용). 자세한 내용 RELEASE_LOG.md 2026-05-04 entry. **다음: AL-1.8-D filter fix → AL-1.8-E QR display 디버깅**

**AL-1.8-D (P0) — Reinforcement choice_type filter mismatch:** `delayed-outcome-trigger.service.ts:660` `fetchFirstDueNoChangeReexposureMeta`가 `.eq("choice_type", "no_change_reexposure")` 단일 매칭. `reinforcementLoopSchedule.server.ts`는 follow-up을 `choice_type='reinforcement_medium_iter2_pattern'` (또는 유사) 로 insert → Reinforcement iter2+ follow-up은 session router가 영영 detect 못 함 → 운영 사용자가 1차 reinforcement에서 chain 정지. 본 sprint 검증 시 SQL hack `UPDATE choice_type='no_change_reexposure'`로 우회. **Fix 옵션 2개**: (a) filter를 `.in("choice_type", ["no_change_reexposure", "reinforcement_*"])` 으로 확장, (b) reinforcement insert도 `choice_type='no_change_reexposure'` 사용 + iter info를 `validation_payload`에만 저장. 분량 5분. **검증 게이트**: AD2 → 1차 consume → `[BTY REEXPOSURE] enter` log 두 번째 cycle에서도 발화 → user_pattern_signatures repeat_count 증가.

**AL-1.8-E (P1) — Action Contract QR display 미렌더:** Backend 정상 (`POST /api/arena/leadership-engine/qr/action-loop-token` 200 OK 두 번 + `[action_contract_actor] TEMP action_loop_token` log + `POST /api/arena/action-contract/secure-link` 200 OK). Frontend `MyPageLeadershipConsole` "Complete by QR" 버튼 클릭 시 QR 코드가 화면에 렌더 안 됨. 가능 원인: (a) QR 라이브러리 (qrcode 등) bundle 누락 또는 Cloudflare Workers 호환성, (b) Client 컴포넌트 conditional render 버그, (c) React state 미반영. **분석 시작점**: `MyPageLeadershipConsole.tsx`의 QR 렌더 분기 + token response 처리 + 화면에 `qr-debug-value` (이전 sprint에 디버그 surface 있음) 노출 여부.

**[x] AL-1.9-C RESOLVED — My-page Stage display fix (2026-05-06):** AL-1.8-G label fix 후 cross-screen 검증으로 발견. Dashboard Stage Identity = FRAME (Stage 3, 200+ XP) vs My-page IdentityHero = STAGE 1: FORGE (default). **Root cause**: `getMyPageIdentityState.ts:54`이 `arena_memberships.core_xp` 를 query하지만 해당 table이 마이그레이션 chain에 **존재하지 않음** (grep 0 hits; `league_memberships`만 존재). `maybeSingle()` error silent drop → `coreXp = 0` → 모든 사용자 STAGE 1: FORGE 강제. **Fix**: `arena_profiles.core_xp_total` 로 전환 (dashboard + 모든 XP writers와 동일 canonical source). 1 file, ~3 lines, logic 0. **Vitest**: 3159/3165 PASS, 회귀 0. **Caveat**: STILLWATER 표시는 별 layer (archetype cutoff strict — Gap 2, AL-1.9 series 차후 cycle backlog). **다음**: AL-1.9-B (UX completion 분석) 또는 AL-1.9-D (archetype mapping cutoff 검토)

**[audit] AL-1.9-A — Signature pipeline 운영 미작동 root cause reframe (2026-05-05):** AL-1.5 OFFICIAL CLOSURE 시점 박힌 가설 ("Pipeline N signature 생성 미작동")이 부분적 진실에 그침을 발견. 단일 caller (`patternSignatureUpsert.server.ts` ← `/api/arena/re-exposure/validate/route.ts:325`만) + 4 silent skip conditions 식별. **Hanbit SQL 검증으로 dominant root cause 정정**: ywamer 11 post-AL17 runs 중 complete_verified=0 (100% in_progress) → AD2 threshold 미도달 → pending 0 → signature 0 (기계적으로 당연). 3-user completion rate ywamer 23.9% / hanbitdds **3.4%** / ddshanbit 22.7% — Arena run **completion rate가 dominant 운영 차단**. Pipeline 자체는 정상 (ddshanbit 검증 시점 입증). Layer 1 (NEW DOMINANT, completion rate UX) + Layer 2 (Pre-AL17 meta, forward-only 자연 회복) + Layer 3 (universal silent skip 모니터링 격차). **Forward-only 정책 lock**: backfill 미수행, 데이터 무결성 보존. **다음 sprint AL-1.9-B (가칭)**: Arena run completion UX 분석 — step 0 abort 패턴 (core_03 ×3) + step 5 abort 패턴 (INCIDENT-* elite 4건 모두 step 5 in_progress) + AD2 threshold UI 명확성. AL-1.8-G subtext "당신의 결정이 보여주는 리더십 패턴" 약속이 운영에서 발현 못 되는 구조적 이유 = 메모리 #6 Product Identity 측면 깊은 분석. **메타 학습**: Audit 단계 Hanbit SQL 1회로 inventory 1차 가설 (Pre-AL17 meta) 정정 — 메모리 #12 (single signal 금지) 적용으로 premature fix 차단. Sprint discipline 측면 fix dispatch 미진행이 정답. **다음**: AL-1.9-B inventory 또는 idle

**[x] AL-1.8-C RESOLVED — Drop dead top-level reinforcement columns (2026-05-05):** Inventory + Hanbit staging DB 직접 검증으로 dead column 확정 (total=26, top_level_loop_nonnull=0, top_level_seed_nonnull=0, jsonb_loop=22, jsonb_seed=3). Code (worker `242d7ec8`/`788e1e38` 2026-04-27 시점)는 이미 JSONB-only writes — top-level columns은 read fallback 외 0 사용. **Option B 적용** (drop columns, mirror 미사용). **Migration**: `supabase/migrations/20260505100000_drop_arena_pending_outcomes_dead_reinforcement_columns.sql` — `DROP INDEX arena_pending_outcomes_reinforcement_seed_unique` + `DROP COLUMN reinforcement_loop` + `DROP COLUMN reinforcement_seeded_from_pending_id`. **Code cleanup**: `reinforcementLoopSchedule.server.ts:34,40` (top-level type + fallback 제거), `route.ts:192` (type assertion 정리). **Test cleanup**: `reinforcementLoopSchedule.server.test.ts:12,16` (top-level → JSONB nested 으로 rewrite), `canonical-reward-loop.integration.test.ts:140,432,477` (mock 의 `reinforcement_loop: null` 제거). **Schema philosophy 정착**: JSONB validation_payload single source of truth. **Vitest**: 13/13 focused PASS, full **3159 passed | 0 failed | 6 skipped**, 회귀 0. **다음**: Migration staging 적용 (Hanbit 직접) — production은 별도 결정. AL-1.8 series 완전 종료.

**[x] AL-1.8 7-step Elite Chain UI 조립 완료 (2026-05-03):** `page.tsx` → `ArenaEntryClient` 연결(모드 선택 랜딩 — Full Arena/Quick Decision). `BtyArenaRunPageClient.tsx` `legacy_escalation` 렌더러 추가. 데이터 검증: INCIDENT-* 시나리오 4개 브랜치(A/B/C/D) 전부 escalation_text + second_choices(cost+direction+pattern_family) + action_decision(AD1/AD2, is_action_commitment) 완비. Pipeline L에서 eliteSetup+escalationBranches 정상 로딩 경로 확인 (selectNextScenario → loadArenaScenarioPayloadFromDb → getScenarioById → en.json). 미커밋 파일 조립: ArenaEntryClient.tsx, quick/page.{tsx,client.tsx}, quickModeService.ts, api/arena/quick/{start,complete,state}/route.ts. **다음: git add + commit → staging E2E 플레이 테스트**

**AL-1.7 Phase 1 cutover 보류 → 사전 인프라 재분류 (2026-05-03):** "BINDING_V1_SECOND 메타 결함 수정"은 올바른 코드(commit e690344)이나, step 4 실행이 운영에서 한 번도 없었음이 확인되어 production push 무기한 보류. AL-1.8 완료 후 통합 cutover. 코드는 AL-1.8 사전 인프라로 retain.

**AL-1.7 Phase 1 C3 완료 — Pipeline N BINDING_V1_SECOND 메타 결함 수정 (2026-05-03):** 3파일: `choice/route.ts` tradeoff block에 `tradeoffDirection`/`tradeoffPatternFamily` 캡처 + event meta 주입 + direction undefined warn log; `reexposureValidation.server.ts` line 64 `.eq("SECOND_CHOICE_CONFIRMED")` → `.in(["SECOND_CHOICE_CONFIRMED", "BINDING_V1_SECOND"])`; `patternSignatureUpsert.server.ts` early return 직전 `console.warn("[pattern_signature][skip]")` 추가. Spec §10 AL17-0 확장 기재(근본 원인, C2 PASS 4/4, 불변량 4개, fix 범위). 타입 오류 0, 테스트 회귀 0. **C5 검증 + staging deploy 필요. 작업 완료(C3)**

**AL-1.5 OFFICIAL CLOSURE (2026-05-02) — HALTED, cutover deferred to AL-1.7:** Soak T+5h HALT. 근본 원인: `user_pattern_signatures` 전체 공백 (total_rows: 0). Pipeline N signature 생성 운영 미작동 → axis baseline 0.50 고착 → 모든 cutoff 충족 불가 → graceful degrade. 12 silent assumptions 전량 cutover 전 발견. AL-1.5.1 (selector fallback 제거 + current_state filter) + AL-1.5.2 (lockService try/catch) 배포 완료 (Worker `e0a5fea7`). Spec §10 AL15-CLOSURE + AL17-0 신설. **다음: AL-1.7 Phase 1 — signature pipeline 코드 추적. 작업 완료**

**AL-1.5 M1+M2 게이트 PASS — staging migration + Worker deploy (2026-05-02):** M1: bty_archetype_naming_locks 사전 존재 확인(archetype_class CHECK + btree EXCLUDE 포함). Fix A(gist EXCLUDE ALTER TABLE), Fix C(anon/authenticated REVOKE). proacl={postgres, service_role only} ✓. PF-3 발견: Supabase auto-grant anon+authenticated → migration 01에 명시적 REVOKE 3개(public/anon/authenticated) 추가. M2: Worker `3f2befe3-c5fa-4787-ab1d-7f61350057ce` 배포 완료. **Soak T+0: 2026-05-02.** T+1h: `node scripts/staging-soak-hour1-check.mjs`. **작업 완료**

**AL-1.5 PF-2 Fix — Option B REVOKE+GRANT+service role client (2026-05-02):** Migration `20260505000001_bty_create_archetype_lock_rpc.sql`에 `revoke execute on function bty_create_archetype_lock(...) from public` + `grant execute ... to service_role` 추가 — §9 Forbidden("archetype 이름을 사용자가 declare/override 금지") 정합. `lockService.ts` step E(RPC 호출부)를 `getSupabaseAdmin()`으로 교체: null이면 `DB_ERROR` fail-fast, SELECT 경로(findCurrentActiveLock, findActiveLockByHash)는 user client 유지(RLS 보존). `lockService.serviceRole.test.ts` 신규(T-1 RPC→admin, T-2 SELECT→user, T-3 null→DB_ERROR). `integration.test.ts` `makeMockSupabase` 업데이트: `vi.mock("@/lib/supabase-admin")` + rpcSpy를 admin client로 이전. 전체 66/66 PASS, spec-drift 7/7 PASS. **작업 완료**

**AL-1.5 Stage 4.A + 4.B — Archetype Determinism Lock v1 통합 테스트 + CI 게이트 (2026-05-02):** Stage 4.A: `bty-app/src/__tests__/archetype/integration.test.ts` 15 IT 케이스 — PATTERN_FORMING 전환(IT-1), 히스테리시스/EXIT 임계값(IT-2), IT3-B EXIT 위반 RPC 없음(IT-3), 결정론 100회(IT-4), race condition→cached_match(IT-5), DB 격리(IT-6), §7.1 응답 계약 금지 필드(IT-7). Stage 4.B: `scripts/archetype-spec-drift-check.mjs` V1~V7 불변량(ENTRY{12/3}/EXIT{8/2} 임계값, STILLWATER specificity=70+축 조건, Method Y AL-2 금지, A0 게이트 배치) + `.github/workflows/archetype-isolation.yml`(L1 LLM 격리 + L2 스펙 드리프트 + L3 테스트). archetype 63/63 PASS. Stage 4.C AL-1.7 보류. **작업 완료**

**[HOTFIX] SUPABASE_SERVICE_ROLE_KEY wrangler secret 재설정 — QR validate server_config_error 해소 (2026-05-01):** [SECRET+INFRA] `wrangler secret list`에서 키 존재 확인했으나 `/api/debug` → `hasServiceRole: false` — 빈 값으로 등록된 것이 원인. `.env.local` JWT 값으로 `wrangler secret put SUPABASE_SERVICE_ROLE_KEY` 재설정 후 재확인: `hasServiceRole: true`. validate route 응답이 `server_config_error(500)`에서 `contract_not_found(409)`(Supabase 연결 정상)으로 변경. Issue A(QR commit 500) 완전 해소. 코드/빌드 변경 없음; 현재 Worker version `7be041a7` 유지. **작업 완료**

**C4 허브 네비·퀵 아레나·자가진단·힐링 트랙·i18n·멘토 데이터셋 + staging deploy (2026-04-30):** `HubTopNav` 단일 row + btyARENA 브랜드, `nav-items`/`BottomNav`에서 Growth 제거·Center 추가, `quick/page.client` textarea `--arena-text`, `AssessmentClient` 280ms 자동 다음(마지막 제외), `BtyArenaRunPageClient` LabUsageStrip 제거, `PremiumMyPageIdentityScreen` 정리, `AwakeningActsTrack` 설명·다음 순서·1열, `i18n` 라벨/Healing 프롬프트, `mentor_training_dataset_v1_1` 대화운영규칙 3줄. staging `npm run deploy` 완료 Worker `e972d34e-d8c5-4984-a990-0e66ec54c443`. **작업 완료**

**C4 LLM 로컬 기본 `gemma4:31b` + getLlmExtraOptions + chat/mentor 샘플링 + 빌드 IPv4 (2026-04-30):** `src/lib/llm.ts` 기본 로컬 모델 변경, `getLlmExtraOptions()`는 `LLM_BASE_URL` 있을 때만 `{ reasoning_effort: "none" }`. `POST /api/chat`, `POST /api/mentor` 본문에 `temperature: 0.7`, mentor에 `top_p: 0.9`, 스프레드로 로컬 전용 옵션 주입(OpenAI 경로는 빈 객체). `package.json`의 `build`/`build:fast`/`build:local`에 `--no-network-family-autoselection` 추가해 `next/font` Google fetch 타임아웃 완화. Git `907662b`, `ab02980` 푸시됨; Cursor 에이전트 샌드박스에서는 Wrangler 배포 단계만 실패(토큰/네트워크) — 로컬 `npm run deploy`로 스테이징 반영. **작업 완료(배포는 로컬 실행)**

**C4 Arena Quick Decision Phase 2b/2c + staging deploy (2026-04-30):** `POST /api/arena/quick/start|complete`, `GET /api/arena/quick/state`, `quickModeService` + `domain/pattern/patternHistory`, migration `20260504000000_add_user_pattern_history.sql`, `activityXp`에 `QUICK_MODE_COMPLETE`. UI: `bty-arena/page.tsx` → `ArenaEntryClient`, `/bty-arena/quick` 3-state 플로우(ko/en). Full Arena 인라인 플로우 변경 없음. 동일 커밋에 Foundry/Mentor/Integrity 소규모 UX 포함. Git `a49acf9`, Worker `7b07ff85-5179-4fab-a584-bbb969619819`. **작업 완료**

**C4 Healing CTA route correction + dear-me public-path guard + staging deploy (2026-04-30):** `HealingPhaseTracker` 3~4단계 CTA를 `/bty/healing/awakening`에서 `/bty/healing`으로 전환해 Second Awakening(30일 + emotional_sessions 10회) 게이트로 Phase 3 사용자가 막히던 문제를 해결. 이제 Healing 허브의 `AwakeningActsTrack`에서 `Record Next Act`로 `user_healing_awakening_acts` 진행을 바로 기록할 수 있음. `middleware.ts` `isPublicPath`에 `/${locale}/dear-me`도 추가해 어떤 인증 체크 흐름에서도 letter 작성 진입이 차단되지 않도록 방어. staging 재배포 완료: `https://bty-arena-staging.ywamer2022.workers.dev`, Worker version `f4320d0b-3023-471c-bad6-faf07ef731a8`. **작업 완료**

**C4 Foundry 허브 확장 + dear-me redirect 제거 + staging deploy (2026-04-30):** `foundry/page.client.tsx`에 (1) `PROGRAM_COMPLETED_EVENT` 수신 시 초록 Arena 복귀 배너(`Arena로 돌아가기`, `나중에`), (2) `program_catalog` 직접 조회 기반 전체 프로그램 접이식 카탈로그(phase tag + `/bty/foundry/program/[id]` 링크), (3) `/api/chat` mode=`foundry` 인라인 Dr. Chi 채팅 + `전체 대화 보기 → /bty/mentor`, (4) 우상단 상시 `Arena →` 네비 링크를 추가. 또한 `middleware.ts`에서 `/[locale]/dear-me`를 `/[locale]/center`로 보내던 308 리디렉트 분기를 제거해 `Write a letter` 클릭 즉시 Center로 튕기던 문제를 해소. staging 배포 완료: `https://bty-arena-staging.ywamer2022.workers.dev`, Worker version `0e422ef6-1f5f-45c6-9c24-462abb357e17`. **작업 완료**

**C4/C3 Assessment-DearMe-LLM-Train follow-up + staging deploy (2026-04-30):** Assessment result history 타입을 snake_case에서 camelCase API 응답으로 정합화(`createdAt/pattern/track/scores`)해 Invalid Date와 기록 누락 오탐을 수정. Dear Me 제출 성공 후 `DEAR_ME_SUBMITTED_EVENT`를 dispatch하도록 연결해 Center 복귀 시 Healing Phase 2 반영 지연을 제거. 신규 `src/lib/llm.ts`로 LLM endpoint/model 오버라이드(`LLM_BASE_URL`, `LLM_MODEL`, `LLM_API_KEY`)를 공통화하고 `chat/route`, `mentor/route`, `letterService`, `layer2Semantic` 4개 호출 지점을 통일. 28-day day 페이지 코치 영역은 placeholder를 실 `CoachChat`(`/api/chat`, day context 주입)으로 전환, 완료 요약은 `TrainShell`에서 `/api/train/completion-pack` 실 API를 사용하도록 변경. staging 배포 완료: `https://bty-arena-staging.ywamer2022.workers.dev`, Worker version `9642e8b0-b5c3-413f-b69c-2d21f55a1ca6`. **작업 완료**

**C4 Center 후속 UX 보강 + staging deploy (2026-04-30):** `CenterPageClient`에서 자존감 진단 카드의 API 필드 매핑을 camelCase(`pattern/track/createdAt`)로 정합화해 "아직 기록이 없어요" 오탐을 제거하고 결과 카드에 `상세 결과 보기 → /[locale]/assessment/result` 링크를 추가. `HealingPhaseTracker`는 활성 단계 CTA를 연결(1단계 `/assessment`, 2단계 `/dear-me`, 3~4단계 `/bty/healing/awakening`), 에너지 기록 카드는 7일 평균/트렌드/30일 총 기록 지표를 노출하도록 확장, `train/day/[day]/page.client.tsx`에는 사이드바 `← Center` 링크를 추가. staging 배포 완료: `https://bty-arena-staging.ywamer2022.workers.dev`, Worker version `92be1e4f-dc96-4c6d-a30f-5b3cbeb1efda`. **작업 완료**

**C4 Center hub UI 전환 + staging deploy (2026-04-30):** `src/app/[locale]/center/page.tsx`를 기존 `PageClient` wizard에서 `CenterPageClient`로 교체. 새 `src/app/[locale]/center/CenterPageClient.tsx`에서 Forced Reset 모드(`currentStage===4` 또는 `forcedResetTriggeredAt`)는 warm intro + `ForcedResetUX`(48h countdown/checklist/return)만 노출하고 Arena 이동을 차단, Normal 모드는 Stage context + `HealingPhaseTracker` + Dear Me/Resilience/Assessment 카드 허브로 렌더. staging 배포 완료: `https://bty-arena-staging.ywamer2022.workers.dev`, Worker version `3750ced8-6f08-424b-83d9-8a99f76dc5d1`. **작업 완료**

**C3/C4 NEXT_SCENARIO_READY Continue loop fix + deploy (2026-04-27):** Staging에서 `NEXT_SCENARIO_READY + gates.next_allowed=true` 상태로 Continue 클릭 시 다음 시나리오 대신 `POST /api/arena/run/complete`가 반복 호출되는 루프를 수정. `useArenaSession.continueNextScenario()`에서 이미 next-ready/unlocked 스냅샷(`bindingRuntimeSnapshot ?? arenaServerSnapshot`)이면 `run/complete`를 건너뛰고 세션 라우터 fetch로 즉시 전환하도록 패치(로컬 run state 정리 유지). 검증: `BtyArenaRunPageClient.snapshot-gates.test.tsx` PASS, `npm run build && npm run cf:build && npm run cf:deploy` PASS. **Deploy DONE:** Worker version **`d75f668f-448f-4508-9c50-02e82cfce1a1`**.

**C3/C4 core_07 post-QR `ACTION_SUBMITTED` loop fix (2026-04-27):** After AD1 + QR commit, binding snapshot path could still emit `ACTION_SUBMITTED` with `next_allowed=false` when session fetch returned no pending row but contract-by-id was already `submitted`; UI also blocked “Load next” when `action_contract.status=submitted` on `NEXT_SCENARIO_READY`. Fixed server mapping (`pending` only → blocked contract snapshot; `submitted|escalated` → `NEXT_SCENARIO_READY` + extras), `qr/validate` commit success → `NEXT_SCENARIO_READY` + `next_allowed`, client gate + Continue CTA + i18n. Tests: action-contract-loop, qr validate, snapshot-gates, n/session, blocking contract, canonical reward loop PASS. **Deploy DONE:** `npm run build && npm run cf:build && npm run cf:deploy` → Worker version **`ee8d9461-9fa7-4851-afab-30a001a003f6`**; `/api/version` smoke OK (`version=2026-04-27-api-version-endpoint-v1`).

**C4 canonical scenario second_choices `cost` gate + core_07 staging unblock (2026-04-27):** Fixed `Invalid second-choice cost` for `core_07_repair_conversation` by adding meaningful `cost` strings to all A/B/C/D `second_choices` X/Y in EN/KO; synced `public/data/scenario`. Shape test now enforces non-empty `cost` on every core’s locale `second_choices`; backfilled gaps in `core_10` (ko) and `core_13`–`core_16` (en+ko). Tests: shape + public action-decision **82/82 PASS**; deploy Worker version `de1ca1cc-2717-4e82-8709-da568ede7088`.

**C3/C4 stale re-exposure deadlock fix + soft reset extension (2026-04-27):** Removed false `REEXPOSURE_DUE` coupling to consumed memory-queue triggers; GET session now gates re-exposure only on a due `arena_pending_outcomes` row with `choice_type=no_change_reexposure` and a non-empty pending id (`fetchFirstDueNoChangeReexposureMeta`). Client router pack classification and `reexposureSnapshotFromSessionPack` now require a real `pending_outcome_id`. `BtyArenaRunPageClient` shows a stale-recovery aligning surface (refetch delayed-outcomes + `recoverStaleReexposureShell`) instead of a deadlocking re-exposure panel when `due` is stale without ids. Added `retryArenaSession({ force })`, latest-`IN_PROGRESS`-only resume via `/api/arena/runs`, and `POST /api/dev/reset-arena-state` `{ mode: "soft_current" }` (keep newest in-progress, abandon older, delete orphan no-change pending; optional contract/risk clears). Tests PASS: `session/next`, `n/session`, `delayed-outcome-e2e`, `arenaSessionRouterClient.reexposure-gate`, `dev/reset-arena-state`, `BtyArenaRunPageClient.snapshot-gates`. **Deploy PASS** — `npm run build && npm run cf:build && npm run cf:deploy` to `https://bty-arena-staging.ywamer2022.workers.dev`; Worker version `aac300cb-4b0b-447a-b6cb-779c9a7fca98`; `GET /api/version` confirms `version=2026-04-27-api-version-endpoint-v1`, `buildTime=2026-04-27T18:21:00Z`.

**C3 canonical mismatch + AD1 ensure extension + deploy (2026-04-27):** Resolved remaining staging blockers. (1) `POST /api/arena/re-exposure/validate` now treats canonical-equivalent scenario ids as same scenario (`core_*` vs `INCIDENT-*`) via normalize checks (`getScenarioByDbId(actual)` and `getScenarioById(expected)` cross-lookup). `run_scenario_mismatch` remains 403 only for unrelated ids and keeps debug payload fields. (2) `ensureEliteBindingActionCommitmentContract` is no longer effectively elite-chain-id-only; canonical `INCIDENT-*` AD1 runs are accepted for contract ensure, with action-label description fallback and robust pattern family fallback to avoid false ensure-fail. `choice/route` passes AD1 action label into ensure call. Tests PASS: `re-exposure/validate/route.test.ts`, `eliteBindingActionCommitment.server.test.ts`, `choice/route.test.ts`. Staging deploy version `07e10170-986f-40bc-9bae-112d5d1117e3`.
**C3 canonical AD1 contract ensure failure hardening + deploy (2026-04-27):** Staging still showed `elite_action_contract_ensure_failed` on canonical AD1. Verified live schema/indexes on Supabase: `bty_action_contracts_user_family_open_unique` (`user_id, pattern_family`) can reject insert with 23505 even when per-run session differs. Updated `ensureEliteBindingActionCommitmentContract` to recover from insert conflicts by reconciling in order: existing by `user+session`, then `user+action_id`, then `user+pattern_family` open statuses (`draft|committed|pending|submitted|escalated`). This allows canonical AD1 to proceed with existing contract id when uniqueness conflict occurs. Also added failure diagnostics: insert DB `code/message/hint/details` + minimal insert payload are logged and surfaced in `/api/arena/choice` 503 response `detail`. Tests PASS: `src/lib/bty/arena/eliteBindingActionCommitment.server.test.ts`, `src/app/api/arena/choice/route.test.ts`. Staging deploy version `f93bdd55-d32e-4b11-acfb-8f48273c114c`.
**C4 re-exposure panel pending id propagation hotfix + deploy (2026-04-27):** Staging showed delayed-outcomes 200 but panel disabled with `missing_pending_outcome_id`. Root cause: panel fallback only read `pendingOutcomeId`, while some responses can arrive in raw DB shape (`id`, `validation_payload`) depending on mapper path/mocks. Patched `ArenaReexposurePanel` resolution to support both normalized + raw shapes: pending id fallback `pendingOutcomeId -> pending_outcome_id -> id`; scenario id fallback `body JSON -> validation_payload.scenario_id -> scenarioId/scenario_id`. Added explicit logs for triage (`[BTY REEXPOSURE] delayed outcomes`, `[BTY REEXPOSURE] panel resolved context`) and made disabled reason precedence `loading` before missing-id so fetch-pending state is not misreported. Tests PASS: `src/components/bty-arena/ArenaReexposurePanel.test.tsx` (raw shape + loading reason) and `src/app/[locale]/bty-arena/BtyArenaRunPageClient.reexposure-chain.integration.test.tsx`. Staging deploy version `8acc251c-8cb1-4cfe-9d23-3f6485c7cc1d`.
**C3 re-exposure seeded-id schema drift fix + deploy (2026-04-27):** Fixed recurring staging 500 on `POST /api/arena/re-exposure/validate` caused by missing top-level column `arena_pending_outcomes.reinforcement_seeded_from_pending_id`. Removed all direct top-level column dependencies and standardized storage to `validation_payload.reinforcement_seeded_from_pending_id` only. `insertReinforcementDelayedOutcome` dedupe now scans pending rows by `source_choice_history_id` and reads seeded id from `validation_payload`, and follow-up insert writes seeded id into JSON payload (no DB column migration). Tests PASS: `src/lib/bty/arena/reinforcementLoopSchedule.server.test.ts` (includes seeded-id-in-validation_payload assertion), `src/app/api/arena/re-exposure/validate/route.test.ts`. Staging deploy version `242d7ec8-ce8e-4f24-8cac-35faa61f3709`.
**C4/C3 next-vs-reexposure intent separation + deploy (2026-04-27):** Prevented re-exposure UI from hijacking normal next scenario progression after reset. Added `playContext` state in `useArenaSession` (`normal`, `next_scenario`, `re_exposure`) and set intent explicitly on entry paths (`continueNextScenario` => `next_scenario`, `beginReexposurePlay` => `re_exposure`, canonical lobby/reset => `normal`). Updated `BtyArenaRunPageClient` shell rules so `next_scenario` intent does not auto-prioritize re-exposure panel on `NEXT_SCENARIO_READY + re_exposure.due` (or stale due shell with loaded play), keeping core_02 normal play UI active. Added regression tests in `BtyArenaRunPageClient.snapshot-gates.test.tsx` for non-hijack behavior + existing re-exposure chain preservation. Staging deploy version `354a2ef1-1e9c-4e8d-b1ea-0b06a9ad38d7`.
**C3 staging state-stuck recovery hardening + deploy (2026-04-27):** Fixed three blockers from repeated debug runs. (1) `POST /api/arena/choice` AD1 ensure-fail path no longer returns fake `ACTION_REQUIRED` with `action_contract.exists=false`; now returns `runtime_state=ERROR`, `qr_allowed=false`, and invariant guard enforces `ACTION_REQUIRED => contract exists + id`. (2) `POST /api/arena/re-exposure/validate` `403 run_scenario_mismatch` now returns debug fields (`expectedScenarioId`, `actualRunScenarioId`, `pendingOutcomeId`, `priorRunId`, `reexposureRunId`, `scenarioIdFromPayload`). (3) Added staging/dev-only `POST /api/dev/reset-arena-state` for current user cleanup (`arena_runs` IN_PROGRESS close, pending `bty_action_contracts` delete, pending `no_change_reexposure` outcomes delete, `arena_no_change_risks` delete). Tests PASS: `choice/route.test.ts`, `re-exposure/validate/route.test.ts`, `dev/reset-arena-state/route.test.ts`. Staging deploy version `eb8d6692-b127-45f0-9bc1-1a0b0e104544`.
**C3 re-exposure validate schema-drift fix + deploy (2026-04-27):** Fixed staging `POST /api/arena/re-exposure/validate` 500 (`column arena_pending_outcomes.reinforcement_loop does not exist`). Removed direct DB column dependency from validate route (`select/update reinforcement_loop`) and moved loop metadata persistence to `validation_payload.reinforcement_loop` only. Updated reinforcement follow-up writer (`insertReinforcementDelayedOutcome`) to store loop metadata inside `validation_payload` instead of top-level column, keeping existing pending-outcomes columns only. Validation PASS: `src/app/api/arena/re-exposure/validate/route.test.ts`, `src/lib/bty/arena/reinforcementLoopSchedule.server.test.ts`. Staging deploy version `788e1e38-4ab1-4331-a2fc-df93257937dd`.
**C3 canonical re-exposure route allow + deploy (2026-04-27):** Fixed `GET /api/arena/re-exposure/core_01_training_system_exposure` returning `400 reexposure_elite_chain_only` by removing legacy elite-only guard in `src/app/api/arena/re-exposure/[scenarioId]/route.ts` and relying on canonical payload resolution path (`loadArenaScenarioPayloadFromDb` / `getScenarioById` 기반). Unsupported ids now fail with `404 reexposure_scenario_not_found`; legacy elite ids remain supported when payload is resolvable. Added route tests (`src/app/api/arena/re-exposure/[scenarioId]/route.test.ts`): canonical core 200 (`source=json`, `dbScenarioId=INCIDENT-01-OWN-01`), unsupported 404, legacy elite 200. Validation PASS + staging deploy version `9af36fae-4a6b-41ce-89b9-04146214bd2b`.
**C3 canonical run/step hard cut (2026-04-26):** Canonical JSON runtime(`source=json`, `core_*`, `INCIDENT-*` binding) no longer calls legacy `POST /api/arena/run/step` in `useArenaSession` primary/tradeoff/resume paths. Progression now stays on `POST /api/arena/choice` snapshot authority only (`TRADEOFF_ACTIVE`/`ACTION_DECISION_ACTIVE`), preserving legacy `run/step` only for non-canonical scenarios. Added integration assertion that canonical flow posts to `/api/arena/choice` and never to `/api/arena/run/step`; build + cf deploy complete (staging version `ff58c495-f4d4-45c1-a34a-6d8ad80b9d91`).
**C4/C3 re-exposure Enter disabled fallback fix + deploy (2026-04-27):** Fixed panel-level disable deadlock when snapshot pending/scenario ids are missing but delayed-outcomes has valid `no_change_reexposure` row. `ArenaReexposurePanel` now picks `no_change_reexposure` first, derives fallback `pendingOutcomeId` + `scenarioId` from selected outcome, and passes them to `beginReexposurePlay`. Hook now accepts override params and uses priority A snapshot → B panel selection → C visible error toast. Added `reexposure-disabled-reason` debug surface for staging (`loading`/`missing_pending_outcome_id`/`no_outcome_selected`). Tests PASS; staging deployed version `2591758f-57b4-48aa-9a15-9e6951e07d70`.
**C3 re-exposure pending bridge RLS fix (service role) + deploy (2026-04-27):** Fixed AD2 500 (`history_seed_failed: RLS`) by moving fallback `user_scenario_choice_history` seed and `arena_pending_outcomes` write path to service-role client in `choice/route`. Added explicit guard error `service_role_missing_for_reexposure_pending_outcome` when service key is unavailable. Revalidated with `choice/route.test.ts` (service-role-missing + pending row shape) and re-exposure integration test. Staging deployed version `8779a0a9-82ae-457d-bea6-abe4e3bede3b`.
**C3 no-change risk → pending-outcome bridge hardening (2026-04-27):** Fixed missing `arena_pending_outcomes` row in AD2 threshold path by making pending creation fail-closed in `choice/route`: history lookup now checks both db/json scenario ids and seeds fallback choice-history when absent; pending insert now writes `choice_type=no_change_reexposure`, `status=pending`, `scheduled_for=now`, `outcome_title=Re-exposure round`, plus structured `validation_payload` (`incident_id`, `scenario_id`, `db_scenario_id`, `axis_group`, `axis_index`, `pattern_family`, `source=no_change_risk`). If pending id is still missing, route returns `500 no_change_reexposure_pending_outcome_create_failed` instead of silently promoting due state. Staging deployed version `dd0ed656-6eeb-4204-9b4b-cd2ffe6d34d4`.
**C3 staging served-version endpoint + deploy verification (2026-04-27):** Added `GET /api/version` to expose `{ app, env, version, buildTime, worker }` and wired Cloudflare staging vars (`BTY_ENV`, `BTY_WORKER_NAME`, `BTY_DEPLOY_VERSION`, `BTY_BUILD_TIME`, `BTY_APP_VERSION`) in `wrangler.toml`. Deployed to staging version `faea9772-fbd9-49af-a8f5-9239ffcc62b3`; live `curl /api/version` confirms latest served tag `2026-04-27-api-version-endpoint-v1`.
**C3 re-exposure pending persistence/query fix + deploy (2026-04-27):** Fixed mismatch where `REEXPOSURE_DUE` shell could render without durable `arena_pending_outcomes` linkage. `choice/route` now resolves history by both db/json scenario ids and seeds fallback `user_scenario_choice_history` when missing, guaranteeing pending row creation/reuse. Snapshot now exposes playable `re_exposure.scenario_id=json_scenario_id` for `/api/arena/re-exposure/[scenarioId]` compatibility. Client adds visible toast on `no_pending_reexposure_for_scenario`. Validation: targeted tests PASS (20/20), staging deployed version `54b542fc-6924-4445-bd62-d53ae80b76a4`.
**C4/C3 re-exposure enter wiring staging deploy (2026-04-27):** Executed `npm run build && npm run cf:build && npm run cf:deploy` after re-exposure CTA wiring fix and gate-precedence patch. Deployment succeeded to `https://bty-arena-staging.ywamer2022.workers.dev` with version `5cf1c626-286f-4fcc-8617-5c280464334a`. Included: Enter scenario click propagation guard, `[BTY REEXPOSURE] enter` trace, pending_outcome_id visible guard, and `NEXT_SCENARIO_READY + re_exposure.due` re-exposure panel priority.
**C4/C3 re-exposure entry wiring + gate priority fix (2026-04-27):** Fixed `REEXPOSURE_DUE` panel CTA wiring so `Enter scenario` click is never swallowed by parent form/propagation and always reaches `beginReexposurePlay` path. Added explicit entry log (`[BTY REEXPOSURE] enter`) and visible guard for missing `pending_outcome_id` to avoid silent no-op. Corrected render precedence so `NEXT_SCENARIO_READY + re_exposure.due=true` renders `arena-play-snapshot-reexposure` instead of `Play paused` blocked shell. Regressions PASS: `ArenaReexposurePanel.test.tsx`, `BtyArenaRunPageClient.snapshot-gates.test.tsx`, `BtyArenaRunPageClient.reexposure-chain.integration.test.tsx`.
**C3 tradeoff/action dbChoice base-mapping lock (2026-04-26):** Fixed canonical `400 second_choice_binding_mismatch` by forcing client payload `db_choice_id` to resolve from `runtimeScenario.base.structure.tradeoff` (and action decision from `base.structure.action_decision`) instead of localized branch values. `useArenaSession` now tracks selected second choice and sends base-mapped ids for tradeoff/action decision bindings. `POST /api/arena/choice` canonical scenario-lib construction now overlays `escalationBranches` dbChoice ids with base mappings before validating, and mismatch responses include debug fields (`expectedDbChoiceId`, `receivedDbChoiceId`, `primaryChoiceId`, `secondChoiceId`/`actionChoiceId`). Added route + integration regressions; tests/build PASS.
**C3 missing primary context fix for tradeoff binding (2026-04-26):** Fixed staging payload gap where tradeoff binding arrived with `primaryChoiceId=null` and produced `expectedDbChoiceId=null`. Client now always sends `primary_choice_id` + `parent_choice_id` on tradeoff, and sends `primary_choice_id` + `parent_choice_id` + `second_choice_id` on action decision. Server now resolves tradeoff primary by precedence (`body.primary_choice_id` → `body.parent_choice_id` → saved run meta) and returns `400 missing_primary_choice_id_for_tradeoff` when absent (no null-mismatch fallback). Tradeoff success also persists `primary_choice_id`/`second_choice_id` into run meta for subsequent action-decision binding. Tests PASS; staging redeployed (`24062779-0218-49d4-a972-189a73752f88`).
**C3 AD2 500 guard + nextScenario chain resume fix (2026-04-26):** Resolved action-decision AD2 500 by converting implicit runtime exceptions into explicit binding guards: `missing_primary_choice_id_for_action_decision`, `missing_second_choice_id_for_action_decision`, `action_decision_binding_missing`, `action_decision_binding_mismatch`, and `action_decision_no_change_risk_invalid` (400s instead of 500). Also fixed post-completion core_01 reset loop by normalizing played/served IDs in selector (`INCIDENT-*` run/history ids mapped back to canonical `core_*` ids before coverage filtering), so NEXT_SCENARIO_READY progression uses incident next chain instead of re-entering fresh entry. Route tests PASS + staging redeployed (`22398cbd-98da-4c8b-b8cd-1ec5296f7bfc`).
**C3 QR post-commit auto-refetch UX sync (2026-04-26):** Added throttled authority refresh wiring for origin browser recovery after mobile/other-tab QR completion. `useArenaSession` now auto-resyncs session gate on `focus`, `visibilitychange(visible)`, and cross-tab storage pulse (`bty-action-contract-updated`) while ACTION_REQUIRED shell is active; `MyPageLeadershipConsole` now does the same for action-contract hub data and dispatches `dispatchBtyActionContractUpdated()` after QR validate success. Added regression test for throttled focus/visible/storage refetch.
**C3 submitted-block guard + manual refresh CTA (2026-04-26):** Added defensive status guard in `runArenaSessionNextCore` so only `status=pending` can emit 409 `action_contract_pending`; non-pending rows (e.g. `submitted`) are logged and ignored. Added sync trace logs (`[BTY SYNC] visibility/focus refetch`, `[BTY SYNC] session refetch complete`) and explicit `Refresh status` CTA on ACTION_REQUIRED shell (`arena-pending-contract-refresh-status`) wired to `retryArenaSession`. Added tests for submitted-not-409 route behavior and refresh CTA invocation.
**C3 QR validate false-success hard-fail + transition verification logs (2026-04-26):** Verified real DB row remained `pending` after “Execution recorded” and patched `POST /api/arena/leadership-engine/qr/validate` to reject tokens missing `contractId` (`422 missing_contract_id`) instead of returning generic `ok:true`. Pending transition now verifies update result row (`select + maybeSingle`) and requires final `status=submitted`; otherwise returns `contract_update_failed`. Added transition diagnostics logs before/after update and tests for missing-contractId + pending transition path.
**C3 staging redeploy for QR transition hardening (2026-04-26):** Ran `npm run build`, `npm run cf:build`, `npm run cf:deploy` and deployed `bty-arena-staging` with version `787daa30-9e4d-4a76-a1e1-7be3c99fdd83`.
**C3 422 binding unresolved on OWN-RE entry (2026-04-26):** Root cause confirmed: staging fresh-entry var pinned to `OWN-RE-02-R1`, while AD2 no-change path in `/api/arena/choice` requires canonical 27-core `dbScenarioId` mapping via `getScenarioByDbId`, causing 422 unresolved. Fix: set worker var `BTY_ARENA_VERTICAL_SLICE_ENTRY_SCENARIO_ID=core_01_training_system` and keep strict 422 only for canonical ids (`INCIDENT-*`); non-canonical ids now skip no-change risk accrual with warning (no silent unknown_incident write, no fallback progression bypass). Tests PASS + staging redeployed (`f4abc7a5-6d95-4846-8ad6-54a3f6247c51`).
**C3 lab route canonicalization (2026-04-26):** Confirmed staging surface was `/en/bty-arena/lab` (not canonical `/en/bty-arena`) and therefore bypassed intended entry behavior. Replaced `src/app/[locale]/bty-arena/lab/page.tsx` with server redirect to `/${locale}/bty-arena` so lab route no longer shows standalone legacy/lab scenario surface. Staging redeployed (`485ffe83-87c3-4235-a1af-e831051f4762`).
**C3 canonical dbScenarioId binding recovery for core entry (2026-04-26):** Fixed runtime id mixing where `dbScenarioId` leaked as folder/json id (`core_*`). `eliteScenarioToScenario` now resolves canonical base binding by core ordinal and emits base `dbScenarioId` (e.g. `INCIDENT-01-OWN-01`) plus base-mapped primary/tradeoff db choice ids. `useArenaSession` run creation now prefers `scenario.dbScenarioId` so `arena_runs.scenario_id` aligns with binding payload. Added fail-closed guard in `POST /api/arena/choice`: canonical json id + `db_scenario_id=core_*` => `422 db_scenario_id_must_be_canonical_base_db_scenario_id`. Added tests (`eliteScenariosCanonical.binding.test.ts`, `choice/route.test.ts`) and redeployed staging (`cb29b569-57b7-4a4b-9c12-8e5ad379ec9c`).
**C3 stale legacy run compatibility for canonical db ids (2026-04-26):** Confirmed staging DB retained active legacy rows (`arena_runs.scenario_id=core_01_training_system`), causing `POST /api/arena/choice` 409 when client sends canonical `db_scenario_id=INCIDENT-*`. Added compatibility migration in `choice route`: if run row is `core_*` and request db id is canonical, update run `scenario_id` to requested canonical db id and continue. Unrelated mismatches still 409, now with debug fields (`currentRunScenarioId`, `requestedDbScenarioId`, `expectedDbScenarioId`, `jsonScenarioId`). Added client runtime schema version (`canonical-db-id-v2`) in local arena state to clear stale saved run state on version mismatch. Tests PASS + staging redeploy (`f2526bb0-ad2e-4cc5-920a-2b0fbfc749f2`).
**C3 canonical JSON binding allowed through choice guard (2026-04-26):** Fixed `binding_only_elite_chain_scenarios` false block for canonical payloads (`json_scenario_id=core_*`, `db_scenario_id=INCIDENT-*`). Route now resolves binding scenario by `json_scenario_id` elite path OR canonical `getScenarioByDbId` path; canonical 27-core DB ids no longer fail guard. Unsupported non-canonical ids still return guard 400; `db_scenario_id=core_*` misuse still 422. Added route coverage for canonical allow + unsupported block and redeployed staging (`acc6d01b-4cb0-43b2-bc79-51f6bbea306b`).
**C3 Arena content source canonical-only switch (2026-04-26):** Root cause confirmed: Arena content path still came from elite converter chain (`scenario-selector` metadata + `loadArenaScenarioPayloadFromDb` → `eliteScenarioToScenario`), which preserved legacy title copy (`Write Them Up or Name the System`) despite canonical ids. Switched runtime source to `src/data/scenario` registry: `scenarioPayloadFromDb` now resolves via `getScenarioById`, maps localized canonical content/choices/escalation into runtime Scenario, and blocks legacy marker leakage. `scenario-selector` catalog now derives from canonical `scenarioList` (not elite metadata), and fresh entry env updated to existing canonical id `core_01_training_system_exposure`. Added regression in `scenarioPayloadFromDb.test.ts` that fails on legacy title and asserts canonical title. Staging redeployed (`201442e1-5e61-46a0-856f-b0295a0763ab`).

**C3 scenario index cleanup (2026-04-26):** Refactored `bty-app/src/data/scenario/index.ts` into incident-group constants (`INCIDENT_01~03`) + merged `scenarioList`, fixed broken spread/parenthesis syntax, and introduced `ScenarioId` derived type so `getScenarioPath`/`getBasePath` accept only known ids.

**C3 pending-only contract blocking gate (2026-04-26):** Updated session blocking source (`fetchBlockingArenaContractForSession`) so `action_contract_pending` is emitted only for `status=pending` with active deadline. Removed `submitted/escalated/approved-awaiting` from 409 blocking query path to prevent stale ACTION_REQUIRED shell after successful QR commit transition. Added focused unit coverage in `blockingArenaActionContract.test.ts`.

**C3 QR commit transition fix (2026-04-26):** Restored post-scan contract progression by updating `POST /api/arena/leadership-engine/qr/validate` to transition `pending -> submitted` on commit tokens (instead of rejecting as `contract_not_pending` unless legacy flag). This clears ACTION_REQUIRED-only lock and surfaces ACTION_SUBMITTED shell semantics. Contract lookup no longer hard-couples to token `sessionId` equality and now resolves run identity from contract (`run_id/session_id`) with fallback for actor checks.

**C3 QR stale-render hardening + inline debug value (2026-04-26):** Added defensive QR refresh flow in `MyPageLeadershipConsole`: clear stale QR state before each token call (`setQrUrl(null)`), then remount QR component with `key={qrUrl}` and render `qr-debug-value` text under QR panel to expose the exact encoded URL on staging. Added regression to prove QR value updates across consecutive token responses (old value cannot stick).

**C3 QR rendering source-of-truth fix (2026-04-26):** Fixed My Page QR panel to encode server-returned `qrUrl` (fallback `url`) directly, instead of reconstructing `window.location.origin + token`. This removes stale-origin drift (e.g. `bty-website` fallback) when staging token route returns the correct staging host URL. Added UI regression test in `MyPageLeadershipConsole.test.tsx` (mocked `QRCodeSVG`) to assert QR value comes from `qrUrl` and not legacy host fallback.

**C3 action-loop-token contractId fallback hardening (2026-04-26):** Fixed staging `POST /api/arena/leadership-engine/qr/action-loop-token` 422 (`missing_session_id`) for contractId-only requests. Route now resolves identifiers with fallback order `session_id -> run_id -> payload.runId`, and splits errors by intent (`contract_not_found` 404 / `contract_user_mismatch` 403 / `contract_not_pending` 409). Response now includes `ok`, `contractId`, `runId`, `qrUrl`, `expiresAt` while keeping existing `token`/`url` for compatibility. Added route tests for contractId-only no-session success and 404/403/409 guards; deployed to staging version `a9f1aa49-6f53-4b57-8344-aabeb0742c6f`.

**C3 legacy pending-contract CTA real-render fix (2026-04-26):** Traced staging-visible `Complete by QR + Complete by secure link` CTA to My Page `ActionContractHub` path (not Arena pending gate), then hardened click wiring there: both CTA buttons now stop event propagation, and `handleRequestQr` in `MyPageLeadershipConsole` now mints QR token with `contractId`-first payload (plus `runId` when present) so QR execution does not depend on `session_id`. Updated real-render regression tests in `MyPageLeadershipConsole.test.tsx` and revalidated with token-route + arena snapshot-gate tests.

**C3 legacy CTA fix deploy (2026-04-26):** Ran `build` + `cf:build` + `cf:deploy` after the My Page CTA path fix; staging deploy succeeded at `https://bty-arena-staging.ywamer2022.workers.dev` with version `f30546dd-a590-46ab-9c24-c3d13652b477`.

**C3 Complete-by-QR browser wiring hardening (2026-04-26):** Added event-guard on `ArenaPendingContractGate` QR CTA (`preventDefault + stopPropagation`) to block accidental bubbling into session retry paths. Added debug trace `console.info("[BTY QR] startPendingContractQrFlow", { contractId, runId })` in `useArenaSession` to confirm runtime click path reaches QR token execution flow. Re-ran requested test/build/deploy sequence and redeployed staging (version `a63a9d9e-b1a4-4ea5-bea5-584652192de9`).

**C3 QR CTA execution-path hardening + redeploy (2026-04-26):** Finalized separation between QR Trigger Engine and QR Execution Flow on ACTION_REQUIRED gate. `Complete by QR` now starts execution token flow only (no session retry/fetch), gated by `qr_allowed===true`, and passes `contractId` to `/api/arena/leadership-engine/qr/action-loop-token`. Server route updated to resolve by `contractId` even without runId. Verified with requested tests + build + cf:build + cf:deploy. Updated staging deployment live at `https://bty-arena-staging.ywamer2022.workers.dev` (version `2fef5d15-754f-4fed-8be2-edcec4e2b4b9`).

**C3 ACTION_REQUIRED QR CTA routing fix (2026-04-26):** Fixed `Complete by QR` path so ACTION_REQUIRED gate no longer re-fetches session router (409 pending contract loop). Added QR CTA on `ArenaPendingContractGate` wired to `useArenaSession.startPendingContractQrFlow`, which calls `/api/arena/leadership-engine/qr/action-loop-token` with `contractId` (+`runId` when available) and redirects to returned commit URL. Extended token route to support `contractId`-only resolution and to derive session from contract row safely. Added tests: snapshot gate verifies QR click calls QR flow handler (not retry-session), and action-loop-token route supports contractId-only minting.

**C3 Cloudflare staging deploy (2026-04-26):** Completed Worker staging deployment using `@opennextjs/cloudflare`. Updated `wrangler.toml` (`name=bty-arena-staging`, `compatibility_date=2026-04-26`, `nodejs_compat`) and added package scripts `cf:build`, `cf:preview`, `cf:deploy`. Ran release regression suite (14 files / 140 tests PASS), `next build`, `opennextjs-cloudflare build`, authenticated via `wrangler login`, and deployed successfully. Staging URL: `https://bty-arena-staging.ywamer2022.workers.dev` (root and `/en/bty-arena` reachable; arena path currently sign-in gated, as expected).

**C3 `/api/arena/event` unresolved-binding hard-fail consistency (2026-04-26):** Extended `src/app/api/arena/event/route.ts` so `JSON_SCENARIO_DECISION_COMPLETED` AD2 no-change branch validates `dbScenarioId` through `getScenarioByDbId` before any risk write. Unresolved mapping now returns `422 action_decision_scenario_binding_unresolved` and exits before accrual. Also forces accrual inputs (`incidentId`, `axisGroup`, `axisIndex`) from canonical scenario mapping instead of trusting client payload. Added route test to lock: invalid dbScenarioId => 422 + `accrueNoChangeRisk` not called.

**C3 AD1 503 client render lock (2026-04-26):** Added UI integration coverage in `src/app/[locale]/bty-arena/BtyArenaRunPageClient.action-decision-503.integration.test.tsx` for the real `useArenaSession + BtyArenaRunPageClient` path: AD1 action-decision call to `/api/arena/choice` mocked as `503` with blocked snapshot (`ACTION_REQUIRED`, `gates.next_allowed=false`, `gates.choice_allowed=false`). Asserted client consumes error-body snapshot, keeps `arena-play-main-pending-contract` shell, and does not render `NEXT_SCENARIO_READY` fallback. This locks server-snapshot-first and No Action → No Progression at render level.

**C3 non-blocking hardening after Release Gate PASS (2026-04-26):** Added two stability reinforcements: (1) `postArenaChoice` now parses error-body snapshots and throws `ArenaChoiceHttpError` carrying blocked snapshot; `useArenaSession` consumes that snapshot on AD1 ensure-fail so UI stays blocked (`ACTION_REQUIRED`) instead of losing state on throw-only path. (2) `POST /api/arena/choice` AD2 risk path now hard-fails with 422 when `getScenarioByDbId` is unresolved, preventing `unknown_incident` accrual writes. Added/updated tests in `src/lib/bty/arena/binding/postArenaChoice.test.ts` and `src/app/api/arena/choice/route.test.ts`.

**C3 `/api/arena/choice` AD1 failure boundary tests (2026-04-26):** Added `src/app/api/arena/choice/route.test.ts` to lock action-contract failure behavior at route level: AD1 + contract ensure fail returns 503 with blocked runtime snapshot (`ACTION_REQUIRED`, next/choice false), AD1 + success remains `ACTION_REQUIRED`, AD2 avoids contract ensure call and returns `NEXT_SCENARIO_READY`. Route updated so failure response includes explicit blocked snapshot fields, preventing fallback progression.

**C3 AD1 action-contract loop test lock (2026-04-26):** Added `src/lib/bty/arena/binding/buildArenaBindingSnapshotResponse.action-contract-loop.test.ts` to lock canonical post–Action Decision routing by `isActionCommitment`: AD1 commitment path emits blocking states (`ACTION_REQUIRED`, `ACTION_SUBMITTED`, `ACTION_AWAITING_VERIFICATION`) with `next_allowed=false` / `choice_allowed=false`; AD2 avoidance path remains `NEXT_SCENARIO_READY`. This guards No Action → No Progression semantics in snapshot contracts.

**C4 re-exposure legacy i18n key cleanup (2026-04-26):** Completed deprecation cleanup for old re-exposure flat keys after namespace consolidation. Removed unused legacy keys from `src/lib/i18n.ts` type definitions and EN/KO dictionaries, leaving only the new re-exposure key family (`Panel`, `BlockedNextV2`, `InternalStatus`, `Validation*`). Verified no remaining `src` references to deprecated keys and reran required re-exposure suite.

**C4 re-exposure copy namespace consolidation (2026-04-26):** Consolidated re-exposure copy to a single namespace-style key family under `arenaRun.arenaReexposure...` (panel/blockedNext/internalStatus/validation). `ArenaReexposurePanel` now uses panel keys, `BtyArenaRunPageClient` uses blockedNext/internalStatus keys, and `useArenaSession` validation toast uses `changed/unstable/no_change` keys. EN/KO parity maintained; tests assert rendered copy and no numeric exposure in intervention-status text.

**C4 re-exposure UI copy i18n promotion (2026-04-26):** Promoted re-exposure strings from `BtyArenaRunPageClient` inline literals to `arenaRun` i18n keys in `src/lib/i18n.ts` (EN/KO): blocked-next title/description/button + intervention sensitivity internal-status copy. Client now renders these via `t` keys only; no raw risk/sensitivity counts shown. Added snapshot-gates coverage for EN/KO key rendering and "no numeric count" assertion on intervention-status copy.

**C3 useArenaSession server-shell precedence unit guard (2026-04-26):** Added pure transition helper `deriveReexposureValidateLocalAssist(...)` in `useArenaSession` and dedicated unit test `src/app/[locale]/bty-arena/hooks/useArenaSession.reexposure-transition.test.ts` to lock snapshot-first semantics: when server entry shell is `REEXPOSURE_DUE`, validate response `next_runtime_state=NEXT_SCENARIO_READY` must not produce local assist snapshot, so server shell remains authoritative. Also verifies `re_exposure_clear_candidate=true` only yields local clear signal and never shell override.

**C4/C3 re-exposure validate → explicit UI transition sync (2026-04-26):** `useArenaSession` now consumes `POST /api/arena/re-exposure/validate` response contract directly (`next_runtime_state`, `re_exposure_clear_candidate`, `intervention_sensitivity_up`). Snapshot-first rule remains: if a server entry shell snapshot is present, that remains authority; otherwise validate contract seeds local `bindingRuntimeSnapshot` fallback (`NEXT_SCENARIO_READY` or `REEXPOSURE_DUE`). `BtyArenaRunPageClient` consumes this signal to clear local due-candidate state (`re_exposure_clear_candidate`) and show a short non-judgmental internal status copy when `intervention_sensitivity_up=true` (no raw counts). Regression coverage updated in snapshot gates and re-exposure chain tests.

**C3/C4 REEXPOSURE_DUE re-exposure execution wiring (2026-04-26):** `POST /api/arena/choice` now seeds a pending re-exposure row (`arena_pending_outcomes`) when AD2 no-change threshold promotes to `REEXPOSURE_DUE`, then returns enriched `re_exposure` snapshot context (`pending_outcome_id`, `incident_id`, `axis_group`, `axis_index`, `pattern_family`) so client enters a real scenario instead of a static block shell. `useArenaSession.beginReexposurePlay` now reads/logs these snapshot fields. `POST /api/arena/re-exposure/validate` now returns `next_runtime_state` plus branch flags (`re_exposure_clear_candidate` for `changed`, `intervention_sensitivity_up` for `no_change`) to finalize changed/unstable/no_change progression explicitly. Verification: `re-exposure/validate` route tests + re-exposure client chain + snapshot gates + noChangeRisk all PASS.

**C3 BTY Scenario Runtime 27-core wiring (2026-04-26):** Reworked `bty-app/src/data/scenario/index.ts` into a full runtime registry with all 27 core bundles grouped by incident (`core_01~09` each), added strict merge/lookup surface (`getIncident`, `getScenarioById`, `getScenarioByDbId`, `getNextScenario`, `getPreviousScenario`), runtime flow guards (`SCENARIO_READY → PRIMARY → TRADEOFF → ACTION_DECISION → ACTION_REQUIRED/NEXT_SCENARIO_READY`), and decision event capture helper. Added `bty-app/src/data/scenario/types.ts`; switched `src/lib/bty/scenario/loader.ts` and `src/lib/bty/scenario/browserLoader.ts` to consume the in-repo registry instead of ad-hoc fetch merge.

**C3 public scenario mirror sync (2026-04-26):** Synced `bty-app/public/data/scenario` to canonical `bty-app/src/data/scenario` for all `core_01~core_27` (`base.json`/`en.json`/`ko.json`), normalized `core_01_training_system_exposure` envelope JSON in src (`base.json`, `en.json`), and re-mirrored to public. Verification: `npm test -- src/data/scenario/public-core-scenarios.action-decision.test.ts` → **54 passed / 0 failed**.

**C3 core shape validator report (2026-04-26):** Added `bty-app/src/data/scenario/core-scenarios.shape.test.ts` to validate canonical `src/data/scenario` shape for all `core_01~core_27`: envelope key prohibition, required base fields, A/B/C/D + X/Y + A_X/B_X/C_X/D_X constraints, en/ko structure parity, AD1/AD2 commitment semantics, and path-scoped error output (`[core] file @ path`). Run results: `npm test -- src/data/scenario/core-scenarios.shape.test.ts` reports current canonical mismatches in multiple cores (expected report mode), while `npm test -- src/data/scenario/public-core-scenarios.action-decision.test.ts` remains **54/54 PASS**.

**C3 canonical shape mismatch full fix (2026-04-26):** Normalized all shape-failing canonical cores in `src/data/scenario` (`core_07`, `core_10~16`, `core_18~22`) using `bty-app/scripts/normalize-core-scenario-shapes.mjs`. Applied schema unification from legacy fields (`primaryChoices`, `secondChoices`, `action_decision_mapping`) to canonical fields (`choices`, `escalationBranches.*.second_choices`, `action_decision.choices[].meaning.is_action_commitment`, `base.structure.primary/tradeoff/action_decision`), including explicit dbChoiceId fill for missing mappings and incident/stage/axis metadata completion. Re-synced mirror to `public/data/scenario`. Verification: `npm test -- src/data/scenario/core-scenarios.shape.test.ts` **28/28 PASS**, `npm test -- src/data/scenario/public-core-scenarios.action-decision.test.ts` **54/54 PASS**.

**C4/C3 Arena runtime-layer wiring on session UI (2026-04-26):** Connected dev JSON runtime path in `bty-app/src/app/[locale]/bty-arena/BtyArenaRunPageClient.tsx` to canonical runtime helpers from `src/data/scenario/index.ts` (`initializeScenarioFlow`, `activatePrimaryChoice`, `selectPrimaryChoice`, `selectTradeoffChoice`, `selectActionDecision`, `getScenarioById`, `getNextScenario`) so local UI transitions obey Primary → Tradeoff → Action Decision and cannot emit `ACTION_REQUIRED` before action decision. Added AD2 no-change risk notice and enabled `Load Next Scenario` to move by `nextScenarioId` with locale-preserving id resolution. Verification: `npm test -- src/data/scenario/core-scenarios.shape.test.ts src/data/scenario/public-core-scenarios.action-decision.test.ts "src/app/[locale]/bty-arena/BtyArenaRunPageClient.snapshot-gates.test.tsx"` → **89/89 PASS**.

**C3 Arena JSON decision-event DB layer wiring (2026-04-26):** Wired `createScenarioDecisionEvent(...)` output from JSON runtime completion (Primary→Tradeoff→Action Decision) into server event persistence path via `POST /api/arena/event` in `BtyArenaRunPageClient` (`eventType: JSON_SCENARIO_DECISION_COMPLETED`, step 5, action choice id, meta decision payload + AD1 action-contract candidate / AD2 no-change-risk candidate flags). Updated `src/app/api/arena/event/route.ts` to canonicalize `meta.scenarioDecisionEvent.userId` to authenticated user id server-side. Added `src/data/scenario/scenario-decision-event.payload.test.ts` for DB mapping/commitment payload correctness. Verification: `core-scenarios.shape` + `public-core-scenarios.action-decision` + `scenario-decision-event.payload` + `snapshot-gates` → **91/91 PASS**.

**C3 no_change risk accrual + reExposureDue candidate on JSON decision events (2026-04-26):** Extended `POST /api/arena/event` for `JSON_SCENARIO_DECISION_COMPLETED` to accrue server-side no-change risk only when `meta.scenarioDecisionEvent.isActionCommitment === false` (AD2 path). Added `src/lib/bty/arena/noChangeRisk.server.ts` with canonical user override + risk aggregation logic and threshold candidate rules (`incident+axis` aggregate >=2 OR repeated `pattern_family` >=2). Added migration `bty-app/supabase/migrations/20260502000000_arena_no_change_risks.sql` (table/index/RLS). Response now includes `reExposureDueCandidate` and event meta stores `no_change_risk_count`, `re_exposure_due_candidate`, `intervention_sensitivity_candidate`; AD1 path keeps `action_contract_candidate` without risk insert. Added tests `src/lib/bty/arena/noChangeRisk.server.test.ts`; full targeted regression: **5 files / 94 tests PASS**.

**C4 Arena runtime-state promotion from no-change candidate (2026-04-26):** Wired `reExposureDueCandidate` response consumption in `bty-app/src/app/[locale]/bty-arena/BtyArenaRunPageClient.tsx` so AD2 no-change threshold candidates are elevated to a client-side `REEXPOSURE_DUE` surface in JSON runtime path. Added strict render priority: `server snapshot REEXPOSURE_DUE` > `reExposureDueCandidate` > local JSON flow state. When promoted, normal next-scenario action is blocked and a pattern-validation surface is shown (non-punitive copy). AD1 keeps `ACTION_REQUIRED`; AD2 with candidate false remains `NEXT_SCENARIO_READY`. Added test suite `src/app/[locale]/bty-arena/BtyArenaRunPageClient.json-reexposure.test.tsx` covering: AD2+true→REEXPOSURE_DUE, AD2+false→NEXT, AD1→ACTION_REQUIRED, and next button blocking under due state. Regression run with snapshot/shape/public/no-change tests: **5 files / 96 tests PASS**.

**C3/C4 canonical runtime promotion parity (2026-04-26):** Applied the same `reExposureDueCandidate → REEXPOSURE_DUE` rule to the non-JSON general runtime path by extending `POST /api/arena/choice` action-decision handling: AD2 threshold candidates now return promoted snapshot (`runtime_state=REEXPOSURE_DUE`, gates next/choice blocked, `re_exposure.due=true`). Updated `BtyArenaRunPageClient` shell gating to honor `effectiveArenaSnapshot` (not server snapshot only), so canonical/binding promotion and JSON-dev promotion do not diverge. Added snapshot regression for effective snapshot due-shell dominance in `BtyArenaRunPageClient.snapshot-gates.test.tsx`. Verification: `snapshot-gates` + `json-reexposure` + `noChangeRisk` + `shape` + `public` → **5 files / 97 tests PASS**.

**C3 canonical reward loop integration (2026-04-25):** Added `src/app/api/arena/canonical-reward-loop.integration.test.ts` covering full loop in one test: (1) `run/complete` with contract present returns `xpDeferredToContractVerification=true` and zero immediate XP, (2) `qr/validate` applies deferred run reward and writes AIR reflection logs, proving Core/Weekly update paths are separate, (3) `re-exposure/validate` branch outcomes (`changed`, `unstable`, `no_change`) apply expected reflection and follow-up behavior (`no_change` weekly+1/core+0). Also added API contract note in `src/app/api/arena/run/complete/route.ts` for `xpDeferredToContractVerification`. Suite run: **8 files / 69 tests PASS**.

**C3 XP/AIR reflection after contract verify + re-exposure completion (2026-04-25):** Implemented `src/lib/bty/arena/reflectionRewards.server.ts` and wired it to runtime endpoints. `run/complete` now defers run XP when Action Contract exists (`xpDeferredToContractVerification=true`), so evidence submission stage stays non-mutating. `qr/validate` now applies deferred run XP only after execution verify/finalize and records AIR reflection events. `re-exposure/validate` now applies branch-specific reflection (`changed` growth, `unstable` partial growth, `no_change` minimal growth + follow-up). `GET /api/arena/leadership-engine/air` now hides raw numeric AIR in user payload (`score_hidden=true`, band-only display contract). Tests updated for all update branches and defer/apply flow: **7 files / 68 tests PASS**.

**C3 REEXPOSURE_DUE no_change explicit regression (2026-04-25):** Added dedicated `no_change` case in `src/app/api/arena/re-exposure/validate/route.test.ts` asserting pending outcome consume + `follow_up_scheduled === true` + `new_pending_outcome_id` exists + `next_scheduled_for` exists + `validation_result === "no_change"`. Ran full related suite: `src/domain/action-contract/index.test.ts`, `src/app/api/bty/action-contract/submit-validation/route.test.ts`, `src/app/api/arena/leadership-engine/qr/validate/route.test.ts`, `src/app/api/arena/re-exposure/validate/route.test.ts`, `src/app/api/arena/session/next/route.test.ts`, `src/app/[locale]/bty-arena/BtyArenaRunPageClient.snapshot-gates.test.tsx` → **6 files / 38 tests PASS**.

**C3 REEXPOSURE_DUE regression tests (2026-04-25):** Added targeted regressions for (1) delayed outcome path resolves to `REEXPOSURE_DUE`, (2) `REEXPOSURE_DUE` session body keeps `scenario: null`, (3) `BtyArenaRunPageClient` renders `ArenaReexposurePanel` over normal play even with completed contract snapshot, (4) `re-exposure/validate` rejects cross-axis mismatch via history/scenario guard, (5) `changed` consumes pending with no follow-up, (6) `unstable/no_change` consume pending and schedule reinforcement follow-up. Result: **3 files / 19 tests PASS**.

**C3 canonical Action Contract transition tests (2026-04-25):** Added targeted Vitest coverage for domain mapping (`toDisplayState`), `submit-validation` canonical separation + self-report auto-approve exception, `qr/validate` awaiting-finalize + legacy pending shortcut flag guard, and `BtyArenaRunPageClient` NEXT_SCENARIO_READY defensive blocking. Result: **4 files / 24 tests PASS**.

**C3 canonical Action Contract states (2026-04-25):** `ACTION_REQUIRED → ACTION_SUBMITTED → ACTION_AWAITING_VERIFICATION → REEXPOSURE_DUE → NEXT_SCENARIO_READY` 분리 구현. `domain/action-contract` display_state 확장, `ActionContractHub` 상태별 UI 분리, `ArenaPendingContractGate` runtime-state 라벨링. `submit-validation`은 기본 제출 시 `submitted` 유지 + `validation_approved_at` 기록(자가보고 auto-approve 예외만 즉시 완료), `qr/validate`는 awaiting 상태에서만 완료 처리하며 legacy `pending→approved`는 `BTY_ALLOW_LEGACY_PENDING_QR_APPROVE=true` 플래그로만 허용. `BtyArenaRunPageClient` snapshot 우선 + NEXT_SCENARIO_READY 추가 차단 가드. `npm run build` ✓.

**C4/C3 JSON draft API wiring (2026-04-25):** `BtyArenaRunPageClient` replaces local-only draft save with clean `saveJsonActionContract()` same-origin POST to `/api/arena/action-contracts`, no fake auth headers; handles 200/401/400/500+network UI states and disables save while submitting. Vitest route **4/4** ✓ · JSON draft Playwright **1/1** ✓ · build ✓ · `npm run lint` blocked by pre-existing QR test `ContractRow.user_id` type errors.

**C3 DB/RLS (2026-04-25):** Added `bty-app/supabase/migrations/20260501000000_bty_action_contracts_insert_policy.sql` for authenticated self-insert policy on `bty_action_contracts`; route Vitest **4/4** ✓; **BLOCKED** on DB apply/authenticated POST because Supabase remote token/MCP auth unavailable and local DB is not running.

**C3 Arena Phase A (2026-04-10):** re-exposure **`validation_result`** → reinforcement loop (`reinforcement_loop` + `reinforcement_seeded_from_pending_id`); validate route + `reinforcementLoopSchedule.server.ts` + migration `20260410120000` — **작업 완료**

**C3 API (2026-04-24):** Added **`POST /api/arena/action-contracts`** (App Router) for JSON-dev Action Contract persistence to `bty_action_contracts` with zod validation + authenticated gate + `details/source` write + `id/status` return; Vitest **`src/app/api/arena/action-contracts/route.test.ts`** 4/4 ✓; `npm run build` ✓ — **작업 완료**

**C5 verify (2026-03-30 · release rollout probe):** `npm run lint` ✓ · Vitest **369 / 2665** ✓ · `npm run build` ✓ · prod smoke **200/401** · deploy **SHA parity unverified** (wrangler vs `e8b848d`) · auth flows **deferred** — **보고 완료**

**C2 / middleware (2026-04-24):** `NEXT_PUBLIC_BTY_DEV_BYPASS_AUTH=true` + non-production only — `bty-app/src/middleware.ts` allows `/[locale]/bty-arena` without login redirect for local scenario JSON testing; production unchanged — **작업 완료**

**C4 / JSON dev UI (2026-04-24):** `BtyArenaRunPageClient` **`JsonScenarioFlow`** — `ACTION_REQUIRED`일 때 로컬 **Action Contract Draft** 패널(필수 필드 검증, 저장 시 API 없음, 확인 문구만); `NEXT_SCENARIO_READY` **Load Next Scenario** 비활성 유지 — **작업 완료**

**C5 (2026-04-10 · My Page pattern signature UX):** `PatternSignaturePanel` — **`myPageStub`** i18n path fix (repeat / shift / confidence labels); Vitest **`MyPageLeadershipConsole.test.tsx`** **11/11** ✓ — **작업 완료**

## SYSTEM MODE: BEHAVIOR ENGINE BUILD

**C3 (2026-03-28):** Action Contract + persistence + AIR/XP hooks — `domain/action-contract` · `lib/bty/action-contract` · migration `20260431230000` · vitest 10 · **작업 완료**

**C3 (2026-03-28):** BTY AI Routing Engine v1 — `bty-app/src/domain/routing` + vitest 11 · **작업 완료**

**C5 verify (2026-03-27 · Arena final integration gate):** `npm run lint` ✓ · Vitest **418 / 2959** ✓ · `npm run build` ✓ · PENDING-014·017·Level-first·tenure 전제 **PASS** — **작업 완료**

**C5 verify (2026-03-27 · Arena branch release gate):** `npm run lint` ✓ · Vitest **417 files / 2954 tests** ✓ · `npm run build` ✓ · PENDING-014·017 전제 **PASS** — **작업 완료**

**C5 verify (2026-03-25 · 재검증):** `next lint`+`npm run lint` **0** · Vitest **2811** · **8 FAIL** onboarding mock · build **PASS** · **NO-GO** — **보고 완료**

**C5 verify (2026-03-25):** Lab org onboarding gate — `self-healing-ci.sh` **PASS** · **397 / 2803** · `next lint` **FAIL**(툴링) · onboard 시퀀스 테스트 **MISSING** — **작업 완료**

**C5 verify (2026-03-24):** `self-healing-ci.sh` **PASS** · 368 files / 2660 tests · build ✓ — **작업 완료**

**Arena release gate (deploy smoke):** `bty-app/scripts/arena-release-gate.sh` + workflow `arena-release-gate.yml` — **작업 완료**

**Cutover readiness (C3):** **`PENDING-014`** and **`PENDING-017`** **remain open** — **must appear** on the cutover checklist (`bty-app/docs/ARENA_PIPELINE_CUTOVER.md` § C3; `bty-app/docs/CI_RELEASE_GATE_MATRIX.md` § C3; `docs/BTY_RELEASE_GATE_CHECK.md`). **Neither blocks** cutover; **both require post-cutover resolution**.

---

## 🔵 ACTIVE TASKS

### C2 — ENGINE (BACKEND / DOMAIN)

#### TASK 1: Memory Engine
- source: user_scenario_choice_history
- output:
  - repeated pattern detection
  - recall trigger ("Last time you...")
- integrate with:
  - scenario-selector
  - scenario-type-router

---

#### TASK 2: Delayed Outcome Engine
- [x] 완료 (2026-04-24)
- scenario JSON `delay_days` + `outcomes[].if.pattern` consumed in scheduler (fallback 7d 유지)
- memory trigger queue `delayed_outcome` enqueue/consume + processed idempotency 확인
- `arenaSessionNextCore`와 연동되어 `REEXPOSURE_DUE` + `re_exposure.trigger_*` 반환

---

#### TASK 3: Perspective Switch Engine
- expand role-based scenarios
- leader → staff view inversion
- scenario pool 확장 필요

---

### C4 — UI (ENFORCEMENT / POLISH)

#### TASK 1: Leadership Engine Display
- AIR / TII / LRI
- convert numeric → narrative/band
- integrate into dashboard (Progress or Team card)

---

#### TASK 2: Avatar Polish
- unify scale
- add visual depth (shadow / ring consistency)
- improve empty state

---

### C5 — QA / VERIFICATION

#### TASK 1: Scenario Flow Verification
- 반복 시나리오 제거 확인
- session/next 다양성 체크

#### TASK 2: Behavior Loop Check
- Arena → Foundry → Arena 흐름 확인
- memory recall trigger 작동 여부

---

## 🟡 BACKLOG

- Forced Reset (48h lockout)
- Healing / Awakening phase
- Team Index full integration
- Elite mentor flow 확장

---

## ⚫ CUTOVER — G-B09 (COMPLETE 2026-03-28)

**G-B09 — Full-repo forbidden pattern sweep** — **DONE.**

| | |
| --- | --- |
| **Normative doc** | **`bty-app/docs/terminology-locks/UX_FLOW_LOCK_V1.md`** §5 |
| **Automation** | **`bty-app/scripts/ux-flow-lock-gb09-sweep.mjs`** (`npm run lint:ux-flow-gb09` in `bty-app`) |
| **Result** | **PASS** — no high-risk §5 phrase hits in **966** `src/` files (2026-03-28 run). |
| **Evidence** | **`docs/BTY_RELEASE_GATE_CHECK.md`** — G-B09 sign-off line. |

---

## 🔴 DO NOT TOUCH

- routing 구조
- UI 시스템 (ScreenShell / InfoCard)
- XP 공식
- avatar API contract

---

## ⚫ WORKFLOW RULE

- C2 → logic
- C4 → UI reflection
- C5 → validation

---

## ONE-LINE

> 이제 Cursor의 역할은  
> “화면을 만드는 것”이 아니라  
> **행동 엔진을 연결하는 것**이다.