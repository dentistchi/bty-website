# BTY_AVATAR_IDENTITY_LOCK

> Governance lock — Identity ≠ State. Create-only, 1회 완결.
> 의미 본문 = Commander 결정분 verbatim. 측정 = Claude Code (read-only inventory).

---

## §0  PROVENANCE

- 결정 출처: RISK-1 트랙 (Avatar identity ↔ Code/Archetype 매핑 product call).
- baseline: outer origin/main `0096f8c`.
- 상속: `BTY_AXIS_CANON_v1_2_ARCHETYPE_BASELINE.md` (Archetype = compute-LIVE + wired + HTTP-served; Avatar = D).
- **Semantic lock only. No implementation behavior is modified by this document.**

---

## §1  LOCK

1. **Identity Anchor = Code** — avatar = Code progression.
2. **State Layer = Archetype** — non-identity, axis-derived, recompute, "현재 패턴 해석".
3. **Identity ≠ State.**

### COMMANDER MEANING (verbatim — Claude Code 재해석·가감 금지)

> BTY의 목표는 특정한 리더 타입이 되는 것이 아니다.
> BTY의 목표는 어제보다 더 자기 자신이 되는 것이다.
> 따라서 Avatar는 고정된 성격이나 상태를 나타내지 않는다.
> Avatar는 성장의 여정을 나타내며, Code progression에 anchor된다.
> Archetype은 현재 관찰되는 행동 패턴의 해석이다.
> Archetype은 Identity가 아니라 State다.
> Archetype은 변할 수 있으며, 변해야 한다.
> Identity는 누적되고, State는 변하고, Evidence는 그것을 입증한다.

---

## §2  EVIDENCE LAYER = AIR / LRI

- role-assignment ONLY. disclosure-scoped.
- "구축됨" 단정 금지. 이 락이 배정하는 역할이며, 현재 구현 입증은 별건이다.

---

## §3  EFFECTS ON PRIOR RISKS

- **persistence NOT PROVEN → load-bearing 해제.** Archetype = Weather(State)이므로 persistence pin 불요.
- **OBS-1 (10/12, ownership·time 미커버) → avatar 무관화.** 잔존분은 archetype surface 만 상속.

---

## §4  OPEN (→ #3 Avatar Mapping Track 및 이후)

- F10 override reconciliation (Code 슬롯 / 날씨 슬롯 분리). 현재: `archetypeName → codeNameOverride → computeLeadershipState Code-name slot` (getMyPageIdentityState.ts:89-95).
- Archetype dashboard surface 설계.
- Code-name display residency 확인 (#3 선결 inventory).
- AIR / LRI evidence presentation 위치.
- Profile Surface alignment.
- Future Narrative / Scenario Copy alignment.

---

## §5  TIME-SCOPED HONESTY

현재 구현 ≠ 영구 정의. Canon은 구현 위에 있다.
