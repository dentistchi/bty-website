# SCENARIO_ARCHITECTURE_V1

**Status:** Normative for scenario library content, run lifecycle, and review in `bty-app`.  
**Related:** `ENGINE_ARCHITECTURE_V1.md`, `UX_FLOW_LOCK_V1.md`, `PATTERN_ACTION_MODEL_V1.md`, `QA_INTEGRITY_FRAMEWORK_V1.md` (integrity modes).

---

## Section 6 — Scenario constraints

**Pool activation QA (escalation content):** Normative constraints for escalation copy (`escalation_text`), `second_choices`, non-empty **`cost`** fields, and choice counts when **writing or reviewing** scenarios for **pool activation** are defined in **`UX_FLOW_LOCK_V1.md` §6** (Scenario QA constraints for pool activation) — use that section as the source of truth so authors and reviewers find the rules without having to know to open the UX flow lock first.
