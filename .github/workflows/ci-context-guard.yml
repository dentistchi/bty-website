name: CI (Context Guard)

on:
  pull_request:
  workflow_dispatch:

jobs:
  context-guard:
    name: Context Guard (docs must match infra/auth/runtime changes)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine base branch
        id: base
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ] && [ -n "${{ github.base_ref }}" ]; then
            echo "BASE_REF=${{ github.base_ref }}" >> "$GITHUB_OUTPUT"
          else
            echo "BASE_REF=main" >> "$GITHUB_OUTPUT"
          fi

      - name: Fetch base branch
        run: |
          git fetch origin "${{ steps.base.outputs.BASE_REF }}:refs/remotes/origin/${{ steps.base.outputs.BASE_REF }}" --depth=1 || true

      - name: Context Guard check
        shell: bash
        run: |
          set -euo pipefail

          BASE="origin/${{ steps.base.outputs.BASE_REF }}"
          if ! git show-ref --verify --quiet "refs/remotes/${BASE}"; then
            echo "Base branch ref not found; skipping guard."
            exit 0
          fi

          CHANGED="$(git diff --name-only "${BASE}...HEAD")"
          echo "Changed files:"
          echo "$CHANGED"

          CONTEXT_DOC="bty-app/docs/CONTEXT.md"

          # 배포 파이프라인, OpenNext/Workers, Next config, middleware, auth/cookie/session, locale route groups만 트리거.
          INFRA_REGEX='(^\.github/workflows/(deploy|deploy-workers|ci-context-guard)\.yml$|^wrangler\.toml$|^bty-app/wrangler\.toml$|^open-next\.config\.(mjs|ts)$|^bty-app/open-next\.config\.(mjs|ts)$|^next\.config\.(js|mjs|ts)$|^bty-app/next\.config\.(js|mjs|ts)$|^bty-app/src/middleware\.(ts|js)$|^bty-app/src/lib/supabase/|^bty-app/src/lib/auth/|^bty-app/src/app/api/auth/|^bty-app/src/app/\[locale\]/auth/|^bty-app/src/app/\[locale\]/\((public|protected)\)/|^bty-app/src/app/\(.*\)/|^bty-app/src/lib/bty/auth/|^bty-app/src/lib/bty/cookie/|^bty-app/src/lib/bty/session/)'

          INFRA_TOUCHED="0"
          while IFS= read -r f; do
            [[ -z "$f" ]] && continue
            if [[ "$f" =~ $INFRA_REGEX ]]; then
              INFRA_TOUCHED="1"
              echo "Infra-trigger file: $f"
              break
            fi
          done <<< "$CHANGED"

          if [[ "$INFRA_TOUCHED" == "1" ]]; then
            if ! echo "$CHANGED" | grep -q "^${CONTEXT_DOC}$"; then
              echo ""
              echo "❌ Context Guard failed."
              echo "You changed infra/auth/runtime-related files but did NOT update ${CONTEXT_DOC}."
              echo ""
              echo "Fix:"
              echo " - Update ${CONTEXT_DOC} to reflect the change (1~5 lines is enough)."
              exit 1
            fi
          fi

          echo "✅ Context Guard passed."

  build-check:
    name: Typecheck + Build (fast gate)
    runs-on: ubuntu-latest
    needs: context-guard
    defaults:
      run:
        working-directory: bty-app
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install deps
        run: npm ci

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build (requires NEXT_PUBLIC_SUPABASE_*)
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
        run: npm run build
