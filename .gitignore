# dependencies
node_modules/

# next.js
.next/
out/

# vercel & cloudflare
.vercel/
.open-next/
.wrangler/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# misc
.DS_Store
dist/

# TypeScript
next-env.d.ts
*.tsbuildinfo
worker-configuration.d.ts

# 주의: package-lock.json은 gitignore에 추가하지 말 것! (반드시 git에 커밋)

# Playwright E2E auth (see e2e/.gitignore)
e2e/.auth/user.json

.env.local
.env.local.*
*.save

.env.local
.env.local.*
*.save

# macOS auto-generated screenshots
Screenshot *.png
**/Screenshot *.png

# .env secrets — root-cause fix per INNER_PUSH_POLICY stash probe (2026-05-14)
.env
.env.*
!.env.example
.env.bak.*

# Supabase CLI link artifacts — environment-specific, not tracked
supabase/.temp/

# Local scratch (STEP 0 capture, ad-hoc inventory dumps) — never committed
tmp/

# --- BTY hygiene: launch media (inert, not build-referenced) ---
assets/

# Slice 3.2I-R2 — local live-generation eval artifacts (never committed)
.eval-artifacts/

# Slice 3.2L-R1.4 real-pixel gate scratch (built + removed per run)
.viewport-harness/

# Teams app package build output (contains only public ids, but is a build artifact)
teams/dist/
